Error tracking disabled because this release is older than 6 weeks.
[
  {
    "updatedAt": "2026-08-19T06:25:54.845Z",
    "createdAt": "2026-08-02T10:41:12.357Z",
    "id": "08g3JNwQPVSxUl2H",
    "name": "v02.UWI_LANE07",
    "description": null,
    "active": true,
    "isArchived": false,
    "nodes": [
      {
        "id": "manual_trigger",
        "name": "Manual Trigger",
        "type": "n8n-nodes-base.manualTrigger",
        "position": [
          220,
          100
        ],
        "parameters": {},
        "typeVersion": 1
      },
      {
        "id": "schedule_trigger",
        "name": "Schedule Trigger",
        "type": "n8n-nodes-base.scheduleTrigger",
        "notes": "Every 24 hours. (Migrated from deprecated n8n-nodes-base.cron, whose stored parameter shape did not match this n8n version's actual Cron node schema and would not have fired.)",
        "position": [
          220,
          220
        ],
        "parameters": {
          "rule": {
            "interval": [
              {
                "field": "days",
                "daysInterval": 1
              }
            ]
          }
        },
        "typeVersion": 1.2
      },
      {
        "id": "initialize_run_metadata",
        "name": "Initialize Run Metadata",
        "type": "n8n-nodes-base.code",
        "position": [
          520,
          100
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst now = new Date();\nconst runStamp = now.toISOString().replace(/[-:]/g, '').replace(/\\.\\d{3}Z$/, 'Z');\nconst runId = '07_GOVERNMENT_SAFETY_ALERTS-' + runStamp + '-001';\nreturn [{ json: {\n  schema_version: '1.0.0',\n  connector_id: '07_GOVERNMENT_SAFETY_ALERTS',\n  connector_name: 'UW-Issaquah Government Safety Alerts Connector',\n  connector_version: 'v0002',\n  lane: '07_GOVERNMENT_SAFETY_ALERTS',\n  workflow_name: 'v02.UWI_LANE07',\n  run_id: runId,\n  run_stamp: runStamp,\n  generated_at: now.toISOString(),\n  published_at: null,\n  output_root: '/files/uw-issy-connectors',\n  canonical_gpx: 'data/route/UnivWA-Issaquah.gpx',\n  route_sections: [{\"route_section_id\":\"WP1_UW\",\"route_section_name\":\"University of Washington / U-District\"},{\"route_section_id\":\"WP2_KENMORE_LFP\",\"route_section_name\":\"Kenmore / Lake Forest Park\"},{\"route_section_id\":\"WP3_BOTHELL\",\"route_section_name\":\"Bothell\"},{\"route_section_id\":\"WP4_WOODINVILLE\",\"route_section_name\":\"Woodinville\"},{\"route_section_id\":\"WP5_REDMOND\",\"route_section_name\":\"Redmond\"},{\"route_section_id\":\"WP6_MARYMOOR_ELST\",\"route_section_name\":\"Marymoor Park / East Lake Sammamish Trail\"},{\"route_section_id\":\"WP7_SAMMAMISH\",\"route_section_name\":\"Sammamish\"},{\"route_section_id\":\"WP8_ISSAQUAH\",\"route_section_name\":\"Issaquah\"}],\n  gazetteer: [\"University of Washington\",\"UW\",\"U-District\",\"University Way NE\",\"Rainier Vista\",\"Kane Hall\",\"Burke-Gilman Trail\",\"NE Pacific St\",\"NE 45th St\",\"Bothell Landing\",\"Marymoor Park\",\"East Lake Sammamish Trail\",\"Kenmore\",\"Lake Forest Park\",\"Bothell\",\"Woodinville\",\"Redmond\",\"Sammamish\",\"Issaquah\",\"SR-522\",\"I-90\",\"I-405\",\"Seattle\",\"Washington\",\"King County\"],\n  source_configs: [{\"key\":\"NWS-01\",\"sourceId\":\"07_GOVERNMENT_SAFETY_ALERTS:NWS-01\",\"sourceName\":\"NWS modern alerts API and CAP products\",\"sourceUrl\":\"https://api.weather.gov/alerts/active?point=47.6505,-122.3046\",\"fetchUrl\":\"https://api.weather.gov/alerts/active?point=47.6505,-122.3046\",\"fetchFormat\":\"json\",\"sourceKind\":\"nws\",\"staleAfterMinutes\":15,\"routeSections\":[\"WP1_UW\",\"WP2_KENMORE_LFP\",\"WP3_BOTHELL\",\"WP4_WOODINVILLE\",\"WP5_REDMOND\",\"WP6_MARYMOOR_ELST\",\"WP7_SAMMAMISH\",\"WP8_ISSAQUAH\"],\"methodHint\":\"route_point_query\"},{\"key\":\"SEA-01\",\"sourceId\":\"07_GOVERNMENT_SAFETY_ALERTS:SEA-01\",\"sourceName\":\"AlertSeattle public feed and WordPress API\",\"sourceUrl\":\"https://alert.seattle.gov/wp-json/wp/v2/posts?per_page=10\",\"fetchUrl\":\"https://alert.seattle.gov/wp-json/wp/v2/posts?per_page=10\",\"fetchFormat\":\"json\",\"sourceKind\":\"wp_json\",\"staleAfterMinutes\":15,\"routeSections\":[\"WP1_UW\"],\"methodHint\":\"text_landmark_match\"},{\"key\":\"UW-01\",\"sourceId\":\"07_GOVERNMENT_SAFETY_ALERTS:UW-01\",\"sourceName\":\"UW Alert blog feed and WordPress API\",\"sourceUrl\":\"https://emergency.uw.edu/wp-json/wp/v2/posts?per_page=10\",\"fetchUrl\":\"https://emergency.uw.edu/wp-json/wp/v2/posts?per_page=10\",\"fetchFormat\":\"json\",\"sourceKind\":\"wp_json\",\"staleAfterMinutes\":15,\"routeSections\":[\"WP1_UW\"],\"methodHint\":\"text_landmark_match\"},{\"key\":\"SEAFD-01\",\"sourceId\":\"07_GOVERNMENT_SAFETY_ALERTS:SEAFD-01\",\"sourceName\":\"Seattle Fire Department Fireline RSS\",\"sourceUrl\":\"https://fireline.seattle.gov/feed/\",\"fetchUrl\":\"https://fireline.seattle.gov/feed/\",\"fetchFormat\":\"string\",\"sourceKind\":\"rss\",\"staleAfterMinutes\":30,\"routeSections\":[\"WP1_UW\"],\"methodHint\":\"text_landmark_match\"},{\"key\":\"SEAPD-01\",\"sourceId\":\"07_GOVERNMENT_SAFETY_ALERTS:SEAPD-01\",\"sourceName\":\"Seattle Police Blotter RSS and Significant Incident Reports page\",\"sourceUrl\":\"https://spdblotter.seattle.gov/feed/\",\"fetchUrl\":\"https://spdblotter.seattle.gov/feed/\",\"fetchFormat\":\"string\",\"sourceKind\":\"rss\",\"staleAfterMinutes\":30,\"routeSections\":[\"WP1_UW\"],\"methodHint\":\"text_landmark_match\"},{\"key\":\"DOH-02\",\"sourceId\":\"07_GOVERNMENT_SAFETY_ALERTS:DOH-02\",\"sourceName\":\"Washington Health Alert Network public table\",\"sourceUrl\":\"https://doh.wa.gov/public-health-provider-resources/washington-health-alert-network\",\"fetchUrl\":\"https://doh.wa.gov/public-health-provider-resources/washington-health-alert-network\",\"fetchFormat\":\"string\",\"sourceKind\":\"html_table\",\"staleAfterMinutes\":360,\"routeSections\":[\"WP1_UW\",\"WP2_KENMORE_LFP\",\"WP3_BOTHELL\",\"WP4_WOODINVILLE\",\"WP5_REDMOND\",\"WP6_MARYMOOR_ELST\",\"WP7_SAMMAMISH\",\"WP8_ISSAQUAH\"],\"methodHint\":\"ugc_same_area_match\"},{\"key\":\"ST-01\",\"sourceId\":\"07_GOVERNMENT_SAFETY_ALERTS:ST-01\",\"sourceName\":\"Sound Transit GTFS Realtime service alerts\",\"sourceUrl\":\"https://s3.amazonaws.com/st-service-alerts-prod/alerts_pb.json\",\"fetchUrl\":\"https://s3.amazonaws.com/st-service-alerts-prod/alerts_pb.json\",\"fetchFormat\":\"json\",\"sourceKind\":\"transit_json\",\"staleAfterMinutes\":15,\"routeSections\":[\"WP1_UW\",\"WP3_BOTHELL\",\"WP5_REDMOND\",\"WP8_ISSAQUAH\"],\"methodHint\":\"route_stop_mapping\"},{\"key\":\"KCMETRO-01\",\"sourceId\":\"07_GOVERNMENT_SAFETY_ALERTS:KCMETRO-01\",\"sourceName\":\"King County Metro GTFS Realtime service alerts\",\"sourceUrl\":\"https://s3.amazonaws.com/kcm-alerts-realtime-prod/alerts_enhanced.json\",\"fetchUrl\":\"https://s3.amazonaws.com/kcm-alerts-realtime-prod/alerts_enhanced.json\",\"fetchFormat\":\"json\",\"sourceKind\":\"transit_json\",\"staleAfterMinutes\":15,\"routeSections\":[\"WP1_UW\",\"WP2_KENMORE_LFP\",\"WP3_BOTHELL\",\"WP5_REDMOND\",\"WP8_ISSAQUAH\"],\"methodHint\":\"route_stop_mapping\"}],\n  freshness_policy: { default_threshold_minutes: 15, doh_threshold_minutes: 360, stale_source_policy: 'preserve_last_known_good' },\n  route_filter_policy: {\n    geometry_requires_line_buffer_intersection: true,\n    text_sources: 'named trail or street match plus impact language',\n    route_point_query: true,\n    overlap_notes_ref: 'OVERLAP_NOTES.md'\n  },\n  manifest_ref: { manifest_id: '07_GOVERNMENT_SAFETY_ALERTS-v0002', schema_version: '1.0.0' }\n} }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "read_last_known_good",
        "name": "Read Last Known Good",
        "type": "n8n-nodes-base.readWriteFile",
        "onError": "continueRegularOutput",
        "position": [
          760,
          180
        ],
        "parameters": {
          "onError": "continueRegularOutput",
          "options": {},
          "operation": "read",
          "fileSelector": "={{ $('Initialize Run Metadata').first().json.output_root }}/last_known_good/07_GOVERNMENT_SAFETY_ALERTS/current.json"
        },
        "typeVersion": 1,
        "alwaysOutputData": true
      },
      {
        "id": "parse_last_known_good",
        "name": "Parse Last Known Good",
        "type": "n8n-nodes-base.code",
        "position": [
          1040,
          180
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst first = $input.first();\nif (!first || first.error || (first.json && first.json.error)) {\n  return [{ json: { bySource: {} } }];\n}\ntry {\n  const buffer = await this.helpers.getBinaryDataBuffer(0, 'data');\n  const text = buffer.toString('utf8');\n  const parsed = JSON.parse(text);\n  if (!parsed || typeof parsed !== 'object' || !Array.isArray(parsed.source_health) || !Array.isArray(parsed.events)) {\n    return [{ json: { bySource: {} } }];\n  }\n  const bySource = {};\n  const observations = Array.isArray(parsed.observations) ? parsed.observations : [];\n  for (const entry of parsed.source_health) {\n    if (!entry || typeof entry !== 'object' || !entry.source_id) continue;\n    if (!['ok', 'empty_but_valid', 'using_last_known_good'].includes(entry.status)) continue;\n    bySource[entry.source_id] = {\n      source_health: entry,\n      events: parsed.events.filter(event => event && event.source_id === entry.source_id),\n      observations: observations.filter(observation => observation && observation.source_id === entry.source_id)\n    };\n  }\n  return [{ json: { bySource } }];\n} catch (error) {\n  return [{ json: { bySource: {} } }];\n}\n"
        },
        "typeVersion": 2
      },
      {
        "id": "fetch_nws_01_alerts",
        "name": "Fetch NWS-01 Alerts",
        "type": "n8n-nodes-base.httpRequest",
        "notes": "NWS modern alerts API and CAP products",
        "onError": "continueRegularOutput",
        "position": [
          220,
          420
        ],
        "parameters": {
          "url": "https://api.weather.gov/alerts/active?point=47.6505,-122.3046",
          "method": "GET",
          "options": {
            "timeout": 30000
          },
          "responseFormat": "json"
        },
        "typeVersion": 4.2
      },
      {
        "id": "land_nws_01_raw_payload",
        "name": "Land NWS-01 Raw Payload",
        "type": "n8n-nodes-base.code",
        "position": [
          760,
          420
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch NWS-01 Alerts').first().json || {};\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst bodyString = typeof body === 'string' ? body : JSON.stringify(body || null);\nconst statusCode = fetch.statusCode ?? fetch.status ?? null;\nconst hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst contentHash = 'hash_' + hashString(bodyString);\nconst filePath = run.output_root + '/raw/07_GOVERNMENT_SAFETY_ALERTS/landings/NWS-01_landing_' + run.run_stamp + '.json';\nconst landing = {\n  schema_version: '1.0.0',\n  connector_id: run.connector_id,\n  source_id: '07_GOVERNMENT_SAFETY_ALERTS:NWS-01',\n  source_name: 'NWS modern alerts API and CAP products',\n  source_url: 'https://api.weather.gov/alerts/active?point=47.6505,-122.3046',\n  retrieved_at: run.generated_at,\n  http_status: statusCode,\n  retrieval_method: 'nws',\n  content_hash: contentHash,\n  body_length: bodyString.length,\n  body: bodyString,\n  fetch_status: fetch.error ? 'failed' : 'ok',\n  fetch_error: fetch.error ? String(fetch.error.message || fetch.error) : null\n};\nconst buffer = Buffer.from(JSON.stringify(landing, null, 2), 'utf8');\nconst binary = await this.helpers.prepareBinaryData(buffer, filePath.split('/').pop(), 'application/json');\nreturn [{ json: {\n  file_path: filePath,\n  source_id: landing.source_id,\n  source_name: landing.source_name,\n  content_hash: contentHash,\n  retrieved_at: run.generated_at,\n  body_length: bodyString.length\n}, binary: { data: binary } }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "write_nws_01_raw_landing",
        "name": "Write NWS-01 Raw Landing",
        "type": "n8n-nodes-base.readWriteFile",
        "position": [
          1180,
          420
        ],
        "parameters": {
          "options": {},
          "fileName": "={{ $json.file_path }}",
          "operation": "write",
          "dataPropertyName": "data"
        },
        "typeVersion": 1
      },
      {
        "id": "normalize_nws_01_events",
        "name": "Normalize NWS-01 Events",
        "type": "n8n-nodes-base.code",
        "position": [
          1600,
          420
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch NWS-01 Alerts').first().json || {};\nconst landing = $('Land NWS-01 Raw Payload').first().json || {};\nconst lkgLookup = ($('Parse Last Known Good').first().json || {}).bySource || {};\nconst sourceId = '07_GOVERNMENT_SAFETY_ALERTS:NWS-01';\nconst sourceName = 'NWS modern alerts API and CAP products';\nconst sourceUrl = 'https://api.weather.gov/alerts/active?point=47.6505,-122.3046';\nconst sourceKind = 'nws';\nconst routeSeedSections = [\"WP1_UW\",\"WP2_KENMORE_LFP\",\"WP3_BOTHELL\",\"WP4_WOODINVILLE\",\"WP5_REDMOND\",\"WP6_MARYMOOR_ELST\",\"WP7_SAMMAMISH\",\"WP8_ISSAQUAH\"];\nconst staleAfterMinutes = 15;\nconst gazetteer = run.gazetteer || ['University of Washington', 'UW', 'U-District', 'University Way NE', 'Rainier Vista', 'Kane Hall', 'Burke-Gilman Trail', 'NE Pacific St', 'NE 45th St', 'Bothell Landing', 'Marymoor Park', 'East Lake Sammamish Trail', 'Kenmore', 'Lake Forest Park', 'Bothell', 'Woodinville', 'Redmond', 'Sammamish', 'Issaquah', 'SR-522', 'I-90', 'I-405', 'Seattle', 'Washington', 'King County'];\nconst now = Date.now();\nconst hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nfunction stripHtml(value) {\n  return String(value || '')\n    .replace(/<script[\\s\\S]*?<\\/script>/gi, ' ')\n    .replace(/<style[\\s\\S]*?<\\/style>/gi, ' ')\n    .replace(/<[^>]+>/g, ' ')\n    .replace(/&nbsp;/g, ' ')\n    .replace(/&amp;/g, '&')\n    .replace(/\\s+/g, ' ')\n    .trim();\n}\nfunction textOf(value) {\n  if (value === null || value === undefined) return '';\n  if (typeof value === 'string') return stripHtml(value);\n  if (Array.isArray(value)) return value.map(textOf).filter(Boolean).join(' | ');\n  if (typeof value === 'object') {\n    if (typeof value.rendered === 'string') return stripHtml(value.rendered);\n    if (typeof value.value === 'string') return stripHtml(value.value);\n    return stripHtml(JSON.stringify(value));\n  }\n  return String(value);\n}\nfunction safeJsonParse(value) {\n  if (typeof value !== 'string') return null;\n  try { return JSON.parse(value); } catch (error) { return null; }\n}\nfunction extractRecords(body) {\n  if (!body) return [];\n  if (sourceKind === 'nws') {\n    if (Array.isArray(body.features)) return body.features;\n    if (Array.isArray(body.items)) return body.items;\n    return [body];\n  }\n  if (sourceKind === 'wp_json') {\n    if (Array.isArray(body)) return body;\n    if (Array.isArray(body.posts)) return body.posts;\n    if (Array.isArray(body.items)) return body.items;\n    return [];\n  }\n  if (sourceKind === 'rss') {\n    const text = typeof body === 'string' ? body : textOf(body);\n    const matches = [...text.matchAll(/<item[\\s\\S]*?<\\/item>/gi)];\n    if (!matches.length) return [];\n    return matches.map((match, index) => {\n      const item = match[0];\n      const getTag = (tag) => {\n        const tagMatch = item.match(new RegExp('<' + tag + '[^>]*>([\\s\\S]*?)</' + tag + '>', 'i'));\n        return tagMatch ? stripHtml(tagMatch[1]) : '';\n      };\n      return { title: getTag('title'), link: getTag('link'), description: getTag('description'), pubDate: getTag('pubDate') || getTag('updated'), guid: getTag('guid') || getTag('link') || String(index) };\n    });\n  }\n  if (sourceKind === 'html_table') {\n    const text = typeof body === 'string' ? body : textOf(body);\n    const rows = [...text.matchAll(/<tr[\\s\\S]*?<\\/tr>/gi)];\n    return rows.map((match, index) => {\n      const row = match[0];\n      const cells = [...row.matchAll(/<t[dh][^>]*>([\\s\\S]*?)<\\/t[dh]>/gi)].map(m => stripHtml(m[1]));\n      return { cells, title: cells.join(' | '), link: '', pubDate: cells[0] || '', guid: cells.join('|') || String(index) };\n    });\n  }\n  if (sourceKind === 'transit_json') {\n    if (Array.isArray(body.entity)) return body.entity;\n    if (Array.isArray(body.entities)) return body.entities;\n    if (Array.isArray(body.alerts)) return body.alerts;\n    if (Array.isArray(body.data)) return body.data;\n    return Array.isArray(body) ? body : [];\n  }\n  return Array.isArray(body) ? body : [];\n}\nfunction getSourceRecordText(record) {\n  if (!record) return '';\n  if (sourceKind === 'nws') {\n    const p = record.properties || record;\n    return [p.headline, p.event, p.description, p.instruction, p.areaDesc, p.senderName, p.response].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'wp_json') {\n    return [record.title, record.excerpt, record.content, record.slug, record.link, record.status, record.date, record.modified].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'rss') {\n    return [record.title, record.description, record.link, record.pubDate, record.guid].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'html_table') {\n    return [record.title, record.pubDate, (record.cells || []).join(' | ')].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'transit_json') {\n    const header = record.header || record;\n    const fields = [header.header_text, header.description_text, header.cause, header.effect, header.url, header.alert_id, header.severity_level];\n    if (record.active_period) {\n      const ap = Array.isArray(record.active_period) ? record.active_period[0] : record.active_period;\n      if (ap) fields.push(ap.start, ap.end);\n    }\n    return fields.map(textOf).filter(Boolean).join(' | ');\n  }\n  return textOf(record);\n}\nfunction getSourceRecordId(record, fallbackIndex) {\n  if (!record) return String(fallbackIndex);\n  if (sourceKind === 'nws') {\n    const p = record.properties || record;\n    return String(p.id || p.cap_id || p.identifier || p.eventCode || p.sent || fallbackIndex);\n  }\n  if (sourceKind === 'wp_json') {\n    return String(record.id || record.post_id || record.slug || record.guid || fallbackIndex);\n  }\n  if (sourceKind === 'rss') {\n    return String(record.guid || record.link || record.title || fallbackIndex);\n  }\n  if (sourceKind === 'html_table') {\n    return String((record.cells || []).join('|') || record.title || fallbackIndex);\n  }\n  if (sourceKind === 'transit_json') {\n    const header = record.header || record;\n    return String(header.id || header.alert_id || header.entity_id || header.url || fallbackIndex);\n  }\n  return String(fallbackIndex);\n}\nfunction firstTimestamp(record, fields) {\n  for (const field of fields) {\n    const parts = String(field).split('.');\n    let value = record;\n    for (const part of parts) {\n      if (value && typeof value === 'object' && part in value) value = value[part];\n      else { value = undefined; break; }\n    }\n    if (value) {\n      const parsed = new Date(value);\n      if (!isNaN(parsed.getTime())) return parsed.toISOString();\n    }\n  }\n  return null;\n}\nfunction detectEventType(text) {\n  const lower = text.toLowerCase();\n  if (/boil[s-]*water/.test(lower)) return 'boil_water_notice';\n  if (/(evacuat|evacuation)/.test(lower)) return 'evacuation';\n  if (/shelter in place|lockdown/.test(lower)) return 'shelter_in_place';\n  if (/hazmat|spill|leak|toxic|gas/.test(lower)) return 'hazmat';\n  if (/dam/.test(lower)) return 'dam_incident';\n  if (/public health|health advisory|advisory|heat|smoke|air quality/.test(lower)) return 'public_health_advisory';\n  if (/civil emergency|public warning|emergency alert|blast|explosion/.test(lower)) return 'civil_emergency';\n  if (/police|incident|fire|rescue|closure/.test(lower)) return 'public_safety_incident';\n  return sourceKind === 'transit_json' ? 'alternate_transport' : 'public_safety_incident';\n}\nfunction detectSeverity(text, eventType) {\n  const lower = text.toLowerCase();\n  if (eventType === 'alternate_transport') return 'info';\n  if (eventType === 'evacuation' || eventType === 'civil_emergency' || eventType === 'hazmat' || eventType === 'dam_incident') return 'emergency';\n  if (/warning/.test(lower)) return 'warning';\n  if (/watch/.test(lower)) return 'watch';\n  if (/advisory|notice/.test(lower)) return 'advisory';\n  if (/resolved|expired/.test(lower)) return 'info';\n  return 'info';\n}\nfunction detectStatus(text) {\n  const lower = text.toLowerCase();\n  if (/resolved|ended|expired|cancelled|canceled/.test(lower)) return 'resolved';\n  if (/monitoring|watch/.test(lower)) return 'monitoring';\n  return 'active';\n}\nfunction routeSectionsForText(text) {\n  const lower = text.toLowerCase();\n  const sections = [];\n  const checks = [\n    ['WP1_UW', ['university of washington', 'u-district', 'university way ne', 'rainier vista', 'kane hall', 'burke-gilman trail', 'ne pacific st', 'ne 45th st', 'uw']],\n    ['WP2_KENMORE_LFP', ['kenmore', 'lake forest park']],\n    ['WP3_BOTHELL', ['bothell', 'bothell landing', 'sr-522', 'bothell way']],\n    ['WP4_WOODINVILLE', ['woodinville']],\n    ['WP5_REDMOND', ['redmond', 'marymoor']],\n    ['WP6_MARYMOOR_ELST', ['marymoor park', 'east lake sammamish trail', 'marymoor connector trail']],\n    ['WP7_SAMMAMISH', ['sammamish', 'east lake sammamish trail']],\n    ['WP8_ISSAQUAH', ['issaquah', 'i-90', 'issaquah approach']]\n  ];\n  for (const [sectionId, tokens] of checks) {\n    if (tokens.some(token => lower.includes(token))) sections.push(sectionId);\n  }\n  return Array.from(new Set(sections.length ? sections : routeSeedSections.filter(sectionId => lower.includes('king county') || lower.includes('seattle') || lower.includes('washington'))));\n}\nfunction classifyOwnership(text) {\n  const lower = text.toLowerCase();\n  if (sourceKind === 'transit_json') return 'observation';\n  if (/air quality|smoke|excessive heat|severe thunderstorm|high wind|flood watch|flood warning|wildfire/.test(lower) && !/evacuat|civil emergency|boil water|hazmat|shelter in place/.test(lower)) return 'observation';\n  if (/boil[s-]*water|evacuat|shelter in place|hazmat|civil emergency|public warning|dam incident|public health|health advisory|police|fire|incident|closure/.test(lower)) return 'event';\n  if (/uw|university of washington|seattle|bothell|redmond|sammamish|issaquah|kenmore|woodinville|marymoor|burke-gilman|east lake sammamish|lake forest park/.test(lower)) return 'event';\n  return 'observation';\n}\nfunction mapRouteRelevance(text, eventType, kind) {\n  const sections = routeSectionsForText(text);\n  const lower = text.toLowerCase();\n  const hasExplicitRouteToken = gazetteer.some(token => lower.includes(token.toLowerCase()));\n  const classification = sections.length ? (hasExplicitRouteToken ? 'confirmed_route_relevant' : 'possible_route_relevant') : 'not_route_relevant';\n  let method = 'route_point_query';\n  if (kind === 'transit_json') method = 'route_stop_mapping';\n  else if (kind === 'nws') method = hasExplicitRouteToken ? 'route_point_query' : 'cap_geometry_intersection';\n  else if (kind === 'html_table' || kind === 'rss' || kind === 'wp_json') method = hasExplicitRouteToken ? 'text_landmark_match' : 'ugc_same_area_match';\n  const manualReviewRequired = classification !== 'confirmed_route_relevant' || kind === 'rss' || kind === 'html_table';\n  const distanceKm = classification === 'confirmed_route_relevant' ? 0 : (classification === 'possible_route_relevant' ? 1.0 : null);\n  return {\n    classification,\n    method,\n    distance_km: distanceKm,\n    matched_tokens: gazetteer.filter(token => lower.includes(token.toLowerCase())),\n    bounding_box_match: sections.length > 0,\n    manual_review_required: manualReviewRequired,\n    reason: classification === 'confirmed_route_relevant' ? 'Strong local token match or direct route evidence.' : (classification === 'possible_route_relevant' ? 'Local proximity or shared locality only.' : 'No route token or direct route evidence found.')\n  };\n}\nfunction locationFromText(text, record) {\n  const lower = text.toLowerCase();\n  const namedArea = gazetteer.find(token => lower.includes(token.toLowerCase())) || null;\n  const point = record && record.geometry && Array.isArray(record.geometry.coordinates) ? record.geometry.coordinates : null;\n  if (point && point.length >= 2 && typeof point[0] === 'number' && typeof point[1] === 'number') {\n    return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: point[1], longitude: point[0], route_section_ids: routeSectionsForText(text) };\n  }\n  if (record && typeof record.x === 'number' && typeof record.y === 'number') {\n    return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: record.y, longitude: record.x, route_section_ids: routeSectionsForText(text) };\n  }\n  return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: null, longitude: null, route_section_ids: routeSectionsForText(text) };\n}\nfunction eventRouteImpact(eventType, relevanceClass, kind, text) {\n  if (kind === 'transit_json') return 'alternate_transport_only';\n  if (relevanceClass === 'confirmed_route_relevant') {\n    if (eventType === 'evacuation' || eventType === 'civil_emergency' || eventType === 'hazmat' || eventType === 'dam_incident' || eventType === 'boil_water_notice') return 'route_access_impacted';\n    return 'route_segment_impacted';\n  }\n  if (relevanceClass === 'possible_route_relevant') return 'manual_review_required';\n  if (/alternate transport/.test(text.toLowerCase())) return 'alternate_transport_only';\n  return 'no_known_route_impact';\n}\nfunction classifyCrossList(text) {\n  const lower = text.toLowerCase();\n  const cross = [];\n  if (/air quality|smoke/.test(lower)) cross.push('03_AIR_QUALITY');\n  if (/excessive heat|severe thunderstorm|high wind/.test(lower)) cross.push('02_WEATHER');\n  if (/flood/.test(lower)) cross.push('05_FLOOD_CONDITIONS');\n  if (/wildfire/.test(lower)) cross.push('04_WILDFIRE');\n  if (/trail closure|bridge closure|construction/.test(lower)) cross.push('01_ROUTE_CONDITIONS', '06_TRAIL_INFRASTRUCTURE_STATUS');\n  if (/alternate transport|transit|bus|service alert|route/.test(lower)) cross.push('02_WEATHER');\n  return Array.from(new Set(cross));\n}\nfunction buildEvent(record, index) {\n  const text = getSourceRecordText(record);\n  const eventType = detectEventType(text);\n  const relevance = mapRouteRelevance(text, eventType, sourceKind);\n  const sourceRecordRef = getSourceRecordId(record, index);\n  const recordHash = 'hash_' + hashString(text || JSON.stringify(record || null));\n  const observedAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.sent', 'properties.effective', 'properties.onset', 'properties.updated', 'properties.expires', 'properties.ends'] : sourceKind === 'wp_json' ? ['date', 'modified'] : sourceKind === 'rss' ? ['pubDate', 'updated'] : sourceKind === 'html_table' ? ['pubDate'] : ['header.created', 'header.updated', 'active_period.start', 'active_period.end']);\n  const effectiveAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.effective', 'properties.onset'] : ['effective_at', 'start', 'date', 'modified']);\n  const expiresAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.expires', 'properties.ends'] : ['expires_at', 'end']);\n  const location = locationFromText(text, record.geometry || record);\n  const routeImpact = eventRouteImpact(eventType, relevance.classification, sourceKind, text);\n  const severity = detectSeverity(text, eventType);\n  const status = detectStatus(text);\n  const isPrimary = relevance.classification !== 'not_route_relevant' && !classifyCrossList(text).length;\n  const officialCategory = sourceKind === 'nws' ? text.split(' | ')[1] || null : null;\n  const publicAction = sourceKind === 'nws' ? textOf(record.properties && record.properties.instruction) || null : null;\n  return {\n    event_id: sourceId + ':' + recordHash,\n    event_type: eventType,\n    status,\n    severity,\n    route_impact: routeImpact,\n    title: text.split(' | ')[0] || text,\n    summary: text,\n    source_id: sourceId,\n    source_event_ref: sourceRecordRef,\n    published_source_ids: [sourceId],\n    observed_at: observedAt || run.generated_at,\n    effective_at: effectiveAt,\n    expires_at: expiresAt,\n    official_category: officialCategory,\n    public_action: publicAction,\n    cross_listed_to: classifyCrossList(text),\n    location,\n    route_relevance: relevance,\n    provenance: {\n      source_url: sourceUrl,\n      retrieved_at: run.generated_at,\n      source_record_hash: recordHash,\n      normalization_version: 'v0002'\n    }\n  };\n}\nfunction buildObservation(record, index) {\n  const text = getSourceRecordText(record);\n  const sourceRecordRef = getSourceRecordId(record, index);\n  const recordHash = 'hash_' + hashString(text || JSON.stringify(record || null));\n  const sections = routeSectionsForText(text);\n  const location = locationFromText(text, record.geometry || record);\n  return {\n    observation_id: sourceId + ':obs:' + recordHash,\n    observation_type: sourceKind === 'transit_json' ? 'alternate_transport' : 'public_health_context',\n    title: text.split(' | ')[0] || text,\n    summary: text,\n    source_id: sourceId,\n    observed_at: firstTimestamp(record, sourceKind === 'nws' ? ['properties.sent', 'properties.updated'] : ['date', 'modified', 'pubDate']) || run.generated_at,\n    status: detectStatus(text),\n    route_section_ids: sections,\n    public_action: null,\n    provenance: {\n      source_url: sourceUrl,\n      retrieved_at: run.generated_at,\n      source_record_hash: recordHash,\n      normalization_version: 'v0002'\n    },\n    related_event_ids: []\n  };\n}\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst records = extractRecords(body);\nlet events = [];\nlet observations = [];\nlet parserWarnings = [];\nlet parserErrors = [];\nfor (let i = 0; i < records.length; i++) {\n  const record = records[i];\n  const text = getSourceRecordText(record);\n  const ownership = classifyOwnership(text);\n  const cross = classifyCrossList(text);\n  if (ownership === 'event' && cross.length && sourceKind !== 'transit_json') {\n    // Cross-listed items stay available as observations so lane 07 does not claim primary ownership.\n    observations.push(buildObservation(record, i));\n    continue;\n  }\n  if (ownership === 'event') {\n    events.push(buildEvent(record, i));\n  } else if (ownership === 'observation') {\n    observations.push(buildObservation(record, i));\n  }\n}\nif (!records.length && body && typeof body === 'object' && !Array.isArray(body)) {\n  parserWarnings.push('No structured records were extracted, but the source response was structurally valid.');\n}\nconst fetchError = fetch.error ? String(fetch.error.message || fetch.error) : (parserErrors.length ? parserErrors[0] : null);\nconst lkg = lkgLookup[sourceId];\nconst lkgRetrievedAt = lkg && lkg.source_health && lkg.source_health.retrieved_at ? Date.parse(lkg.source_health.retrieved_at) : NaN;\nconst lkgStaleAfterMinutes = lkg && lkg.source_health && typeof lkg.source_health.stale_after_minutes === 'number' ? lkg.source_health.stale_after_minutes : staleAfterMinutes;\nconst lkgUsable = !!fetchError && !!lkg && Number.isFinite(lkgRetrievedAt) && ((Date.now() - lkgRetrievedAt) / 60000) <= lkgStaleAfterMinutes && Array.isArray(lkg.source_health) === false;\nlet sourceHealth;\nlet finalEvents = events;\nlet finalObservations = observations;\nlet warnings = parserWarnings.slice();\nlet errors = parserErrors.slice();\nif (fetchError && lkg && Number.isFinite(lkgRetrievedAt) && ((Date.now() - lkgRetrievedAt) / 60000) <= lkgStaleAfterMinutes) {\n  finalEvents = Array.isArray(lkg.events) ? lkg.events : [];\n  finalObservations = Array.isArray(lkg.observations) ? lkg.observations : [];\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: 'using_last_known_good',\n    retrieved_at: lkg.source_health.retrieved_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: finalEvents.length,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: lkg.source_health.last_observation_at ?? lkg.source_health.retrieved_at,\n    warnings: [...(lkg.source_health.warnings || []), 'Live fetch failed; serving last-known-good data retrieved at ' + lkg.source_health.retrieved_at + '.'],\n    errors: [fetchError]\n  };\n} else if (fetchError) {\n  finalEvents = [];\n  finalObservations = [];\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: 'failed',\n    retrieved_at: run.generated_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: 0,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: run.generated_at,\n    warnings: warnings,\n    errors: [fetchError]\n  };\n} else {\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: finalEvents.length ? 'ok' : 'empty_but_valid',\n    retrieved_at: run.generated_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: finalEvents.length,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: finalObservations.length ? finalObservations[finalObservations.length - 1].observed_at : run.generated_at,\n    warnings: finalEvents.length ? warnings : [...warnings, 'No primary lane-07 events retained for this source in this run.'],\n    errors: []\n  };\n}\nconst rawLandingPath = landing.file_path || null;\nreturn [{ json: {\n  schema_version: '1.0.0',\n  connector_id: run.connector_id,\n  connector_name: run.connector_name,\n  connector_version: run.connector_version,\n  lane: run.lane,\n  run_id: run.run_id,\n  generated_at: run.generated_at,\n  source_id: sourceId,\n  source_name: sourceName,\n  source_url: sourceUrl,\n  source_kind: sourceKind,\n  raw_landing_path: rawLandingPath,\n  raw_content_hash: landing.content_hash || null,\n  source_health: sourceHealth,\n  events: finalEvents,\n  observations: finalObservations,\n  route_sections: Array.from(new Set([...new Set(finalEvents.flatMap(event => Array.isArray(event.location && event.location.route_section_ids) ? event.location.route_section_ids : [])), ...new Set(finalObservations.flatMap(observation => Array.isArray(observation.route_section_ids) ? observation.route_section_ids : []))])),\n  warnings,\n  errors\n} }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "fetch_sea_01_alerts",
        "name": "Fetch SEA-01 Alerts",
        "type": "n8n-nodes-base.httpRequest",
        "notes": "AlertSeattle public feed and WordPress API",
        "onError": "continueRegularOutput",
        "position": [
          220,
          640
        ],
        "parameters": {
          "url": "https://alert.seattle.gov/wp-json/wp/v2/posts?per_page=10",
          "method": "GET",
          "options": {
            "timeout": 30000
          },
          "responseFormat": "json"
        },
        "typeVersion": 4.2
      },
      {
        "id": "land_sea_01_raw_payload",
        "name": "Land SEA-01 Raw Payload",
        "type": "n8n-nodes-base.code",
        "position": [
          760,
          640
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch SEA-01 Alerts').first().json || {};\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst bodyString = typeof body === 'string' ? body : JSON.stringify(body || null);\nconst statusCode = fetch.statusCode ?? fetch.status ?? null;\nconst hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst contentHash = 'hash_' + hashString(bodyString);\nconst filePath = run.output_root + '/raw/07_GOVERNMENT_SAFETY_ALERTS/landings/SEA-01_landing_' + run.run_stamp + '.json';\nconst landing = {\n  schema_version: '1.0.0',\n  connector_id: run.connector_id,\n  source_id: '07_GOVERNMENT_SAFETY_ALERTS:SEA-01',\n  source_name: 'AlertSeattle public feed and WordPress API',\n  source_url: 'https://alert.seattle.gov/wp-json/wp/v2/posts?per_page=10',\n  retrieved_at: run.generated_at,\n  http_status: statusCode,\n  retrieval_method: 'wp_json',\n  content_hash: contentHash,\n  body_length: bodyString.length,\n  body: bodyString,\n  fetch_status: fetch.error ? 'failed' : 'ok',\n  fetch_error: fetch.error ? String(fetch.error.message || fetch.error) : null\n};\nconst buffer = Buffer.from(JSON.stringify(landing, null, 2), 'utf8');\nconst binary = await this.helpers.prepareBinaryData(buffer, filePath.split('/').pop(), 'application/json');\nreturn [{ json: {\n  file_path: filePath,\n  source_id: landing.source_id,\n  source_name: landing.source_name,\n  content_hash: contentHash,\n  retrieved_at: run.generated_at,\n  body_length: bodyString.length\n}, binary: { data: binary } }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "write_sea_01_raw_landing",
        "name": "Write SEA-01 Raw Landing",
        "type": "n8n-nodes-base.readWriteFile",
        "position": [
          1180,
          640
        ],
        "parameters": {
          "options": {},
          "fileName": "={{ $json.file_path }}",
          "operation": "write",
          "dataPropertyName": "data"
        },
        "typeVersion": 1
      },
      {
        "id": "normalize_sea_01_events",
        "name": "Normalize SEA-01 Events",
        "type": "n8n-nodes-base.code",
        "position": [
          1600,
          640
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch SEA-01 Alerts').first().json || {};\nconst landing = $('Land SEA-01 Raw Payload').first().json || {};\nconst lkgLookup = ($('Parse Last Known Good').first().json || {}).bySource || {};\nconst sourceId = '07_GOVERNMENT_SAFETY_ALERTS:SEA-01';\nconst sourceName = 'AlertSeattle public feed and WordPress API';\nconst sourceUrl = 'https://alert.seattle.gov/wp-json/wp/v2/posts?per_page=10';\nconst sourceKind = 'wp_json';\nconst routeSeedSections = [\"WP1_UW\"];\nconst staleAfterMinutes = 15;\nconst gazetteer = run.gazetteer || ['University of Washington', 'UW', 'U-District', 'University Way NE', 'Rainier Vista', 'Kane Hall', 'Burke-Gilman Trail', 'NE Pacific St', 'NE 45th St', 'Bothell Landing', 'Marymoor Park', 'East Lake Sammamish Trail', 'Kenmore', 'Lake Forest Park', 'Bothell', 'Woodinville', 'Redmond', 'Sammamish', 'Issaquah', 'SR-522', 'I-90', 'I-405', 'Seattle', 'Washington', 'King County'];\nconst now = Date.now();\nconst hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nfunction stripHtml(value) {\n  return String(value || '')\n    .replace(/<script[\\s\\S]*?<\\/script>/gi, ' ')\n    .replace(/<style[\\s\\S]*?<\\/style>/gi, ' ')\n    .replace(/<[^>]+>/g, ' ')\n    .replace(/&nbsp;/g, ' ')\n    .replace(/&amp;/g, '&')\n    .replace(/\\s+/g, ' ')\n    .trim();\n}\nfunction textOf(value) {\n  if (value === null || value === undefined) return '';\n  if (typeof value === 'string') return stripHtml(value);\n  if (Array.isArray(value)) return value.map(textOf).filter(Boolean).join(' | ');\n  if (typeof value === 'object') {\n    if (typeof value.rendered === 'string') return stripHtml(value.rendered);\n    if (typeof value.value === 'string') return stripHtml(value.value);\n    return stripHtml(JSON.stringify(value));\n  }\n  return String(value);\n}\nfunction safeJsonParse(value) {\n  if (typeof value !== 'string') return null;\n  try { return JSON.parse(value); } catch (error) { return null; }\n}\nfunction extractRecords(body) {\n  if (!body) return [];\n  if (sourceKind === 'nws') {\n    if (Array.isArray(body.features)) return body.features;\n    if (Array.isArray(body.items)) return body.items;\n    return [body];\n  }\n  if (sourceKind === 'wp_json') {\n    if (Array.isArray(body)) return body;\n    if (Array.isArray(body.posts)) return body.posts;\n    if (Array.isArray(body.items)) return body.items;\n    return [];\n  }\n  if (sourceKind === 'rss') {\n    const text = typeof body === 'string' ? body : textOf(body);\n    const matches = [...text.matchAll(/<item[\\s\\S]*?<\\/item>/gi)];\n    if (!matches.length) return [];\n    return matches.map((match, index) => {\n      const item = match[0];\n      const getTag = (tag) => {\n        const tagMatch = item.match(new RegExp('<' + tag + '[^>]*>([\\s\\S]*?)</' + tag + '>', 'i'));\n        return tagMatch ? stripHtml(tagMatch[1]) : '';\n      };\n      return { title: getTag('title'), link: getTag('link'), description: getTag('description'), pubDate: getTag('pubDate') || getTag('updated'), guid: getTag('guid') || getTag('link') || String(index) };\n    });\n  }\n  if (sourceKind === 'html_table') {\n    const text = typeof body === 'string' ? body : textOf(body);\n    const rows = [...text.matchAll(/<tr[\\s\\S]*?<\\/tr>/gi)];\n    return rows.map((match, index) => {\n      const row = match[0];\n      const cells = [...row.matchAll(/<t[dh][^>]*>([\\s\\S]*?)<\\/t[dh]>/gi)].map(m => stripHtml(m[1]));\n      return { cells, title: cells.join(' | '), link: '', pubDate: cells[0] || '', guid: cells.join('|') || String(index) };\n    });\n  }\n  if (sourceKind === 'transit_json') {\n    if (Array.isArray(body.entity)) return body.entity;\n    if (Array.isArray(body.entities)) return body.entities;\n    if (Array.isArray(body.alerts)) return body.alerts;\n    if (Array.isArray(body.data)) return body.data;\n    return Array.isArray(body) ? body : [];\n  }\n  return Array.isArray(body) ? body : [];\n}\nfunction getSourceRecordText(record) {\n  if (!record) return '';\n  if (sourceKind === 'nws') {\n    const p = record.properties || record;\n    return [p.headline, p.event, p.description, p.instruction, p.areaDesc, p.senderName, p.response].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'wp_json') {\n    return [record.title, record.excerpt, record.content, record.slug, record.link, record.status, record.date, record.modified].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'rss') {\n    return [record.title, record.description, record.link, record.pubDate, record.guid].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'html_table') {\n    return [record.title, record.pubDate, (record.cells || []).join(' | ')].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'transit_json') {\n    const header = record.header || record;\n    const fields = [header.header_text, header.description_text, header.cause, header.effect, header.url, header.alert_id, header.severity_level];\n    if (record.active_period) {\n      const ap = Array.isArray(record.active_period) ? record.active_period[0] : record.active_period;\n      if (ap) fields.push(ap.start, ap.end);\n    }\n    return fields.map(textOf).filter(Boolean).join(' | ');\n  }\n  return textOf(record);\n}\nfunction getSourceRecordId(record, fallbackIndex) {\n  if (!record) return String(fallbackIndex);\n  if (sourceKind === 'nws') {\n    const p = record.properties || record;\n    return String(p.id || p.cap_id || p.identifier || p.eventCode || p.sent || fallbackIndex);\n  }\n  if (sourceKind === 'wp_json') {\n    return String(record.id || record.post_id || record.slug || record.guid || fallbackIndex);\n  }\n  if (sourceKind === 'rss') {\n    return String(record.guid || record.link || record.title || fallbackIndex);\n  }\n  if (sourceKind === 'html_table') {\n    return String((record.cells || []).join('|') || record.title || fallbackIndex);\n  }\n  if (sourceKind === 'transit_json') {\n    const header = record.header || record;\n    return String(header.id || header.alert_id || header.entity_id || header.url || fallbackIndex);\n  }\n  return String(fallbackIndex);\n}\nfunction firstTimestamp(record, fields) {\n  for (const field of fields) {\n    const parts = String(field).split('.');\n    let value = record;\n    for (const part of parts) {\n      if (value && typeof value === 'object' && part in value) value = value[part];\n      else { value = undefined; break; }\n    }\n    if (value) {\n      const parsed = new Date(value);\n      if (!isNaN(parsed.getTime())) return parsed.toISOString();\n    }\n  }\n  return null;\n}\nfunction detectEventType(text) {\n  const lower = text.toLowerCase();\n  if (/boil[s-]*water/.test(lower)) return 'boil_water_notice';\n  if (/(evacuat|evacuation)/.test(lower)) return 'evacuation';\n  if (/shelter in place|lockdown/.test(lower)) return 'shelter_in_place';\n  if (/hazmat|spill|leak|toxic|gas/.test(lower)) return 'hazmat';\n  if (/dam/.test(lower)) return 'dam_incident';\n  if (/public health|health advisory|advisory|heat|smoke|air quality/.test(lower)) return 'public_health_advisory';\n  if (/civil emergency|public warning|emergency alert|blast|explosion/.test(lower)) return 'civil_emergency';\n  if (/police|incident|fire|rescue|closure/.test(lower)) return 'public_safety_incident';\n  return sourceKind === 'transit_json' ? 'alternate_transport' : 'public_safety_incident';\n}\nfunction detectSeverity(text, eventType) {\n  const lower = text.toLowerCase();\n  if (eventType === 'alternate_transport') return 'info';\n  if (eventType === 'evacuation' || eventType === 'civil_emergency' || eventType === 'hazmat' || eventType === 'dam_incident') return 'emergency';\n  if (/warning/.test(lower)) return 'warning';\n  if (/watch/.test(lower)) return 'watch';\n  if (/advisory|notice/.test(lower)) return 'advisory';\n  if (/resolved|expired/.test(lower)) return 'info';\n  return 'info';\n}\nfunction detectStatus(text) {\n  const lower = text.toLowerCase();\n  if (/resolved|ended|expired|cancelled|canceled/.test(lower)) return 'resolved';\n  if (/monitoring|watch/.test(lower)) return 'monitoring';\n  return 'active';\n}\nfunction routeSectionsForText(text) {\n  const lower = text.toLowerCase();\n  const sections = [];\n  const checks = [\n    ['WP1_UW', ['university of washington', 'u-district', 'university way ne', 'rainier vista', 'kane hall', 'burke-gilman trail', 'ne pacific st', 'ne 45th st', 'uw']],\n    ['WP2_KENMORE_LFP', ['kenmore', 'lake forest park']],\n    ['WP3_BOTHELL', ['bothell', 'bothell landing', 'sr-522', 'bothell way']],\n    ['WP4_WOODINVILLE', ['woodinville']],\n    ['WP5_REDMOND', ['redmond', 'marymoor']],\n    ['WP6_MARYMOOR_ELST', ['marymoor park', 'east lake sammamish trail', 'marymoor connector trail']],\n    ['WP7_SAMMAMISH', ['sammamish', 'east lake sammamish trail']],\n    ['WP8_ISSAQUAH', ['issaquah', 'i-90', 'issaquah approach']]\n  ];\n  for (const [sectionId, tokens] of checks) {\n    if (tokens.some(token => lower.includes(token))) sections.push(sectionId);\n  }\n  return Array.from(new Set(sections.length ? sections : routeSeedSections.filter(sectionId => lower.includes('king county') || lower.includes('seattle') || lower.includes('washington'))));\n}\nfunction classifyOwnership(text) {\n  const lower = text.toLowerCase();\n  if (sourceKind === 'transit_json') return 'observation';\n  if (/air quality|smoke|excessive heat|severe thunderstorm|high wind|flood watch|flood warning|wildfire/.test(lower) && !/evacuat|civil emergency|boil water|hazmat|shelter in place/.test(lower)) return 'observation';\n  if (/boil[s-]*water|evacuat|shelter in place|hazmat|civil emergency|public warning|dam incident|public health|health advisory|police|fire|incident|closure/.test(lower)) return 'event';\n  if (/uw|university of washington|seattle|bothell|redmond|sammamish|issaquah|kenmore|woodinville|marymoor|burke-gilman|east lake sammamish|lake forest park/.test(lower)) return 'event';\n  return 'observation';\n}\nfunction mapRouteRelevance(text, eventType, kind) {\n  const sections = routeSectionsForText(text);\n  const lower = text.toLowerCase();\n  const hasExplicitRouteToken = gazetteer.some(token => lower.includes(token.toLowerCase()));\n  const classification = sections.length ? (hasExplicitRouteToken ? 'confirmed_route_relevant' : 'possible_route_relevant') : 'not_route_relevant';\n  let method = 'text_landmark_match';\n  if (kind === 'transit_json') method = 'route_stop_mapping';\n  else if (kind === 'nws') method = hasExplicitRouteToken ? 'route_point_query' : 'cap_geometry_intersection';\n  else if (kind === 'html_table' || kind === 'rss' || kind === 'wp_json') method = hasExplicitRouteToken ? 'text_landmark_match' : 'ugc_same_area_match';\n  const manualReviewRequired = classification !== 'confirmed_route_relevant' || kind === 'rss' || kind === 'html_table';\n  const distanceKm = classification === 'confirmed_route_relevant' ? 0 : (classification === 'possible_route_relevant' ? 1.0 : null);\n  return {\n    classification,\n    method,\n    distance_km: distanceKm,\n    matched_tokens: gazetteer.filter(token => lower.includes(token.toLowerCase())),\n    bounding_box_match: sections.length > 0,\n    manual_review_required: manualReviewRequired,\n    reason: classification === 'confirmed_route_relevant' ? 'Strong local token match or direct route evidence.' : (classification === 'possible_route_relevant' ? 'Local proximity or shared locality only.' : 'No route token or direct route evidence found.')\n  };\n}\nfunction locationFromText(text, record) {\n  const lower = text.toLowerCase();\n  const namedArea = gazetteer.find(token => lower.includes(token.toLowerCase())) || null;\n  const point = record && record.geometry && Array.isArray(record.geometry.coordinates) ? record.geometry.coordinates : null;\n  if (point && point.length >= 2 && typeof point[0] === 'number' && typeof point[1] === 'number') {\n    return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: point[1], longitude: point[0], route_section_ids: routeSectionsForText(text) };\n  }\n  if (record && typeof record.x === 'number' && typeof record.y === 'number') {\n    return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: record.y, longitude: record.x, route_section_ids: routeSectionsForText(text) };\n  }\n  return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: null, longitude: null, route_section_ids: routeSectionsForText(text) };\n}\nfunction eventRouteImpact(eventType, relevanceClass, kind, text) {\n  if (kind === 'transit_json') return 'alternate_transport_only';\n  if (relevanceClass === 'confirmed_route_relevant') {\n    if (eventType === 'evacuation' || eventType === 'civil_emergency' || eventType === 'hazmat' || eventType === 'dam_incident' || eventType === 'boil_water_notice') return 'route_access_impacted';\n    return 'route_segment_impacted';\n  }\n  if (relevanceClass === 'possible_route_relevant') return 'manual_review_required';\n  if (/alternate transport/.test(text.toLowerCase())) return 'alternate_transport_only';\n  return 'no_known_route_impact';\n}\nfunction classifyCrossList(text) {\n  const lower = text.toLowerCase();\n  const cross = [];\n  if (/air quality|smoke/.test(lower)) cross.push('03_AIR_QUALITY');\n  if (/excessive heat|severe thunderstorm|high wind/.test(lower)) cross.push('02_WEATHER');\n  if (/flood/.test(lower)) cross.push('05_FLOOD_CONDITIONS');\n  if (/wildfire/.test(lower)) cross.push('04_WILDFIRE');\n  if (/trail closure|bridge closure|construction/.test(lower)) cross.push('01_ROUTE_CONDITIONS', '06_TRAIL_INFRASTRUCTURE_STATUS');\n  if (/alternate transport|transit|bus|service alert|route/.test(lower)) cross.push('02_WEATHER');\n  return Array.from(new Set(cross));\n}\nfunction buildEvent(record, index) {\n  const text = getSourceRecordText(record);\n  const eventType = detectEventType(text);\n  const relevance = mapRouteRelevance(text, eventType, sourceKind);\n  const sourceRecordRef = getSourceRecordId(record, index);\n  const recordHash = 'hash_' + hashString(text || JSON.stringify(record || null));\n  const observedAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.sent', 'properties.effective', 'properties.onset', 'properties.updated', 'properties.expires', 'properties.ends'] : sourceKind === 'wp_json' ? ['date', 'modified'] : sourceKind === 'rss' ? ['pubDate', 'updated'] : sourceKind === 'html_table' ? ['pubDate'] : ['header.created', 'header.updated', 'active_period.start', 'active_period.end']);\n  const effectiveAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.effective', 'properties.onset'] : ['effective_at', 'start', 'date', 'modified']);\n  const expiresAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.expires', 'properties.ends'] : ['expires_at', 'end']);\n  const location = locationFromText(text, record.geometry || record);\n  const routeImpact = eventRouteImpact(eventType, relevance.classification, sourceKind, text);\n  const severity = detectSeverity(text, eventType);\n  const status = detectStatus(text);\n  const isPrimary = relevance.classification !== 'not_route_relevant' && !classifyCrossList(text).length;\n  const officialCategory = sourceKind === 'nws' ? text.split(' | ')[1] || null : null;\n  const publicAction = sourceKind === 'nws' ? textOf(record.properties && record.properties.instruction) || null : null;\n  return {\n    event_id: sourceId + ':' + recordHash,\n    event_type: eventType,\n    status,\n    severity,\n    route_impact: routeImpact,\n    title: text.split(' | ')[0] || text,\n    summary: text,\n    source_id: sourceId,\n    source_event_ref: sourceRecordRef,\n    published_source_ids: [sourceId],\n    observed_at: observedAt || run.generated_at,\n    effective_at: effectiveAt,\n    expires_at: expiresAt,\n    official_category: officialCategory,\n    public_action: publicAction,\n    cross_listed_to: classifyCrossList(text),\n    location,\n    route_relevance: relevance,\n    provenance: {\n      source_url: sourceUrl,\n      retrieved_at: run.generated_at,\n      source_record_hash: recordHash,\n      normalization_version: 'v0002'\n    }\n  };\n}\nfunction buildObservation(record, index) {\n  const text = getSourceRecordText(record);\n  const sourceRecordRef = getSourceRecordId(record, index);\n  const recordHash = 'hash_' + hashString(text || JSON.stringify(record || null));\n  const sections = routeSectionsForText(text);\n  const location = locationFromText(text, record.geometry || record);\n  return {\n    observation_id: sourceId + ':obs:' + recordHash,\n    observation_type: sourceKind === 'transit_json' ? 'alternate_transport' : 'public_health_context',\n    title: text.split(' | ')[0] || text,\n    summary: text,\n    source_id: sourceId,\n    observed_at: firstTimestamp(record, sourceKind === 'nws' ? ['properties.sent', 'properties.updated'] : ['date', 'modified', 'pubDate']) || run.generated_at,\n    status: detectStatus(text),\n    route_section_ids: sections,\n    public_action: null,\n    provenance: {\n      source_url: sourceUrl,\n      retrieved_at: run.generated_at,\n      source_record_hash: recordHash,\n      normalization_version: 'v0002'\n    },\n    related_event_ids: []\n  };\n}\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst records = extractRecords(body);\nlet events = [];\nlet observations = [];\nlet parserWarnings = [];\nlet parserErrors = [];\nfor (let i = 0; i < records.length; i++) {\n  const record = records[i];\n  const text = getSourceRecordText(record);\n  const ownership = classifyOwnership(text);\n  const cross = classifyCrossList(text);\n  if (ownership === 'event' && cross.length && sourceKind !== 'transit_json') {\n    // Cross-listed items stay available as observations so lane 07 does not claim primary ownership.\n    observations.push(buildObservation(record, i));\n    continue;\n  }\n  if (ownership === 'event') {\n    events.push(buildEvent(record, i));\n  } else if (ownership === 'observation') {\n    observations.push(buildObservation(record, i));\n  }\n}\nif (!records.length && body && typeof body === 'object' && !Array.isArray(body)) {\n  parserWarnings.push('No structured records were extracted, but the source response was structurally valid.');\n}\nconst fetchError = fetch.error ? String(fetch.error.message || fetch.error) : (parserErrors.length ? parserErrors[0] : null);\nconst lkg = lkgLookup[sourceId];\nconst lkgRetrievedAt = lkg && lkg.source_health && lkg.source_health.retrieved_at ? Date.parse(lkg.source_health.retrieved_at) : NaN;\nconst lkgStaleAfterMinutes = lkg && lkg.source_health && typeof lkg.source_health.stale_after_minutes === 'number' ? lkg.source_health.stale_after_minutes : staleAfterMinutes;\nconst lkgUsable = !!fetchError && !!lkg && Number.isFinite(lkgRetrievedAt) && ((Date.now() - lkgRetrievedAt) / 60000) <= lkgStaleAfterMinutes && Array.isArray(lkg.source_health) === false;\nlet sourceHealth;\nlet finalEvents = events;\nlet finalObservations = observations;\nlet warnings = parserWarnings.slice();\nlet errors = parserErrors.slice();\nif (fetchError && lkg && Number.isFinite(lkgRetrievedAt) && ((Date.now() - lkgRetrievedAt) / 60000) <= lkgStaleAfterMinutes) {\n  finalEvents = Array.isArray(lkg.events) ? lkg.events : [];\n  finalObservations = Array.isArray(lkg.observations) ? lkg.observations : [];\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: 'using_last_known_good',\n    retrieved_at: lkg.source_health.retrieved_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: finalEvents.length,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: lkg.source_health.last_observation_at ?? lkg.source_health.retrieved_at,\n    warnings: [...(lkg.source_health.warnings || []), 'Live fetch failed; serving last-known-good data retrieved at ' + lkg.source_health.retrieved_at + '.'],\n    errors: [fetchError]\n  };\n} else if (fetchError) {\n  finalEvents = [];\n  finalObservations = [];\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: 'failed',\n    retrieved_at: run.generated_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: 0,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: run.generated_at,\n    warnings: warnings,\n    errors: [fetchError]\n  };\n} else {\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: finalEvents.length ? 'ok' : 'empty_but_valid',\n    retrieved_at: run.generated_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: finalEvents.length,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: finalObservations.length ? finalObservations[finalObservations.length - 1].observed_at : run.generated_at,\n    warnings: finalEvents.length ? warnings : [...warnings, 'No primary lane-07 events retained for this source in this run.'],\n    errors: []\n  };\n}\nconst rawLandingPath = landing.file_path || null;\nreturn [{ json: {\n  schema_version: '1.0.0',\n  connector_id: run.connector_id,\n  connector_name: run.connector_name,\n  connector_version: run.connector_version,\n  lane: run.lane,\n  run_id: run.run_id,\n  generated_at: run.generated_at,\n  source_id: sourceId,\n  source_name: sourceName,\n  source_url: sourceUrl,\n  source_kind: sourceKind,\n  raw_landing_path: rawLandingPath,\n  raw_content_hash: landing.content_hash || null,\n  source_health: sourceHealth,\n  events: finalEvents,\n  observations: finalObservations,\n  route_sections: Array.from(new Set([...new Set(finalEvents.flatMap(event => Array.isArray(event.location && event.location.route_section_ids) ? event.location.route_section_ids : [])), ...new Set(finalObservations.flatMap(observation => Array.isArray(observation.route_section_ids) ? observation.route_section_ids : []))])),\n  warnings,\n  errors\n} }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "fetch_uw_01_alerts",
        "name": "Fetch UW-01 Alerts",
        "type": "n8n-nodes-base.httpRequest",
        "notes": "UW Alert blog feed and WordPress API",
        "onError": "continueRegularOutput",
        "position": [
          220,
          860
        ],
        "parameters": {
          "url": "https://emergency.uw.edu/wp-json/wp/v2/posts?per_page=10",
          "method": "GET",
          "options": {
            "timeout": 30000
          },
          "responseFormat": "json"
        },
        "typeVersion": 4.2
      },
      {
        "id": "land_uw_01_raw_payload",
        "name": "Land UW-01 Raw Payload",
        "type": "n8n-nodes-base.code",
        "position": [
          760,
          860
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch UW-01 Alerts').first().json || {};\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst bodyString = typeof body === 'string' ? body : JSON.stringify(body || null);\nconst statusCode = fetch.statusCode ?? fetch.status ?? null;\nconst hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst contentHash = 'hash_' + hashString(bodyString);\nconst filePath = run.output_root + '/raw/07_GOVERNMENT_SAFETY_ALERTS/landings/UW-01_landing_' + run.run_stamp + '.json';\nconst landing = {\n  schema_version: '1.0.0',\n  connector_id: run.connector_id,\n  source_id: '07_GOVERNMENT_SAFETY_ALERTS:UW-01',\n  source_name: 'UW Alert blog feed and WordPress API',\n  source_url: 'https://emergency.uw.edu/wp-json/wp/v2/posts?per_page=10',\n  retrieved_at: run.generated_at,\n  http_status: statusCode,\n  retrieval_method: 'wp_json',\n  content_hash: contentHash,\n  body_length: bodyString.length,\n  body: bodyString,\n  fetch_status: fetch.error ? 'failed' : 'ok',\n  fetch_error: fetch.error ? String(fetch.error.message || fetch.error) : null\n};\nconst buffer = Buffer.from(JSON.stringify(landing, null, 2), 'utf8');\nconst binary = await this.helpers.prepareBinaryData(buffer, filePath.split('/').pop(), 'application/json');\nreturn [{ json: {\n  file_path: filePath,\n  source_id: landing.source_id,\n  source_name: landing.source_name,\n  content_hash: contentHash,\n  retrieved_at: run.generated_at,\n  body_length: bodyString.length\n}, binary: { data: binary } }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "write_uw_01_raw_landing",
        "name": "Write UW-01 Raw Landing",
        "type": "n8n-nodes-base.readWriteFile",
        "position": [
          1180,
          860
        ],
        "parameters": {
          "options": {},
          "fileName": "={{ $json.file_path }}",
          "operation": "write",
          "dataPropertyName": "data"
        },
        "typeVersion": 1
      },
      {
        "id": "normalize_uw_01_events",
        "name": "Normalize UW-01 Events",
        "type": "n8n-nodes-base.code",
        "position": [
          1600,
          860
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch UW-01 Alerts').first().json || {};\nconst landing = $('Land UW-01 Raw Payload').first().json || {};\nconst lkgLookup = ($('Parse Last Known Good').first().json || {}).bySource || {};\nconst sourceId = '07_GOVERNMENT_SAFETY_ALERTS:UW-01';\nconst sourceName = 'UW Alert blog feed and WordPress API';\nconst sourceUrl = 'https://emergency.uw.edu/wp-json/wp/v2/posts?per_page=10';\nconst sourceKind = 'wp_json';\nconst routeSeedSections = [\"WP1_UW\"];\nconst staleAfterMinutes = 15;\nconst gazetteer = run.gazetteer || ['University of Washington', 'UW', 'U-District', 'University Way NE', 'Rainier Vista', 'Kane Hall', 'Burke-Gilman Trail', 'NE Pacific St', 'NE 45th St', 'Bothell Landing', 'Marymoor Park', 'East Lake Sammamish Trail', 'Kenmore', 'Lake Forest Park', 'Bothell', 'Woodinville', 'Redmond', 'Sammamish', 'Issaquah', 'SR-522', 'I-90', 'I-405', 'Seattle', 'Washington', 'King County'];\nconst now = Date.now();\nconst hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nfunction stripHtml(value) {\n  return String(value || '')\n    .replace(/<script[\\s\\S]*?<\\/script>/gi, ' ')\n    .replace(/<style[\\s\\S]*?<\\/style>/gi, ' ')\n    .replace(/<[^>]+>/g, ' ')\n    .replace(/&nbsp;/g, ' ')\n    .replace(/&amp;/g, '&')\n    .replace(/\\s+/g, ' ')\n    .trim();\n}\nfunction textOf(value) {\n  if (value === null || value === undefined) return '';\n  if (typeof value === 'string') return stripHtml(value);\n  if (Array.isArray(value)) return value.map(textOf).filter(Boolean).join(' | ');\n  if (typeof value === 'object') {\n    if (typeof value.rendered === 'string') return stripHtml(value.rendered);\n    if (typeof value.value === 'string') return stripHtml(value.value);\n    return stripHtml(JSON.stringify(value));\n  }\n  return String(value);\n}\nfunction safeJsonParse(value) {\n  if (typeof value !== 'string') return null;\n  try { return JSON.parse(value); } catch (error) { return null; }\n}\nfunction extractRecords(body) {\n  if (!body) return [];\n  if (sourceKind === 'nws') {\n    if (Array.isArray(body.features)) return body.features;\n    if (Array.isArray(body.items)) return body.items;\n    return [body];\n  }\n  if (sourceKind === 'wp_json') {\n    if (Array.isArray(body)) return body;\n    if (Array.isArray(body.posts)) return body.posts;\n    if (Array.isArray(body.items)) return body.items;\n    return [];\n  }\n  if (sourceKind === 'rss') {\n    const text = typeof body === 'string' ? body : textOf(body);\n    const matches = [...text.matchAll(/<item[\\s\\S]*?<\\/item>/gi)];\n    if (!matches.length) return [];\n    return matches.map((match, index) => {\n      const item = match[0];\n      const getTag = (tag) => {\n        const tagMatch = item.match(new RegExp('<' + tag + '[^>]*>([\\s\\S]*?)</' + tag + '>', 'i'));\n        return tagMatch ? stripHtml(tagMatch[1]) : '';\n      };\n      return { title: getTag('title'), link: getTag('link'), description: getTag('description'), pubDate: getTag('pubDate') || getTag('updated'), guid: getTag('guid') || getTag('link') || String(index) };\n    });\n  }\n  if (sourceKind === 'html_table') {\n    const text = typeof body === 'string' ? body : textOf(body);\n    const rows = [...text.matchAll(/<tr[\\s\\S]*?<\\/tr>/gi)];\n    return rows.map((match, index) => {\n      const row = match[0];\n      const cells = [...row.matchAll(/<t[dh][^>]*>([\\s\\S]*?)<\\/t[dh]>/gi)].map(m => stripHtml(m[1]));\n      return { cells, title: cells.join(' | '), link: '', pubDate: cells[0] || '', guid: cells.join('|') || String(index) };\n    });\n  }\n  if (sourceKind === 'transit_json') {\n    if (Array.isArray(body.entity)) return body.entity;\n    if (Array.isArray(body.entities)) return body.entities;\n    if (Array.isArray(body.alerts)) return body.alerts;\n    if (Array.isArray(body.data)) return body.data;\n    return Array.isArray(body) ? body : [];\n  }\n  return Array.isArray(body) ? body : [];\n}\nfunction getSourceRecordText(record) {\n  if (!record) return '';\n  if (sourceKind === 'nws') {\n    const p = record.properties || record;\n    return [p.headline, p.event, p.description, p.instruction, p.areaDesc, p.senderName, p.response].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'wp_json') {\n    return [record.title, record.excerpt, record.content, record.slug, record.link, record.status, record.date, record.modified].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'rss') {\n    return [record.title, record.description, record.link, record.pubDate, record.guid].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'html_table') {\n    return [record.title, record.pubDate, (record.cells || []).join(' | ')].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'transit_json') {\n    const header = record.header || record;\n    const fields = [header.header_text, header.description_text, header.cause, header.effect, header.url, header.alert_id, header.severity_level];\n    if (record.active_period) {\n      const ap = Array.isArray(record.active_period) ? record.active_period[0] : record.active_period;\n      if (ap) fields.push(ap.start, ap.end);\n    }\n    return fields.map(textOf).filter(Boolean).join(' | ');\n  }\n  return textOf(record);\n}\nfunction getSourceRecordId(record, fallbackIndex) {\n  if (!record) return String(fallbackIndex);\n  if (sourceKind === 'nws') {\n    const p = record.properties || record;\n    return String(p.id || p.cap_id || p.identifier || p.eventCode || p.sent || fallbackIndex);\n  }\n  if (sourceKind === 'wp_json') {\n    return String(record.id || record.post_id || record.slug || record.guid || fallbackIndex);\n  }\n  if (sourceKind === 'rss') {\n    return String(record.guid || record.link || record.title || fallbackIndex);\n  }\n  if (sourceKind === 'html_table') {\n    return String((record.cells || []).join('|') || record.title || fallbackIndex);\n  }\n  if (sourceKind === 'transit_json') {\n    const header = record.header || record;\n    return String(header.id || header.alert_id || header.entity_id || header.url || fallbackIndex);\n  }\n  return String(fallbackIndex);\n}\nfunction firstTimestamp(record, fields) {\n  for (const field of fields) {\n    const parts = String(field).split('.');\n    let value = record;\n    for (const part of parts) {\n      if (value && typeof value === 'object' && part in value) value = value[part];\n      else { value = undefined; break; }\n    }\n    if (value) {\n      const parsed = new Date(value);\n      if (!isNaN(parsed.getTime())) return parsed.toISOString();\n    }\n  }\n  return null;\n}\nfunction detectEventType(text) {\n  const lower = text.toLowerCase();\n  if (/boil[s-]*water/.test(lower)) return 'boil_water_notice';\n  if (/(evacuat|evacuation)/.test(lower)) return 'evacuation';\n  if (/shelter in place|lockdown/.test(lower)) return 'shelter_in_place';\n  if (/hazmat|spill|leak|toxic|gas/.test(lower)) return 'hazmat';\n  if (/dam/.test(lower)) return 'dam_incident';\n  if (/public health|health advisory|advisory|heat|smoke|air quality/.test(lower)) return 'public_health_advisory';\n  if (/civil emergency|public warning|emergency alert|blast|explosion/.test(lower)) return 'civil_emergency';\n  if (/police|incident|fire|rescue|closure/.test(lower)) return 'public_safety_incident';\n  return sourceKind === 'transit_json' ? 'alternate_transport' : 'public_safety_incident';\n}\nfunction detectSeverity(text, eventType) {\n  const lower = text.toLowerCase();\n  if (eventType === 'alternate_transport') return 'info';\n  if (eventType === 'evacuation' || eventType === 'civil_emergency' || eventType === 'hazmat' || eventType === 'dam_incident') return 'emergency';\n  if (/warning/.test(lower)) return 'warning';\n  if (/watch/.test(lower)) return 'watch';\n  if (/advisory|notice/.test(lower)) return 'advisory';\n  if (/resolved|expired/.test(lower)) return 'info';\n  return 'info';\n}\nfunction detectStatus(text) {\n  const lower = text.toLowerCase();\n  if (/resolved|ended|expired|cancelled|canceled/.test(lower)) return 'resolved';\n  if (/monitoring|watch/.test(lower)) return 'monitoring';\n  return 'active';\n}\nfunction routeSectionsForText(text) {\n  const lower = text.toLowerCase();\n  const sections = [];\n  const checks = [\n    ['WP1_UW', ['university of washington', 'u-district', 'university way ne', 'rainier vista', 'kane hall', 'burke-gilman trail', 'ne pacific st', 'ne 45th st', 'uw']],\n    ['WP2_KENMORE_LFP', ['kenmore', 'lake forest park']],\n    ['WP3_BOTHELL', ['bothell', 'bothell landing', 'sr-522', 'bothell way']],\n    ['WP4_WOODINVILLE', ['woodinville']],\n    ['WP5_REDMOND', ['redmond', 'marymoor']],\n    ['WP6_MARYMOOR_ELST', ['marymoor park', 'east lake sammamish trail', 'marymoor connector trail']],\n    ['WP7_SAMMAMISH', ['sammamish', 'east lake sammamish trail']],\n    ['WP8_ISSAQUAH', ['issaquah', 'i-90', 'issaquah approach']]\n  ];\n  for (const [sectionId, tokens] of checks) {\n    if (tokens.some(token => lower.includes(token))) sections.push(sectionId);\n  }\n  return Array.from(new Set(sections.length ? sections : routeSeedSections.filter(sectionId => lower.includes('king county') || lower.includes('seattle') || lower.includes('washington'))));\n}\nfunction classifyOwnership(text) {\n  const lower = text.toLowerCase();\n  if (sourceKind === 'transit_json') return 'observation';\n  if (/air quality|smoke|excessive heat|severe thunderstorm|high wind|flood watch|flood warning|wildfire/.test(lower) && !/evacuat|civil emergency|boil water|hazmat|shelter in place/.test(lower)) return 'observation';\n  if (/boil[s-]*water|evacuat|shelter in place|hazmat|civil emergency|public warning|dam incident|public health|health advisory|police|fire|incident|closure/.test(lower)) return 'event';\n  if (/uw|university of washington|seattle|bothell|redmond|sammamish|issaquah|kenmore|woodinville|marymoor|burke-gilman|east lake sammamish|lake forest park/.test(lower)) return 'event';\n  return 'observation';\n}\nfunction mapRouteRelevance(text, eventType, kind) {\n  const sections = routeSectionsForText(text);\n  const lower = text.toLowerCase();\n  const hasExplicitRouteToken = gazetteer.some(token => lower.includes(token.toLowerCase()));\n  const classification = sections.length ? (hasExplicitRouteToken ? 'confirmed_route_relevant' : 'possible_route_relevant') : 'not_route_relevant';\n  let method = 'text_landmark_match';\n  if (kind === 'transit_json') method = 'route_stop_mapping';\n  else if (kind === 'nws') method = hasExplicitRouteToken ? 'route_point_query' : 'cap_geometry_intersection';\n  else if (kind === 'html_table' || kind === 'rss' || kind === 'wp_json') method = hasExplicitRouteToken ? 'text_landmark_match' : 'ugc_same_area_match';\n  const manualReviewRequired = classification !== 'confirmed_route_relevant' || kind === 'rss' || kind === 'html_table';\n  const distanceKm = classification === 'confirmed_route_relevant' ? 0 : (classification === 'possible_route_relevant' ? 1.0 : null);\n  return {\n    classification,\n    method,\n    distance_km: distanceKm,\n    matched_tokens: gazetteer.filter(token => lower.includes(token.toLowerCase())),\n    bounding_box_match: sections.length > 0,\n    manual_review_required: manualReviewRequired,\n    reason: classification === 'confirmed_route_relevant' ? 'Strong local token match or direct route evidence.' : (classification === 'possible_route_relevant' ? 'Local proximity or shared locality only.' : 'No route token or direct route evidence found.')\n  };\n}\nfunction locationFromText(text, record) {\n  const lower = text.toLowerCase();\n  const namedArea = gazetteer.find(token => lower.includes(token.toLowerCase())) || null;\n  const point = record && record.geometry && Array.isArray(record.geometry.coordinates) ? record.geometry.coordinates : null;\n  if (point && point.length >= 2 && typeof point[0] === 'number' && typeof point[1] === 'number') {\n    return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: point[1], longitude: point[0], route_section_ids: routeSectionsForText(text) };\n  }\n  if (record && typeof record.x === 'number' && typeof record.y === 'number') {\n    return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: record.y, longitude: record.x, route_section_ids: routeSectionsForText(text) };\n  }\n  return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: null, longitude: null, route_section_ids: routeSectionsForText(text) };\n}\nfunction eventRouteImpact(eventType, relevanceClass, kind, text) {\n  if (kind === 'transit_json') return 'alternate_transport_only';\n  if (relevanceClass === 'confirmed_route_relevant') {\n    if (eventType === 'evacuation' || eventType === 'civil_emergency' || eventType === 'hazmat' || eventType === 'dam_incident' || eventType === 'boil_water_notice') return 'route_access_impacted';\n    return 'route_segment_impacted';\n  }\n  if (relevanceClass === 'possible_route_relevant') return 'manual_review_required';\n  if (/alternate transport/.test(text.toLowerCase())) return 'alternate_transport_only';\n  return 'no_known_route_impact';\n}\nfunction classifyCrossList(text) {\n  const lower = text.toLowerCase();\n  const cross = [];\n  if (/air quality|smoke/.test(lower)) cross.push('03_AIR_QUALITY');\n  if (/excessive heat|severe thunderstorm|high wind/.test(lower)) cross.push('02_WEATHER');\n  if (/flood/.test(lower)) cross.push('05_FLOOD_CONDITIONS');\n  if (/wildfire/.test(lower)) cross.push('04_WILDFIRE');\n  if (/trail closure|bridge closure|construction/.test(lower)) cross.push('01_ROUTE_CONDITIONS', '06_TRAIL_INFRASTRUCTURE_STATUS');\n  if (/alternate transport|transit|bus|service alert|route/.test(lower)) cross.push('02_WEATHER');\n  return Array.from(new Set(cross));\n}\nfunction buildEvent(record, index) {\n  const text = getSourceRecordText(record);\n  const eventType = detectEventType(text);\n  const relevance = mapRouteRelevance(text, eventType, sourceKind);\n  const sourceRecordRef = getSourceRecordId(record, index);\n  const recordHash = 'hash_' + hashString(text || JSON.stringify(record || null));\n  const observedAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.sent', 'properties.effective', 'properties.onset', 'properties.updated', 'properties.expires', 'properties.ends'] : sourceKind === 'wp_json' ? ['date', 'modified'] : sourceKind === 'rss' ? ['pubDate', 'updated'] : sourceKind === 'html_table' ? ['pubDate'] : ['header.created', 'header.updated', 'active_period.start', 'active_period.end']);\n  const effectiveAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.effective', 'properties.onset'] : ['effective_at', 'start', 'date', 'modified']);\n  const expiresAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.expires', 'properties.ends'] : ['expires_at', 'end']);\n  const location = locationFromText(text, record.geometry || record);\n  const routeImpact = eventRouteImpact(eventType, relevance.classification, sourceKind, text);\n  const severity = detectSeverity(text, eventType);\n  const status = detectStatus(text);\n  const isPrimary = relevance.classification !== 'not_route_relevant' && !classifyCrossList(text).length;\n  const officialCategory = sourceKind === 'nws' ? text.split(' | ')[1] || null : null;\n  const publicAction = sourceKind === 'nws' ? textOf(record.properties && record.properties.instruction) || null : null;\n  return {\n    event_id: sourceId + ':' + recordHash,\n    event_type: eventType,\n    status,\n    severity,\n    route_impact: routeImpact,\n    title: text.split(' | ')[0] || text,\n    summary: text,\n    source_id: sourceId,\n    source_event_ref: sourceRecordRef,\n    published_source_ids: [sourceId],\n    observed_at: observedAt || run.generated_at,\n    effective_at: effectiveAt,\n    expires_at: expiresAt,\n    official_category: officialCategory,\n    public_action: publicAction,\n    cross_listed_to: classifyCrossList(text),\n    location,\n    route_relevance: relevance,\n    provenance: {\n      source_url: sourceUrl,\n      retrieved_at: run.generated_at,\n      source_record_hash: recordHash,\n      normalization_version: 'v0002'\n    }\n  };\n}\nfunction buildObservation(record, index) {\n  const text = getSourceRecordText(record);\n  const sourceRecordRef = getSourceRecordId(record, index);\n  const recordHash = 'hash_' + hashString(text || JSON.stringify(record || null));\n  const sections = routeSectionsForText(text);\n  const location = locationFromText(text, record.geometry || record);\n  return {\n    observation_id: sourceId + ':obs:' + recordHash,\n    observation_type: sourceKind === 'transit_json' ? 'alternate_transport' : 'public_health_context',\n    title: text.split(' | ')[0] || text,\n    summary: text,\n    source_id: sourceId,\n    observed_at: firstTimestamp(record, sourceKind === 'nws' ? ['properties.sent', 'properties.updated'] : ['date', 'modified', 'pubDate']) || run.generated_at,\n    status: detectStatus(text),\n    route_section_ids: sections,\n    public_action: null,\n    provenance: {\n      source_url: sourceUrl,\n      retrieved_at: run.generated_at,\n      source_record_hash: recordHash,\n      normalization_version: 'v0002'\n    },\n    related_event_ids: []\n  };\n}\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst records = extractRecords(body);\nlet events = [];\nlet observations = [];\nlet parserWarnings = [];\nlet parserErrors = [];\nfor (let i = 0; i < records.length; i++) {\n  const record = records[i];\n  const text = getSourceRecordText(record);\n  const ownership = classifyOwnership(text);\n  const cross = classifyCrossList(text);\n  if (ownership === 'event' && cross.length && sourceKind !== 'transit_json') {\n    // Cross-listed items stay available as observations so lane 07 does not claim primary ownership.\n    observations.push(buildObservation(record, i));\n    continue;\n  }\n  if (ownership === 'event') {\n    events.push(buildEvent(record, i));\n  } else if (ownership === 'observation') {\n    observations.push(buildObservation(record, i));\n  }\n}\nif (!records.length && body && typeof body === 'object' && !Array.isArray(body)) {\n  parserWarnings.push('No structured records were extracted, but the source response was structurally valid.');\n}\nconst fetchError = fetch.error ? String(fetch.error.message || fetch.error) : (parserErrors.length ? parserErrors[0] : null);\nconst lkg = lkgLookup[sourceId];\nconst lkgRetrievedAt = lkg && lkg.source_health && lkg.source_health.retrieved_at ? Date.parse(lkg.source_health.retrieved_at) : NaN;\nconst lkgStaleAfterMinutes = lkg && lkg.source_health && typeof lkg.source_health.stale_after_minutes === 'number' ? lkg.source_health.stale_after_minutes : staleAfterMinutes;\nconst lkgUsable = !!fetchError && !!lkg && Number.isFinite(lkgRetrievedAt) && ((Date.now() - lkgRetrievedAt) / 60000) <= lkgStaleAfterMinutes && Array.isArray(lkg.source_health) === false;\nlet sourceHealth;\nlet finalEvents = events;\nlet finalObservations = observations;\nlet warnings = parserWarnings.slice();\nlet errors = parserErrors.slice();\nif (fetchError && lkg && Number.isFinite(lkgRetrievedAt) && ((Date.now() - lkgRetrievedAt) / 60000) <= lkgStaleAfterMinutes) {\n  finalEvents = Array.isArray(lkg.events) ? lkg.events : [];\n  finalObservations = Array.isArray(lkg.observations) ? lkg.observations : [];\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: 'using_last_known_good',\n    retrieved_at: lkg.source_health.retrieved_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: finalEvents.length,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: lkg.source_health.last_observation_at ?? lkg.source_health.retrieved_at,\n    warnings: [...(lkg.source_health.warnings || []), 'Live fetch failed; serving last-known-good data retrieved at ' + lkg.source_health.retrieved_at + '.'],\n    errors: [fetchError]\n  };\n} else if (fetchError) {\n  finalEvents = [];\n  finalObservations = [];\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: 'failed',\n    retrieved_at: run.generated_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: 0,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: run.generated_at,\n    warnings: warnings,\n    errors: [fetchError]\n  };\n} else {\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: finalEvents.length ? 'ok' : 'empty_but_valid',\n    retrieved_at: run.generated_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: finalEvents.length,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: finalObservations.length ? finalObservations[finalObservations.length - 1].observed_at : run.generated_at,\n    warnings: finalEvents.length ? warnings : [...warnings, 'No primary lane-07 events retained for this source in this run.'],\n    errors: []\n  };\n}\nconst rawLandingPath = landing.file_path || null;\nreturn [{ json: {\n  schema_version: '1.0.0',\n  connector_id: run.connector_id,\n  connector_name: run.connector_name,\n  connector_version: run.connector_version,\n  lane: run.lane,\n  run_id: run.run_id,\n  generated_at: run.generated_at,\n  source_id: sourceId,\n  source_name: sourceName,\n  source_url: sourceUrl,\n  source_kind: sourceKind,\n  raw_landing_path: rawLandingPath,\n  raw_content_hash: landing.content_hash || null,\n  source_health: sourceHealth,\n  events: finalEvents,\n  observations: finalObservations,\n  route_sections: Array.from(new Set([...new Set(finalEvents.flatMap(event => Array.isArray(event.location && event.location.route_section_ids) ? event.location.route_section_ids : [])), ...new Set(finalObservations.flatMap(observation => Array.isArray(observation.route_section_ids) ? observation.route_section_ids : []))])),\n  warnings,\n  errors\n} }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "fetch_seafd_01_alerts",
        "name": "Fetch SEAFD-01 Alerts",
        "type": "n8n-nodes-base.httpRequest",
        "notes": "Seattle Fire Department Fireline RSS",
        "onError": "continueRegularOutput",
        "position": [
          220,
          1080
        ],
        "parameters": {
          "url": "https://fireline.seattle.gov/feed/",
          "method": "GET",
          "options": {
            "timeout": 30000
          },
          "responseFormat": "string"
        },
        "typeVersion": 4.2
      },
      {
        "id": "land_seafd_01_raw_payload",
        "name": "Land SEAFD-01 Raw Payload",
        "type": "n8n-nodes-base.code",
        "position": [
          760,
          1080
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch SEAFD-01 Alerts').first().json || {};\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst bodyString = typeof body === 'string' ? body : JSON.stringify(body || null);\nconst statusCode = fetch.statusCode ?? fetch.status ?? null;\nconst hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst contentHash = 'hash_' + hashString(bodyString);\nconst filePath = run.output_root + '/raw/07_GOVERNMENT_SAFETY_ALERTS/landings/SEAFD-01_landing_' + run.run_stamp + '.json';\nconst landing = {\n  schema_version: '1.0.0',\n  connector_id: run.connector_id,\n  source_id: '07_GOVERNMENT_SAFETY_ALERTS:SEAFD-01',\n  source_name: 'Seattle Fire Department Fireline RSS',\n  source_url: 'https://fireline.seattle.gov/feed/',\n  retrieved_at: run.generated_at,\n  http_status: statusCode,\n  retrieval_method: 'rss',\n  content_hash: contentHash,\n  body_length: bodyString.length,\n  body: bodyString,\n  fetch_status: fetch.error ? 'failed' : 'ok',\n  fetch_error: fetch.error ? String(fetch.error.message || fetch.error) : null\n};\nconst buffer = Buffer.from(JSON.stringify(landing, null, 2), 'utf8');\nconst binary = await this.helpers.prepareBinaryData(buffer, filePath.split('/').pop(), 'application/json');\nreturn [{ json: {\n  file_path: filePath,\n  source_id: landing.source_id,\n  source_name: landing.source_name,\n  content_hash: contentHash,\n  retrieved_at: run.generated_at,\n  body_length: bodyString.length\n}, binary: { data: binary } }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "write_seafd_01_raw_landing",
        "name": "Write SEAFD-01 Raw Landing",
        "type": "n8n-nodes-base.readWriteFile",
        "position": [
          1180,
          1080
        ],
        "parameters": {
          "options": {},
          "fileName": "={{ $json.file_path }}",
          "operation": "write",
          "dataPropertyName": "data"
        },
        "typeVersion": 1
      },
      {
        "id": "normalize_seafd_01_events",
        "name": "Normalize SEAFD-01 Events",
        "type": "n8n-nodes-base.code",
        "position": [
          1600,
          1080
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch SEAFD-01 Alerts').first().json || {};\nconst landing = $('Land SEAFD-01 Raw Payload').first().json || {};\nconst lkgLookup = ($('Parse Last Known Good').first().json || {}).bySource || {};\nconst sourceId = '07_GOVERNMENT_SAFETY_ALERTS:SEAFD-01';\nconst sourceName = 'Seattle Fire Department Fireline RSS';\nconst sourceUrl = 'https://fireline.seattle.gov/feed/';\nconst sourceKind = 'rss';\nconst routeSeedSections = [\"WP1_UW\"];\nconst staleAfterMinutes = 30;\nconst gazetteer = run.gazetteer || ['University of Washington', 'UW', 'U-District', 'University Way NE', 'Rainier Vista', 'Kane Hall', 'Burke-Gilman Trail', 'NE Pacific St', 'NE 45th St', 'Bothell Landing', 'Marymoor Park', 'East Lake Sammamish Trail', 'Kenmore', 'Lake Forest Park', 'Bothell', 'Woodinville', 'Redmond', 'Sammamish', 'Issaquah', 'SR-522', 'I-90', 'I-405', 'Seattle', 'Washington', 'King County'];\nconst now = Date.now();\nconst hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nfunction stripHtml(value) {\n  return String(value || '')\n    .replace(/<script[\\s\\S]*?<\\/script>/gi, ' ')\n    .replace(/<style[\\s\\S]*?<\\/style>/gi, ' ')\n    .replace(/<[^>]+>/g, ' ')\n    .replace(/&nbsp;/g, ' ')\n    .replace(/&amp;/g, '&')\n    .replace(/\\s+/g, ' ')\n    .trim();\n}\nfunction textOf(value) {\n  if (value === null || value === undefined) return '';\n  if (typeof value === 'string') return stripHtml(value);\n  if (Array.isArray(value)) return value.map(textOf).filter(Boolean).join(' | ');\n  if (typeof value === 'object') {\n    if (typeof value.rendered === 'string') return stripHtml(value.rendered);\n    if (typeof value.value === 'string') return stripHtml(value.value);\n    return stripHtml(JSON.stringify(value));\n  }\n  return String(value);\n}\nfunction safeJsonParse(value) {\n  if (typeof value !== 'string') return null;\n  try { return JSON.parse(value); } catch (error) { return null; }\n}\nfunction extractRecords(body) {\n  if (!body) return [];\n  if (sourceKind === 'nws') {\n    if (Array.isArray(body.features)) return body.features;\n    if (Array.isArray(body.items)) return body.items;\n    return [body];\n  }\n  if (sourceKind === 'wp_json') {\n    if (Array.isArray(body)) return body;\n    if (Array.isArray(body.posts)) return body.posts;\n    if (Array.isArray(body.items)) return body.items;\n    return [];\n  }\n  if (sourceKind === 'rss') {\n    const text = typeof body === 'string' ? body : textOf(body);\n    const matches = [...text.matchAll(/<item[\\s\\S]*?<\\/item>/gi)];\n    if (!matches.length) return [];\n    return matches.map((match, index) => {\n      const item = match[0];\n      const getTag = (tag) => {\n        const tagMatch = item.match(new RegExp('<' + tag + '[^>]*>([\\s\\S]*?)</' + tag + '>', 'i'));\n        return tagMatch ? stripHtml(tagMatch[1]) : '';\n      };\n      return { title: getTag('title'), link: getTag('link'), description: getTag('description'), pubDate: getTag('pubDate') || getTag('updated'), guid: getTag('guid') || getTag('link') || String(index) };\n    });\n  }\n  if (sourceKind === 'html_table') {\n    const text = typeof body === 'string' ? body : textOf(body);\n    const rows = [...text.matchAll(/<tr[\\s\\S]*?<\\/tr>/gi)];\n    return rows.map((match, index) => {\n      const row = match[0];\n      const cells = [...row.matchAll(/<t[dh][^>]*>([\\s\\S]*?)<\\/t[dh]>/gi)].map(m => stripHtml(m[1]));\n      return { cells, title: cells.join(' | '), link: '', pubDate: cells[0] || '', guid: cells.join('|') || String(index) };\n    });\n  }\n  if (sourceKind === 'transit_json') {\n    if (Array.isArray(body.entity)) return body.entity;\n    if (Array.isArray(body.entities)) return body.entities;\n    if (Array.isArray(body.alerts)) return body.alerts;\n    if (Array.isArray(body.data)) return body.data;\n    return Array.isArray(body) ? body : [];\n  }\n  return Array.isArray(body) ? body : [];\n}\nfunction getSourceRecordText(record) {\n  if (!record) return '';\n  if (sourceKind === 'nws') {\n    const p = record.properties || record;\n    return [p.headline, p.event, p.description, p.instruction, p.areaDesc, p.senderName, p.response].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'wp_json') {\n    return [record.title, record.excerpt, record.content, record.slug, record.link, record.status, record.date, record.modified].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'rss') {\n    return [record.title, record.description, record.link, record.pubDate, record.guid].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'html_table') {\n    return [record.title, record.pubDate, (record.cells || []).join(' | ')].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'transit_json') {\n    const header = record.header || record;\n    const fields = [header.header_text, header.description_text, header.cause, header.effect, header.url, header.alert_id, header.severity_level];\n    if (record.active_period) {\n      const ap = Array.isArray(record.active_period) ? record.active_period[0] : record.active_period;\n      if (ap) fields.push(ap.start, ap.end);\n    }\n    return fields.map(textOf).filter(Boolean).join(' | ');\n  }\n  return textOf(record);\n}\nfunction getSourceRecordId(record, fallbackIndex) {\n  if (!record) return String(fallbackIndex);\n  if (sourceKind === 'nws') {\n    const p = record.properties || record;\n    return String(p.id || p.cap_id || p.identifier || p.eventCode || p.sent || fallbackIndex);\n  }\n  if (sourceKind === 'wp_json') {\n    return String(record.id || record.post_id || record.slug || record.guid || fallbackIndex);\n  }\n  if (sourceKind === 'rss') {\n    return String(record.guid || record.link || record.title || fallbackIndex);\n  }\n  if (sourceKind === 'html_table') {\n    return String((record.cells || []).join('|') || record.title || fallbackIndex);\n  }\n  if (sourceKind === 'transit_json') {\n    const header = record.header || record;\n    return String(header.id || header.alert_id || header.entity_id || header.url || fallbackIndex);\n  }\n  return String(fallbackIndex);\n}\nfunction firstTimestamp(record, fields) {\n  for (const field of fields) {\n    const parts = String(field).split('.');\n    let value = record;\n    for (const part of parts) {\n      if (value && typeof value === 'object' && part in value) value = value[part];\n      else { value = undefined; break; }\n    }\n    if (value) {\n      const parsed = new Date(value);\n      if (!isNaN(parsed.getTime())) return parsed.toISOString();\n    }\n  }\n  return null;\n}\nfunction detectEventType(text) {\n  const lower = text.toLowerCase();\n  if (/boil[s-]*water/.test(lower)) return 'boil_water_notice';\n  if (/(evacuat|evacuation)/.test(lower)) return 'evacuation';\n  if (/shelter in place|lockdown/.test(lower)) return 'shelter_in_place';\n  if (/hazmat|spill|leak|toxic|gas/.test(lower)) return 'hazmat';\n  if (/dam/.test(lower)) return 'dam_incident';\n  if (/public health|health advisory|advisory|heat|smoke|air quality/.test(lower)) return 'public_health_advisory';\n  if (/civil emergency|public warning|emergency alert|blast|explosion/.test(lower)) return 'civil_emergency';\n  if (/police|incident|fire|rescue|closure/.test(lower)) return 'public_safety_incident';\n  return sourceKind === 'transit_json' ? 'alternate_transport' : 'public_safety_incident';\n}\nfunction detectSeverity(text, eventType) {\n  const lower = text.toLowerCase();\n  if (eventType === 'alternate_transport') return 'info';\n  if (eventType === 'evacuation' || eventType === 'civil_emergency' || eventType === 'hazmat' || eventType === 'dam_incident') return 'emergency';\n  if (/warning/.test(lower)) return 'warning';\n  if (/watch/.test(lower)) return 'watch';\n  if (/advisory|notice/.test(lower)) return 'advisory';\n  if (/resolved|expired/.test(lower)) return 'info';\n  return 'info';\n}\nfunction detectStatus(text) {\n  const lower = text.toLowerCase();\n  if (/resolved|ended|expired|cancelled|canceled/.test(lower)) return 'resolved';\n  if (/monitoring|watch/.test(lower)) return 'monitoring';\n  return 'active';\n}\nfunction routeSectionsForText(text) {\n  const lower = text.toLowerCase();\n  const sections = [];\n  const checks = [\n    ['WP1_UW', ['university of washington', 'u-district', 'university way ne', 'rainier vista', 'kane hall', 'burke-gilman trail', 'ne pacific st', 'ne 45th st', 'uw']],\n    ['WP2_KENMORE_LFP', ['kenmore', 'lake forest park']],\n    ['WP3_BOTHELL', ['bothell', 'bothell landing', 'sr-522', 'bothell way']],\n    ['WP4_WOODINVILLE', ['woodinville']],\n    ['WP5_REDMOND', ['redmond', 'marymoor']],\n    ['WP6_MARYMOOR_ELST', ['marymoor park', 'east lake sammamish trail', 'marymoor connector trail']],\n    ['WP7_SAMMAMISH', ['sammamish', 'east lake sammamish trail']],\n    ['WP8_ISSAQUAH', ['issaquah', 'i-90', 'issaquah approach']]\n  ];\n  for (const [sectionId, tokens] of checks) {\n    if (tokens.some(token => lower.includes(token))) sections.push(sectionId);\n  }\n  return Array.from(new Set(sections.length ? sections : routeSeedSections.filter(sectionId => lower.includes('king county') || lower.includes('seattle') || lower.includes('washington'))));\n}\nfunction classifyOwnership(text) {\n  const lower = text.toLowerCase();\n  if (sourceKind === 'transit_json') return 'observation';\n  if (/air quality|smoke|excessive heat|severe thunderstorm|high wind|flood watch|flood warning|wildfire/.test(lower) && !/evacuat|civil emergency|boil water|hazmat|shelter in place/.test(lower)) return 'observation';\n  if (/boil[s-]*water|evacuat|shelter in place|hazmat|civil emergency|public warning|dam incident|public health|health advisory|police|fire|incident|closure/.test(lower)) return 'event';\n  if (/uw|university of washington|seattle|bothell|redmond|sammamish|issaquah|kenmore|woodinville|marymoor|burke-gilman|east lake sammamish|lake forest park/.test(lower)) return 'event';\n  return 'observation';\n}\nfunction mapRouteRelevance(text, eventType, kind) {\n  const sections = routeSectionsForText(text);\n  const lower = text.toLowerCase();\n  const hasExplicitRouteToken = gazetteer.some(token => lower.includes(token.toLowerCase()));\n  const classification = sections.length ? (hasExplicitRouteToken ? 'confirmed_route_relevant' : 'possible_route_relevant') : 'not_route_relevant';\n  let method = 'text_landmark_match';\n  if (kind === 'transit_json') method = 'route_stop_mapping';\n  else if (kind === 'nws') method = hasExplicitRouteToken ? 'route_point_query' : 'cap_geometry_intersection';\n  else if (kind === 'html_table' || kind === 'rss' || kind === 'wp_json') method = hasExplicitRouteToken ? 'text_landmark_match' : 'ugc_same_area_match';\n  const manualReviewRequired = classification !== 'confirmed_route_relevant' || kind === 'rss' || kind === 'html_table';\n  const distanceKm = classification === 'confirmed_route_relevant' ? 0 : (classification === 'possible_route_relevant' ? 1.0 : null);\n  return {\n    classification,\n    method,\n    distance_km: distanceKm,\n    matched_tokens: gazetteer.filter(token => lower.includes(token.toLowerCase())),\n    bounding_box_match: sections.length > 0,\n    manual_review_required: manualReviewRequired,\n    reason: classification === 'confirmed_route_relevant' ? 'Strong local token match or direct route evidence.' : (classification === 'possible_route_relevant' ? 'Local proximity or shared locality only.' : 'No route token or direct route evidence found.')\n  };\n}\nfunction locationFromText(text, record) {\n  const lower = text.toLowerCase();\n  const namedArea = gazetteer.find(token => lower.includes(token.toLowerCase())) || null;\n  const point = record && record.geometry && Array.isArray(record.geometry.coordinates) ? record.geometry.coordinates : null;\n  if (point && point.length >= 2 && typeof point[0] === 'number' && typeof point[1] === 'number') {\n    return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: point[1], longitude: point[0], route_section_ids: routeSectionsForText(text) };\n  }\n  if (record && typeof record.x === 'number' && typeof record.y === 'number') {\n    return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: record.y, longitude: record.x, route_section_ids: routeSectionsForText(text) };\n  }\n  return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: null, longitude: null, route_section_ids: routeSectionsForText(text) };\n}\nfunction eventRouteImpact(eventType, relevanceClass, kind, text) {\n  if (kind === 'transit_json') return 'alternate_transport_only';\n  if (relevanceClass === 'confirmed_route_relevant') {\n    if (eventType === 'evacuation' || eventType === 'civil_emergency' || eventType === 'hazmat' || eventType === 'dam_incident' || eventType === 'boil_water_notice') return 'route_access_impacted';\n    return 'route_segment_impacted';\n  }\n  if (relevanceClass === 'possible_route_relevant') return 'manual_review_required';\n  if (/alternate transport/.test(text.toLowerCase())) return 'alternate_transport_only';\n  return 'no_known_route_impact';\n}\nfunction classifyCrossList(text) {\n  const lower = text.toLowerCase();\n  const cross = [];\n  if (/air quality|smoke/.test(lower)) cross.push('03_AIR_QUALITY');\n  if (/excessive heat|severe thunderstorm|high wind/.test(lower)) cross.push('02_WEATHER');\n  if (/flood/.test(lower)) cross.push('05_FLOOD_CONDITIONS');\n  if (/wildfire/.test(lower)) cross.push('04_WILDFIRE');\n  if (/trail closure|bridge closure|construction/.test(lower)) cross.push('01_ROUTE_CONDITIONS', '06_TRAIL_INFRASTRUCTURE_STATUS');\n  if (/alternate transport|transit|bus|service alert|route/.test(lower)) cross.push('02_WEATHER');\n  return Array.from(new Set(cross));\n}\nfunction buildEvent(record, index) {\n  const text = getSourceRecordText(record);\n  const eventType = detectEventType(text);\n  const relevance = mapRouteRelevance(text, eventType, sourceKind);\n  const sourceRecordRef = getSourceRecordId(record, index);\n  const recordHash = 'hash_' + hashString(text || JSON.stringify(record || null));\n  const observedAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.sent', 'properties.effective', 'properties.onset', 'properties.updated', 'properties.expires', 'properties.ends'] : sourceKind === 'wp_json' ? ['date', 'modified'] : sourceKind === 'rss' ? ['pubDate', 'updated'] : sourceKind === 'html_table' ? ['pubDate'] : ['header.created', 'header.updated', 'active_period.start', 'active_period.end']);\n  const effectiveAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.effective', 'properties.onset'] : ['effective_at', 'start', 'date', 'modified']);\n  const expiresAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.expires', 'properties.ends'] : ['expires_at', 'end']);\n  const location = locationFromText(text, record.geometry || record);\n  const routeImpact = eventRouteImpact(eventType, relevance.classification, sourceKind, text);\n  const severity = detectSeverity(text, eventType);\n  const status = detectStatus(text);\n  const isPrimary = relevance.classification !== 'not_route_relevant' && !classifyCrossList(text).length;\n  const officialCategory = sourceKind === 'nws' ? text.split(' | ')[1] || null : null;\n  const publicAction = sourceKind === 'nws' ? textOf(record.properties && record.properties.instruction) || null : null;\n  return {\n    event_id: sourceId + ':' + recordHash,\n    event_type: eventType,\n    status,\n    severity,\n    route_impact: routeImpact,\n    title: text.split(' | ')[0] || text,\n    summary: text,\n    source_id: sourceId,\n    source_event_ref: sourceRecordRef,\n    published_source_ids: [sourceId],\n    observed_at: observedAt || run.generated_at,\n    effective_at: effectiveAt,\n    expires_at: expiresAt,\n    official_category: officialCategory,\n    public_action: publicAction,\n    cross_listed_to: classifyCrossList(text),\n    location,\n    route_relevance: relevance,\n    provenance: {\n      source_url: sourceUrl,\n      retrieved_at: run.generated_at,\n      source_record_hash: recordHash,\n      normalization_version: 'v0002'\n    }\n  };\n}\nfunction buildObservation(record, index) {\n  const text = getSourceRecordText(record);\n  const sourceRecordRef = getSourceRecordId(record, index);\n  const recordHash = 'hash_' + hashString(text || JSON.stringify(record || null));\n  const sections = routeSectionsForText(text);\n  const location = locationFromText(text, record.geometry || record);\n  return {\n    observation_id: sourceId + ':obs:' + recordHash,\n    observation_type: sourceKind === 'transit_json' ? 'alternate_transport' : 'public_health_context',\n    title: text.split(' | ')[0] || text,\n    summary: text,\n    source_id: sourceId,\n    observed_at: firstTimestamp(record, sourceKind === 'nws' ? ['properties.sent', 'properties.updated'] : ['date', 'modified', 'pubDate']) || run.generated_at,\n    status: detectStatus(text),\n    route_section_ids: sections,\n    public_action: null,\n    provenance: {\n      source_url: sourceUrl,\n      retrieved_at: run.generated_at,\n      source_record_hash: recordHash,\n      normalization_version: 'v0002'\n    },\n    related_event_ids: []\n  };\n}\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst records = extractRecords(body);\nlet events = [];\nlet observations = [];\nlet parserWarnings = [];\nlet parserErrors = [];\nfor (let i = 0; i < records.length; i++) {\n  const record = records[i];\n  const text = getSourceRecordText(record);\n  const ownership = classifyOwnership(text);\n  const cross = classifyCrossList(text);\n  if (ownership === 'event' && cross.length && sourceKind !== 'transit_json') {\n    // Cross-listed items stay available as observations so lane 07 does not claim primary ownership.\n    observations.push(buildObservation(record, i));\n    continue;\n  }\n  if (ownership === 'event') {\n    events.push(buildEvent(record, i));\n  } else if (ownership === 'observation') {\n    observations.push(buildObservation(record, i));\n  }\n}\nif (!records.length && body && typeof body === 'object' && !Array.isArray(body)) {\n  parserWarnings.push('No structured records were extracted, but the source response was structurally valid.');\n}\nconst fetchError = fetch.error ? String(fetch.error.message || fetch.error) : (parserErrors.length ? parserErrors[0] : null);\nconst lkg = lkgLookup[sourceId];\nconst lkgRetrievedAt = lkg && lkg.source_health && lkg.source_health.retrieved_at ? Date.parse(lkg.source_health.retrieved_at) : NaN;\nconst lkgStaleAfterMinutes = lkg && lkg.source_health && typeof lkg.source_health.stale_after_minutes === 'number' ? lkg.source_health.stale_after_minutes : staleAfterMinutes;\nconst lkgUsable = !!fetchError && !!lkg && Number.isFinite(lkgRetrievedAt) && ((Date.now() - lkgRetrievedAt) / 60000) <= lkgStaleAfterMinutes && Array.isArray(lkg.source_health) === false;\nlet sourceHealth;\nlet finalEvents = events;\nlet finalObservations = observations;\nlet warnings = parserWarnings.slice();\nlet errors = parserErrors.slice();\nif (fetchError && lkg && Number.isFinite(lkgRetrievedAt) && ((Date.now() - lkgRetrievedAt) / 60000) <= lkgStaleAfterMinutes) {\n  finalEvents = Array.isArray(lkg.events) ? lkg.events : [];\n  finalObservations = Array.isArray(lkg.observations) ? lkg.observations : [];\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: 'using_last_known_good',\n    retrieved_at: lkg.source_health.retrieved_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: finalEvents.length,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: lkg.source_health.last_observation_at ?? lkg.source_health.retrieved_at,\n    warnings: [...(lkg.source_health.warnings || []), 'Live fetch failed; serving last-known-good data retrieved at ' + lkg.source_health.retrieved_at + '.'],\n    errors: [fetchError]\n  };\n} else if (fetchError) {\n  finalEvents = [];\n  finalObservations = [];\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: 'failed',\n    retrieved_at: run.generated_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: 0,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: run.generated_at,\n    warnings: warnings,\n    errors: [fetchError]\n  };\n} else {\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: finalEvents.length ? 'ok' : 'empty_but_valid',\n    retrieved_at: run.generated_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: finalEvents.length,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: finalObservations.length ? finalObservations[finalObservations.length - 1].observed_at : run.generated_at,\n    warnings: finalEvents.length ? warnings : [...warnings, 'No primary lane-07 events retained for this source in this run.'],\n    errors: []\n  };\n}\nconst rawLandingPath = landing.file_path || null;\nreturn [{ json: {\n  schema_version: '1.0.0',\n  connector_id: run.connector_id,\n  connector_name: run.connector_name,\n  connector_version: run.connector_version,\n  lane: run.lane,\n  run_id: run.run_id,\n  generated_at: run.generated_at,\n  source_id: sourceId,\n  source_name: sourceName,\n  source_url: sourceUrl,\n  source_kind: sourceKind,\n  raw_landing_path: rawLandingPath,\n  raw_content_hash: landing.content_hash || null,\n  source_health: sourceHealth,\n  events: finalEvents,\n  observations: finalObservations,\n  route_sections: Array.from(new Set([...new Set(finalEvents.flatMap(event => Array.isArray(event.location && event.location.route_section_ids) ? event.location.route_section_ids : [])), ...new Set(finalObservations.flatMap(observation => Array.isArray(observation.route_section_ids) ? observation.route_section_ids : []))])),\n  warnings,\n  errors\n} }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "fetch_seapd_01_alerts",
        "name": "Fetch SEAPD-01 Alerts",
        "type": "n8n-nodes-base.httpRequest",
        "notes": "Seattle Police Blotter RSS and Significant Incident Reports page",
        "onError": "continueRegularOutput",
        "position": [
          220,
          1300
        ],
        "parameters": {
          "url": "https://spdblotter.seattle.gov/feed/",
          "method": "GET",
          "options": {
            "timeout": 30000
          },
          "responseFormat": "string"
        },
        "typeVersion": 4.2
      },
      {
        "id": "land_seapd_01_raw_payload",
        "name": "Land SEAPD-01 Raw Payload",
        "type": "n8n-nodes-base.code",
        "position": [
          760,
          1300
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch SEAPD-01 Alerts').first().json || {};\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst bodyString = typeof body === 'string' ? body : JSON.stringify(body || null);\nconst statusCode = fetch.statusCode ?? fetch.status ?? null;\nconst hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst contentHash = 'hash_' + hashString(bodyString);\nconst filePath = run.output_root + '/raw/07_GOVERNMENT_SAFETY_ALERTS/landings/SEAPD-01_landing_' + run.run_stamp + '.json';\nconst landing = {\n  schema_version: '1.0.0',\n  connector_id: run.connector_id,\n  source_id: '07_GOVERNMENT_SAFETY_ALERTS:SEAPD-01',\n  source_name: 'Seattle Police Blotter RSS and Significant Incident Reports page',\n  source_url: 'https://spdblotter.seattle.gov/feed/',\n  retrieved_at: run.generated_at,\n  http_status: statusCode,\n  retrieval_method: 'rss',\n  content_hash: contentHash,\n  body_length: bodyString.length,\n  body: bodyString,\n  fetch_status: fetch.error ? 'failed' : 'ok',\n  fetch_error: fetch.error ? String(fetch.error.message || fetch.error) : null\n};\nconst buffer = Buffer.from(JSON.stringify(landing, null, 2), 'utf8');\nconst binary = await this.helpers.prepareBinaryData(buffer, filePath.split('/').pop(), 'application/json');\nreturn [{ json: {\n  file_path: filePath,\n  source_id: landing.source_id,\n  source_name: landing.source_name,\n  content_hash: contentHash,\n  retrieved_at: run.generated_at,\n  body_length: bodyString.length\n}, binary: { data: binary } }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "write_seapd_01_raw_landing",
        "name": "Write SEAPD-01 Raw Landing",
        "type": "n8n-nodes-base.readWriteFile",
        "position": [
          1180,
          1300
        ],
        "parameters": {
          "options": {},
          "fileName": "={{ $json.file_path }}",
          "operation": "write",
          "dataPropertyName": "data"
        },
        "typeVersion": 1
      },
      {
        "id": "normalize_seapd_01_events",
        "name": "Normalize SEAPD-01 Events",
        "type": "n8n-nodes-base.code",
        "position": [
          1600,
          1300
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch SEAPD-01 Alerts').first().json || {};\nconst landing = $('Land SEAPD-01 Raw Payload').first().json || {};\nconst lkgLookup = ($('Parse Last Known Good').first().json || {}).bySource || {};\nconst sourceId = '07_GOVERNMENT_SAFETY_ALERTS:SEAPD-01';\nconst sourceName = 'Seattle Police Blotter RSS and Significant Incident Reports page';\nconst sourceUrl = 'https://spdblotter.seattle.gov/feed/';\nconst sourceKind = 'rss';\nconst routeSeedSections = [\"WP1_UW\"];\nconst staleAfterMinutes = 30;\nconst gazetteer = run.gazetteer || ['University of Washington', 'UW', 'U-District', 'University Way NE', 'Rainier Vista', 'Kane Hall', 'Burke-Gilman Trail', 'NE Pacific St', 'NE 45th St', 'Bothell Landing', 'Marymoor Park', 'East Lake Sammamish Trail', 'Kenmore', 'Lake Forest Park', 'Bothell', 'Woodinville', 'Redmond', 'Sammamish', 'Issaquah', 'SR-522', 'I-90', 'I-405', 'Seattle', 'Washington', 'King County'];\nconst now = Date.now();\nconst hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nfunction stripHtml(value) {\n  return String(value || '')\n    .replace(/<script[\\s\\S]*?<\\/script>/gi, ' ')\n    .replace(/<style[\\s\\S]*?<\\/style>/gi, ' ')\n    .replace(/<[^>]+>/g, ' ')\n    .replace(/&nbsp;/g, ' ')\n    .replace(/&amp;/g, '&')\n    .replace(/\\s+/g, ' ')\n    .trim();\n}\nfunction textOf(value) {\n  if (value === null || value === undefined) return '';\n  if (typeof value === 'string') return stripHtml(value);\n  if (Array.isArray(value)) return value.map(textOf).filter(Boolean).join(' | ');\n  if (typeof value === 'object') {\n    if (typeof value.rendered === 'string') return stripHtml(value.rendered);\n    if (typeof value.value === 'string') return stripHtml(value.value);\n    return stripHtml(JSON.stringify(value));\n  }\n  return String(value);\n}\nfunction safeJsonParse(value) {\n  if (typeof value !== 'string') return null;\n  try { return JSON.parse(value); } catch (error) { return null; }\n}\nfunction extractRecords(body) {\n  if (!body) return [];\n  if (sourceKind === 'nws') {\n    if (Array.isArray(body.features)) return body.features;\n    if (Array.isArray(body.items)) return body.items;\n    return [body];\n  }\n  if (sourceKind === 'wp_json') {\n    if (Array.isArray(body)) return body;\n    if (Array.isArray(body.posts)) return body.posts;\n    if (Array.isArray(body.items)) return body.items;\n    return [];\n  }\n  if (sourceKind === 'rss') {\n    const text = typeof body === 'string' ? body : textOf(body);\n    const matches = [...text.matchAll(/<item[\\s\\S]*?<\\/item>/gi)];\n    if (!matches.length) return [];\n    return matches.map((match, index) => {\n      const item = match[0];\n      const getTag = (tag) => {\n        const tagMatch = item.match(new RegExp('<' + tag + '[^>]*>([\\s\\S]*?)</' + tag + '>', 'i'));\n        return tagMatch ? stripHtml(tagMatch[1]) : '';\n      };\n      return { title: getTag('title'), link: getTag('link'), description: getTag('description'), pubDate: getTag('pubDate') || getTag('updated'), guid: getTag('guid') || getTag('link') || String(index) };\n    });\n  }\n  if (sourceKind === 'html_table') {\n    const text = typeof body === 'string' ? body : textOf(body);\n    const rows = [...text.matchAll(/<tr[\\s\\S]*?<\\/tr>/gi)];\n    return rows.map((match, index) => {\n      const row = match[0];\n      const cells = [...row.matchAll(/<t[dh][^>]*>([\\s\\S]*?)<\\/t[dh]>/gi)].map(m => stripHtml(m[1]));\n      return { cells, title: cells.join(' | '), link: '', pubDate: cells[0] || '', guid: cells.join('|') || String(index) };\n    });\n  }\n  if (sourceKind === 'transit_json') {\n    if (Array.isArray(body.entity)) return body.entity;\n    if (Array.isArray(body.entities)) return body.entities;\n    if (Array.isArray(body.alerts)) return body.alerts;\n    if (Array.isArray(body.data)) return body.data;\n    return Array.isArray(body) ? body : [];\n  }\n  return Array.isArray(body) ? body : [];\n}\nfunction getSourceRecordText(record) {\n  if (!record) return '';\n  if (sourceKind === 'nws') {\n    const p = record.properties || record;\n    return [p.headline, p.event, p.description, p.instruction, p.areaDesc, p.senderName, p.response].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'wp_json') {\n    return [record.title, record.excerpt, record.content, record.slug, record.link, record.status, record.date, record.modified].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'rss') {\n    return [record.title, record.description, record.link, record.pubDate, record.guid].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'html_table') {\n    return [record.title, record.pubDate, (record.cells || []).join(' | ')].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'transit_json') {\n    const header = record.header || record;\n    const fields = [header.header_text, header.description_text, header.cause, header.effect, header.url, header.alert_id, header.severity_level];\n    if (record.active_period) {\n      const ap = Array.isArray(record.active_period) ? record.active_period[0] : record.active_period;\n      if (ap) fields.push(ap.start, ap.end);\n    }\n    return fields.map(textOf).filter(Boolean).join(' | ');\n  }\n  return textOf(record);\n}\nfunction getSourceRecordId(record, fallbackIndex) {\n  if (!record) return String(fallbackIndex);\n  if (sourceKind === 'nws') {\n    const p = record.properties || record;\n    return String(p.id || p.cap_id || p.identifier || p.eventCode || p.sent || fallbackIndex);\n  }\n  if (sourceKind === 'wp_json') {\n    return String(record.id || record.post_id || record.slug || record.guid || fallbackIndex);\n  }\n  if (sourceKind === 'rss') {\n    return String(record.guid || record.link || record.title || fallbackIndex);\n  }\n  if (sourceKind === 'html_table') {\n    return String((record.cells || []).join('|') || record.title || fallbackIndex);\n  }\n  if (sourceKind === 'transit_json') {\n    const header = record.header || record;\n    return String(header.id || header.alert_id || header.entity_id || header.url || fallbackIndex);\n  }\n  return String(fallbackIndex);\n}\nfunction firstTimestamp(record, fields) {\n  for (const field of fields) {\n    const parts = String(field).split('.');\n    let value = record;\n    for (const part of parts) {\n      if (value && typeof value === 'object' && part in value) value = value[part];\n      else { value = undefined; break; }\n    }\n    if (value) {\n      const parsed = new Date(value);\n      if (!isNaN(parsed.getTime())) return parsed.toISOString();\n    }\n  }\n  return null;\n}\nfunction detectEventType(text) {\n  const lower = text.toLowerCase();\n  if (/boil[s-]*water/.test(lower)) return 'boil_water_notice';\n  if (/(evacuat|evacuation)/.test(lower)) return 'evacuation';\n  if (/shelter in place|lockdown/.test(lower)) return 'shelter_in_place';\n  if (/hazmat|spill|leak|toxic|gas/.test(lower)) return 'hazmat';\n  if (/dam/.test(lower)) return 'dam_incident';\n  if (/public health|health advisory|advisory|heat|smoke|air quality/.test(lower)) return 'public_health_advisory';\n  if (/civil emergency|public warning|emergency alert|blast|explosion/.test(lower)) return 'civil_emergency';\n  if (/police|incident|fire|rescue|closure/.test(lower)) return 'public_safety_incident';\n  return sourceKind === 'transit_json' ? 'alternate_transport' : 'public_safety_incident';\n}\nfunction detectSeverity(text, eventType) {\n  const lower = text.toLowerCase();\n  if (eventType === 'alternate_transport') return 'info';\n  if (eventType === 'evacuation' || eventType === 'civil_emergency' || eventType === 'hazmat' || eventType === 'dam_incident') return 'emergency';\n  if (/warning/.test(lower)) return 'warning';\n  if (/watch/.test(lower)) return 'watch';\n  if (/advisory|notice/.test(lower)) return 'advisory';\n  if (/resolved|expired/.test(lower)) return 'info';\n  return 'info';\n}\nfunction detectStatus(text) {\n  const lower = text.toLowerCase();\n  if (/resolved|ended|expired|cancelled|canceled/.test(lower)) return 'resolved';\n  if (/monitoring|watch/.test(lower)) return 'monitoring';\n  return 'active';\n}\nfunction routeSectionsForText(text) {\n  const lower = text.toLowerCase();\n  const sections = [];\n  const checks = [\n    ['WP1_UW', ['university of washington', 'u-district', 'university way ne', 'rainier vista', 'kane hall', 'burke-gilman trail', 'ne pacific st', 'ne 45th st', 'uw']],\n    ['WP2_KENMORE_LFP', ['kenmore', 'lake forest park']],\n    ['WP3_BOTHELL', ['bothell', 'bothell landing', 'sr-522', 'bothell way']],\n    ['WP4_WOODINVILLE', ['woodinville']],\n    ['WP5_REDMOND', ['redmond', 'marymoor']],\n    ['WP6_MARYMOOR_ELST', ['marymoor park', 'east lake sammamish trail', 'marymoor connector trail']],\n    ['WP7_SAMMAMISH', ['sammamish', 'east lake sammamish trail']],\n    ['WP8_ISSAQUAH', ['issaquah', 'i-90', 'issaquah approach']]\n  ];\n  for (const [sectionId, tokens] of checks) {\n    if (tokens.some(token => lower.includes(token))) sections.push(sectionId);\n  }\n  return Array.from(new Set(sections.length ? sections : routeSeedSections.filter(sectionId => lower.includes('king county') || lower.includes('seattle') || lower.includes('washington'))));\n}\nfunction classifyOwnership(text) {\n  const lower = text.toLowerCase();\n  if (sourceKind === 'transit_json') return 'observation';\n  if (/air quality|smoke|excessive heat|severe thunderstorm|high wind|flood watch|flood warning|wildfire/.test(lower) && !/evacuat|civil emergency|boil water|hazmat|shelter in place/.test(lower)) return 'observation';\n  if (/boil[s-]*water|evacuat|shelter in place|hazmat|civil emergency|public warning|dam incident|public health|health advisory|police|fire|incident|closure/.test(lower)) return 'event';\n  if (/uw|university of washington|seattle|bothell|redmond|sammamish|issaquah|kenmore|woodinville|marymoor|burke-gilman|east lake sammamish|lake forest park/.test(lower)) return 'event';\n  return 'observation';\n}\nfunction mapRouteRelevance(text, eventType, kind) {\n  const sections = routeSectionsForText(text);\n  const lower = text.toLowerCase();\n  const hasExplicitRouteToken = gazetteer.some(token => lower.includes(token.toLowerCase()));\n  const classification = sections.length ? (hasExplicitRouteToken ? 'confirmed_route_relevant' : 'possible_route_relevant') : 'not_route_relevant';\n  let method = 'text_landmark_match';\n  if (kind === 'transit_json') method = 'route_stop_mapping';\n  else if (kind === 'nws') method = hasExplicitRouteToken ? 'route_point_query' : 'cap_geometry_intersection';\n  else if (kind === 'html_table' || kind === 'rss' || kind === 'wp_json') method = hasExplicitRouteToken ? 'text_landmark_match' : 'ugc_same_area_match';\n  const manualReviewRequired = classification !== 'confirmed_route_relevant' || kind === 'rss' || kind === 'html_table';\n  const distanceKm = classification === 'confirmed_route_relevant' ? 0 : (classification === 'possible_route_relevant' ? 1.0 : null);\n  return {\n    classification,\n    method,\n    distance_km: distanceKm,\n    matched_tokens: gazetteer.filter(token => lower.includes(token.toLowerCase())),\n    bounding_box_match: sections.length > 0,\n    manual_review_required: manualReviewRequired,\n    reason: classification === 'confirmed_route_relevant' ? 'Strong local token match or direct route evidence.' : (classification === 'possible_route_relevant' ? 'Local proximity or shared locality only.' : 'No route token or direct route evidence found.')\n  };\n}\nfunction locationFromText(text, record) {\n  const lower = text.toLowerCase();\n  const namedArea = gazetteer.find(token => lower.includes(token.toLowerCase())) || null;\n  const point = record && record.geometry && Array.isArray(record.geometry.coordinates) ? record.geometry.coordinates : null;\n  if (point && point.length >= 2 && typeof point[0] === 'number' && typeof point[1] === 'number') {\n    return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: point[1], longitude: point[0], route_section_ids: routeSectionsForText(text) };\n  }\n  if (record && typeof record.x === 'number' && typeof record.y === 'number') {\n    return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: record.y, longitude: record.x, route_section_ids: routeSectionsForText(text) };\n  }\n  return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: null, longitude: null, route_section_ids: routeSectionsForText(text) };\n}\nfunction eventRouteImpact(eventType, relevanceClass, kind, text) {\n  if (kind === 'transit_json') return 'alternate_transport_only';\n  if (relevanceClass === 'confirmed_route_relevant') {\n    if (eventType === 'evacuation' || eventType === 'civil_emergency' || eventType === 'hazmat' || eventType === 'dam_incident' || eventType === 'boil_water_notice') return 'route_access_impacted';\n    return 'route_segment_impacted';\n  }\n  if (relevanceClass === 'possible_route_relevant') return 'manual_review_required';\n  if (/alternate transport/.test(text.toLowerCase())) return 'alternate_transport_only';\n  return 'no_known_route_impact';\n}\nfunction classifyCrossList(text) {\n  const lower = text.toLowerCase();\n  const cross = [];\n  if (/air quality|smoke/.test(lower)) cross.push('03_AIR_QUALITY');\n  if (/excessive heat|severe thunderstorm|high wind/.test(lower)) cross.push('02_WEATHER');\n  if (/flood/.test(lower)) cross.push('05_FLOOD_CONDITIONS');\n  if (/wildfire/.test(lower)) cross.push('04_WILDFIRE');\n  if (/trail closure|bridge closure|construction/.test(lower)) cross.push('01_ROUTE_CONDITIONS', '06_TRAIL_INFRASTRUCTURE_STATUS');\n  if (/alternate transport|transit|bus|service alert|route/.test(lower)) cross.push('02_WEATHER');\n  return Array.from(new Set(cross));\n}\nfunction buildEvent(record, index) {\n  const text = getSourceRecordText(record);\n  const eventType = detectEventType(text);\n  const relevance = mapRouteRelevance(text, eventType, sourceKind);\n  const sourceRecordRef = getSourceRecordId(record, index);\n  const recordHash = 'hash_' + hashString(text || JSON.stringify(record || null));\n  const observedAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.sent', 'properties.effective', 'properties.onset', 'properties.updated', 'properties.expires', 'properties.ends'] : sourceKind === 'wp_json' ? ['date', 'modified'] : sourceKind === 'rss' ? ['pubDate', 'updated'] : sourceKind === 'html_table' ? ['pubDate'] : ['header.created', 'header.updated', 'active_period.start', 'active_period.end']);\n  const effectiveAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.effective', 'properties.onset'] : ['effective_at', 'start', 'date', 'modified']);\n  const expiresAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.expires', 'properties.ends'] : ['expires_at', 'end']);\n  const location = locationFromText(text, record.geometry || record);\n  const routeImpact = eventRouteImpact(eventType, relevance.classification, sourceKind, text);\n  const severity = detectSeverity(text, eventType);\n  const status = detectStatus(text);\n  const isPrimary = relevance.classification !== 'not_route_relevant' && !classifyCrossList(text).length;\n  const officialCategory = sourceKind === 'nws' ? text.split(' | ')[1] || null : null;\n  const publicAction = sourceKind === 'nws' ? textOf(record.properties && record.properties.instruction) || null : null;\n  return {\n    event_id: sourceId + ':' + recordHash,\n    event_type: eventType,\n    status,\n    severity,\n    route_impact: routeImpact,\n    title: text.split(' | ')[0] || text,\n    summary: text,\n    source_id: sourceId,\n    source_event_ref: sourceRecordRef,\n    published_source_ids: [sourceId],\n    observed_at: observedAt || run.generated_at,\n    effective_at: effectiveAt,\n    expires_at: expiresAt,\n    official_category: officialCategory,\n    public_action: publicAction,\n    cross_listed_to: classifyCrossList(text),\n    location,\n    route_relevance: relevance,\n    provenance: {\n      source_url: sourceUrl,\n      retrieved_at: run.generated_at,\n      source_record_hash: recordHash,\n      normalization_version: 'v0002'\n    }\n  };\n}\nfunction buildObservation(record, index) {\n  const text = getSourceRecordText(record);\n  const sourceRecordRef = getSourceRecordId(record, index);\n  const recordHash = 'hash_' + hashString(text || JSON.stringify(record || null));\n  const sections = routeSectionsForText(text);\n  const location = locationFromText(text, record.geometry || record);\n  return {\n    observation_id: sourceId + ':obs:' + recordHash,\n    observation_type: sourceKind === 'transit_json' ? 'alternate_transport' : 'public_health_context',\n    title: text.split(' | ')[0] || text,\n    summary: text,\n    source_id: sourceId,\n    observed_at: firstTimestamp(record, sourceKind === 'nws' ? ['properties.sent', 'properties.updated'] : ['date', 'modified', 'pubDate']) || run.generated_at,\n    status: detectStatus(text),\n    route_section_ids: sections,\n    public_action: null,\n    provenance: {\n      source_url: sourceUrl,\n      retrieved_at: run.generated_at,\n      source_record_hash: recordHash,\n      normalization_version: 'v0002'\n    },\n    related_event_ids: []\n  };\n}\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst records = extractRecords(body);\nlet events = [];\nlet observations = [];\nlet parserWarnings = [];\nlet parserErrors = [];\nfor (let i = 0; i < records.length; i++) {\n  const record = records[i];\n  const text = getSourceRecordText(record);\n  const ownership = classifyOwnership(text);\n  const cross = classifyCrossList(text);\n  if (ownership === 'event' && cross.length && sourceKind !== 'transit_json') {\n    // Cross-listed items stay available as observations so lane 07 does not claim primary ownership.\n    observations.push(buildObservation(record, i));\n    continue;\n  }\n  if (ownership === 'event') {\n    events.push(buildEvent(record, i));\n  } else if (ownership === 'observation') {\n    observations.push(buildObservation(record, i));\n  }\n}\nif (!records.length && body && typeof body === 'object' && !Array.isArray(body)) {\n  parserWarnings.push('No structured records were extracted, but the source response was structurally valid.');\n}\nconst fetchError = fetch.error ? String(fetch.error.message || fetch.error) : (parserErrors.length ? parserErrors[0] : null);\nconst lkg = lkgLookup[sourceId];\nconst lkgRetrievedAt = lkg && lkg.source_health && lkg.source_health.retrieved_at ? Date.parse(lkg.source_health.retrieved_at) : NaN;\nconst lkgStaleAfterMinutes = lkg && lkg.source_health && typeof lkg.source_health.stale_after_minutes === 'number' ? lkg.source_health.stale_after_minutes : staleAfterMinutes;\nconst lkgUsable = !!fetchError && !!lkg && Number.isFinite(lkgRetrievedAt) && ((Date.now() - lkgRetrievedAt) / 60000) <= lkgStaleAfterMinutes && Array.isArray(lkg.source_health) === false;\nlet sourceHealth;\nlet finalEvents = events;\nlet finalObservations = observations;\nlet warnings = parserWarnings.slice();\nlet errors = parserErrors.slice();\nif (fetchError && lkg && Number.isFinite(lkgRetrievedAt) && ((Date.now() - lkgRetrievedAt) / 60000) <= lkgStaleAfterMinutes) {\n  finalEvents = Array.isArray(lkg.events) ? lkg.events : [];\n  finalObservations = Array.isArray(lkg.observations) ? lkg.observations : [];\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: 'using_last_known_good',\n    retrieved_at: lkg.source_health.retrieved_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: finalEvents.length,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: lkg.source_health.last_observation_at ?? lkg.source_health.retrieved_at,\n    warnings: [...(lkg.source_health.warnings || []), 'Live fetch failed; serving last-known-good data retrieved at ' + lkg.source_health.retrieved_at + '.'],\n    errors: [fetchError]\n  };\n} else if (fetchError) {\n  finalEvents = [];\n  finalObservations = [];\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: 'failed',\n    retrieved_at: run.generated_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: 0,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: run.generated_at,\n    warnings: warnings,\n    errors: [fetchError]\n  };\n} else {\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: finalEvents.length ? 'ok' : 'empty_but_valid',\n    retrieved_at: run.generated_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: finalEvents.length,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: finalObservations.length ? finalObservations[finalObservations.length - 1].observed_at : run.generated_at,\n    warnings: finalEvents.length ? warnings : [...warnings, 'No primary lane-07 events retained for this source in this run.'],\n    errors: []\n  };\n}\nconst rawLandingPath = landing.file_path || null;\nreturn [{ json: {\n  schema_version: '1.0.0',\n  connector_id: run.connector_id,\n  connector_name: run.connector_name,\n  connector_version: run.connector_version,\n  lane: run.lane,\n  run_id: run.run_id,\n  generated_at: run.generated_at,\n  source_id: sourceId,\n  source_name: sourceName,\n  source_url: sourceUrl,\n  source_kind: sourceKind,\n  raw_landing_path: rawLandingPath,\n  raw_content_hash: landing.content_hash || null,\n  source_health: sourceHealth,\n  events: finalEvents,\n  observations: finalObservations,\n  route_sections: Array.from(new Set([...new Set(finalEvents.flatMap(event => Array.isArray(event.location && event.location.route_section_ids) ? event.location.route_section_ids : [])), ...new Set(finalObservations.flatMap(observation => Array.isArray(observation.route_section_ids) ? observation.route_section_ids : []))])),\n  warnings,\n  errors\n} }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "fetch_doh_02_alerts",
        "name": "Fetch DOH-02 Alerts",
        "type": "n8n-nodes-base.httpRequest",
        "notes": "Washington Health Alert Network public table",
        "onError": "continueRegularOutput",
        "position": [
          220,
          1520
        ],
        "parameters": {
          "url": "https://doh.wa.gov/public-health-provider-resources/washington-health-alert-network",
          "method": "GET",
          "options": {
            "timeout": 30000
          },
          "responseFormat": "string"
        },
        "typeVersion": 4.2
      },
      {
        "id": "land_doh_02_raw_payload",
        "name": "Land DOH-02 Raw Payload",
        "type": "n8n-nodes-base.code",
        "position": [
          760,
          1520
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch DOH-02 Alerts').first().json || {};\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst bodyString = typeof body === 'string' ? body : JSON.stringify(body || null);\nconst statusCode = fetch.statusCode ?? fetch.status ?? null;\nconst hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst contentHash = 'hash_' + hashString(bodyString);\nconst filePath = run.output_root + '/raw/07_GOVERNMENT_SAFETY_ALERTS/landings/DOH-02_landing_' + run.run_stamp + '.json';\nconst landing = {\n  schema_version: '1.0.0',\n  connector_id: run.connector_id,\n  source_id: '07_GOVERNMENT_SAFETY_ALERTS:DOH-02',\n  source_name: 'Washington Health Alert Network public table',\n  source_url: 'https://doh.wa.gov/public-health-provider-resources/washington-health-alert-network',\n  retrieved_at: run.generated_at,\n  http_status: statusCode,\n  retrieval_method: 'html_table',\n  content_hash: contentHash,\n  body_length: bodyString.length,\n  body: bodyString,\n  fetch_status: fetch.error ? 'failed' : 'ok',\n  fetch_error: fetch.error ? String(fetch.error.message || fetch.error) : null\n};\nconst buffer = Buffer.from(JSON.stringify(landing, null, 2), 'utf8');\nconst binary = await this.helpers.prepareBinaryData(buffer, filePath.split('/').pop(), 'application/json');\nreturn [{ json: {\n  file_path: filePath,\n  source_id: landing.source_id,\n  source_name: landing.source_name,\n  content_hash: contentHash,\n  retrieved_at: run.generated_at,\n  body_length: bodyString.length\n}, binary: { data: binary } }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "write_doh_02_raw_landing",
        "name": "Write DOH-02 Raw Landing",
        "type": "n8n-nodes-base.readWriteFile",
        "position": [
          1180,
          1520
        ],
        "parameters": {
          "options": {},
          "fileName": "={{ $json.file_path }}",
          "operation": "write",
          "dataPropertyName": "data"
        },
        "typeVersion": 1
      },
      {
        "id": "normalize_doh_02_events",
        "name": "Normalize DOH-02 Events",
        "type": "n8n-nodes-base.code",
        "position": [
          1600,
          1520
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch DOH-02 Alerts').first().json || {};\nconst landing = $('Land DOH-02 Raw Payload').first().json || {};\nconst lkgLookup = ($('Parse Last Known Good').first().json || {}).bySource || {};\nconst sourceId = '07_GOVERNMENT_SAFETY_ALERTS:DOH-02';\nconst sourceName = 'Washington Health Alert Network public table';\nconst sourceUrl = 'https://doh.wa.gov/public-health-provider-resources/washington-health-alert-network';\nconst sourceKind = 'html_table';\nconst routeSeedSections = [\"WP1_UW\",\"WP2_KENMORE_LFP\",\"WP3_BOTHELL\",\"WP4_WOODINVILLE\",\"WP5_REDMOND\",\"WP6_MARYMOOR_ELST\",\"WP7_SAMMAMISH\",\"WP8_ISSAQUAH\"];\nconst staleAfterMinutes = 360;\nconst gazetteer = run.gazetteer || ['University of Washington', 'UW', 'U-District', 'University Way NE', 'Rainier Vista', 'Kane Hall', 'Burke-Gilman Trail', 'NE Pacific St', 'NE 45th St', 'Bothell Landing', 'Marymoor Park', 'East Lake Sammamish Trail', 'Kenmore', 'Lake Forest Park', 'Bothell', 'Woodinville', 'Redmond', 'Sammamish', 'Issaquah', 'SR-522', 'I-90', 'I-405', 'Seattle', 'Washington', 'King County'];\nconst now = Date.now();\nconst hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nfunction stripHtml(value) {\n  return String(value || '')\n    .replace(/<script[\\s\\S]*?<\\/script>/gi, ' ')\n    .replace(/<style[\\s\\S]*?<\\/style>/gi, ' ')\n    .replace(/<[^>]+>/g, ' ')\n    .replace(/&nbsp;/g, ' ')\n    .replace(/&amp;/g, '&')\n    .replace(/\\s+/g, ' ')\n    .trim();\n}\nfunction textOf(value) {\n  if (value === null || value === undefined) return '';\n  if (typeof value === 'string') return stripHtml(value);\n  if (Array.isArray(value)) return value.map(textOf).filter(Boolean).join(' | ');\n  if (typeof value === 'object') {\n    if (typeof value.rendered === 'string') return stripHtml(value.rendered);\n    if (typeof value.value === 'string') return stripHtml(value.value);\n    return stripHtml(JSON.stringify(value));\n  }\n  return String(value);\n}\nfunction safeJsonParse(value) {\n  if (typeof value !== 'string') return null;\n  try { return JSON.parse(value); } catch (error) { return null; }\n}\nfunction extractRecords(body) {\n  if (!body) return [];\n  if (sourceKind === 'nws') {\n    if (Array.isArray(body.features)) return body.features;\n    if (Array.isArray(body.items)) return body.items;\n    return [body];\n  }\n  if (sourceKind === 'wp_json') {\n    if (Array.isArray(body)) return body;\n    if (Array.isArray(body.posts)) return body.posts;\n    if (Array.isArray(body.items)) return body.items;\n    return [];\n  }\n  if (sourceKind === 'rss') {\n    const text = typeof body === 'string' ? body : textOf(body);\n    const matches = [...text.matchAll(/<item[\\s\\S]*?<\\/item>/gi)];\n    if (!matches.length) return [];\n    return matches.map((match, index) => {\n      const item = match[0];\n      const getTag = (tag) => {\n        const tagMatch = item.match(new RegExp('<' + tag + '[^>]*>([\\s\\S]*?)</' + tag + '>', 'i'));\n        return tagMatch ? stripHtml(tagMatch[1]) : '';\n      };\n      return { title: getTag('title'), link: getTag('link'), description: getTag('description'), pubDate: getTag('pubDate') || getTag('updated'), guid: getTag('guid') || getTag('link') || String(index) };\n    });\n  }\n  if (sourceKind === 'html_table') {\n    const text = typeof body === 'string' ? body : textOf(body);\n    const rows = [...text.matchAll(/<tr[\\s\\S]*?<\\/tr>/gi)];\n    return rows.map((match, index) => {\n      const row = match[0];\n      const cells = [...row.matchAll(/<t[dh][^>]*>([\\s\\S]*?)<\\/t[dh]>/gi)].map(m => stripHtml(m[1]));\n      return { cells, title: cells.join(' | '), link: '', pubDate: cells[0] || '', guid: cells.join('|') || String(index) };\n    });\n  }\n  if (sourceKind === 'transit_json') {\n    if (Array.isArray(body.entity)) return body.entity;\n    if (Array.isArray(body.entities)) return body.entities;\n    if (Array.isArray(body.alerts)) return body.alerts;\n    if (Array.isArray(body.data)) return body.data;\n    return Array.isArray(body) ? body : [];\n  }\n  return Array.isArray(body) ? body : [];\n}\nfunction getSourceRecordText(record) {\n  if (!record) return '';\n  if (sourceKind === 'nws') {\n    const p = record.properties || record;\n    return [p.headline, p.event, p.description, p.instruction, p.areaDesc, p.senderName, p.response].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'wp_json') {\n    return [record.title, record.excerpt, record.content, record.slug, record.link, record.status, record.date, record.modified].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'rss') {\n    return [record.title, record.description, record.link, record.pubDate, record.guid].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'html_table') {\n    return [record.title, record.pubDate, (record.cells || []).join(' | ')].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'transit_json') {\n    const header = record.header || record;\n    const fields = [header.header_text, header.description_text, header.cause, header.effect, header.url, header.alert_id, header.severity_level];\n    if (record.active_period) {\n      const ap = Array.isArray(record.active_period) ? record.active_period[0] : record.active_period;\n      if (ap) fields.push(ap.start, ap.end);\n    }\n    return fields.map(textOf).filter(Boolean).join(' | ');\n  }\n  return textOf(record);\n}\nfunction getSourceRecordId(record, fallbackIndex) {\n  if (!record) return String(fallbackIndex);\n  if (sourceKind === 'nws') {\n    const p = record.properties || record;\n    return String(p.id || p.cap_id || p.identifier || p.eventCode || p.sent || fallbackIndex);\n  }\n  if (sourceKind === 'wp_json') {\n    return String(record.id || record.post_id || record.slug || record.guid || fallbackIndex);\n  }\n  if (sourceKind === 'rss') {\n    return String(record.guid || record.link || record.title || fallbackIndex);\n  }\n  if (sourceKind === 'html_table') {\n    return String((record.cells || []).join('|') || record.title || fallbackIndex);\n  }\n  if (sourceKind === 'transit_json') {\n    const header = record.header || record;\n    return String(header.id || header.alert_id || header.entity_id || header.url || fallbackIndex);\n  }\n  return String(fallbackIndex);\n}\nfunction firstTimestamp(record, fields) {\n  for (const field of fields) {\n    const parts = String(field).split('.');\n    let value = record;\n    for (const part of parts) {\n      if (value && typeof value === 'object' && part in value) value = value[part];\n      else { value = undefined; break; }\n    }\n    if (value) {\n      const parsed = new Date(value);\n      if (!isNaN(parsed.getTime())) return parsed.toISOString();\n    }\n  }\n  return null;\n}\nfunction detectEventType(text) {\n  const lower = text.toLowerCase();\n  if (/boil[s-]*water/.test(lower)) return 'boil_water_notice';\n  if (/(evacuat|evacuation)/.test(lower)) return 'evacuation';\n  if (/shelter in place|lockdown/.test(lower)) return 'shelter_in_place';\n  if (/hazmat|spill|leak|toxic|gas/.test(lower)) return 'hazmat';\n  if (/dam/.test(lower)) return 'dam_incident';\n  if (/public health|health advisory|advisory|heat|smoke|air quality/.test(lower)) return 'public_health_advisory';\n  if (/civil emergency|public warning|emergency alert|blast|explosion/.test(lower)) return 'civil_emergency';\n  if (/police|incident|fire|rescue|closure/.test(lower)) return 'public_safety_incident';\n  return sourceKind === 'transit_json' ? 'alternate_transport' : 'public_safety_incident';\n}\nfunction detectSeverity(text, eventType) {\n  const lower = text.toLowerCase();\n  if (eventType === 'alternate_transport') return 'info';\n  if (eventType === 'evacuation' || eventType === 'civil_emergency' || eventType === 'hazmat' || eventType === 'dam_incident') return 'emergency';\n  if (/warning/.test(lower)) return 'warning';\n  if (/watch/.test(lower)) return 'watch';\n  if (/advisory|notice/.test(lower)) return 'advisory';\n  if (/resolved|expired/.test(lower)) return 'info';\n  return 'info';\n}\nfunction detectStatus(text) {\n  const lower = text.toLowerCase();\n  if (/resolved|ended|expired|cancelled|canceled/.test(lower)) return 'resolved';\n  if (/monitoring|watch/.test(lower)) return 'monitoring';\n  return 'active';\n}\nfunction routeSectionsForText(text) {\n  const lower = text.toLowerCase();\n  const sections = [];\n  const checks = [\n    ['WP1_UW', ['university of washington', 'u-district', 'university way ne', 'rainier vista', 'kane hall', 'burke-gilman trail', 'ne pacific st', 'ne 45th st', 'uw']],\n    ['WP2_KENMORE_LFP', ['kenmore', 'lake forest park']],\n    ['WP3_BOTHELL', ['bothell', 'bothell landing', 'sr-522', 'bothell way']],\n    ['WP4_WOODINVILLE', ['woodinville']],\n    ['WP5_REDMOND', ['redmond', 'marymoor']],\n    ['WP6_MARYMOOR_ELST', ['marymoor park', 'east lake sammamish trail', 'marymoor connector trail']],\n    ['WP7_SAMMAMISH', ['sammamish', 'east lake sammamish trail']],\n    ['WP8_ISSAQUAH', ['issaquah', 'i-90', 'issaquah approach']]\n  ];\n  for (const [sectionId, tokens] of checks) {\n    if (tokens.some(token => lower.includes(token))) sections.push(sectionId);\n  }\n  return Array.from(new Set(sections.length ? sections : routeSeedSections.filter(sectionId => lower.includes('king county') || lower.includes('seattle') || lower.includes('washington'))));\n}\nfunction classifyOwnership(text) {\n  const lower = text.toLowerCase();\n  if (sourceKind === 'transit_json') return 'observation';\n  if (/air quality|smoke|excessive heat|severe thunderstorm|high wind|flood watch|flood warning|wildfire/.test(lower) && !/evacuat|civil emergency|boil water|hazmat|shelter in place/.test(lower)) return 'observation';\n  if (/boil[s-]*water|evacuat|shelter in place|hazmat|civil emergency|public warning|dam incident|public health|health advisory|police|fire|incident|closure/.test(lower)) return 'event';\n  if (/uw|university of washington|seattle|bothell|redmond|sammamish|issaquah|kenmore|woodinville|marymoor|burke-gilman|east lake sammamish|lake forest park/.test(lower)) return 'event';\n  return 'observation';\n}\nfunction mapRouteRelevance(text, eventType, kind) {\n  const sections = routeSectionsForText(text);\n  const lower = text.toLowerCase();\n  const hasExplicitRouteToken = gazetteer.some(token => lower.includes(token.toLowerCase()));\n  const classification = sections.length ? (hasExplicitRouteToken ? 'confirmed_route_relevant' : 'possible_route_relevant') : 'not_route_relevant';\n  let method = 'ugc_same_area_match';\n  if (kind === 'transit_json') method = 'route_stop_mapping';\n  else if (kind === 'nws') method = hasExplicitRouteToken ? 'route_point_query' : 'cap_geometry_intersection';\n  else if (kind === 'html_table' || kind === 'rss' || kind === 'wp_json') method = hasExplicitRouteToken ? 'text_landmark_match' : 'ugc_same_area_match';\n  const manualReviewRequired = classification !== 'confirmed_route_relevant' || kind === 'rss' || kind === 'html_table';\n  const distanceKm = classification === 'confirmed_route_relevant' ? 0 : (classification === 'possible_route_relevant' ? 1.0 : null);\n  return {\n    classification,\n    method,\n    distance_km: distanceKm,\n    matched_tokens: gazetteer.filter(token => lower.includes(token.toLowerCase())),\n    bounding_box_match: sections.length > 0,\n    manual_review_required: manualReviewRequired,\n    reason: classification === 'confirmed_route_relevant' ? 'Strong local token match or direct route evidence.' : (classification === 'possible_route_relevant' ? 'Local proximity or shared locality only.' : 'No route token or direct route evidence found.')\n  };\n}\nfunction locationFromText(text, record) {\n  const lower = text.toLowerCase();\n  const namedArea = gazetteer.find(token => lower.includes(token.toLowerCase())) || null;\n  const point = record && record.geometry && Array.isArray(record.geometry.coordinates) ? record.geometry.coordinates : null;\n  if (point && point.length >= 2 && typeof point[0] === 'number' && typeof point[1] === 'number') {\n    return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: point[1], longitude: point[0], route_section_ids: routeSectionsForText(text) };\n  }\n  if (record && typeof record.x === 'number' && typeof record.y === 'number') {\n    return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: record.y, longitude: record.x, route_section_ids: routeSectionsForText(text) };\n  }\n  return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: null, longitude: null, route_section_ids: routeSectionsForText(text) };\n}\nfunction eventRouteImpact(eventType, relevanceClass, kind, text) {\n  if (kind === 'transit_json') return 'alternate_transport_only';\n  if (relevanceClass === 'confirmed_route_relevant') {\n    if (eventType === 'evacuation' || eventType === 'civil_emergency' || eventType === 'hazmat' || eventType === 'dam_incident' || eventType === 'boil_water_notice') return 'route_access_impacted';\n    return 'route_segment_impacted';\n  }\n  if (relevanceClass === 'possible_route_relevant') return 'manual_review_required';\n  if (/alternate transport/.test(text.toLowerCase())) return 'alternate_transport_only';\n  return 'no_known_route_impact';\n}\nfunction classifyCrossList(text) {\n  const lower = text.toLowerCase();\n  const cross = [];\n  if (/air quality|smoke/.test(lower)) cross.push('03_AIR_QUALITY');\n  if (/excessive heat|severe thunderstorm|high wind/.test(lower)) cross.push('02_WEATHER');\n  if (/flood/.test(lower)) cross.push('05_FLOOD_CONDITIONS');\n  if (/wildfire/.test(lower)) cross.push('04_WILDFIRE');\n  if (/trail closure|bridge closure|construction/.test(lower)) cross.push('01_ROUTE_CONDITIONS', '06_TRAIL_INFRASTRUCTURE_STATUS');\n  if (/alternate transport|transit|bus|service alert|route/.test(lower)) cross.push('02_WEATHER');\n  return Array.from(new Set(cross));\n}\nfunction buildEvent(record, index) {\n  const text = getSourceRecordText(record);\n  const eventType = detectEventType(text);\n  const relevance = mapRouteRelevance(text, eventType, sourceKind);\n  const sourceRecordRef = getSourceRecordId(record, index);\n  const recordHash = 'hash_' + hashString(text || JSON.stringify(record || null));\n  const observedAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.sent', 'properties.effective', 'properties.onset', 'properties.updated', 'properties.expires', 'properties.ends'] : sourceKind === 'wp_json' ? ['date', 'modified'] : sourceKind === 'rss' ? ['pubDate', 'updated'] : sourceKind === 'html_table' ? ['pubDate'] : ['header.created', 'header.updated', 'active_period.start', 'active_period.end']);\n  const effectiveAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.effective', 'properties.onset'] : ['effective_at', 'start', 'date', 'modified']);\n  const expiresAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.expires', 'properties.ends'] : ['expires_at', 'end']);\n  const location = locationFromText(text, record.geometry || record);\n  const routeImpact = eventRouteImpact(eventType, relevance.classification, sourceKind, text);\n  const severity = detectSeverity(text, eventType);\n  const status = detectStatus(text);\n  const isPrimary = relevance.classification !== 'not_route_relevant' && !classifyCrossList(text).length;\n  const officialCategory = sourceKind === 'nws' ? text.split(' | ')[1] || null : null;\n  const publicAction = sourceKind === 'nws' ? textOf(record.properties && record.properties.instruction) || null : null;\n  return {\n    event_id: sourceId + ':' + recordHash,\n    event_type: eventType,\n    status,\n    severity,\n    route_impact: routeImpact,\n    title: text.split(' | ')[0] || text,\n    summary: text,\n    source_id: sourceId,\n    source_event_ref: sourceRecordRef,\n    published_source_ids: [sourceId],\n    observed_at: observedAt || run.generated_at,\n    effective_at: effectiveAt,\n    expires_at: expiresAt,\n    official_category: officialCategory,\n    public_action: publicAction,\n    cross_listed_to: classifyCrossList(text),\n    location,\n    route_relevance: relevance,\n    provenance: {\n      source_url: sourceUrl,\n      retrieved_at: run.generated_at,\n      source_record_hash: recordHash,\n      normalization_version: 'v0002'\n    }\n  };\n}\nfunction buildObservation(record, index) {\n  const text = getSourceRecordText(record);\n  const sourceRecordRef = getSourceRecordId(record, index);\n  const recordHash = 'hash_' + hashString(text || JSON.stringify(record || null));\n  const sections = routeSectionsForText(text);\n  const location = locationFromText(text, record.geometry || record);\n  return {\n    observation_id: sourceId + ':obs:' + recordHash,\n    observation_type: sourceKind === 'transit_json' ? 'alternate_transport' : 'public_health_context',\n    title: text.split(' | ')[0] || text,\n    summary: text,\n    source_id: sourceId,\n    observed_at: firstTimestamp(record, sourceKind === 'nws' ? ['properties.sent', 'properties.updated'] : ['date', 'modified', 'pubDate']) || run.generated_at,\n    status: detectStatus(text),\n    route_section_ids: sections,\n    public_action: null,\n    provenance: {\n      source_url: sourceUrl,\n      retrieved_at: run.generated_at,\n      source_record_hash: recordHash,\n      normalization_version: 'v0002'\n    },\n    related_event_ids: []\n  };\n}\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst records = extractRecords(body);\nlet events = [];\nlet observations = [];\nlet parserWarnings = [];\nlet parserErrors = [];\nfor (let i = 0; i < records.length; i++) {\n  const record = records[i];\n  const text = getSourceRecordText(record);\n  const ownership = classifyOwnership(text);\n  const cross = classifyCrossList(text);\n  if (ownership === 'event' && cross.length && sourceKind !== 'transit_json') {\n    // Cross-listed items stay available as observations so lane 07 does not claim primary ownership.\n    observations.push(buildObservation(record, i));\n    continue;\n  }\n  if (ownership === 'event') {\n    events.push(buildEvent(record, i));\n  } else if (ownership === 'observation') {\n    observations.push(buildObservation(record, i));\n  }\n}\nif (!records.length && body && typeof body === 'object' && !Array.isArray(body)) {\n  parserWarnings.push('No structured records were extracted, but the source response was structurally valid.');\n}\nconst fetchError = fetch.error ? String(fetch.error.message || fetch.error) : (parserErrors.length ? parserErrors[0] : null);\nconst lkg = lkgLookup[sourceId];\nconst lkgRetrievedAt = lkg && lkg.source_health && lkg.source_health.retrieved_at ? Date.parse(lkg.source_health.retrieved_at) : NaN;\nconst lkgStaleAfterMinutes = lkg && lkg.source_health && typeof lkg.source_health.stale_after_minutes === 'number' ? lkg.source_health.stale_after_minutes : staleAfterMinutes;\nconst lkgUsable = !!fetchError && !!lkg && Number.isFinite(lkgRetrievedAt) && ((Date.now() - lkgRetrievedAt) / 60000) <= lkgStaleAfterMinutes && Array.isArray(lkg.source_health) === false;\nlet sourceHealth;\nlet finalEvents = events;\nlet finalObservations = observations;\nlet warnings = parserWarnings.slice();\nlet errors = parserErrors.slice();\nif (fetchError && lkg && Number.isFinite(lkgRetrievedAt) && ((Date.now() - lkgRetrievedAt) / 60000) <= lkgStaleAfterMinutes) {\n  finalEvents = Array.isArray(lkg.events) ? lkg.events : [];\n  finalObservations = Array.isArray(lkg.observations) ? lkg.observations : [];\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: 'using_last_known_good',\n    retrieved_at: lkg.source_health.retrieved_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: finalEvents.length,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: lkg.source_health.last_observation_at ?? lkg.source_health.retrieved_at,\n    warnings: [...(lkg.source_health.warnings || []), 'Live fetch failed; serving last-known-good data retrieved at ' + lkg.source_health.retrieved_at + '.'],\n    errors: [fetchError]\n  };\n} else if (fetchError) {\n  finalEvents = [];\n  finalObservations = [];\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: 'failed',\n    retrieved_at: run.generated_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: 0,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: run.generated_at,\n    warnings: warnings,\n    errors: [fetchError]\n  };\n} else {\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: finalEvents.length ? 'ok' : 'empty_but_valid',\n    retrieved_at: run.generated_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: finalEvents.length,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: finalObservations.length ? finalObservations[finalObservations.length - 1].observed_at : run.generated_at,\n    warnings: finalEvents.length ? warnings : [...warnings, 'No primary lane-07 events retained for this source in this run.'],\n    errors: []\n  };\n}\nconst rawLandingPath = landing.file_path || null;\nreturn [{ json: {\n  schema_version: '1.0.0',\n  connector_id: run.connector_id,\n  connector_name: run.connector_name,\n  connector_version: run.connector_version,\n  lane: run.lane,\n  run_id: run.run_id,\n  generated_at: run.generated_at,\n  source_id: sourceId,\n  source_name: sourceName,\n  source_url: sourceUrl,\n  source_kind: sourceKind,\n  raw_landing_path: rawLandingPath,\n  raw_content_hash: landing.content_hash || null,\n  source_health: sourceHealth,\n  events: finalEvents,\n  observations: finalObservations,\n  route_sections: Array.from(new Set([...new Set(finalEvents.flatMap(event => Array.isArray(event.location && event.location.route_section_ids) ? event.location.route_section_ids : [])), ...new Set(finalObservations.flatMap(observation => Array.isArray(observation.route_section_ids) ? observation.route_section_ids : []))])),\n  warnings,\n  errors\n} }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "fetch_st_01_alerts",
        "name": "Fetch ST-01 Alerts",
        "type": "n8n-nodes-base.httpRequest",
        "notes": "Sound Transit GTFS Realtime service alerts",
        "onError": "continueRegularOutput",
        "position": [
          220,
          1740
        ],
        "parameters": {
          "url": "https://s3.amazonaws.com/st-service-alerts-prod/alerts_pb.json",
          "method": "GET",
          "options": {
            "timeout": 30000
          },
          "responseFormat": "json"
        },
        "typeVersion": 4.2
      },
      {
        "id": "land_st_01_raw_payload",
        "name": "Land ST-01 Raw Payload",
        "type": "n8n-nodes-base.code",
        "position": [
          760,
          1740
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch ST-01 Alerts').first().json || {};\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst bodyString = typeof body === 'string' ? body : JSON.stringify(body || null);\nconst statusCode = fetch.statusCode ?? fetch.status ?? null;\nconst hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst contentHash = 'hash_' + hashString(bodyString);\nconst filePath = run.output_root + '/raw/07_GOVERNMENT_SAFETY_ALERTS/landings/ST-01_landing_' + run.run_stamp + '.json';\nconst landing = {\n  schema_version: '1.0.0',\n  connector_id: run.connector_id,\n  source_id: '07_GOVERNMENT_SAFETY_ALERTS:ST-01',\n  source_name: 'Sound Transit GTFS Realtime service alerts',\n  source_url: 'https://s3.amazonaws.com/st-service-alerts-prod/alerts_pb.json',\n  retrieved_at: run.generated_at,\n  http_status: statusCode,\n  retrieval_method: 'transit_json',\n  content_hash: contentHash,\n  body_length: bodyString.length,\n  body: bodyString,\n  fetch_status: fetch.error ? 'failed' : 'ok',\n  fetch_error: fetch.error ? String(fetch.error.message || fetch.error) : null\n};\nconst buffer = Buffer.from(JSON.stringify(landing, null, 2), 'utf8');\nconst binary = await this.helpers.prepareBinaryData(buffer, filePath.split('/').pop(), 'application/json');\nreturn [{ json: {\n  file_path: filePath,\n  source_id: landing.source_id,\n  source_name: landing.source_name,\n  content_hash: contentHash,\n  retrieved_at: run.generated_at,\n  body_length: bodyString.length\n}, binary: { data: binary } }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "write_st_01_raw_landing",
        "name": "Write ST-01 Raw Landing",
        "type": "n8n-nodes-base.readWriteFile",
        "position": [
          1180,
          1740
        ],
        "parameters": {
          "options": {},
          "fileName": "={{ $json.file_path }}",
          "operation": "write",
          "dataPropertyName": "data"
        },
        "typeVersion": 1
      },
      {
        "id": "normalize_st_01_events",
        "name": "Normalize ST-01 Events",
        "type": "n8n-nodes-base.code",
        "position": [
          1600,
          1740
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch ST-01 Alerts').first().json || {};\nconst landing = $('Land ST-01 Raw Payload').first().json || {};\nconst lkgLookup = ($('Parse Last Known Good').first().json || {}).bySource || {};\nconst sourceId = '07_GOVERNMENT_SAFETY_ALERTS:ST-01';\nconst sourceName = 'Sound Transit GTFS Realtime service alerts';\nconst sourceUrl = 'https://s3.amazonaws.com/st-service-alerts-prod/alerts_pb.json';\nconst sourceKind = 'transit_json';\nconst routeSeedSections = [\"WP1_UW\",\"WP3_BOTHELL\",\"WP5_REDMOND\",\"WP8_ISSAQUAH\"];\nconst staleAfterMinutes = 15;\nconst gazetteer = run.gazetteer || ['University of Washington', 'UW', 'U-District', 'University Way NE', 'Rainier Vista', 'Kane Hall', 'Burke-Gilman Trail', 'NE Pacific St', 'NE 45th St', 'Bothell Landing', 'Marymoor Park', 'East Lake Sammamish Trail', 'Kenmore', 'Lake Forest Park', 'Bothell', 'Woodinville', 'Redmond', 'Sammamish', 'Issaquah', 'SR-522', 'I-90', 'I-405', 'Seattle', 'Washington', 'King County'];\nconst now = Date.now();\nconst hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nfunction stripHtml(value) {\n  return String(value || '')\n    .replace(/<script[\\s\\S]*?<\\/script>/gi, ' ')\n    .replace(/<style[\\s\\S]*?<\\/style>/gi, ' ')\n    .replace(/<[^>]+>/g, ' ')\n    .replace(/&nbsp;/g, ' ')\n    .replace(/&amp;/g, '&')\n    .replace(/\\s+/g, ' ')\n    .trim();\n}\nfunction textOf(value) {\n  if (value === null || value === undefined) return '';\n  if (typeof value === 'string') return stripHtml(value);\n  if (Array.isArray(value)) return value.map(textOf).filter(Boolean).join(' | ');\n  if (typeof value === 'object') {\n    if (typeof value.rendered === 'string') return stripHtml(value.rendered);\n    if (typeof value.value === 'string') return stripHtml(value.value);\n    return stripHtml(JSON.stringify(value));\n  }\n  return String(value);\n}\nfunction safeJsonParse(value) {\n  if (typeof value !== 'string') return null;\n  try { return JSON.parse(value); } catch (error) { return null; }\n}\nfunction extractRecords(body) {\n  if (!body) return [];\n  if (sourceKind === 'nws') {\n    if (Array.isArray(body.features)) return body.features;\n    if (Array.isArray(body.items)) return body.items;\n    return [body];\n  }\n  if (sourceKind === 'wp_json') {\n    if (Array.isArray(body)) return body;\n    if (Array.isArray(body.posts)) return body.posts;\n    if (Array.isArray(body.items)) return body.items;\n    return [];\n  }\n  if (sourceKind === 'rss') {\n    const text = typeof body === 'string' ? body : textOf(body);\n    const matches = [...text.matchAll(/<item[\\s\\S]*?<\\/item>/gi)];\n    if (!matches.length) return [];\n    return matches.map((match, index) => {\n      const item = match[0];\n      const getTag = (tag) => {\n        const tagMatch = item.match(new RegExp('<' + tag + '[^>]*>([\\s\\S]*?)</' + tag + '>', 'i'));\n        return tagMatch ? stripHtml(tagMatch[1]) : '';\n      };\n      return { title: getTag('title'), link: getTag('link'), description: getTag('description'), pubDate: getTag('pubDate') || getTag('updated'), guid: getTag('guid') || getTag('link') || String(index) };\n    });\n  }\n  if (sourceKind === 'html_table') {\n    const text = typeof body === 'string' ? body : textOf(body);\n    const rows = [...text.matchAll(/<tr[\\s\\S]*?<\\/tr>/gi)];\n    return rows.map((match, index) => {\n      const row = match[0];\n      const cells = [...row.matchAll(/<t[dh][^>]*>([\\s\\S]*?)<\\/t[dh]>/gi)].map(m => stripHtml(m[1]));\n      return { cells, title: cells.join(' | '), link: '', pubDate: cells[0] || '', guid: cells.join('|') || String(index) };\n    });\n  }\n  if (sourceKind === 'transit_json') {\n    if (Array.isArray(body.entity)) return body.entity;\n    if (Array.isArray(body.entities)) return body.entities;\n    if (Array.isArray(body.alerts)) return body.alerts;\n    if (Array.isArray(body.data)) return body.data;\n    return Array.isArray(body) ? body : [];\n  }\n  return Array.isArray(body) ? body : [];\n}\nfunction getSourceRecordText(record) {\n  if (!record) return '';\n  if (sourceKind === 'nws') {\n    const p = record.properties || record;\n    return [p.headline, p.event, p.description, p.instruction, p.areaDesc, p.senderName, p.response].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'wp_json') {\n    return [record.title, record.excerpt, record.content, record.slug, record.link, record.status, record.date, record.modified].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'rss') {\n    return [record.title, record.description, record.link, record.pubDate, record.guid].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'html_table') {\n    return [record.title, record.pubDate, (record.cells || []).join(' | ')].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'transit_json') {\n    const header = record.header || record;\n    const fields = [header.header_text, header.description_text, header.cause, header.effect, header.url, header.alert_id, header.severity_level];\n    if (record.active_period) {\n      const ap = Array.isArray(record.active_period) ? record.active_period[0] : record.active_period;\n      if (ap) fields.push(ap.start, ap.end);\n    }\n    return fields.map(textOf).filter(Boolean).join(' | ');\n  }\n  return textOf(record);\n}\nfunction getSourceRecordId(record, fallbackIndex) {\n  if (!record) return String(fallbackIndex);\n  if (sourceKind === 'nws') {\n    const p = record.properties || record;\n    return String(p.id || p.cap_id || p.identifier || p.eventCode || p.sent || fallbackIndex);\n  }\n  if (sourceKind === 'wp_json') {\n    return String(record.id || record.post_id || record.slug || record.guid || fallbackIndex);\n  }\n  if (sourceKind === 'rss') {\n    return String(record.guid || record.link || record.title || fallbackIndex);\n  }\n  if (sourceKind === 'html_table') {\n    return String((record.cells || []).join('|') || record.title || fallbackIndex);\n  }\n  if (sourceKind === 'transit_json') {\n    const header = record.header || record;\n    return String(header.id || header.alert_id || header.entity_id || header.url || fallbackIndex);\n  }\n  return String(fallbackIndex);\n}\nfunction firstTimestamp(record, fields) {\n  for (const field of fields) {\n    const parts = String(field).split('.');\n    let value = record;\n    for (const part of parts) {\n      if (value && typeof value === 'object' && part in value) value = value[part];\n      else { value = undefined; break; }\n    }\n    if (value) {\n      const parsed = new Date(value);\n      if (!isNaN(parsed.getTime())) return parsed.toISOString();\n    }\n  }\n  return null;\n}\nfunction detectEventType(text) {\n  const lower = text.toLowerCase();\n  if (/boil[s-]*water/.test(lower)) return 'boil_water_notice';\n  if (/(evacuat|evacuation)/.test(lower)) return 'evacuation';\n  if (/shelter in place|lockdown/.test(lower)) return 'shelter_in_place';\n  if (/hazmat|spill|leak|toxic|gas/.test(lower)) return 'hazmat';\n  if (/dam/.test(lower)) return 'dam_incident';\n  if (/public health|health advisory|advisory|heat|smoke|air quality/.test(lower)) return 'public_health_advisory';\n  if (/civil emergency|public warning|emergency alert|blast|explosion/.test(lower)) return 'civil_emergency';\n  if (/police|incident|fire|rescue|closure/.test(lower)) return 'public_safety_incident';\n  return sourceKind === 'transit_json' ? 'alternate_transport' : 'public_safety_incident';\n}\nfunction detectSeverity(text, eventType) {\n  const lower = text.toLowerCase();\n  if (eventType === 'alternate_transport') return 'info';\n  if (eventType === 'evacuation' || eventType === 'civil_emergency' || eventType === 'hazmat' || eventType === 'dam_incident') return 'emergency';\n  if (/warning/.test(lower)) return 'warning';\n  if (/watch/.test(lower)) return 'watch';\n  if (/advisory|notice/.test(lower)) return 'advisory';\n  if (/resolved|expired/.test(lower)) return 'info';\n  return 'info';\n}\nfunction detectStatus(text) {\n  const lower = text.toLowerCase();\n  if (/resolved|ended|expired|cancelled|canceled/.test(lower)) return 'resolved';\n  if (/monitoring|watch/.test(lower)) return 'monitoring';\n  return 'active';\n}\nfunction routeSectionsForText(text) {\n  const lower = text.toLowerCase();\n  const sections = [];\n  const checks = [\n    ['WP1_UW', ['university of washington', 'u-district', 'university way ne', 'rainier vista', 'kane hall', 'burke-gilman trail', 'ne pacific st', 'ne 45th st', 'uw']],\n    ['WP2_KENMORE_LFP', ['kenmore', 'lake forest park']],\n    ['WP3_BOTHELL', ['bothell', 'bothell landing', 'sr-522', 'bothell way']],\n    ['WP4_WOODINVILLE', ['woodinville']],\n    ['WP5_REDMOND', ['redmond', 'marymoor']],\n    ['WP6_MARYMOOR_ELST', ['marymoor park', 'east lake sammamish trail', 'marymoor connector trail']],\n    ['WP7_SAMMAMISH', ['sammamish', 'east lake sammamish trail']],\n    ['WP8_ISSAQUAH', ['issaquah', 'i-90', 'issaquah approach']]\n  ];\n  for (const [sectionId, tokens] of checks) {\n    if (tokens.some(token => lower.includes(token))) sections.push(sectionId);\n  }\n  return Array.from(new Set(sections.length ? sections : routeSeedSections.filter(sectionId => lower.includes('king county') || lower.includes('seattle') || lower.includes('washington'))));\n}\nfunction classifyOwnership(text) {\n  const lower = text.toLowerCase();\n  if (sourceKind === 'transit_json') return 'observation';\n  if (/air quality|smoke|excessive heat|severe thunderstorm|high wind|flood watch|flood warning|wildfire/.test(lower) && !/evacuat|civil emergency|boil water|hazmat|shelter in place/.test(lower)) return 'observation';\n  if (/boil[s-]*water|evacuat|shelter in place|hazmat|civil emergency|public warning|dam incident|public health|health advisory|police|fire|incident|closure/.test(lower)) return 'event';\n  if (/uw|university of washington|seattle|bothell|redmond|sammamish|issaquah|kenmore|woodinville|marymoor|burke-gilman|east lake sammamish|lake forest park/.test(lower)) return 'event';\n  return 'observation';\n}\nfunction mapRouteRelevance(text, eventType, kind) {\n  const sections = routeSectionsForText(text);\n  const lower = text.toLowerCase();\n  const hasExplicitRouteToken = gazetteer.some(token => lower.includes(token.toLowerCase()));\n  const classification = sections.length ? (hasExplicitRouteToken ? 'confirmed_route_relevant' : 'possible_route_relevant') : 'not_route_relevant';\n  let method = 'route_stop_mapping';\n  if (kind === 'transit_json') method = 'route_stop_mapping';\n  else if (kind === 'nws') method = hasExplicitRouteToken ? 'route_point_query' : 'cap_geometry_intersection';\n  else if (kind === 'html_table' || kind === 'rss' || kind === 'wp_json') method = hasExplicitRouteToken ? 'text_landmark_match' : 'ugc_same_area_match';\n  const manualReviewRequired = classification !== 'confirmed_route_relevant' || kind === 'rss' || kind === 'html_table';\n  const distanceKm = classification === 'confirmed_route_relevant' ? 0 : (classification === 'possible_route_relevant' ? 1.0 : null);\n  return {\n    classification,\n    method,\n    distance_km: distanceKm,\n    matched_tokens: gazetteer.filter(token => lower.includes(token.toLowerCase())),\n    bounding_box_match: sections.length > 0,\n    manual_review_required: manualReviewRequired,\n    reason: classification === 'confirmed_route_relevant' ? 'Strong local token match or direct route evidence.' : (classification === 'possible_route_relevant' ? 'Local proximity or shared locality only.' : 'No route token or direct route evidence found.')\n  };\n}\nfunction locationFromText(text, record) {\n  const lower = text.toLowerCase();\n  const namedArea = gazetteer.find(token => lower.includes(token.toLowerCase())) || null;\n  const point = record && record.geometry && Array.isArray(record.geometry.coordinates) ? record.geometry.coordinates : null;\n  if (point && point.length >= 2 && typeof point[0] === 'number' && typeof point[1] === 'number') {\n    return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: point[1], longitude: point[0], route_section_ids: routeSectionsForText(text) };\n  }\n  if (record && typeof record.x === 'number' && typeof record.y === 'number') {\n    return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: record.y, longitude: record.x, route_section_ids: routeSectionsForText(text) };\n  }\n  return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: null, longitude: null, route_section_ids: routeSectionsForText(text) };\n}\nfunction eventRouteImpact(eventType, relevanceClass, kind, text) {\n  if (kind === 'transit_json') return 'alternate_transport_only';\n  if (relevanceClass === 'confirmed_route_relevant') {\n    if (eventType === 'evacuation' || eventType === 'civil_emergency' || eventType === 'hazmat' || eventType === 'dam_incident' || eventType === 'boil_water_notice') return 'route_access_impacted';\n    return 'route_segment_impacted';\n  }\n  if (relevanceClass === 'possible_route_relevant') return 'manual_review_required';\n  if (/alternate transport/.test(text.toLowerCase())) return 'alternate_transport_only';\n  return 'no_known_route_impact';\n}\nfunction classifyCrossList(text) {\n  const lower = text.toLowerCase();\n  const cross = [];\n  if (/air quality|smoke/.test(lower)) cross.push('03_AIR_QUALITY');\n  if (/excessive heat|severe thunderstorm|high wind/.test(lower)) cross.push('02_WEATHER');\n  if (/flood/.test(lower)) cross.push('05_FLOOD_CONDITIONS');\n  if (/wildfire/.test(lower)) cross.push('04_WILDFIRE');\n  if (/trail closure|bridge closure|construction/.test(lower)) cross.push('01_ROUTE_CONDITIONS', '06_TRAIL_INFRASTRUCTURE_STATUS');\n  if (/alternate transport|transit|bus|service alert|route/.test(lower)) cross.push('02_WEATHER');\n  return Array.from(new Set(cross));\n}\nfunction buildEvent(record, index) {\n  const text = getSourceRecordText(record);\n  const eventType = detectEventType(text);\n  const relevance = mapRouteRelevance(text, eventType, sourceKind);\n  const sourceRecordRef = getSourceRecordId(record, index);\n  const recordHash = 'hash_' + hashString(text || JSON.stringify(record || null));\n  const observedAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.sent', 'properties.effective', 'properties.onset', 'properties.updated', 'properties.expires', 'properties.ends'] : sourceKind === 'wp_json' ? ['date', 'modified'] : sourceKind === 'rss' ? ['pubDate', 'updated'] : sourceKind === 'html_table' ? ['pubDate'] : ['header.created', 'header.updated', 'active_period.start', 'active_period.end']);\n  const effectiveAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.effective', 'properties.onset'] : ['effective_at', 'start', 'date', 'modified']);\n  const expiresAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.expires', 'properties.ends'] : ['expires_at', 'end']);\n  const location = locationFromText(text, record.geometry || record);\n  const routeImpact = eventRouteImpact(eventType, relevance.classification, sourceKind, text);\n  const severity = detectSeverity(text, eventType);\n  const status = detectStatus(text);\n  const isPrimary = relevance.classification !== 'not_route_relevant' && !classifyCrossList(text).length;\n  const officialCategory = sourceKind === 'nws' ? text.split(' | ')[1] || null : null;\n  const publicAction = sourceKind === 'nws' ? textOf(record.properties && record.properties.instruction) || null : null;\n  return {\n    event_id: sourceId + ':' + recordHash,\n    event_type: eventType,\n    status,\n    severity,\n    route_impact: routeImpact,\n    title: text.split(' | ')[0] || text,\n    summary: text,\n    source_id: sourceId,\n    source_event_ref: sourceRecordRef,\n    published_source_ids: [sourceId],\n    observed_at: observedAt || run.generated_at,\n    effective_at: effectiveAt,\n    expires_at: expiresAt,\n    official_category: officialCategory,\n    public_action: publicAction,\n    cross_listed_to: classifyCrossList(text),\n    location,\n    route_relevance: relevance,\n    provenance: {\n      source_url: sourceUrl,\n      retrieved_at: run.generated_at,\n      source_record_hash: recordHash,\n      normalization_version: 'v0002'\n    }\n  };\n}\nfunction buildObservation(record, index) {\n  const text = getSourceRecordText(record);\n  const sourceRecordRef = getSourceRecordId(record, index);\n  const recordHash = 'hash_' + hashString(text || JSON.stringify(record || null));\n  const sections = routeSectionsForText(text);\n  const location = locationFromText(text, record.geometry || record);\n  return {\n    observation_id: sourceId + ':obs:' + recordHash,\n    observation_type: sourceKind === 'transit_json' ? 'alternate_transport' : 'public_health_context',\n    title: text.split(' | ')[0] || text,\n    summary: text,\n    source_id: sourceId,\n    observed_at: firstTimestamp(record, sourceKind === 'nws' ? ['properties.sent', 'properties.updated'] : ['date', 'modified', 'pubDate']) || run.generated_at,\n    status: detectStatus(text),\n    route_section_ids: sections,\n    public_action: null,\n    provenance: {\n      source_url: sourceUrl,\n      retrieved_at: run.generated_at,\n      source_record_hash: recordHash,\n      normalization_version: 'v0002'\n    },\n    related_event_ids: []\n  };\n}\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst records = extractRecords(body);\nlet events = [];\nlet observations = [];\nlet parserWarnings = [];\nlet parserErrors = [];\nfor (let i = 0; i < records.length; i++) {\n  const record = records[i];\n  const text = getSourceRecordText(record);\n  const ownership = classifyOwnership(text);\n  const cross = classifyCrossList(text);\n  if (ownership === 'event' && cross.length && sourceKind !== 'transit_json') {\n    // Cross-listed items stay available as observations so lane 07 does not claim primary ownership.\n    observations.push(buildObservation(record, i));\n    continue;\n  }\n  if (ownership === 'event') {\n    events.push(buildEvent(record, i));\n  } else if (ownership === 'observation') {\n    observations.push(buildObservation(record, i));\n  }\n}\nif (!records.length && body && typeof body === 'object' && !Array.isArray(body)) {\n  parserWarnings.push('No structured records were extracted, but the source response was structurally valid.');\n}\nconst fetchError = fetch.error ? String(fetch.error.message || fetch.error) : (parserErrors.length ? parserErrors[0] : null);\nconst lkg = lkgLookup[sourceId];\nconst lkgRetrievedAt = lkg && lkg.source_health && lkg.source_health.retrieved_at ? Date.parse(lkg.source_health.retrieved_at) : NaN;\nconst lkgStaleAfterMinutes = lkg && lkg.source_health && typeof lkg.source_health.stale_after_minutes === 'number' ? lkg.source_health.stale_after_minutes : staleAfterMinutes;\nconst lkgUsable = !!fetchError && !!lkg && Number.isFinite(lkgRetrievedAt) && ((Date.now() - lkgRetrievedAt) / 60000) <= lkgStaleAfterMinutes && Array.isArray(lkg.source_health) === false;\nlet sourceHealth;\nlet finalEvents = events;\nlet finalObservations = observations;\nlet warnings = parserWarnings.slice();\nlet errors = parserErrors.slice();\nif (fetchError && lkg && Number.isFinite(lkgRetrievedAt) && ((Date.now() - lkgRetrievedAt) / 60000) <= lkgStaleAfterMinutes) {\n  finalEvents = Array.isArray(lkg.events) ? lkg.events : [];\n  finalObservations = Array.isArray(lkg.observations) ? lkg.observations : [];\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: 'using_last_known_good',\n    retrieved_at: lkg.source_health.retrieved_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: finalEvents.length,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: lkg.source_health.last_observation_at ?? lkg.source_health.retrieved_at,\n    warnings: [...(lkg.source_health.warnings || []), 'Live fetch failed; serving last-known-good data retrieved at ' + lkg.source_health.retrieved_at + '.'],\n    errors: [fetchError]\n  };\n} else if (fetchError) {\n  finalEvents = [];\n  finalObservations = [];\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: 'failed',\n    retrieved_at: run.generated_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: 0,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: run.generated_at,\n    warnings: warnings,\n    errors: [fetchError]\n  };\n} else {\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: finalEvents.length ? 'ok' : 'empty_but_valid',\n    retrieved_at: run.generated_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: finalEvents.length,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: finalObservations.length ? finalObservations[finalObservations.length - 1].observed_at : run.generated_at,\n    warnings: finalEvents.length ? warnings : [...warnings, 'No primary lane-07 events retained for this source in this run.'],\n    errors: []\n  };\n}\nconst rawLandingPath = landing.file_path || null;\nreturn [{ json: {\n  schema_version: '1.0.0',\n  connector_id: run.connector_id,\n  connector_name: run.connector_name,\n  connector_version: run.connector_version,\n  lane: run.lane,\n  run_id: run.run_id,\n  generated_at: run.generated_at,\n  source_id: sourceId,\n  source_name: sourceName,\n  source_url: sourceUrl,\n  source_kind: sourceKind,\n  raw_landing_path: rawLandingPath,\n  raw_content_hash: landing.content_hash || null,\n  source_health: sourceHealth,\n  events: finalEvents,\n  observations: finalObservations,\n  route_sections: Array.from(new Set([...new Set(finalEvents.flatMap(event => Array.isArray(event.location && event.location.route_section_ids) ? event.location.route_section_ids : [])), ...new Set(finalObservations.flatMap(observation => Array.isArray(observation.route_section_ids) ? observation.route_section_ids : []))])),\n  warnings,\n  errors\n} }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "fetch_kcmetro_01_alerts",
        "name": "Fetch KCMETRO-01 Alerts",
        "type": "n8n-nodes-base.httpRequest",
        "notes": "King County Metro GTFS Realtime service alerts",
        "onError": "continueRegularOutput",
        "position": [
          220,
          1960
        ],
        "parameters": {
          "url": "https://s3.amazonaws.com/kcm-alerts-realtime-prod/alerts_enhanced.json",
          "method": "GET",
          "options": {
            "timeout": 30000
          },
          "responseFormat": "json"
        },
        "typeVersion": 4.2
      },
      {
        "id": "land_kcmetro_01_raw_payload",
        "name": "Land KCMETRO-01 Raw Payload",
        "type": "n8n-nodes-base.code",
        "position": [
          760,
          1960
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch KCMETRO-01 Alerts').first().json || {};\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst bodyString = typeof body === 'string' ? body : JSON.stringify(body || null);\nconst statusCode = fetch.statusCode ?? fetch.status ?? null;\nconst hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst contentHash = 'hash_' + hashString(bodyString);\nconst filePath = run.output_root + '/raw/07_GOVERNMENT_SAFETY_ALERTS/landings/KCMETRO-01_landing_' + run.run_stamp + '.json';\nconst landing = {\n  schema_version: '1.0.0',\n  connector_id: run.connector_id,\n  source_id: '07_GOVERNMENT_SAFETY_ALERTS:KCMETRO-01',\n  source_name: 'King County Metro GTFS Realtime service alerts',\n  source_url: 'https://s3.amazonaws.com/kcm-alerts-realtime-prod/alerts_enhanced.json',\n  retrieved_at: run.generated_at,\n  http_status: statusCode,\n  retrieval_method: 'transit_json',\n  content_hash: contentHash,\n  body_length: bodyString.length,\n  body: bodyString,\n  fetch_status: fetch.error ? 'failed' : 'ok',\n  fetch_error: fetch.error ? String(fetch.error.message || fetch.error) : null\n};\nconst buffer = Buffer.from(JSON.stringify(landing, null, 2), 'utf8');\nconst binary = await this.helpers.prepareBinaryData(buffer, filePath.split('/').pop(), 'application/json');\nreturn [{ json: {\n  file_path: filePath,\n  source_id: landing.source_id,\n  source_name: landing.source_name,\n  content_hash: contentHash,\n  retrieved_at: run.generated_at,\n  body_length: bodyString.length\n}, binary: { data: binary } }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "write_kcmetro_01_raw_landing",
        "name": "Write KCMETRO-01 Raw Landing",
        "type": "n8n-nodes-base.readWriteFile",
        "position": [
          1180,
          1960
        ],
        "parameters": {
          "options": {},
          "fileName": "={{ $json.file_path }}",
          "operation": "write",
          "dataPropertyName": "data"
        },
        "typeVersion": 1
      },
      {
        "id": "normalize_kcmetro_01_events",
        "name": "Normalize KCMETRO-01 Events",
        "type": "n8n-nodes-base.code",
        "position": [
          1600,
          1960
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch KCMETRO-01 Alerts').first().json || {};\nconst landing = $('Land KCMETRO-01 Raw Payload').first().json || {};\nconst lkgLookup = ($('Parse Last Known Good').first().json || {}).bySource || {};\nconst sourceId = '07_GOVERNMENT_SAFETY_ALERTS:KCMETRO-01';\nconst sourceName = 'King County Metro GTFS Realtime service alerts';\nconst sourceUrl = 'https://s3.amazonaws.com/kcm-alerts-realtime-prod/alerts_enhanced.json';\nconst sourceKind = 'transit_json';\nconst routeSeedSections = [\"WP1_UW\",\"WP2_KENMORE_LFP\",\"WP3_BOTHELL\",\"WP5_REDMOND\",\"WP8_ISSAQUAH\"];\nconst staleAfterMinutes = 15;\nconst gazetteer = run.gazetteer || ['University of Washington', 'UW', 'U-District', 'University Way NE', 'Rainier Vista', 'Kane Hall', 'Burke-Gilman Trail', 'NE Pacific St', 'NE 45th St', 'Bothell Landing', 'Marymoor Park', 'East Lake Sammamish Trail', 'Kenmore', 'Lake Forest Park', 'Bothell', 'Woodinville', 'Redmond', 'Sammamish', 'Issaquah', 'SR-522', 'I-90', 'I-405', 'Seattle', 'Washington', 'King County'];\nconst now = Date.now();\nconst hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nfunction stripHtml(value) {\n  return String(value || '')\n    .replace(/<script[\\s\\S]*?<\\/script>/gi, ' ')\n    .replace(/<style[\\s\\S]*?<\\/style>/gi, ' ')\n    .replace(/<[^>]+>/g, ' ')\n    .replace(/&nbsp;/g, ' ')\n    .replace(/&amp;/g, '&')\n    .replace(/\\s+/g, ' ')\n    .trim();\n}\nfunction textOf(value) {\n  if (value === null || value === undefined) return '';\n  if (typeof value === 'string') return stripHtml(value);\n  if (Array.isArray(value)) return value.map(textOf).filter(Boolean).join(' | ');\n  if (typeof value === 'object') {\n    if (typeof value.rendered === 'string') return stripHtml(value.rendered);\n    if (typeof value.value === 'string') return stripHtml(value.value);\n    return stripHtml(JSON.stringify(value));\n  }\n  return String(value);\n}\nfunction safeJsonParse(value) {\n  if (typeof value !== 'string') return null;\n  try { return JSON.parse(value); } catch (error) { return null; }\n}\nfunction extractRecords(body) {\n  if (!body) return [];\n  if (sourceKind === 'nws') {\n    if (Array.isArray(body.features)) return body.features;\n    if (Array.isArray(body.items)) return body.items;\n    return [body];\n  }\n  if (sourceKind === 'wp_json') {\n    if (Array.isArray(body)) return body;\n    if (Array.isArray(body.posts)) return body.posts;\n    if (Array.isArray(body.items)) return body.items;\n    return [];\n  }\n  if (sourceKind === 'rss') {\n    const text = typeof body === 'string' ? body : textOf(body);\n    const matches = [...text.matchAll(/<item[\\s\\S]*?<\\/item>/gi)];\n    if (!matches.length) return [];\n    return matches.map((match, index) => {\n      const item = match[0];\n      const getTag = (tag) => {\n        const tagMatch = item.match(new RegExp('<' + tag + '[^>]*>([\\s\\S]*?)</' + tag + '>', 'i'));\n        return tagMatch ? stripHtml(tagMatch[1]) : '';\n      };\n      return { title: getTag('title'), link: getTag('link'), description: getTag('description'), pubDate: getTag('pubDate') || getTag('updated'), guid: getTag('guid') || getTag('link') || String(index) };\n    });\n  }\n  if (sourceKind === 'html_table') {\n    const text = typeof body === 'string' ? body : textOf(body);\n    const rows = [...text.matchAll(/<tr[\\s\\S]*?<\\/tr>/gi)];\n    return rows.map((match, index) => {\n      const row = match[0];\n      const cells = [...row.matchAll(/<t[dh][^>]*>([\\s\\S]*?)<\\/t[dh]>/gi)].map(m => stripHtml(m[1]));\n      return { cells, title: cells.join(' | '), link: '', pubDate: cells[0] || '', guid: cells.join('|') || String(index) };\n    });\n  }\n  if (sourceKind === 'transit_json') {\n    if (Array.isArray(body.entity)) return body.entity;\n    if (Array.isArray(body.entities)) return body.entities;\n    if (Array.isArray(body.alerts)) return body.alerts;\n    if (Array.isArray(body.data)) return body.data;\n    return Array.isArray(body) ? body : [];\n  }\n  return Array.isArray(body) ? body : [];\n}\nfunction getSourceRecordText(record) {\n  if (!record) return '';\n  if (sourceKind === 'nws') {\n    const p = record.properties || record;\n    return [p.headline, p.event, p.description, p.instruction, p.areaDesc, p.senderName, p.response].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'wp_json') {\n    return [record.title, record.excerpt, record.content, record.slug, record.link, record.status, record.date, record.modified].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'rss') {\n    return [record.title, record.description, record.link, record.pubDate, record.guid].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'html_table') {\n    return [record.title, record.pubDate, (record.cells || []).join(' | ')].map(textOf).filter(Boolean).join(' | ');\n  }\n  if (sourceKind === 'transit_json') {\n    const header = record.header || record;\n    const fields = [header.header_text, header.description_text, header.cause, header.effect, header.url, header.alert_id, header.severity_level];\n    if (record.active_period) {\n      const ap = Array.isArray(record.active_period) ? record.active_period[0] : record.active_period;\n      if (ap) fields.push(ap.start, ap.end);\n    }\n    return fields.map(textOf).filter(Boolean).join(' | ');\n  }\n  return textOf(record);\n}\nfunction getSourceRecordId(record, fallbackIndex) {\n  if (!record) return String(fallbackIndex);\n  if (sourceKind === 'nws') {\n    const p = record.properties || record;\n    return String(p.id || p.cap_id || p.identifier || p.eventCode || p.sent || fallbackIndex);\n  }\n  if (sourceKind === 'wp_json') {\n    return String(record.id || record.post_id || record.slug || record.guid || fallbackIndex);\n  }\n  if (sourceKind === 'rss') {\n    return String(record.guid || record.link || record.title || fallbackIndex);\n  }\n  if (sourceKind === 'html_table') {\n    return String((record.cells || []).join('|') || record.title || fallbackIndex);\n  }\n  if (sourceKind === 'transit_json') {\n    const header = record.header || record;\n    return String(header.id || header.alert_id || header.entity_id || header.url || fallbackIndex);\n  }\n  return String(fallbackIndex);\n}\nfunction firstTimestamp(record, fields) {\n  for (const field of fields) {\n    const parts = String(field).split('.');\n    let value = record;\n    for (const part of parts) {\n      if (value && typeof value === 'object' && part in value) value = value[part];\n      else { value = undefined; break; }\n    }\n    if (value) {\n      const parsed = new Date(value);\n      if (!isNaN(parsed.getTime())) return parsed.toISOString();\n    }\n  }\n  return null;\n}\nfunction detectEventType(text) {\n  const lower = text.toLowerCase();\n  if (/boil[s-]*water/.test(lower)) return 'boil_water_notice';\n  if (/(evacuat|evacuation)/.test(lower)) return 'evacuation';\n  if (/shelter in place|lockdown/.test(lower)) return 'shelter_in_place';\n  if (/hazmat|spill|leak|toxic|gas/.test(lower)) return 'hazmat';\n  if (/dam/.test(lower)) return 'dam_incident';\n  if (/public health|health advisory|advisory|heat|smoke|air quality/.test(lower)) return 'public_health_advisory';\n  if (/civil emergency|public warning|emergency alert|blast|explosion/.test(lower)) return 'civil_emergency';\n  if (/police|incident|fire|rescue|closure/.test(lower)) return 'public_safety_incident';\n  return sourceKind === 'transit_json' ? 'alternate_transport' : 'public_safety_incident';\n}\nfunction detectSeverity(text, eventType) {\n  const lower = text.toLowerCase();\n  if (eventType === 'alternate_transport') return 'info';\n  if (eventType === 'evacuation' || eventType === 'civil_emergency' || eventType === 'hazmat' || eventType === 'dam_incident') return 'emergency';\n  if (/warning/.test(lower)) return 'warning';\n  if (/watch/.test(lower)) return 'watch';\n  if (/advisory|notice/.test(lower)) return 'advisory';\n  if (/resolved|expired/.test(lower)) return 'info';\n  return 'info';\n}\nfunction detectStatus(text) {\n  const lower = text.toLowerCase();\n  if (/resolved|ended|expired|cancelled|canceled/.test(lower)) return 'resolved';\n  if (/monitoring|watch/.test(lower)) return 'monitoring';\n  return 'active';\n}\nfunction routeSectionsForText(text) {\n  const lower = text.toLowerCase();\n  const sections = [];\n  const checks = [\n    ['WP1_UW', ['university of washington', 'u-district', 'university way ne', 'rainier vista', 'kane hall', 'burke-gilman trail', 'ne pacific st', 'ne 45th st', 'uw']],\n    ['WP2_KENMORE_LFP', ['kenmore', 'lake forest park']],\n    ['WP3_BOTHELL', ['bothell', 'bothell landing', 'sr-522', 'bothell way']],\n    ['WP4_WOODINVILLE', ['woodinville']],\n    ['WP5_REDMOND', ['redmond', 'marymoor']],\n    ['WP6_MARYMOOR_ELST', ['marymoor park', 'east lake sammamish trail', 'marymoor connector trail']],\n    ['WP7_SAMMAMISH', ['sammamish', 'east lake sammamish trail']],\n    ['WP8_ISSAQUAH', ['issaquah', 'i-90', 'issaquah approach']]\n  ];\n  for (const [sectionId, tokens] of checks) {\n    if (tokens.some(token => lower.includes(token))) sections.push(sectionId);\n  }\n  return Array.from(new Set(sections.length ? sections : routeSeedSections.filter(sectionId => lower.includes('king county') || lower.includes('seattle') || lower.includes('washington'))));\n}\nfunction classifyOwnership(text) {\n  const lower = text.toLowerCase();\n  if (sourceKind === 'transit_json') return 'observation';\n  if (/air quality|smoke|excessive heat|severe thunderstorm|high wind|flood watch|flood warning|wildfire/.test(lower) && !/evacuat|civil emergency|boil water|hazmat|shelter in place/.test(lower)) return 'observation';\n  if (/boil[s-]*water|evacuat|shelter in place|hazmat|civil emergency|public warning|dam incident|public health|health advisory|police|fire|incident|closure/.test(lower)) return 'event';\n  if (/uw|university of washington|seattle|bothell|redmond|sammamish|issaquah|kenmore|woodinville|marymoor|burke-gilman|east lake sammamish|lake forest park/.test(lower)) return 'event';\n  return 'observation';\n}\nfunction mapRouteRelevance(text, eventType, kind) {\n  const sections = routeSectionsForText(text);\n  const lower = text.toLowerCase();\n  const hasExplicitRouteToken = gazetteer.some(token => lower.includes(token.toLowerCase()));\n  const classification = sections.length ? (hasExplicitRouteToken ? 'confirmed_route_relevant' : 'possible_route_relevant') : 'not_route_relevant';\n  let method = 'route_stop_mapping';\n  if (kind === 'transit_json') method = 'route_stop_mapping';\n  else if (kind === 'nws') method = hasExplicitRouteToken ? 'route_point_query' : 'cap_geometry_intersection';\n  else if (kind === 'html_table' || kind === 'rss' || kind === 'wp_json') method = hasExplicitRouteToken ? 'text_landmark_match' : 'ugc_same_area_match';\n  const manualReviewRequired = classification !== 'confirmed_route_relevant' || kind === 'rss' || kind === 'html_table';\n  const distanceKm = classification === 'confirmed_route_relevant' ? 0 : (classification === 'possible_route_relevant' ? 1.0 : null);\n  return {\n    classification,\n    method,\n    distance_km: distanceKm,\n    matched_tokens: gazetteer.filter(token => lower.includes(token.toLowerCase())),\n    bounding_box_match: sections.length > 0,\n    manual_review_required: manualReviewRequired,\n    reason: classification === 'confirmed_route_relevant' ? 'Strong local token match or direct route evidence.' : (classification === 'possible_route_relevant' ? 'Local proximity or shared locality only.' : 'No route token or direct route evidence found.')\n  };\n}\nfunction locationFromText(text, record) {\n  const lower = text.toLowerCase();\n  const namedArea = gazetteer.find(token => lower.includes(token.toLowerCase())) || null;\n  const point = record && record.geometry && Array.isArray(record.geometry.coordinates) ? record.geometry.coordinates : null;\n  if (point && point.length >= 2 && typeof point[0] === 'number' && typeof point[1] === 'number') {\n    return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: point[1], longitude: point[0], route_section_ids: routeSectionsForText(text) };\n  }\n  if (record && typeof record.x === 'number' && typeof record.y === 'number') {\n    return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: record.y, longitude: record.x, route_section_ids: routeSectionsForText(text) };\n  }\n  return { name: namedArea || text.slice(0, 120), named_area: namedArea, latitude: null, longitude: null, route_section_ids: routeSectionsForText(text) };\n}\nfunction eventRouteImpact(eventType, relevanceClass, kind, text) {\n  if (kind === 'transit_json') return 'alternate_transport_only';\n  if (relevanceClass === 'confirmed_route_relevant') {\n    if (eventType === 'evacuation' || eventType === 'civil_emergency' || eventType === 'hazmat' || eventType === 'dam_incident' || eventType === 'boil_water_notice') return 'route_access_impacted';\n    return 'route_segment_impacted';\n  }\n  if (relevanceClass === 'possible_route_relevant') return 'manual_review_required';\n  if (/alternate transport/.test(text.toLowerCase())) return 'alternate_transport_only';\n  return 'no_known_route_impact';\n}\nfunction classifyCrossList(text) {\n  const lower = text.toLowerCase();\n  const cross = [];\n  if (/air quality|smoke/.test(lower)) cross.push('03_AIR_QUALITY');\n  if (/excessive heat|severe thunderstorm|high wind/.test(lower)) cross.push('02_WEATHER');\n  if (/flood/.test(lower)) cross.push('05_FLOOD_CONDITIONS');\n  if (/wildfire/.test(lower)) cross.push('04_WILDFIRE');\n  if (/trail closure|bridge closure|construction/.test(lower)) cross.push('01_ROUTE_CONDITIONS', '06_TRAIL_INFRASTRUCTURE_STATUS');\n  if (/alternate transport|transit|bus|service alert|route/.test(lower)) cross.push('02_WEATHER');\n  return Array.from(new Set(cross));\n}\nfunction buildEvent(record, index) {\n  const text = getSourceRecordText(record);\n  const eventType = detectEventType(text);\n  const relevance = mapRouteRelevance(text, eventType, sourceKind);\n  const sourceRecordRef = getSourceRecordId(record, index);\n  const recordHash = 'hash_' + hashString(text || JSON.stringify(record || null));\n  const observedAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.sent', 'properties.effective', 'properties.onset', 'properties.updated', 'properties.expires', 'properties.ends'] : sourceKind === 'wp_json' ? ['date', 'modified'] : sourceKind === 'rss' ? ['pubDate', 'updated'] : sourceKind === 'html_table' ? ['pubDate'] : ['header.created', 'header.updated', 'active_period.start', 'active_period.end']);\n  const effectiveAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.effective', 'properties.onset'] : ['effective_at', 'start', 'date', 'modified']);\n  const expiresAt = firstTimestamp(record, sourceKind === 'nws' ? ['properties.expires', 'properties.ends'] : ['expires_at', 'end']);\n  const location = locationFromText(text, record.geometry || record);\n  const routeImpact = eventRouteImpact(eventType, relevance.classification, sourceKind, text);\n  const severity = detectSeverity(text, eventType);\n  const status = detectStatus(text);\n  const isPrimary = relevance.classification !== 'not_route_relevant' && !classifyCrossList(text).length;\n  const officialCategory = sourceKind === 'nws' ? text.split(' | ')[1] || null : null;\n  const publicAction = sourceKind === 'nws' ? textOf(record.properties && record.properties.instruction) || null : null;\n  return {\n    event_id: sourceId + ':' + recordHash,\n    event_type: eventType,\n    status,\n    severity,\n    route_impact: routeImpact,\n    title: text.split(' | ')[0] || text,\n    summary: text,\n    source_id: sourceId,\n    source_event_ref: sourceRecordRef,\n    published_source_ids: [sourceId],\n    observed_at: observedAt || run.generated_at,\n    effective_at: effectiveAt,\n    expires_at: expiresAt,\n    official_category: officialCategory,\n    public_action: publicAction,\n    cross_listed_to: classifyCrossList(text),\n    location,\n    route_relevance: relevance,\n    provenance: {\n      source_url: sourceUrl,\n      retrieved_at: run.generated_at,\n      source_record_hash: recordHash,\n      normalization_version: 'v0002'\n    }\n  };\n}\nfunction buildObservation(record, index) {\n  const text = getSourceRecordText(record);\n  const sourceRecordRef = getSourceRecordId(record, index);\n  const recordHash = 'hash_' + hashString(text || JSON.stringify(record || null));\n  const sections = routeSectionsForText(text);\n  const location = locationFromText(text, record.geometry || record);\n  return {\n    observation_id: sourceId + ':obs:' + recordHash,\n    observation_type: sourceKind === 'transit_json' ? 'alternate_transport' : 'public_health_context',\n    title: text.split(' | ')[0] || text,\n    summary: text,\n    source_id: sourceId,\n    observed_at: firstTimestamp(record, sourceKind === 'nws' ? ['properties.sent', 'properties.updated'] : ['date', 'modified', 'pubDate']) || run.generated_at,\n    status: detectStatus(text),\n    route_section_ids: sections,\n    public_action: null,\n    provenance: {\n      source_url: sourceUrl,\n      retrieved_at: run.generated_at,\n      source_record_hash: recordHash,\n      normalization_version: 'v0002'\n    },\n    related_event_ids: []\n  };\n}\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst records = extractRecords(body);\nlet events = [];\nlet observations = [];\nlet parserWarnings = [];\nlet parserErrors = [];\nfor (let i = 0; i < records.length; i++) {\n  const record = records[i];\n  const text = getSourceRecordText(record);\n  const ownership = classifyOwnership(text);\n  const cross = classifyCrossList(text);\n  if (ownership === 'event' && cross.length && sourceKind !== 'transit_json') {\n    // Cross-listed items stay available as observations so lane 07 does not claim primary ownership.\n    observations.push(buildObservation(record, i));\n    continue;\n  }\n  if (ownership === 'event') {\n    events.push(buildEvent(record, i));\n  } else if (ownership === 'observation') {\n    observations.push(buildObservation(record, i));\n  }\n}\nif (!records.length && body && typeof body === 'object' && !Array.isArray(body)) {\n  parserWarnings.push('No structured records were extracted, but the source response was structurally valid.');\n}\nconst fetchError = fetch.error ? String(fetch.error.message || fetch.error) : (parserErrors.length ? parserErrors[0] : null);\nconst lkg = lkgLookup[sourceId];\nconst lkgRetrievedAt = lkg && lkg.source_health && lkg.source_health.retrieved_at ? Date.parse(lkg.source_health.retrieved_at) : NaN;\nconst lkgStaleAfterMinutes = lkg && lkg.source_health && typeof lkg.source_health.stale_after_minutes === 'number' ? lkg.source_health.stale_after_minutes : staleAfterMinutes;\nconst lkgUsable = !!fetchError && !!lkg && Number.isFinite(lkgRetrievedAt) && ((Date.now() - lkgRetrievedAt) / 60000) <= lkgStaleAfterMinutes && Array.isArray(lkg.source_health) === false;\nlet sourceHealth;\nlet finalEvents = events;\nlet finalObservations = observations;\nlet warnings = parserWarnings.slice();\nlet errors = parserErrors.slice();\nif (fetchError && lkg && Number.isFinite(lkgRetrievedAt) && ((Date.now() - lkgRetrievedAt) / 60000) <= lkgStaleAfterMinutes) {\n  finalEvents = Array.isArray(lkg.events) ? lkg.events : [];\n  finalObservations = Array.isArray(lkg.observations) ? lkg.observations : [];\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: 'using_last_known_good',\n    retrieved_at: lkg.source_health.retrieved_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: finalEvents.length,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: lkg.source_health.last_observation_at ?? lkg.source_health.retrieved_at,\n    warnings: [...(lkg.source_health.warnings || []), 'Live fetch failed; serving last-known-good data retrieved at ' + lkg.source_health.retrieved_at + '.'],\n    errors: [fetchError]\n  };\n} else if (fetchError) {\n  finalEvents = [];\n  finalObservations = [];\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: 'failed',\n    retrieved_at: run.generated_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: 0,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: run.generated_at,\n    warnings: warnings,\n    errors: [fetchError]\n  };\n} else {\n  sourceHealth = {\n    schema_version: '1.0.0',\n    connector_id: run.connector_id,\n    source_id: sourceId,\n    source_name: sourceName,\n    status: finalEvents.length ? 'ok' : 'empty_but_valid',\n    retrieved_at: run.generated_at,\n    stale_after_minutes: staleAfterMinutes,\n    record_count: finalEvents.length,\n    http_status: fetch.statusCode ?? fetch.status ?? null,\n    last_observation_at: finalObservations.length ? finalObservations[finalObservations.length - 1].observed_at : run.generated_at,\n    warnings: finalEvents.length ? warnings : [...warnings, 'No primary lane-07 events retained for this source in this run.'],\n    errors: []\n  };\n}\nconst rawLandingPath = landing.file_path || null;\nreturn [{ json: {\n  schema_version: '1.0.0',\n  connector_id: run.connector_id,\n  connector_name: run.connector_name,\n  connector_version: run.connector_version,\n  lane: run.lane,\n  run_id: run.run_id,\n  generated_at: run.generated_at,\n  source_id: sourceId,\n  source_name: sourceName,\n  source_url: sourceUrl,\n  source_kind: sourceKind,\n  raw_landing_path: rawLandingPath,\n  raw_content_hash: landing.content_hash || null,\n  source_health: sourceHealth,\n  events: finalEvents,\n  observations: finalObservations,\n  route_sections: Array.from(new Set([...new Set(finalEvents.flatMap(event => Array.isArray(event.location && event.location.route_section_ids) ? event.location.route_section_ids : [])), ...new Set(finalObservations.flatMap(observation => Array.isArray(observation.route_section_ids) ? observation.route_section_ids : []))])),\n  warnings,\n  errors\n} }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "merge_normalized_branches",
        "name": "Merge Normalized Branches",
        "type": "n8n-nodes-base.merge",
        "position": [
          2020,
          1040
        ],
        "parameters": {
          "numberInputs": 8
        },
        "typeVersion": 3.1
      },
      {
        "id": "aggregate_normalized_branches",
        "name": "Aggregate Normalized Branches",
        "type": "n8n-nodes-base.code",
        "position": [
          2260,
          1040
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst run = $('Initialize Run Metadata').first().json;\nconst inputs = $input.all().map(item => item.json || {});\nconst sourceHealth = inputs.map(item => item.source_health).filter(Boolean);\nconst events = inputs.flatMap(item => Array.isArray(item.events) ? item.events : []);\nconst observations = inputs.flatMap(item => Array.isArray(item.observations) ? item.observations : []);\nconst sourceWarnings = inputs.flatMap(item => Array.isArray(item.warnings) ? item.warnings : []);\nconst sourceErrors = inputs.flatMap(item => Array.isArray(item.errors) ? item.errors : []);\nconst sourceIdsUsed = inputs.map(item => item.source_id).filter(Boolean);\nconst sourcePayloadRefs = inputs.map(item => item.raw_landing_path).filter(Boolean);\nconst allNotes = inputs.flatMap(item => Array.isArray(item.provenance_notes) ? item.provenance_notes : []);\nreturn [{ json: {\n  schema_version: run.schema_version,\n  connector_id: run.connector_id,\n  connector_name: run.connector_name,\n  connector_version: run.connector_version,\n  lane: run.lane,\n  run_id: run.run_id,\n  generated_at: run.generated_at,\n  published_at: null,\n  output_root: run.output_root,\n  run_stamp: run.run_stamp,\n  metadata: {},\n  manifest_ref: { manifest_id: run.connector_id + '-v0002', schema_version: '1.0.0' },\n  source_health: sourceHealth,\n  events: events,\n  observations: observations,\n  route_sections: [],\n  provenance: {\n    source_ids_used: sourceIdsUsed,\n    route_gpx_ref: run.canonical_gpx,\n    source_payload_refs: sourcePayloadRefs,\n    normalization_notes: allNotes.concat(sourceWarnings),\n    research_trace_refs: [\n      'IMPLEMENTATION_RECOMMENDATION.md',\n      'ROUTE_RELEVANCE_AND_THRESHOLDS.md',\n      'OVERLAP_NOTES.md',\n      '00_SHARED_AUTONOMOUS_CONNECTOR_BUILD_STANDARD.md'\n    ]\n  },\n  source_warnings: sourceWarnings,\n  source_errors: sourceErrors,\n  source_payload_refs: sourcePayloadRefs,\n  route_seed_sections: [\"WP1_UW\",\"WP2_KENMORE_LFP\",\"WP3_BOTHELL\",\"WP4_WOODINVILLE\",\"WP5_REDMOND\",\"WP6_MARYMOOR_ELST\",\"WP7_SAMMAMISH\",\"WP8_ISSAQUAH\"],\n  source_items: inputs\n} }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "deduplicate_events",
        "name": "Deduplicate Events",
        "type": "n8n-nodes-base.code",
        "position": [
          2500,
          1040
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst input = $input.first().json;\nfunction clean(value) { return String(value || '').toLowerCase().replace(/[^a-z0-9]+/g, ' ').replace(/\\s+/g, ' ').trim(); }\nfunction eventKey(event) {\n  return [\n    event.event_id || '',\n    clean(event.title),\n    clean(event.summary),\n    clean(event.source_event_ref),\n    clean(event.event_type),\n    clean((event.location && event.location.named_area) || ''),\n    String(event.observed_at || ''),\n    String(event.effective_at || '')\n  ].join('|');\n}\nfunction observationKey(observation) {\n  return [observation.observation_id || '', clean(observation.title), clean(observation.summary), clean(observation.source_id), String(observation.observed_at || '')].join('|');\n}\nconst seenEvents = new Map();\nconst dedupedEvents = [];\nconst duplicates = [];\nfor (const event of input.events || []) {\n  const key = eventKey(event);\n  if (seenEvents.has(key)) {\n    const prior = seenEvents.get(key);\n    prior.published_source_ids = Array.from(new Set([...(prior.published_source_ids || []), ...(event.published_source_ids || [])]));\n    prior.cross_listed_to = Array.from(new Set([...(prior.cross_listed_to || []), ...(event.cross_listed_to || [])]));\n    prior.location = prior.location || event.location;\n    prior.route_relevance = prior.route_relevance || event.route_relevance;\n    duplicates.push(event);\n    continue;\n  }\n  const clone = JSON.parse(JSON.stringify(event));\n  seenEvents.set(key, clone);\n  dedupedEvents.push(clone);\n}\nconst seenObservations = new Map();\nconst dedupedObservations = [];\nconst duplicateObservations = [];\nfor (const observation of input.observations || []) {\n  const key = observationKey(observation);\n  if (seenObservations.has(key)) {\n    duplicateObservations.push(observation);\n    continue;\n  }\n  const clone = JSON.parse(JSON.stringify(observation));\n  seenObservations.set(key, clone);\n  dedupedObservations.push(clone);\n}\nreturn [{ json: {\n  ...input,\n  events_before_dedup: (input.events || []).length,\n  observations_before_dedup: (input.observations || []).length,\n  deduplicated_events: dedupedEvents,\n  deduplicated_observations: dedupedObservations,\n  duplicates_removed: duplicates.length,\n  duplicate_events: duplicates,\n  duplicate_observations_removed: duplicateObservations.length,\n  duplicate_observations: duplicateObservations\n} }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "build_route_sections_rollups",
        "name": "Build Route Sections Rollups",
        "type": "n8n-nodes-base.code",
        "position": [
          2740,
          1040
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst input = $input.first().json;\nconst sectionMap = {\n  'WP1_UW': { route_section_id: 'WP1_UW', route_section_name: 'University of Washington / U-District', status: 'clear', event_ids: [], observation_ids: [], updated_at: input.generated_at },\n  'WP2_KENMORE_LFP': { route_section_id: 'WP2_KENMORE_LFP', route_section_name: 'Kenmore / Lake Forest Park', status: 'clear', event_ids: [], observation_ids: [], updated_at: input.generated_at },\n  'WP3_BOTHELL': { route_section_id: 'WP3_BOTHELL', route_section_name: 'Bothell', status: 'clear', event_ids: [], observation_ids: [], updated_at: input.generated_at },\n  'WP4_WOODINVILLE': { route_section_id: 'WP4_WOODINVILLE', route_section_name: 'Woodinville', status: 'clear', event_ids: [], observation_ids: [], updated_at: input.generated_at },\n  'WP5_REDMOND': { route_section_id: 'WP5_REDMOND', route_section_name: 'Redmond', status: 'clear', event_ids: [], observation_ids: [], updated_at: input.generated_at },\n  'WP6_MARYMOOR_ELST': { route_section_id: 'WP6_MARYMOOR_ELST', route_section_name: 'Marymoor Park / East Lake Sammamish Trail', status: 'clear', event_ids: [], observation_ids: [], updated_at: input.generated_at },\n  'WP7_SAMMAMISH': { route_section_id: 'WP7_SAMMAMISH', route_section_name: 'Sammamish', status: 'clear', event_ids: [], observation_ids: [], updated_at: input.generated_at },\n  'WP8_ISSAQUAH': { route_section_id: 'WP8_ISSAQUAH', route_section_name: 'Issaquah', status: 'clear', event_ids: [], observation_ids: [], updated_at: input.generated_at }\n};\nfunction applyImpact(section, event) {\n  const impact = String(event.route_impact || '');\n  if (impact === 'route_access_impacted') section.status = 'closed';\n  else if (impact === 'route_segment_impacted' && section.status !== 'closed') section.status = 'impacted';\n  else if ((impact === 'manual_review_required' || impact === 'possible_route_impact') && section.status === 'clear') section.status = 'caution';\n  else if (impact === 'alternate_transport_only' && section.status === 'clear') section.status = 'alternate_only';\n}\nfor (const event of input.deduplicated_events || []) {\n  const ids = Array.isArray(event.location && event.location.route_section_ids) ? event.location.route_section_ids : [];\n  for (const id of ids) {\n    const section = sectionMap[id];\n    if (!section) continue;\n    section.event_ids.push(event.event_id);\n    applyImpact(section, event);\n  }\n}\nfor (const observation of input.deduplicated_observations || []) {\n  const ids = Array.isArray(observation.route_section_ids) ? observation.route_section_ids : [];\n  for (const id of ids) {\n    const section = sectionMap[id];\n    if (!section) continue;\n    section.observation_ids.push(observation.observation_id);\n    if (section.status === 'clear') section.status = observation.observation_type === 'alternate_transport' ? 'alternate_only' : 'caution';\n  }\n}\nreturn [{ json: { ...input, route_sections: Object.values(sectionMap) } }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "validate_candidate_envelope",
        "name": "Validate Candidate Envelope",
        "type": "n8n-nodes-base.code",
        "position": [
          2980,
          1040
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst input = $input.first().json;\nconst errors = [];\nconst warnings = [];\nconst requiredRootFields = ['schema_version','connector_id','connector_name','connector_version','lane','run_id','generated_at','published_at','source_health','events','observations','route_sections','provenance','manifest_ref','metadata'];\nfor (const field of requiredRootFields) {\n  if (!(field in input)) errors.push('Missing required root field: ' + field);\n}\nif (!Array.isArray(input.source_health)) errors.push('source_health must be an array');\nif (!Array.isArray(input.events)) errors.push('events must be an array');\nif (!Array.isArray(input.observations)) errors.push('observations must be an array');\nif (!Array.isArray(input.route_sections)) errors.push('route_sections must be an array');\nconst allowedDataStatus = ['ok','degraded','stale','no_relevant_events','failed_validation','failed_fetch','blocked','using_last_known_good'];\nconst allowedEventType = ['civil_emergency','evacuation','shelter_in_place','hazmat','public_safety_incident','public_health_advisory','boil_water_notice','dam_incident','alternate_transport'];\nconst allowedStatus = ['active','monitoring','resolved','expired'];\nconst allowedSeverity = ['none','info','advisory','watch','warning','emergency'];\nconst allowedImpact = ['no_known_route_impact','possible_route_impact','route_segment_impacted','route_access_impacted','alternate_transport_only','manual_review_required'];\nconst allowedClass = ['confirmed_route_relevant','possible_route_relevant','alternate_transport_only','not_route_relevant'];\nconst allowedMethod = ['route_point_query','cap_geometry_intersection','ugc_same_area_match','text_landmark_match','local_gazetteer_coordinate_match','route_stop_mapping','manual_review_hold'];\nif (input.data_status && !allowedDataStatus.includes(input.data_status)) errors.push('Invalid data_status: ' + input.data_status);\nfor (const source of input.source_health || []) {\n  const required = ['schema_version','connector_id','source_id','source_name','status','retrieved_at','stale_after_minutes','record_count','http_status','last_observation_at','warnings','errors'];\n  for (const field of required) if (!(field in source)) errors.push('Source health missing field: ' + field + ' (' + (source.source_id || 'unknown') + ')');\n}\nfor (const event of input.events || []) {\n  const required = ['event_id','event_type','status','severity','route_impact','title','summary','source_id','source_event_ref','published_source_ids','observed_at','effective_at','expires_at','location','route_relevance','provenance'];\n  for (const field of required) if (!(field in event)) errors.push('Event missing field: ' + field + ' (' + (event.event_id || 'unknown') + ')');\n  if (!allowedEventType.includes(event.event_type)) errors.push('Invalid event_type: ' + event.event_type + ' (' + (event.event_id || 'unknown') + ')');\n  if (!allowedStatus.includes(event.status)) errors.push('Invalid event status: ' + event.status + ' (' + (event.event_id || 'unknown') + ')');\n  if (!allowedSeverity.includes(event.severity)) errors.push('Invalid severity: ' + event.severity + ' (' + (event.event_id || 'unknown') + ')');\n  if (!allowedImpact.includes(event.route_impact)) errors.push('Invalid route_impact: ' + event.route_impact + ' (' + (event.event_id || 'unknown') + ')');\n  if (!event.route_relevance || !allowedClass.includes(event.route_relevance.classification)) errors.push('Invalid route_relevance.classification (' + (event.event_id || 'unknown') + ')');\n  if (!event.route_relevance || !allowedMethod.includes(event.route_relevance.method)) errors.push('Invalid route_relevance.method (' + (event.event_id || 'unknown') + ')');\n}\nfor (const obs of input.observations || []) {\n  const required = ['observation_id','observation_type','title','summary','source_id','observed_at','status'];\n  for (const field of required) if (!(field in obs)) errors.push('Observation missing field: ' + field + ' (' + (obs.observation_id || 'unknown') + ')');\n}\n\n\n\nif (!input.metadata || typeof input.metadata !== 'object') errors.push('metadata must be an object');\nconst candidateValidationPassed = errors.length === 0;\nif (!candidateValidationPassed) warnings.push('Validation failed; candidate will be quarantined and published output suppressed.');\nreturn [{ json: { ...input, validation_errors: errors, validation_warnings: warnings, candidate_validation_passed: candidateValidationPassed, validator_version: '1.0.0' } }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "build_candidate_artifact",
        "name": "Build Candidate Artifact",
        "type": "n8n-nodes-base.code",
        "position": [
          3220,
          1040
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst input = $input.first().json;\nconst hashString = (inputText) => { let hash = 2166136261; for (let i = 0; i < inputText.length; i++) { hash ^= inputText.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst sourceStatuses = (input.source_health || []).map(source => source.status);\nconst anyFailed = sourceStatuses.some(status => status === 'failed');\nconst anyStale = sourceStatuses.some(status => status === 'stale' || status === 'using_last_known_good');\nconst anyLkg = sourceStatuses.some(status => status === 'using_last_known_good');\nconst allFailed = (input.source_health || []).length > 0 && sourceStatuses.every(status => status === 'failed');\nconst allLkg = sourceStatuses.length > 0 && sourceStatuses.every(status => status === 'using_last_known_good');\nconst hasEvents = (input.deduplicated_events || input.events || []).length > 0;\nconst schemaValid = input.candidate_validation_passed !== false;\nlet dataStatus = 'ok';\nif (!schemaValid) dataStatus = 'failed_validation';\nelse if (allFailed) dataStatus = 'failed_fetch';\nelse if (allLkg) dataStatus = 'using_last_known_good';\nelse if (!hasEvents && !anyFailed && !anyStale) dataStatus = 'no_relevant_events';\nelse if (anyFailed || anyStale) dataStatus = 'degraded';\nconst freshnessState = allFailed ? 'unknown' : ((anyFailed || anyStale) ? 'stale' : 'fresh');\nconst staleSourceIds = (input.source_health || []).filter(source => source.status === 'stale' || source.status === 'using_last_known_good').map(source => source.source_id);\nconst mergedEvents = input.deduplicated_events || input.events || [];\nconst mergedObservations = input.deduplicated_observations || input.observations || [];\nconst nowIso = input.generated_at;\nconst normalizedEnvelope = {\n  schema_version: input.schema_version,\n  connector_id: input.connector_id,\n  connector_name: input.connector_name,\n  connector_version: input.connector_version,\n  lane: input.lane,\n  run_id: input.run_id,\n  generated_at: input.generated_at,\n  published_at: null,\n  data_status: dataStatus,\n  freshness: {\n    overall_state: freshnessState,\n    computed_at: nowIso,\n    oldest_relevant_source_age_minutes: Math.max(0, ...((input.source_health || []).map(source => {\n      const retrieved = Date.parse(source.retrieved_at || '');\n      return Number.isFinite(retrieved) ? Math.floor((Date.now() - retrieved) / 60000) : 0;\n    }))),\n    stale_source_ids: staleSourceIds\n  },\n  manifest_ref: input.manifest_ref || { manifest_id: input.connector_id + '-v0002', schema_version: '1.0.0' },\n  source_health: input.source_health || [],\n  connector_health: {\n    schema_version: '1.0.0',\n    connector_id: input.connector_id,\n    status: schemaValid ? ((anyFailed || anyStale) ? 'degraded' : 'ok') : 'failed',\n    failed_stage: schemaValid ? null : 'validation',\n    warning_count: (input.validation_warnings || []).length + (input.source_warnings || []).length,\n    error_count: (input.validation_errors || []).length + (input.source_errors || []).length,\n    used_last_known_good: anyLkg,\n    candidate_written: true,\n    published_written: false\n  },\n  events: mergedEvents,\n  observations: mergedObservations,\n  route_sections: input.route_sections || [],\n  provenance: {\n    source_ids_used: (input.provenance && input.provenance.source_ids_used) || (input.source_health || []).map(source => source.source_id),\n    route_gpx_ref: (input.provenance && input.provenance.route_gpx_ref) || input.canonical_gpx || 'data/route/UnivWA-Issaquah.gpx',\n    source_payload_refs: (input.provenance && input.provenance.source_payload_refs) || input.source_payload_refs || [],\n    normalization_notes: (input.provenance && input.provenance.normalization_notes) || [],\n    research_trace_refs: (input.provenance && input.provenance.research_trace_refs) || []\n  },\n  validation_state: {\n    schema_valid: schemaValid,\n    route_relevance_valid: true,\n    freshness_valid: true,\n    dedup_valid: true,\n    candidate_validation_passed: schemaValid,\n    published_from_candidate: false,\n    validator_version: input.validator_version || '1.0.0'\n  },\n  metadata: {\n    summary_headline: hasEvents ? 'Route-relevant government safety alerts detected.' : 'No current route-relevant government safety alerts.',\n    alternate_transport_block_present: mergedObservations.some(obs => obs.observation_type === 'alternate_transport'),\n    manual_review_queue_count: mergedEvents.filter(event => event.route_relevance && event.route_relevance.manual_review_required).length,\n    overlap_notes_ref: 'OVERLAP_NOTES.md',\n    source_notes: input.source_warnings || [],\n    lkg_used_source_ids: (input.source_health || []).filter(source => source.status === 'using_last_known_good').map(source => source.source_id),\n    lkg_used: anyLkg,\n    notes: (input.validation_warnings || []).concat(input.source_warnings || [])\n  }\n};\nconst normalizedPath = input.output_root + '/normalized/07_GOVERNMENT_SAFETY_ALERTS/07_GOVERNMENT_SAFETY_ALERTS_normalized_' + input.run_stamp + '.json';\nconst candidatePath = input.output_root + '/candidate/07_GOVERNMENT_SAFETY_ALERTS/07_GOVERNMENT_SAFETY_ALERTS_candidate_' + input.run_stamp + '.json';\nconst normalizedBuffer = Buffer.from(JSON.stringify(normalizedEnvelope, null, 2), 'utf8');\nconst candidateBuffer = Buffer.from(JSON.stringify(normalizedEnvelope, null, 2), 'utf8');\nconst normalizedBinary = await this.helpers.prepareBinaryData(normalizedBuffer, normalizedPath.split('/').pop(), 'application/json');\nconst candidateBinary = await this.helpers.prepareBinaryData(candidateBuffer, candidatePath.split('/').pop(), 'application/json');\nreturn [\n  { json: { file_path: normalizedPath, file_name: normalizedPath.split('/').pop(), artifact_type: 'normalized', candidate_envelope: normalizedEnvelope, content_hash: 'hash_' + hashString(JSON.stringify(normalizedEnvelope)) }, binary: { data: normalizedBinary } },\n  { json: { file_path: candidatePath, file_name: candidatePath.split('/').pop(), artifact_type: 'candidate', candidate_envelope: normalizedEnvelope, content_hash: 'hash_' + hashString(JSON.stringify(normalizedEnvelope)) }, binary: { data: candidateBinary } }\n];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "write_candidate_artifact",
        "name": "Write Candidate Artifact",
        "type": "n8n-nodes-base.readWriteFile",
        "position": [
          3460,
          1040
        ],
        "parameters": {
          "options": {},
          "fileName": "={{ $json.file_path }}",
          "operation": "write",
          "dataPropertyName": "data"
        },
        "typeVersion": 1
      },
      {
        "id": "publish_gate_decision",
        "name": "Publish Gate Decision",
        "type": "n8n-nodes-base.code",
        "position": [
          3700,
          1040
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "\nconst validation = $('Validate Candidate Envelope').first().json;\nconst candidate = $('Build Candidate Artifact').first().json.candidate_envelope;\nconst shouldPublish = !!validation.candidate_validation_passed;\nconst reason = shouldPublish ? 'Validation passed' : 'Validation failed: ' + ((validation.validation_errors || []).slice(0, 5).join('; ') || 'unknown');\nreturn [{ json: {\n  should_publish: shouldPublish,\n  reason,\n  candidate_envelope: candidate,\n  validation_errors: validation.validation_errors || [],\n  validation_warnings: validation.validation_warnings || [],\n  run_id: candidate.run_id,\n  run_stamp: candidate.generated_at.replace(/[-:]/g, '').replace(/\\.\\d{3}Z$/, 'Z'),\n  output_root: candidate.provenance && candidate.provenance.output_root ? candidate.provenance.output_root : '/files/uw-issy-connectors',\n  connector_id: candidate.connector_id,\n  connector_name: candidate.connector_name,\n  connector_version: candidate.connector_version,\n  lane: candidate.lane,\n  generated_at: candidate.generated_at,\n  source_health: candidate.source_health || [],\n  route_sections: candidate.route_sections || [],\n  freshness: candidate.freshness || null,\n  provenance: candidate.provenance || null,\n  source_errors: candidate.source_errors || [],\n  source_warnings: candidate.source_warnings || []\n} }];\n"
        },
        "typeVersion": 2
      },
      {
        "id": "build_final_artifact_bundle",
        "name": "Build Final Artifact Bundle",
        "type": "n8n-nodes-base.code",
        "position": [
          3940,
          1040
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "const gate = $input.first().json;\nconst candidateEnvelope = gate.candidate_envelope || $('Build Candidate Artifact').first().json.candidate_envelope;\nconst runStamp = gate.run_stamp || candidateEnvelope.generated_at.replace(/[-:]/g, '').replace(/\\.\\d{3}Z$/, 'Z');\nconst publishedAt = new Date().toISOString();\nconst outputRoot = gate.output_root || '/files/uw-issy-connectors';\nconst laneRoot = '07_GOVERNMENT_SAFETY_ALERTS';\nconst publishedPath = outputRoot + '/published/' + laneRoot + '/' + laneRoot + '_published_' + runStamp + '.json';\nconst publishedCurrentPath = outputRoot + '/published/' + laneRoot + '/current.json';\nconst lkgCurrentPath = outputRoot + '/last_known_good/' + laneRoot + '/current.json';\nconst lkgStablePath = outputRoot + '/last_known_good/' + laneRoot + '/' + laneRoot + '_lkg.json';\nconst lkgArchivePath = outputRoot + '/last_known_good/' + laneRoot + '/archive/' + runStamp + '.json';\nconst validationLogPath = outputRoot + '/logs/' + laneRoot + '/validation_log_' + runStamp + '.jsonl';\nconst statusPath = outputRoot + '/health/' + laneRoot + '/status.json';\nconst handoffPath = outputRoot + '/handoff/' + laneRoot + '/' + gate.connector_id + '-' + runStamp + '-001.json';\nconst executionEvidencePath = outputRoot + '/health/' + laneRoot + '/execution_evidence_' + runStamp + '.json';\nconst validationErrors = gate.validation_errors || [];\nconst validationWarnings = gate.validation_warnings || [];\nconst sourceHealth = gate.source_health || [];\nconst sourceStatuses = sourceHealth.map(source => source.status);\nconst anyFailed = sourceStatuses.some(status => status === 'failed');\nconst anyStale = sourceStatuses.some(status => status === 'stale' || status === 'using_last_known_good');\nconst anyLkg = sourceStatuses.some(status => status === 'using_last_known_good');\nconst allFailed = sourceHealth.length > 0 && sourceStatuses.every(status => status === 'failed');\nconst allLkg = sourceStatuses.length > 0 && sourceStatuses.every(status => status === 'using_last_known_good');\nconst events = candidateEnvelope.events || [];\nconst hasEvents = events.length > 0;\nlet dataStatus = candidateEnvelope.data_status;\nif (!validationErrors.length && allFailed) dataStatus = 'failed_fetch';\nelse if (!validationErrors.length && allLkg) dataStatus = 'using_last_known_good';\nelse if (!validationErrors.length && !hasEvents && !anyFailed && !anyStale) dataStatus = 'no_relevant_events';\nelse if (!validationErrors.length && (anyFailed || anyStale)) dataStatus = 'degraded';\nconst freshnessState = allFailed ? 'unknown' : ((anyFailed || anyStale) ? 'stale' : 'fresh');\nconst publishedEnvelope = gate.should_publish ? {\n  ...candidateEnvelope,\n  published_at: publishedAt,\n  data_status: dataStatus,\n  freshness: {\n    ...(candidateEnvelope.freshness || {}),\n    overall_state: freshnessState,\n    computed_at: publishedAt,\n    stale_source_ids: (sourceHealth || []).filter(source => source.status === 'stale' || source.status === 'using_last_known_good').map(source => source.source_id)\n  },\n  connector_health: {\n    ...(candidateEnvelope.connector_health || {}),\n    status: (anyFailed || anyStale) ? 'degraded' : 'ok',\n    failed_stage: null,\n    warning_count: (validationWarnings.length + (gate.source_warnings || []).length),\n    error_count: (validationErrors.length + (gate.source_errors || []).length),\n    used_last_known_good: anyLkg,\n    candidate_written: true,\n    published_written: true\n  },\n  validation_state: {\n    ...(candidateEnvelope.validation_state || {}),\n    published_from_candidate: true,\n    candidate_validation_passed: true\n  }\n} : null;\nconst statusDoc = {\n  lane_id: laneRoot,\n  run_id: gate.run_id || candidateEnvelope.run_id,\n  last_fetch_at: gate.generated_at || candidateEnvelope.generated_at,\n  last_success_at: gate.should_publish ? publishedAt : null,\n  status: gate.should_publish ? (anyFailed || anyStale ? 'degraded' : 'ok') : (validationErrors.length ? 'failed_validation' : (allFailed ? 'failed_fetch' : 'degraded')),\n  source_health: sourceHealth,\n  error_messages: validationErrors.concat(gate.source_errors || []),\n  stale_data_fields: (sourceHealth || []).filter(source => source.status === 'stale' || source.status === 'using_last_known_good').map(source => source.source_id),\n  used_last_known_good: anyLkg,\n  published_artifact_ref: gate.should_publish ? publishedPath : null,\n  last_known_good_ref: lkgCurrentPath\n};\nconst handoffDoc = {\n  run_id: gate.run_id || candidateEnvelope.run_id,\n  connector_id: gate.connector_id,\n  lane: gate.lane || candidateEnvelope.lane,\n  connector_version: gate.connector_version || candidateEnvelope.connector_version,\n  generated_at: gate.generated_at || candidateEnvelope.generated_at,\n  published_at: gate.should_publish ? publishedAt : null,\n  published_artifact: gate.should_publish ? publishedCurrentPath : null,\n  status: gate.should_publish ? (anyFailed || anyStale ? 'degraded' : 'ok') : 'failed',\n  ready_for_workflow_08: !!gate.should_publish,\n  source_ids_used: (gate.source_health || []).map(source => source.source_id),\n  route_sections: gate.route_sections || [],\n  candidate_ref: candidateEnvelope.run_id,\n  metadata: {\n    used_last_known_good: anyLkg,\n    validation_errors: validationErrors,\n    validation_warnings: validationWarnings\n  }\n};\nconst executionEvidenceDoc = {\n  run_id: gate.run_id || candidateEnvelope.run_id,\n  lane_id: laneRoot,\n  workflow_id: '08g3JNwQPVSxUl2H',\n  workflow_name: 'v02.UWI_LANE07',\n  connector_version: 'v0002',\n  generated_at: gate.generated_at || candidateEnvelope.generated_at,\n  evaluated_at: publishedAt,\n  n8n_project: 'UWISSY',\n  should_publish: !!gate.should_publish,\n  data_status: dataStatus,\n  status: gate.should_publish ? ((anyFailed || anyStale) ? 'degraded' : 'ok') : 'failed',\n  source_count: sourceHealth.length,\n  failed_source_count: sourceHealth.filter(source => source.status === 'failed').length,\n  event_count: Array.isArray(candidateEnvelope.events) ? candidateEnvelope.events.length : 0,\n  observation_count: Array.isArray(candidateEnvelope.observations) ? candidateEnvelope.observations.length : 0,\n  artifact_paths: {\n    candidate: outputRoot + '/candidate/' + laneRoot + '/' + laneRoot + '_candidate_' + runStamp + '.json',\n    normalized: outputRoot + '/normalized/' + laneRoot + '/' + laneRoot + '_normalized_' + runStamp + '.json',\n    published: gate.should_publish ? publishedPath : null,\n    published_current: gate.should_publish ? publishedCurrentPath : null,\n    last_known_good_current: gate.should_publish ? lkgCurrentPath : null,\n    validation_log: validationLogPath,\n    status: statusPath,\n    handoff: handoffPath\n  }\n};\nconst validationLogLines = [];\nfor (const source of sourceHealth) {\n  validationLogLines.push(JSON.stringify({\n    source_id: source.source_id,\n    run_id: gate.run_id || candidateEnvelope.run_id,\n    stage: 'source_normalize',\n    check: 'source_health',\n    result: source.status === 'failed' ? 'fail' : 'pass',\n    message: source.status + ' / ' + source.record_count + ' record(s)',\n    timestamp: gate.generated_at || candidateEnvelope.generated_at\n  }));\n}\nvalidationLogLines.push(JSON.stringify({\n  source_id: 'CONNECTOR',\n  run_id: gate.run_id || candidateEnvelope.run_id,\n  stage: 'validation',\n  check: 'candidate_schema',\n  result: gate.should_publish ? 'pass' : 'fail',\n  message: gate.should_publish ? 'Candidate passed validation.' : (validationErrors.join('; ') || 'Validation failed.'),\n  timestamp: gate.generated_at || candidateEnvelope.generated_at\n}));\nvalidationLogLines.push(JSON.stringify({\n  source_id: 'CONNECTOR',\n  run_id: gate.run_id || candidateEnvelope.run_id,\n  stage: 'publish_gate',\n  check: 'promotion',\n  result: gate.should_publish ? 'pass' : 'fail',\n  message: gate.should_publish ? 'Candidate promoted to published.' : gate.reason,\n  timestamp: publishedAt\n}));\nconst validationLogText = validationLogLines.join('\\n');\nconst makeBinaryItem = async (filePath, payload, artifactType, mime = 'application/json') => {\n  const body = typeof payload === 'string' ? payload : JSON.stringify(payload, null, 2);\n  const buffer = Buffer.from(body, 'utf8');\n  const binary = await this.helpers.prepareBinaryData(buffer, filePath.split('/').pop(), mime);\n  return {\n    json: {\n      file_path: filePath,\n      file_name: filePath.split('/').pop(),\n      artifact_type: artifactType,\n      content_hash: 'hash_' + (function(inputText){ let hash = 2166136261; for (let i = 0; i < inputText.length; i++) { hash ^= inputText.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); })(body)\n    },\n    binary: { data: binary }\n  };\n};\nconst items = [];\nif (gate.should_publish && publishedEnvelope) {\n  items.push(await makeBinaryItem(publishedPath, publishedEnvelope, 'published'));\n  items.push(await makeBinaryItem(publishedCurrentPath, { run_id: gate.run_id || candidateEnvelope.run_id, artifact: publishedPath }, 'published_current'));\n  items.push(await makeBinaryItem(lkgCurrentPath, publishedEnvelope, 'last_known_good_current'));\n  items.push(await makeBinaryItem(lkgStablePath, publishedEnvelope, 'last_known_good_stable'));\n  items.push(await makeBinaryItem(lkgArchivePath, publishedEnvelope, 'last_known_good_archive'));\n}\nitems.push(await makeBinaryItem(validationLogPath, validationLogText, 'validation_log', 'text/plain'));\nitems.push(await makeBinaryItem(statusPath, statusDoc, 'status'));\nitems.push(await makeBinaryItem(handoffPath, handoffDoc, 'handoff'));\nitems.push(await makeBinaryItem(executionEvidencePath, executionEvidenceDoc, 'execution_evidence'));\nreturn items;"
        },
        "typeVersion": 2
      },
      {
        "id": "write_final_artifacts",
        "name": "Write Final Artifacts",
        "type": "n8n-nodes-base.readWriteFile",
        "position": [
          4180,
          1040
        ],
        "parameters": {
          "options": {},
          "fileName": "={{ $json.file_path }}",
          "operation": "write",
          "dataPropertyName": "data"
        },
        "typeVersion": 1
      },
      {
        "id": "final_status_report",
        "name": "Final Status Report",
        "type": "n8n-nodes-base.code",
        "position": [
          4420,
          1040
        ],
        "parameters": {
          "mode": "runOnceForAllItems",
          "jsCode": "const gate = $('Publish Gate Decision').first().json;\nconst bundleItems = $('Build Final Artifact Bundle').all();\nconst candidate = $('Build Candidate Artifact').first().json.candidate_envelope;\nconst sourceHealth = Array.isArray(gate.source_health) ? gate.source_health : (Array.isArray(candidate.source_health) ? candidate.source_health : []);\nconst sourceStatuses = sourceHealth.map(source => source.status);\nconst failedSourceCount = sourceStatuses.filter(status => status === 'failed').length;\nconst usingLastKnownGood = sourceStatuses.includes('using_last_known_good') || !!(candidate.connector_health && candidate.connector_health.used_last_known_good);\nconst staleOrDegraded = sourceStatuses.some(status => ['failed', 'stale', 'degraded', 'using_last_known_good'].includes(status));\nconst dataStatus = gate.data_status || candidate.data_status || 'unknown';\nconst runStamp = gate.run_stamp || (candidate.generated_at || '').replace(/[-:]/g, '').replace(/\\.\\d{3}Z$/, 'Z');\nconst outputRoot = gate.output_root || '/files/uw-issy-connectors';\nconst laneRoot = '07_GOVERNMENT_SAFETY_ALERTS';\nconst publishedPath = gate.should_publish ? outputRoot + '/published/' + laneRoot + '/' + laneRoot + '_published_' + runStamp + '.json' : null;\nconst artifactTypes = bundleItems.map(item => item.json && item.json.artifact_type).filter(Boolean);\nlet status = 'FAILED';\nif (gate.should_publish) status = (staleOrDegraded || usingLastKnownGood || ['degraded', 'using_last_known_good', 'failed_fetch', 'stale'].includes(dataStatus)) ? 'DEGRADED' : 'PASSED';\nconst events = Array.isArray(candidate.events) ? candidate.events : [];\nconst observations = Array.isArray(candidate.observations) ? candidate.observations : [];\nreturn [{ json: {\n  run_id: gate.run_id || candidate.run_id,\n  status,\n  data_status: dataStatus,\n  candidate_written: true,\n  published_written: !!gate.should_publish,\n  quarantine_written: false,\n  validation_log_written: artifactTypes.includes('validation_log'),\n  status_written: artifactTypes.includes('status'),\n  handoff_written: artifactTypes.includes('handoff'),\n  execution_evidence_written: artifactTypes.includes('execution_evidence'),\n  artifact_count_written: bundleItems.length,\n  event_count: events.length,\n  source_count: sourceHealth.length,\n  failed_source_count: failedSourceCount,\n  using_last_known_good: usingLastKnownGood,\n  government_alert_count: events.length,\n  observation_count: observations.length,\n  published_path: publishedPath,\n  timestamp: candidate.generated_at\n} }];"
        },
        "typeVersion": 2
      }
    ],
    "connections": {
      "Manual Trigger": {
        "main": [
          [
            {
              "node": "Initialize Run Metadata",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Schedule Trigger": {
        "main": [
          [
            {
              "node": "Initialize Run Metadata",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Deduplicate Events": {
        "main": [
          [
            {
              "node": "Build Route Sections Rollups",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Fetch ST-01 Alerts": {
        "main": [
          [
            {
              "node": "Land ST-01 Raw Payload",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Fetch UW-01 Alerts": {
        "main": [
          [
            {
              "node": "Land UW-01 Raw Payload",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Fetch DOH-02 Alerts": {
        "main": [
          [
            {
              "node": "Land DOH-02 Raw Payload",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Fetch NWS-01 Alerts": {
        "main": [
          [
            {
              "node": "Land NWS-01 Raw Payload",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Fetch SEA-01 Alerts": {
        "main": [
          [
            {
              "node": "Land SEA-01 Raw Payload",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Read Last Known Good": {
        "main": [
          [
            {
              "node": "Parse Last Known Good",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Fetch SEAFD-01 Alerts": {
        "main": [
          [
            {
              "node": "Land SEAFD-01 Raw Payload",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Fetch SEAPD-01 Alerts": {
        "main": [
          [
            {
              "node": "Land SEAPD-01 Raw Payload",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Publish Gate Decision": {
        "main": [
          [
            {
              "node": "Build Final Artifact Bundle",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Write Final Artifacts": {
        "main": [
          [
            {
              "node": "Final Status Report",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Land ST-01 Raw Payload": {
        "main": [
          [
            {
              "node": "Write ST-01 Raw Landing",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Land UW-01 Raw Payload": {
        "main": [
          [
            {
              "node": "Write UW-01 Raw Landing",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Normalize ST-01 Events": {
        "main": [
          [
            {
              "node": "Merge Normalized Branches",
              "type": "main",
              "index": 6
            }
          ]
        ]
      },
      "Normalize UW-01 Events": {
        "main": [
          [
            {
              "node": "Merge Normalized Branches",
              "type": "main",
              "index": 2
            }
          ]
        ]
      },
      "Fetch KCMETRO-01 Alerts": {
        "main": [
          [
            {
              "node": "Land KCMETRO-01 Raw Payload",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Initialize Run Metadata": {
        "main": [
          [
            {
              "node": "Read Last Known Good",
              "type": "main",
              "index": 0
            },
            {
              "node": "Fetch NWS-01 Alerts",
              "type": "main",
              "index": 0
            },
            {
              "node": "Fetch SEA-01 Alerts",
              "type": "main",
              "index": 0
            },
            {
              "node": "Fetch UW-01 Alerts",
              "type": "main",
              "index": 0
            },
            {
              "node": "Fetch SEAFD-01 Alerts",
              "type": "main",
              "index": 0
            },
            {
              "node": "Fetch SEAPD-01 Alerts",
              "type": "main",
              "index": 0
            },
            {
              "node": "Fetch DOH-02 Alerts",
              "type": "main",
              "index": 0
            },
            {
              "node": "Fetch ST-01 Alerts",
              "type": "main",
              "index": 0
            },
            {
              "node": "Fetch KCMETRO-01 Alerts",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Land DOH-02 Raw Payload": {
        "main": [
          [
            {
              "node": "Write DOH-02 Raw Landing",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Land NWS-01 Raw Payload": {
        "main": [
          [
            {
              "node": "Write NWS-01 Raw Landing",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Land SEA-01 Raw Payload": {
        "main": [
          [
            {
              "node": "Write SEA-01 Raw Landing",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Normalize DOH-02 Events": {
        "main": [
          [
            {
              "node": "Merge Normalized Branches",
              "type": "main",
              "index": 5
            }
          ]
        ]
      },
      "Normalize NWS-01 Events": {
        "main": [
          [
            {
              "node": "Merge Normalized Branches",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Normalize SEA-01 Events": {
        "main": [
          [
            {
              "node": "Merge Normalized Branches",
              "type": "main",
              "index": 1
            }
          ]
        ]
      },
      "Write ST-01 Raw Landing": {
        "main": [
          [
            {
              "node": "Normalize ST-01 Events",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Write UW-01 Raw Landing": {
        "main": [
          [
            {
              "node": "Normalize UW-01 Events",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Build Candidate Artifact": {
        "main": [
          [
            {
              "node": "Write Candidate Artifact",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Write Candidate Artifact": {
        "main": [
          [
            {
              "node": "Publish Gate Decision",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Write DOH-02 Raw Landing": {
        "main": [
          [
            {
              "node": "Normalize DOH-02 Events",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Write NWS-01 Raw Landing": {
        "main": [
          [
            {
              "node": "Normalize NWS-01 Events",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Write SEA-01 Raw Landing": {
        "main": [
          [
            {
              "node": "Normalize SEA-01 Events",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Land SEAFD-01 Raw Payload": {
        "main": [
          [
            {
              "node": "Write SEAFD-01 Raw Landing",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Land SEAPD-01 Raw Payload": {
        "main": [
          [
            {
              "node": "Write SEAPD-01 Raw Landing",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Merge Normalized Branches": {
        "main": [
          [
            {
              "node": "Aggregate Normalized Branches",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Normalize SEAFD-01 Events": {
        "main": [
          [
            {
              "node": "Merge Normalized Branches",
              "type": "main",
              "index": 3
            }
          ]
        ]
      },
      "Normalize SEAPD-01 Events": {
        "main": [
          [
            {
              "node": "Merge Normalized Branches",
              "type": "main",
              "index": 4
            }
          ]
        ]
      },
      "Write SEAFD-01 Raw Landing": {
        "main": [
          [
            {
              "node": "Normalize SEAFD-01 Events",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Write SEAPD-01 Raw Landing": {
        "main": [
          [
            {
              "node": "Normalize SEAPD-01 Events",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Build Final Artifact Bundle": {
        "main": [
          [
            {
              "node": "Write Final Artifacts",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Land KCMETRO-01 Raw Payload": {
        "main": [
          [
            {
              "node": "Write KCMETRO-01 Raw Landing",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Normalize KCMETRO-01 Events": {
        "main": [
          [
            {
              "node": "Merge Normalized Branches",
              "type": "main",
              "index": 7
            }
          ]
        ]
      },
      "Validate Candidate Envelope": {
        "main": [
          [
            {
              "node": "Build Candidate Artifact",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Build Route Sections Rollups": {
        "main": [
          [
            {
              "node": "Validate Candidate Envelope",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Write KCMETRO-01 Raw Landing": {
        "main": [
          [
            {
              "node": "Normalize KCMETRO-01 Events",
              "type": "main",
              "index": 0
            }
          ]
        ]
      },
      "Aggregate Normalized Branches": {
        "main": [
          [
            {
              "node": "Deduplicate Events",
              "type": "main",
              "index": 0
            }
          ]
        ]
      }
    },
    "settings": {
      "timezone": "America/Los_Angeles",
      "executionOrder": "v1"
    },
    "staticData": {
      "node:Schedule Trigger": {
        "recurrenceRules": []
      }
    },
    "meta": null,
    "pinData": null,
    "versionId": "f46394a3-bc05-475c-8e50-0d52cc3cecde",
    "activeVersionId": "f46394a3-bc05-475c-8e50-0d52cc3cecde",
    "versionCounter": 5,
    "triggerCount": 1,
    "tags": [],
    "shared": [
      {
        "updatedAt": "2026-08-02T10:41:12.357Z",
        "createdAt": "2026-08-02T10:41:12.357Z",
        "role": "workflow:owner",
        "workflowId": "08g3JNwQPVSxUl2H",
        "projectId": "Y0Ygmqe59jevHoeV",
        "project": {
          "updatedAt": "2026-07-10T05:38:45.677Z",
          "createdAt": "2026-06-02T20:51:11.579Z",
          "id": "Y0Ygmqe59jevHoeV",
          "name": "John Brooks <john@biketourfrance.net>",
          "type": "personal",
          "icon": null,
          "description": null,
          "creatorId": "5dadc5f6-6bff-44cf-9566-6b82f3d768dc"
        }
      }
    ],
    "versionMetadata": {
      "name": "v02.UWI_LANE07",
      "description": null
    }
  }
]
