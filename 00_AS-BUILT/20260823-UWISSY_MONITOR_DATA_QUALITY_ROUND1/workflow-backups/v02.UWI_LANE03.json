{
  "updatedAt": "2026-08-19T05:42:31.973Z",
  "createdAt": "2026-08-02T10:05:10.721Z",
  "id": "qlM2XIv2BbFSh3in",
  "name": "v02.UWI_LANE03",
  "description": null,
  "active": false,
  "isArchived": false,
  "nodes": [
    {
      "id": "manual_trigger",
      "name": "Manual Trigger",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        220,
        120
      ],
      "parameters": {},
      "notes": ""
    },
    {
      "id": "schedule_trigger",
      "name": "Schedule Trigger",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        220,
        240
      ],
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "0 3,13 * * *"
            }
          ]
        }
      },
      "notes": "Every 60 minutes (1 hour). (Migrated from deprecated n8n-nodes-base.cron, whose stored parameter shape did not match this n8n version's actual Cron node schema and would not have fired.)"
    },
    {
      "id": "initialize_run_metadata",
      "name": "Initialize Run Metadata",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        500,
        180
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const now = new Date();\nconst runStamp = now.toISOString().replace(/[-:]/g, '').replace(/\\.\\d{3}Z$/, 'Z');\nconst sourceConfigs = {\n  \"ECO_01\": {\n    \"source_id\": \"03_AIR_QUALITY:ECO-01\",\n    \"source_name\": \"Washington State Department of Ecology Air Quality Monitoring Hourly Results\",\n    \"source_url\": \"https://gis.ecology.wa.gov/arcgis/rest/services/AirQualityMonitoringHourlyResults/MapServer/0/query?f=json&where=HourPriorToLatest%3D0&outFields=*&returnGeometry=true\",\n    \"retrieval_method\": \"arcgis_rest_query\",\n    \"freshness_threshold_minutes\": 90,\n    \"family\": \"ecology_point\",\n    \"source_folder\": \"ECO-01\"\n  },\n  \"ECO_02\": {\n    \"source_id\": \"03_AIR_QUALITY:ECO-02\",\n    \"source_name\": \"Washington State Department of Ecology Smoke Forecast\",\n    \"source_url\": \"https://gis.ecology.wa.gov/arcgis/rest/services/SmokeForecast/MapServer/0/query?f=json&where=1%3D1&outFields=*&returnGeometry=true\",\n    \"retrieval_method\": \"arcgis_rest_query\",\n    \"freshness_threshold_minutes\": 720,\n    \"family\": \"ecology_forecast\",\n    \"source_folder\": \"ECO-02\"\n  },\n  \"PSCAA_02\": {\n    \"source_id\": \"03_AIR_QUALITY:PSCAA-02\",\n    \"source_name\": \"Puget Sound Clean Air Agency Air Quality Burn Ban Status\",\n    \"source_url\": \"https://www.pscleanair.gov/168/Air-Quality-Burn-Ban-Status\",\n    \"retrieval_method\": \"html_page_fetch\",\n    \"freshness_threshold_minutes\": 1440,\n    \"family\": \"burn_ban\",\n    \"source_folder\": \"PSCAA-02\"\n  },\n  \"AIRNOW_02\": {\n    \"source_id\": \"03_AIR_QUALITY:AIRNOW-02\",\n    \"source_name\": \"AirNow public file products\",\n    \"source_url\": \"https://files.airnowtech.org/airnow/today/reportingarea.dat\",\n    \"retrieval_method\": \"public_file_download\",\n    \"freshness_threshold_minutes\": 90,\n    \"family\": \"airnow_context\",\n    \"source_folder\": \"AIRNOW-02\"\n  },\n  \"WASMOKE_01\": {\n    \"source_id\": \"03_AIR_QUALITY:WASMOKE-01\",\n    \"source_name\": \"Washington Smoke Blog RSS\",\n    \"source_url\": \"https://wasmoke.blogspot.com/feeds/posts/default?alt=rss\",\n    \"retrieval_method\": \"rss_feed_fetch\",\n    \"freshness_threshold_minutes\": 720,\n    \"family\": \"smoke_rss\",\n    \"source_folder\": \"WASMOKE-01\"\n  },\n  \"NWS_AQ_01\": {\n    \"source_id\": \"03_AIR_QUALITY:NWS-AQ-01\",\n    \"source_name\": \"NWS Air Quality Alert feed\",\n    \"source_url\": \"https://api.weather.gov/alerts/active?area=WA&event=Air%20Quality%20Alert\",\n    \"retrieval_method\": \"geojson_api\",\n    \"freshness_threshold_minutes\": 15,\n    \"family\": \"nws_alert\",\n    \"source_folder\": \"NWS-AQ-01\"\n  },\n  \"PSCAA_01\": {\n    \"source_id\": \"03_AIR_QUALITY:PSCAA-01\",\n    \"source_name\": \"Puget Sound Clean Air Agency NetworkMap session source\",\n    \"source_url\": \"https://secure.pscleanair.org/AirQuality/NetworkMap\",\n    \"retrieval_method\": \"session_bootstrap_page_fetch\",\n    \"freshness_threshold_minutes\": 90,\n    \"family\": \"pscaa_station\",\n    \"source_folder\": \"PSCAA-01\"\n  },\n  \"AIRNOW_01\": {\n    \"source_id\": \"03_AIR_QUALITY:AIRNOW-01\",\n    \"source_name\": \"AirNow API observations\",\n    \"source_url\": \"={{ $env.AIRNOW_API_KEY ? (\\\"https://www.airnowapi.org/aq/observation/zipCode/current/?format=application/json&zipCode=98027&distance=25&API_KEY=\\\" + $env.AIRNOW_API_KEY) : \\\"https://www.airnowapi.org/aq/observation/zipCode/current/?format=application/json&zipCode=98027&distance=25&API_KEY=\\\" }}\",\n    \"retrieval_method\": \"api_key_query_parameter\",\n    \"freshness_threshold_minutes\": 90,\n    \"family\": \"airnow_api\",\n    \"source_folder\": \"AIRNOW-01\"\n  }\n};\nreturn [{ json: {\n  schema_version: '1.0.0',\n  connector_id: '03_AIR_QUALITY',\n  connector_name: 'UW-Issaquah Air Quality Connector',\n  connector_version: 'v0002',\n  lane: '03_AIR_QUALITY',\n  workflow_name: 'v02.UWI_LANE03',\n  run_id: '03_AIR_QUALITY-' + runStamp + '-001',\n  run_stamp: runStamp,\n  generated_at: now.toISOString(),\n  published_at: null,\n  output_root: '/files/uw-issy-connectors',\n  canonical_gpx: 'data/route/UnivWA-Issaquah.gpx',\n  source_configs: sourceConfigs,\n  freshness_policy: { default_threshold_minutes: 90, stale_source_policy: 'preserve_last_known_good' },\n  route_filter_policy: { point_threshold_km: 12.87, polygon_requires_route_intersection: true, text_only_requires_lookup_match: true }\n} }];"
      },
      "notes": ""
    },
    {
      "id": "read_last_known_good",
      "name": "Read Last Known Good",
      "type": "n8n-nodes-base.readWriteFile",
      "typeVersion": 1,
      "position": [
        760,
        180
      ],
      "onError": "continueRegularOutput",
      "parameters": {
        "operation": "read",
        "fileSelector": "={{ $('Initialize Run Metadata').first().json.output_root }}/last_known_good/03_AIR_QUALITY/current.json",
        "options": {}
      },
      "alwaysOutputData": true
    },
    {
      "id": "parse_last_known_good",
      "name": "Parse Last Known Good",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1010,
        180
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst first = $input.first();\nif (!first || first.error || (first.json && first.json.error) || !first.binary || !first.binary.data) { return [{ json: { bySource: {} } }]; }\nlet parsed;\ntry { const buffer = await this.helpers.getBinaryDataBuffer(0, 'data'); parsed = JSON.parse(buffer.toString('utf8')); } catch (error) { return [{ json: { bySource: {} } }]; }\nif (!parsed || typeof parsed !== 'object' || !Array.isArray(parsed.source_health) || !Array.isArray(parsed.events)) { return [{ json: { bySource: {} } }]; }\nconst observations = Array.isArray(parsed.observations) ? parsed.observations : [];\nconst routeSections = Array.isArray(parsed.route_sections) ? parsed.route_sections : [];\nconst bySource = {};\nfor (const entry of parsed.source_health) {\n  if (!entry || typeof entry !== 'object' || !entry.source_id) continue;\n  if (!['ok', 'empty_but_valid', 'using_last_known_good'].includes(entry.status)) continue;\n  bySource[entry.source_id] = { source_health: entry, events: parsed.events.filter((event) => event && event.source_id === entry.source_id), observations: observations.filter((observation) => observation && observation.source_id === entry.source_id), route_sections: routeSections };\n}\nreturn [{ json: { bySource } }];"
      },
      "notes": ""
    },
    {
      "id": "fetch_eco_01",
      "name": "Fetch ECO-01 Monitor Results",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        220,
        420
      ],
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "GET",
        "url": "https://gis.ecology.wa.gov/arcgis/rest/services/AirQualityMonitoringHourlyResults/MapServer/0/query?f=json&where=HourPriorToLatest%3D0&outFields=*&returnGeometry=true",
        "responseFormat": "json",
        "options": {
          "timeout": 30000
        }
      },
      "notes": ""
    },
    {
      "id": "land_eco_01",
      "name": "Land ECO-01 Raw Payload",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        500,
        420
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch ECO-01 Monitor Results').first().json || {};\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst bodyText = typeof body === 'string' ? body : JSON.stringify(body ?? null);\nconst statusCode = fetch.statusCode ?? fetch.status ?? null;\nconst contentHash = 'hash_' + hashString(bodyText);\nconst filePath = run.output_root + '/raw/03_AIR_QUALITY/landings/ECO-01_landing_' + run.run_stamp + '.json';\nconst landing = { schema_version: '1.0.0', connector_id: run.connector_id, source_id: '03_AIR_QUALITY:ECO-01', source_name: 'Washington State Department of Ecology Air Quality Monitoring Hourly Results', source_url: 'https://gis.ecology.wa.gov/arcgis/rest/services/AirQualityMonitoringHourlyResults/MapServer/0/query?f=json&where=HourPriorToLatest%3D0&outFields=*&returnGeometry=true', retrieved_at: run.generated_at, http_status: statusCode, retrieval_method: 'arcgis_rest_query', content_hash: contentHash, body_length: bodyText.length, body: body, fetch_error: fetch.error ? stringifyError(fetch.error) : null };\nconst buffer = Buffer.from(JSON.stringify(landing, null, 2), 'utf8');\nconst binary = await this.helpers.prepareBinaryData(buffer, filePath.split('/').pop(), 'application/json');\nreturn [{ json: { file_path: filePath, source_id: landing.source_id, source_name: landing.source_name, content_hash: contentHash, retrieved_at: run.generated_at }, binary: { data: binary } }];"
      }
    },
    {
      "id": "write_eco_01_raw",
      "name": "Write ECO-01 Raw Landing",
      "type": "n8n-nodes-base.readWriteFile",
      "typeVersion": 1,
      "position": [
        760,
        420
      ],
      "parameters": {
        "operation": "write",
        "fileName": "={{ $json.file_path }}",
        "dataPropertyName": "data",
        "options": {}
      }
    },
    {
      "id": "normalize_eco_01",
      "name": "Normalize ECO-01 Events",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1010,
        420
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch ECO-01 Monitor Results').first().json || {};\nconst landing = $('Land ECO-01 Raw Payload').first().json || {};\nconst parseLkg = $('Parse Last Known Good').first().json || { bySource: {} };\nconst sourceConfig = run.source_configs && run.source_configs['ECO_01'] ? run.source_configs['ECO_01'] : {\n  \"source_id\": \"03_AIR_QUALITY:ECO-01\",\n  \"source_name\": \"Washington State Department of Ecology Air Quality Monitoring Hourly Results\",\n  \"family\": \"ecology_point\",\n  \"stale_after_minutes\": 90\n};\nconst lkg = parseLkg.bySource && parseLkg.bySource['03_AIR_QUALITY:ECO-01'] ? parseLkg.bySource['03_AIR_QUALITY:ECO-01'] : null;\nconst fetchErrorText = fetch.error ? stringifyError(fetch.error) : null;\nconst nowIso = run.generated_at;\nconst staleAfterMinutes = Number(sourceConfig.freshness_threshold_minutes ?? 90);\nconst sourceWarnings = [];\nconst sourceErrors = [];\nlet events = [];\nlet observations = [];\nlet routeSections = [];\nlet lastObservationAt = null;\nlet sourceTimestamp = null;\nconst routeSectionForStation = (name, lat, lon) => { const label = normalizeText(name).toLowerCase(); if (label.includes('issaquah') || label.includes('lake sammamish')) return 'aqp4_issaquah'; if (label.includes('bellevue') || label.includes('redmond') || label.includes('marymoor')) return 'aqp3_bellevue'; if (label.includes('lake forest park') || label.includes('kenmore') || label.includes('bothell')) return 'aqp2_lake_forest_park'; if (Number.isFinite(lat) && Number.isFinite(lon)) return 'aqp1_seattle'; return 'aqp1_seattle'; };\nconst pointDistanceKm = (aLat, aLon, bLat, bLon) => { const toRad = (deg) => (deg * Math.PI) / 180; const r = 6371; const dLat = toRad(bLat - aLat); const dLon = toRad(bLon - aLon); const lat1 = toRad(aLat); const lat2 = toRad(bLat); const x = Math.sin(dLat / 2) ** 2 + Math.cos(lat1) * Math.cos(lat2) * Math.sin(dLon / 2) ** 2; return 2 * r * Math.asin(Math.min(1, Math.sqrt(x))); };\nconst sectionAnchors = { aqp1_seattle: { lat: 47.655, lon: -122.333 }, aqp2_lake_forest_park: { lat: 47.754, lon: -122.289 }, aqp3_bellevue: { lat: 47.614, lon: -122.178 }, aqp4_issaquah: { lat: 47.532, lon: -122.032 } };\nconst severityFromCategory = (category) => { const text = normalizeText(category).toLowerCase(); if (text.includes('good')) return 'good'; if (text.includes('moderate')) return 'moderate'; if (text.includes('sensitive')) return 'usg'; if (text.includes('unhealthy')) return text.includes('very') ? 'very_unhealthy' : 'unhealthy'; if (text.includes('hazardous')) return 'hazardous'; return 'unknown'; };\nconst qFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return null; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst pickPollutant = (attrs) => { const candidates = [['pm25', Number(attrs.AQI_PM25), Number(attrs.PM25_Value), 'ug/m3', attrs.AQI_PM25_Cat], ['ozone', Number(attrs.AQI_O3), Number(attrs.O3_Value), 'ppm', attrs.AQI_O3_Cat], ['pm10', Number(attrs.AQI_PM10), Number(attrs.PM10_Value), 'ug/m3', attrs.AQI_PM10_Cat], ['no2', Number(attrs.AQI_NO2), Number(attrs.NO2_Value), 'ppb', attrs.AQI_NO2_Cat]].filter((candidate) => Number.isFinite(candidate[1])); if (!candidates.length) return null; candidates.sort((a, b) => b[1] - a[1]); return candidates[0][0]; };\nconst buildObservation = (feature, index) => { const attrs = safeObject(feature.attributes || feature); const geom = safeObject(feature.geometry || attrs.geometry || {}); const lat = Number(geom.y ?? geom.latitude ?? attrs.Latitude ?? attrs.latitude); const lon = Number(geom.x ?? geom.longitude ?? attrs.Longitude ?? attrs.longitude); const observedAt = toIsoUtc(attrs.DateTime_PST || attrs.ObservedAt || attrs.observed_at, run.generated_at); const siteId = String(attrs.SiteId ?? attrs.site_id ?? attrs.Site ?? attrs.site ?? index + 1); const siteName = normalizeText(attrs.SiteName ?? attrs.Site ?? attrs.site_name ?? 'Unknown site'); const siteLocation = normalizeText(attrs.SiteLocation ?? attrs.site_location ?? attrs.Location ?? attrs.location ?? ''); const aqiValue = Number(attrs.AQIValue ?? attrs.AQI ?? attrs.aqi_value); const aqiCategory = attrs.AQICategory ?? attrs.category ?? qFromAqi(aqiValue) ?? 'Unknown'; const severity = severityFromAqi(aqiValue) !== 'unknown' ? severityFromAqi(aqiValue) : severityFromCategory(aqiCategory); const routeSectionId = routeSectionForStation(siteName, lat, lon); const anchor = sectionAnchors[routeSectionId]; const distanceKm = Number.isFinite(lat) && Number.isFinite(lon) && anchor ? Number(pointDistanceKm(lat, lon, anchor.lat, anchor.lon).toFixed(2)) : null; const routeRelevance = { method: 'point_to_route_distance', distance_km: distanceKm, threshold_km: 12.87, route_section_id: routeSectionId }; const pm25 = Number.isFinite(Number(attrs.AQI_PM25)) || Number.isFinite(Number(attrs.PM25_Value)) ? { aqi: Number(attrs.AQI_PM25 ?? attrs.PM25_AQI ?? null), category: attrs.AQI_PM25_Cat ?? null, value: Number(attrs.PM25_Value ?? attrs.PM25 ?? null), units: 'ug/m3' } : null; const ozone = Number.isFinite(Number(attrs.AQI_O3)) || Number.isFinite(Number(attrs.O3_Value)) ? { aqi: Number(attrs.AQI_O3 ?? attrs.O3_AQI ?? null), category: attrs.AQI_O3_Cat ?? null, value: Number(attrs.O3_Value ?? attrs.O3 ?? null), units: 'ppm' } : null; const pm10 = Number.isFinite(Number(attrs.AQI_PM10)) || Number.isFinite(Number(attrs.PM10_Value)) ? { aqi: Number(attrs.AQI_PM10 ?? attrs.PM10_AQI ?? null), category: attrs.AQI_PM10_Cat ?? null, value: Number(attrs.PM10_Value ?? attrs.PM10 ?? null), units: 'ug/m3' } : null; const no2 = Number.isFinite(Number(attrs.AQI_NO2)) || Number.isFinite(Number(attrs.NO2_Value)) ? { aqi: Number(attrs.AQI_NO2 ?? attrs.NO2_AQI ?? null), category: attrs.AQI_NO2_Cat ?? null, value: Number(attrs.NO2_Value ?? attrs.NO2 ?? null), units: 'ppb' } : null; const observation = { observation_id: '03_AIR_QUALITY:ECO-01:' + siteId + ':' + observedAt, observation_type: 'current_air_quality', source_id: '03_AIR_QUALITY:ECO-01', source_record_id: siteId, observed_at: observedAt, retrieved_at: nowIso, station_name: siteName, station_location: siteLocation || null, aqi_value: Number.isFinite(aqiValue) ? aqiValue : null, aqi_category: aqiCategory, severity, dominant_pollutant: pickPollutant(attrs), pollutants: { pm25, pm10, ozone, no2 }, wildfire_smoke_related: false, route_relevance: routeRelevance, location: Number.isFinite(lat) && Number.isFinite(lon) ? { latitude: lat, longitude: lon, epsg: 4326 } : null }; if (Number.isFinite(aqiValue) && !lastObservationAt) lastObservationAt = observedAt; if (Number.isFinite(aqiValue) || Number.isFinite(lat) || Number.isFinite(lon)) sourceTimestamp = observedAt; return observation; };\nconst normalizeForecastLabel = (text) => { const value = normalizeText(text).toLowerCase(); if (value.includes('hazardous')) return 'Hazardous'; if (value.includes('very unhealthy')) return 'Very Unhealthy'; if (value.includes('unhealthy for sensitive')) return 'Unhealthy for Sensitive Groups'; if (value.includes('unhealthy')) return 'Unhealthy'; if (value.includes('moderate')) return 'Moderate'; if (value.includes('good')) return 'Good'; return normalizeText(text) || 'Unknown'; };\nconst body = fetch.body !== undefined ? fetch.body : (landing.body !== undefined ? landing.body : fetch);\nconst bodyText = typeof body === 'string' ? body : JSON.stringify(body ?? null);\nconst bodyObj = safeObject(body);\nconst bodyArr = safeArray(body);\nconst liveRecords = (() => { switch ('ecology_point') { case 'ecology_point': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); observations = features.map((feature, index) => buildObservation(feature, index)).filter((observation) => { const sectionId = observation.route_relevance.route_section_id; const anchor = sectionAnchors[sectionId]; if (!anchor || !observation.location) return true; const dist = Number(pointDistanceKm(observation.location.latitude, observation.location.longitude, anchor.lat, anchor.lon).toFixed(2)); observation.route_relevance.distance_km = dist; return dist <= observation.route_relevance.threshold_km; }); routeSections = []; return []; } case 'ecology_forecast': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); events = features.map((feature, index) => { const attrs = safeObject(feature.attributes || feature); const geom = safeObject(feature.geometry || attrs.geometry || {}); const siteName = normalizeText(attrs.SiteName ?? attrs.Site ?? attrs.site ?? attrs.Area ?? attrs.area ?? 'Seattle-Bellevue-Kent Valley'); const sourceRecordId = String(attrs.SmokeForecastId ?? attrs.ObjectId ?? attrs.objectid ?? attrs.id ?? index + 1); const label = normalizeForecastLabel(attrs.Day1 ?? attrs.DisplayText ?? attrs.Category ?? attrs.CategoryLabel ?? attrs.Day2 ?? attrs.Day3 ?? 'Good'); const severity = severityFromCategory(label); const geomText = JSON.stringify(geom); const eventRouteRelevant = Boolean(geomText && (geomText.includes('[') || geomText.includes('{'))); return { event_id: '03_AIR_QUALITY:ECO-01:' + siteName.replace(/[^a-z0-9]+/gi, '-').toLowerCase() + ':' + sourceRecordId, event_type: 'smoke_forecast', title: 'Ecology smoke forecast for ' + siteName, status: 'forecast', severity, category_label: label, effective_at: attrs.Date ? String(attrs.Date) : nowIso, expires_at: null, route_relevant: eventRouteRelevant, wildfire_smoke_related: true, areas: [siteName], source_id: '03_AIR_QUALITY:ECO-01', source_record_id: sourceRecordId, geometry_type: 'polygon', manual_review_required: false, route_sections: eventRouteRelevant ? ['aqp1_seattle', 'aqp2_lake_forest_park', 'aqp3_bellevue', 'aqp4_issaquah'] : [] }; }).filter((event) => event.route_relevant); routeSections = []; return []; } case 'burn_ban': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); let burnBanStatus = 'unknown'; if (lower.includes('stage 2')) burnBanStatus = 'stage_2'; else if (lower.includes('stage 1')) burnBanStatus = 'stage_1'; else if (lower.includes('no burn ban') || lower.includes('no ban') || lower.includes('burn ban status: no')) burnBanStatus = 'no_ban'; const event = { event_id: '03_AIR_QUALITY:ECO-01:burn-ban:' + run.run_stamp, event_type: 'burn_ban', title: 'PSCAA burn ban status', status: 'current', severity: burnBanStatus === 'unknown' ? 'unknown' : 'good', category_label: burnBanStatus === 'stage_2' ? 'Stage 2' : burnBanStatus === 'stage_1' ? 'Stage 1' : burnBanStatus === 'no_ban' ? 'No Ban' : 'Unknown', effective_at: null, expires_at: null, route_relevant: true, wildfire_smoke_related: false, areas: ['King County'], source_id: '03_AIR_QUALITY:ECO-01', source_record_id: 'king-county-burn-ban', geometry_type: 'text_only', manual_review_required: burnBanStatus === 'unknown', route_sections: ['aqp4_issaquah'] }; events = [event]; sourceWarnings.push(burnBanStatus === 'unknown' ? 'Burn-ban status could not be parsed confidently.' : 'Burn-ban status parsed from page text.'); return []; } case 'airnow_context': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); const matchAreas = ['seattle', 'bellevue', 'eastside', 'king county', 'issaquah', 'cascade foothills', 'seattle-bellevue-kent valley']; const matches = matchAreas.filter((area) => lower.includes(area)); if (matches.length) { sourceWarnings.push('AirNow reporting area matched contextual area names only; no primary route classification performed.'); } routeSections = []; events = []; observations = []; return []; } case 'smoke_rss': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); const lookup = ['Seattle', 'Bellevue', 'Eastside', 'King County', 'Issaquah', 'Cascade foothills', 'Seattle-Bellevue-Kent Valley']; const matched = lookup.filter((entry) => lower.includes(entry.toLowerCase())); if (matched.length) { const areaLabel = matched.join(', '); events = [{ event_id: '03_AIR_QUALITY:ECO-01:' + hashString(areaLabel + text).slice(0, 12), event_type: 'smoke_forecast', title: 'Washington Smoke Blog context for ' + areaLabel, status: 'forecast', severity: lower.includes('hazard') ? 'unhealthy' : lower.includes('smoke') ? 'moderate' : 'unknown', category_label: 'Contextual smoke report', effective_at: nowIso, expires_at: null, route_relevant: true, wildfire_smoke_related: true, areas: matched, source_id: '03_AIR_QUALITY:ECO-01', source_record_id: hashString(text).slice(0, 12), geometry_type: 'text_only', manual_review_required: false, route_sections: ['aqp3_bellevue', 'aqp4_issaquah'] }]; } else { events = []; } routeSections = []; return []; } case 'nws_alert': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); const relevant = features.filter((feature) => { const props = safeObject(feature.properties || feature); const areaDesc = normalizeText(props.areaDesc ?? props.area_desc ?? ''); const geocode = JSON.stringify(props.geocode ?? {}); const text = (areaDesc + ' ' + geocode).toLowerCase(); return normalizeText(props.event ?? '').toLowerCase() === 'air quality alert' && (text.includes('king county') || text.includes('washington') || text.includes('wa') || feature.geometry); }); events = relevant.map((feature, index) => { const props = safeObject(feature.properties || feature); const areaDesc = normalizeText(props.areaDesc ?? props.area_desc ?? 'King County'); const severity = severityFromCategory(props.severity ?? props.urgency ?? 'unknown'); const expires = toIsoUtc(props.expires ?? props.sent ?? null, null); return { event_id: '03_AIR_QUALITY:ECO-01:' + String(props.id ?? props.eventCode ?? index + 1), event_type: 'air_quality_alert', title: normalizeText(props.headline ?? props.event ?? 'Air Quality Alert'), status: 'current', severity, category_label: normalizeText(props.event ?? 'Air Quality Alert'), effective_at: toIsoUtc(props.sent ?? props.onset ?? null, nowIso), expires_at: expires, route_relevant: true, wildfire_smoke_related: false, areas: areaDesc ? areaDesc.split(',').map((part) => part.trim()).filter(Boolean) : ['King County'], source_id: '03_AIR_QUALITY:ECO-01', source_record_id: String(props.id ?? props.eventCode ?? index + 1), geometry_type: feature.geometry ? 'polygon' : 'county_zone', manual_review_required: false, route_sections: ['aqp4_issaquah'] }; }); routeSections = []; return []; } case 'pscaa_station': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); if (lower.includes('aqi') || lower.includes('station')) { sourceWarnings.push('PSCAA station page was reachable but no canonical station snapshot was extracted in this build.'); } routeSections = []; events = []; observations = []; return []; } case 'airnow_api': { const records = safeArray(bodyObj || bodyArr); const relevant = records.filter((record) => record && typeof record === 'object'); if (relevant.length) { observations = relevant.slice(0, 4).map((record, index) => { const aqiValue = Number(record.AQI ?? record.aqi ?? record.Value ?? null); const category = normalizeText(record.Category ?? record.category ?? qFromAqi(aqiValue) ?? 'Unknown'); const observedAt = toIsoUtc(record.DateObserved ? String(record.DateObserved) + 'T' + String(record.HourObserved ?? 0) + ':00:00Z' : record.ObservedAt ?? null, nowIso); const routeSectionId = index % 4 === 0 ? 'aqp1_seattle' : index % 4 === 1 ? 'aqp2_lake_forest_park' : index % 4 === 2 ? 'aqp3_bellevue' : 'aqp4_issaquah'; return { observation_id: '03_AIR_QUALITY:ECO-01:' + String(record.SiteID ?? record.zipCode ?? index + 1) + ':' + observedAt, observation_type: 'current_air_quality', source_id: '03_AIR_QUALITY:ECO-01', source_record_id: String(record.SiteID ?? record.zipCode ?? index + 1), observed_at: observedAt, retrieved_at: nowIso, station_name: normalizeText(record.ReportingArea ?? record.Name ?? 'AirNow reporting area'), station_location: null, aqi_value: Number.isFinite(aqiValue) ? aqiValue : null, aqi_category: category, severity: severityFromCategory(category), dominant_pollutant: null, pollutants: { pm25: null, pm10: null, ozone: null, no2: null }, wildfire_smoke_related: false, route_relevance: { method: 'point_to_route_distance', distance_km: null, threshold_km: 12.87, route_section_id: routeSectionId }, location: null }; }); } routeSections = []; return []; } default: return []; } })();\nconst liveCount = events.length + observations.length;\nconst cachedEvents = lkg ? safeArray(lkg.events) : [];\nconst cachedObservations = lkg ? safeArray(lkg.observations) : [];\nconst cachedRouteSections = lkg ? safeArray(lkg.route_sections) : [];\nconst cachedRecordCount = cachedEvents.length + cachedObservations.length;\nconst lkgRetrievedAt = lkg && lkg.source_health ? lkg.source_health.retrieved_at : null;\nconst lkgStaleAfterMinutes = lkg && lkg.source_health && Number.isFinite(Number(lkg.source_health.stale_after_minutes)) ? Number(lkg.source_health.stale_after_minutes) : staleAfterMinutes;\nconst lkgAgeMinutes = Number.isFinite(Date.parse(lkgRetrievedAt)) ? (Date.now() - Date.parse(lkgRetrievedAt)) / 60000 : Infinity;\nconst lkgUsable = !!lkg && Number.isFinite(lkgAgeMinutes) && lkgAgeMinutes <= lkgStaleAfterMinutes;\nif (fetchErrorText) sourceErrors.push(fetchErrorText);\nlet sourceHealth = null;\nif (fetchErrorText) {\n  if (lkgUsable) {\n    events = cachedEvents;\n    observations = cachedObservations;\n    routeSections = cachedRouteSections;\n    sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:ECO-01', source_name: 'Washington State Department of Ecology Air Quality Monitoring Hourly Results', status: 'using_last_known_good', retrieved_at: lkg.source_health.retrieved_at, stale_after_minutes: lkg.source_health.stale_after_minutes ?? staleAfterMinutes, record_count: cachedRecordCount, http_status: lkg.source_health.http_status ?? null, last_observation_at: lkg.source_health.last_observation_at ?? lkg.source_health.retrieved_at, warnings: [...safeArray(lkg.source_health.warnings), 'Live fetch failed; serving last-known-good data.'], errors: sourceErrors };\n  } else {\n    sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:ECO-01', source_name: 'Washington State Department of Ecology Air Quality Monitoring Hourly Results', status: 'failed', retrieved_at: run.generated_at, stale_after_minutes: staleAfterMinutes, record_count: 0, http_status: fetch.statusCode ?? fetch.status ?? null, last_observation_at: null, warnings: sourceWarnings, errors: sourceErrors };\n  }\n} else {\n  const lastObservationValue = maxOrNull([ ...observations.map((observation) => Date.parse(observation.observed_at)).filter((value) => Number.isFinite(value)), ...events.map((event) => Date.parse(event.effective_at)).filter((value) => Number.isFinite(value)) ]);\n  const status = liveCount ? 'ok' : 'empty_but_valid';\n  sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:ECO-01', source_name: 'Washington State Department of Ecology Air Quality Monitoring Hourly Results', status, retrieved_at: run.generated_at, stale_after_minutes: staleAfterMinutes, record_count: liveCount, http_status: fetch.statusCode ?? fetch.status ?? null, last_observation_at: Number.isFinite(lastObservationValue) ? new Date(lastObservationValue).toISOString() : (liveCount ? run.generated_at : null), warnings: sourceWarnings, errors: sourceErrors };\n}\nconst provenanceSources = [{ source_id: '03_AIR_QUALITY:ECO-01', source_name: 'Washington State Department of Ecology Air Quality Monitoring Hourly Results', retrieved_at: sourceHealth.retrieved_at, source_timestamp: sourceHealth.last_observation_at, status: sourceHealth.status, stale: sourceHealth.status === 'using_last_known_good' || sourceHealth.status === 'stale' }];\nreturn [{ json: { source_id: '03_AIR_QUALITY:ECO-01', source_name: 'Washington State Department of Ecology Air Quality Monitoring Hourly Results', source_health: sourceHealth, events, observations, route_sections: routeSections, warnings: sourceWarnings, errors: sourceErrors, provenance: { source_ids_used: ['03_AIR_QUALITY:ECO-01'], sources: provenanceSources } } }];"
      }
    },
    {
      "id": "fetch_eco_02",
      "name": "Fetch ECO-02 Smoke Forecast",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        220,
        620
      ],
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "GET",
        "url": "https://gis.ecology.wa.gov/arcgis/rest/services/SmokeForecast/MapServer/0/query?f=json&where=1%3D1&outFields=*&returnGeometry=true",
        "responseFormat": "json",
        "options": {
          "timeout": 30000
        }
      },
      "notes": ""
    },
    {
      "id": "land_eco_02",
      "name": "Land ECO-02 Raw Payload",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        500,
        620
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch ECO-02 Smoke Forecast').first().json || {};\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst bodyText = typeof body === 'string' ? body : JSON.stringify(body ?? null);\nconst statusCode = fetch.statusCode ?? fetch.status ?? null;\nconst contentHash = 'hash_' + hashString(bodyText);\nconst filePath = run.output_root + '/raw/03_AIR_QUALITY/landings/ECO-02_landing_' + run.run_stamp + '.json';\nconst landing = { schema_version: '1.0.0', connector_id: run.connector_id, source_id: '03_AIR_QUALITY:ECO-02', source_name: 'Washington State Department of Ecology Smoke Forecast', source_url: 'https://gis.ecology.wa.gov/arcgis/rest/services/SmokeForecast/MapServer/0/query?f=json&where=1%3D1&outFields=*&returnGeometry=true', retrieved_at: run.generated_at, http_status: statusCode, retrieval_method: 'arcgis_rest_query', content_hash: contentHash, body_length: bodyText.length, body: body, fetch_error: fetch.error ? stringifyError(fetch.error) : null };\nconst buffer = Buffer.from(JSON.stringify(landing, null, 2), 'utf8');\nconst binary = await this.helpers.prepareBinaryData(buffer, filePath.split('/').pop(), 'application/json');\nreturn [{ json: { file_path: filePath, source_id: landing.source_id, source_name: landing.source_name, content_hash: contentHash, retrieved_at: run.generated_at }, binary: { data: binary } }];"
      }
    },
    {
      "id": "write_eco_02_raw",
      "name": "Write ECO-02 Raw Landing",
      "type": "n8n-nodes-base.readWriteFile",
      "typeVersion": 1,
      "position": [
        760,
        620
      ],
      "parameters": {
        "operation": "write",
        "fileName": "={{ $json.file_path }}",
        "dataPropertyName": "data",
        "options": {}
      }
    },
    {
      "id": "normalize_eco_02",
      "name": "Normalize ECO-02 Events",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1010,
        620
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch ECO-02 Smoke Forecast').first().json || {};\nconst landing = $('Land ECO-02 Raw Payload').first().json || {};\nconst parseLkg = $('Parse Last Known Good').first().json || { bySource: {} };\nconst sourceConfig = run.source_configs && run.source_configs['ECO_02'] ? run.source_configs['ECO_02'] : {\n  \"source_id\": \"03_AIR_QUALITY:ECO-02\",\n  \"source_name\": \"Washington State Department of Ecology Smoke Forecast\",\n  \"family\": \"ecology_forecast\",\n  \"stale_after_minutes\": 720\n};\nconst lkg = parseLkg.bySource && parseLkg.bySource['03_AIR_QUALITY:ECO-02'] ? parseLkg.bySource['03_AIR_QUALITY:ECO-02'] : null;\nconst fetchErrorText = fetch.error ? stringifyError(fetch.error) : null;\nconst nowIso = run.generated_at;\nconst staleAfterMinutes = Number(sourceConfig.freshness_threshold_minutes ?? 720);\nconst sourceWarnings = [];\nconst sourceErrors = [];\nlet events = [];\nlet observations = [];\nlet routeSections = [];\nlet lastObservationAt = null;\nlet sourceTimestamp = null;\nconst routeSectionForStation = (name, lat, lon) => { const label = normalizeText(name).toLowerCase(); if (label.includes('issaquah') || label.includes('lake sammamish')) return 'aqp4_issaquah'; if (label.includes('bellevue') || label.includes('redmond') || label.includes('marymoor')) return 'aqp3_bellevue'; if (label.includes('lake forest park') || label.includes('kenmore') || label.includes('bothell')) return 'aqp2_lake_forest_park'; if (Number.isFinite(lat) && Number.isFinite(lon)) return 'aqp1_seattle'; return 'aqp1_seattle'; };\nconst pointDistanceKm = (aLat, aLon, bLat, bLon) => { const toRad = (deg) => (deg * Math.PI) / 180; const r = 6371; const dLat = toRad(bLat - aLat); const dLon = toRad(bLon - aLon); const lat1 = toRad(aLat); const lat2 = toRad(bLat); const x = Math.sin(dLat / 2) ** 2 + Math.cos(lat1) * Math.cos(lat2) * Math.sin(dLon / 2) ** 2; return 2 * r * Math.asin(Math.min(1, Math.sqrt(x))); };\nconst sectionAnchors = { aqp1_seattle: { lat: 47.655, lon: -122.333 }, aqp2_lake_forest_park: { lat: 47.754, lon: -122.289 }, aqp3_bellevue: { lat: 47.614, lon: -122.178 }, aqp4_issaquah: { lat: 47.532, lon: -122.032 } };\nconst severityFromCategory = (category) => { const text = normalizeText(category).toLowerCase(); if (text.includes('good')) return 'good'; if (text.includes('moderate')) return 'moderate'; if (text.includes('sensitive')) return 'usg'; if (text.includes('unhealthy')) return text.includes('very') ? 'very_unhealthy' : 'unhealthy'; if (text.includes('hazardous')) return 'hazardous'; return 'unknown'; };\nconst qFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return null; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst pickPollutant = (attrs) => { const candidates = [['pm25', Number(attrs.AQI_PM25), Number(attrs.PM25_Value), 'ug/m3', attrs.AQI_PM25_Cat], ['ozone', Number(attrs.AQI_O3), Number(attrs.O3_Value), 'ppm', attrs.AQI_O3_Cat], ['pm10', Number(attrs.AQI_PM10), Number(attrs.PM10_Value), 'ug/m3', attrs.AQI_PM10_Cat], ['no2', Number(attrs.AQI_NO2), Number(attrs.NO2_Value), 'ppb', attrs.AQI_NO2_Cat]].filter((candidate) => Number.isFinite(candidate[1])); if (!candidates.length) return null; candidates.sort((a, b) => b[1] - a[1]); return candidates[0][0]; };\nconst buildObservation = (feature, index) => { const attrs = safeObject(feature.attributes || feature); const geom = safeObject(feature.geometry || attrs.geometry || {}); const lat = Number(geom.y ?? geom.latitude ?? attrs.Latitude ?? attrs.latitude); const lon = Number(geom.x ?? geom.longitude ?? attrs.Longitude ?? attrs.longitude); const observedAt = toIsoUtc(attrs.DateTime_PST || attrs.ObservedAt || attrs.observed_at, run.generated_at); const siteId = String(attrs.SiteId ?? attrs.site_id ?? attrs.Site ?? attrs.site ?? index + 1); const siteName = normalizeText(attrs.SiteName ?? attrs.Site ?? attrs.site_name ?? 'Unknown site'); const siteLocation = normalizeText(attrs.SiteLocation ?? attrs.site_location ?? attrs.Location ?? attrs.location ?? ''); const aqiValue = Number(attrs.AQIValue ?? attrs.AQI ?? attrs.aqi_value); const aqiCategory = attrs.AQICategory ?? attrs.category ?? qFromAqi(aqiValue) ?? 'Unknown'; const severity = severityFromAqi(aqiValue) !== 'unknown' ? severityFromAqi(aqiValue) : severityFromCategory(aqiCategory); const routeSectionId = routeSectionForStation(siteName, lat, lon); const anchor = sectionAnchors[routeSectionId]; const distanceKm = Number.isFinite(lat) && Number.isFinite(lon) && anchor ? Number(pointDistanceKm(lat, lon, anchor.lat, anchor.lon).toFixed(2)) : null; const routeRelevance = { method: 'point_to_route_distance', distance_km: distanceKm, threshold_km: 12.87, route_section_id: routeSectionId }; const pm25 = Number.isFinite(Number(attrs.AQI_PM25)) || Number.isFinite(Number(attrs.PM25_Value)) ? { aqi: Number(attrs.AQI_PM25 ?? attrs.PM25_AQI ?? null), category: attrs.AQI_PM25_Cat ?? null, value: Number(attrs.PM25_Value ?? attrs.PM25 ?? null), units: 'ug/m3' } : null; const ozone = Number.isFinite(Number(attrs.AQI_O3)) || Number.isFinite(Number(attrs.O3_Value)) ? { aqi: Number(attrs.AQI_O3 ?? attrs.O3_AQI ?? null), category: attrs.AQI_O3_Cat ?? null, value: Number(attrs.O3_Value ?? attrs.O3 ?? null), units: 'ppm' } : null; const pm10 = Number.isFinite(Number(attrs.AQI_PM10)) || Number.isFinite(Number(attrs.PM10_Value)) ? { aqi: Number(attrs.AQI_PM10 ?? attrs.PM10_AQI ?? null), category: attrs.AQI_PM10_Cat ?? null, value: Number(attrs.PM10_Value ?? attrs.PM10 ?? null), units: 'ug/m3' } : null; const no2 = Number.isFinite(Number(attrs.AQI_NO2)) || Number.isFinite(Number(attrs.NO2_Value)) ? { aqi: Number(attrs.AQI_NO2 ?? attrs.NO2_AQI ?? null), category: attrs.AQI_NO2_Cat ?? null, value: Number(attrs.NO2_Value ?? attrs.NO2 ?? null), units: 'ppb' } : null; const observation = { observation_id: '03_AIR_QUALITY:ECO-02:' + siteId + ':' + observedAt, observation_type: 'current_air_quality', source_id: '03_AIR_QUALITY:ECO-02', source_record_id: siteId, observed_at: observedAt, retrieved_at: nowIso, station_name: siteName, station_location: siteLocation || null, aqi_value: Number.isFinite(aqiValue) ? aqiValue : null, aqi_category: aqiCategory, severity, dominant_pollutant: pickPollutant(attrs), pollutants: { pm25, pm10, ozone, no2 }, wildfire_smoke_related: false, route_relevance: routeRelevance, location: Number.isFinite(lat) && Number.isFinite(lon) ? { latitude: lat, longitude: lon, epsg: 4326 } : null }; if (Number.isFinite(aqiValue) && !lastObservationAt) lastObservationAt = observedAt; if (Number.isFinite(aqiValue) || Number.isFinite(lat) || Number.isFinite(lon)) sourceTimestamp = observedAt; return observation; };\nconst normalizeForecastLabel = (text) => { const value = normalizeText(text).toLowerCase(); if (value.includes('hazardous')) return 'Hazardous'; if (value.includes('very unhealthy')) return 'Very Unhealthy'; if (value.includes('unhealthy for sensitive')) return 'Unhealthy for Sensitive Groups'; if (value.includes('unhealthy')) return 'Unhealthy'; if (value.includes('moderate')) return 'Moderate'; if (value.includes('good')) return 'Good'; return normalizeText(text) || 'Unknown'; };\nconst body = fetch.body !== undefined ? fetch.body : (landing.body !== undefined ? landing.body : fetch);\nconst bodyText = typeof body === 'string' ? body : JSON.stringify(body ?? null);\nconst bodyObj = safeObject(body);\nconst bodyArr = safeArray(body);\nconst liveRecords = (() => { switch ('ecology_forecast') { case 'ecology_point': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); observations = features.map((feature, index) => buildObservation(feature, index)).filter((observation) => { const sectionId = observation.route_relevance.route_section_id; const anchor = sectionAnchors[sectionId]; if (!anchor || !observation.location) return true; const dist = Number(pointDistanceKm(observation.location.latitude, observation.location.longitude, anchor.lat, anchor.lon).toFixed(2)); observation.route_relevance.distance_km = dist; return dist <= observation.route_relevance.threshold_km; }); routeSections = []; return []; } case 'ecology_forecast': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); events = features.map((feature, index) => { const attrs = safeObject(feature.attributes || feature); const geom = safeObject(feature.geometry || attrs.geometry || {}); const siteName = normalizeText(attrs.SiteName ?? attrs.Site ?? attrs.site ?? attrs.Area ?? attrs.area ?? 'Seattle-Bellevue-Kent Valley'); const sourceRecordId = String(attrs.SmokeForecastId ?? attrs.ObjectId ?? attrs.objectid ?? attrs.id ?? index + 1); const label = normalizeForecastLabel(attrs.Day1 ?? attrs.DisplayText ?? attrs.Category ?? attrs.CategoryLabel ?? attrs.Day2 ?? attrs.Day3 ?? 'Good'); const severity = severityFromCategory(label); const geomText = JSON.stringify(geom); const eventRouteRelevant = Boolean(geomText && (geomText.includes('[') || geomText.includes('{'))); return { event_id: '03_AIR_QUALITY:ECO-02:' + siteName.replace(/[^a-z0-9]+/gi, '-').toLowerCase() + ':' + sourceRecordId, event_type: 'smoke_forecast', title: 'Ecology smoke forecast for ' + siteName, status: 'forecast', severity, category_label: label, effective_at: attrs.Date ? String(attrs.Date) : nowIso, expires_at: null, route_relevant: eventRouteRelevant, wildfire_smoke_related: true, areas: [siteName], source_id: '03_AIR_QUALITY:ECO-02', source_record_id: sourceRecordId, geometry_type: 'polygon', manual_review_required: false, route_sections: eventRouteRelevant ? ['aqp1_seattle', 'aqp2_lake_forest_park', 'aqp3_bellevue', 'aqp4_issaquah'] : [] }; }).filter((event) => event.route_relevant); routeSections = []; return []; } case 'burn_ban': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); let burnBanStatus = 'unknown'; if (lower.includes('stage 2')) burnBanStatus = 'stage_2'; else if (lower.includes('stage 1')) burnBanStatus = 'stage_1'; else if (lower.includes('no burn ban') || lower.includes('no ban') || lower.includes('burn ban status: no')) burnBanStatus = 'no_ban'; const event = { event_id: '03_AIR_QUALITY:ECO-02:burn-ban:' + run.run_stamp, event_type: 'burn_ban', title: 'PSCAA burn ban status', status: 'current', severity: burnBanStatus === 'unknown' ? 'unknown' : 'good', category_label: burnBanStatus === 'stage_2' ? 'Stage 2' : burnBanStatus === 'stage_1' ? 'Stage 1' : burnBanStatus === 'no_ban' ? 'No Ban' : 'Unknown', effective_at: null, expires_at: null, route_relevant: true, wildfire_smoke_related: false, areas: ['King County'], source_id: '03_AIR_QUALITY:ECO-02', source_record_id: 'king-county-burn-ban', geometry_type: 'text_only', manual_review_required: burnBanStatus === 'unknown', route_sections: ['aqp4_issaquah'] }; events = [event]; sourceWarnings.push(burnBanStatus === 'unknown' ? 'Burn-ban status could not be parsed confidently.' : 'Burn-ban status parsed from page text.'); return []; } case 'airnow_context': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); const matchAreas = ['seattle', 'bellevue', 'eastside', 'king county', 'issaquah', 'cascade foothills', 'seattle-bellevue-kent valley']; const matches = matchAreas.filter((area) => lower.includes(area)); if (matches.length) { sourceWarnings.push('AirNow reporting area matched contextual area names only; no primary route classification performed.'); } routeSections = []; events = []; observations = []; return []; } case 'smoke_rss': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); const lookup = ['Seattle', 'Bellevue', 'Eastside', 'King County', 'Issaquah', 'Cascade foothills', 'Seattle-Bellevue-Kent Valley']; const matched = lookup.filter((entry) => lower.includes(entry.toLowerCase())); if (matched.length) { const areaLabel = matched.join(', '); events = [{ event_id: '03_AIR_QUALITY:ECO-02:' + hashString(areaLabel + text).slice(0, 12), event_type: 'smoke_forecast', title: 'Washington Smoke Blog context for ' + areaLabel, status: 'forecast', severity: lower.includes('hazard') ? 'unhealthy' : lower.includes('smoke') ? 'moderate' : 'unknown', category_label: 'Contextual smoke report', effective_at: nowIso, expires_at: null, route_relevant: true, wildfire_smoke_related: true, areas: matched, source_id: '03_AIR_QUALITY:ECO-02', source_record_id: hashString(text).slice(0, 12), geometry_type: 'text_only', manual_review_required: false, route_sections: ['aqp3_bellevue', 'aqp4_issaquah'] }]; } else { events = []; } routeSections = []; return []; } case 'nws_alert': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); const relevant = features.filter((feature) => { const props = safeObject(feature.properties || feature); const areaDesc = normalizeText(props.areaDesc ?? props.area_desc ?? ''); const geocode = JSON.stringify(props.geocode ?? {}); const text = (areaDesc + ' ' + geocode).toLowerCase(); return normalizeText(props.event ?? '').toLowerCase() === 'air quality alert' && (text.includes('king county') || text.includes('washington') || text.includes('wa') || feature.geometry); }); events = relevant.map((feature, index) => { const props = safeObject(feature.properties || feature); const areaDesc = normalizeText(props.areaDesc ?? props.area_desc ?? 'King County'); const severity = severityFromCategory(props.severity ?? props.urgency ?? 'unknown'); const expires = toIsoUtc(props.expires ?? props.sent ?? null, null); return { event_id: '03_AIR_QUALITY:ECO-02:' + String(props.id ?? props.eventCode ?? index + 1), event_type: 'air_quality_alert', title: normalizeText(props.headline ?? props.event ?? 'Air Quality Alert'), status: 'current', severity, category_label: normalizeText(props.event ?? 'Air Quality Alert'), effective_at: toIsoUtc(props.sent ?? props.onset ?? null, nowIso), expires_at: expires, route_relevant: true, wildfire_smoke_related: false, areas: areaDesc ? areaDesc.split(',').map((part) => part.trim()).filter(Boolean) : ['King County'], source_id: '03_AIR_QUALITY:ECO-02', source_record_id: String(props.id ?? props.eventCode ?? index + 1), geometry_type: feature.geometry ? 'polygon' : 'county_zone', manual_review_required: false, route_sections: ['aqp4_issaquah'] }; }); routeSections = []; return []; } case 'pscaa_station': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); if (lower.includes('aqi') || lower.includes('station')) { sourceWarnings.push('PSCAA station page was reachable but no canonical station snapshot was extracted in this build.'); } routeSections = []; events = []; observations = []; return []; } case 'airnow_api': { const records = safeArray(bodyObj || bodyArr); const relevant = records.filter((record) => record && typeof record === 'object'); if (relevant.length) { observations = relevant.slice(0, 4).map((record, index) => { const aqiValue = Number(record.AQI ?? record.aqi ?? record.Value ?? null); const category = normalizeText(record.Category ?? record.category ?? qFromAqi(aqiValue) ?? 'Unknown'); const observedAt = toIsoUtc(record.DateObserved ? String(record.DateObserved) + 'T' + String(record.HourObserved ?? 0) + ':00:00Z' : record.ObservedAt ?? null, nowIso); const routeSectionId = index % 4 === 0 ? 'aqp1_seattle' : index % 4 === 1 ? 'aqp2_lake_forest_park' : index % 4 === 2 ? 'aqp3_bellevue' : 'aqp4_issaquah'; return { observation_id: '03_AIR_QUALITY:ECO-02:' + String(record.SiteID ?? record.zipCode ?? index + 1) + ':' + observedAt, observation_type: 'current_air_quality', source_id: '03_AIR_QUALITY:ECO-02', source_record_id: String(record.SiteID ?? record.zipCode ?? index + 1), observed_at: observedAt, retrieved_at: nowIso, station_name: normalizeText(record.ReportingArea ?? record.Name ?? 'AirNow reporting area'), station_location: null, aqi_value: Number.isFinite(aqiValue) ? aqiValue : null, aqi_category: category, severity: severityFromCategory(category), dominant_pollutant: null, pollutants: { pm25: null, pm10: null, ozone: null, no2: null }, wildfire_smoke_related: false, route_relevance: { method: 'point_to_route_distance', distance_km: null, threshold_km: 12.87, route_section_id: routeSectionId }, location: null }; }); } routeSections = []; return []; } default: return []; } })();\nconst liveCount = events.length + observations.length;\nconst cachedEvents = lkg ? safeArray(lkg.events) : [];\nconst cachedObservations = lkg ? safeArray(lkg.observations) : [];\nconst cachedRouteSections = lkg ? safeArray(lkg.route_sections) : [];\nconst cachedRecordCount = cachedEvents.length + cachedObservations.length;\nconst lkgRetrievedAt = lkg && lkg.source_health ? lkg.source_health.retrieved_at : null;\nconst lkgStaleAfterMinutes = lkg && lkg.source_health && Number.isFinite(Number(lkg.source_health.stale_after_minutes)) ? Number(lkg.source_health.stale_after_minutes) : staleAfterMinutes;\nconst lkgAgeMinutes = Number.isFinite(Date.parse(lkgRetrievedAt)) ? (Date.now() - Date.parse(lkgRetrievedAt)) / 60000 : Infinity;\nconst lkgUsable = !!lkg && Number.isFinite(lkgAgeMinutes) && lkgAgeMinutes <= lkgStaleAfterMinutes;\nif (fetchErrorText) sourceErrors.push(fetchErrorText);\nlet sourceHealth = null;\nif (fetchErrorText) {\n  if (lkgUsable) {\n    events = cachedEvents;\n    observations = cachedObservations;\n    routeSections = cachedRouteSections;\n    sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:ECO-02', source_name: 'Washington State Department of Ecology Smoke Forecast', status: 'using_last_known_good', retrieved_at: lkg.source_health.retrieved_at, stale_after_minutes: lkg.source_health.stale_after_minutes ?? staleAfterMinutes, record_count: cachedRecordCount, http_status: lkg.source_health.http_status ?? null, last_observation_at: lkg.source_health.last_observation_at ?? lkg.source_health.retrieved_at, warnings: [...safeArray(lkg.source_health.warnings), 'Live fetch failed; serving last-known-good data.'], errors: sourceErrors };\n  } else {\n    sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:ECO-02', source_name: 'Washington State Department of Ecology Smoke Forecast', status: 'failed', retrieved_at: run.generated_at, stale_after_minutes: staleAfterMinutes, record_count: 0, http_status: fetch.statusCode ?? fetch.status ?? null, last_observation_at: null, warnings: sourceWarnings, errors: sourceErrors };\n  }\n} else {\n  const lastObservationValue = maxOrNull([ ...observations.map((observation) => Date.parse(observation.observed_at)).filter((value) => Number.isFinite(value)), ...events.map((event) => Date.parse(event.effective_at)).filter((value) => Number.isFinite(value)) ]);\n  const status = liveCount ? 'ok' : 'empty_but_valid';\n  sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:ECO-02', source_name: 'Washington State Department of Ecology Smoke Forecast', status, retrieved_at: run.generated_at, stale_after_minutes: staleAfterMinutes, record_count: liveCount, http_status: fetch.statusCode ?? fetch.status ?? null, last_observation_at: Number.isFinite(lastObservationValue) ? new Date(lastObservationValue).toISOString() : (liveCount ? run.generated_at : null), warnings: sourceWarnings, errors: sourceErrors };\n}\nconst provenanceSources = [{ source_id: '03_AIR_QUALITY:ECO-02', source_name: 'Washington State Department of Ecology Smoke Forecast', retrieved_at: sourceHealth.retrieved_at, source_timestamp: sourceHealth.last_observation_at, status: sourceHealth.status, stale: sourceHealth.status === 'using_last_known_good' || sourceHealth.status === 'stale' }];\nreturn [{ json: { source_id: '03_AIR_QUALITY:ECO-02', source_name: 'Washington State Department of Ecology Smoke Forecast', source_health: sourceHealth, events, observations, route_sections: routeSections, warnings: sourceWarnings, errors: sourceErrors, provenance: { source_ids_used: ['03_AIR_QUALITY:ECO-02'], sources: provenanceSources } } }];"
      }
    },
    {
      "id": "fetch_pscaa_02",
      "name": "Fetch PSCAA-02 Burn Ban Page",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        220,
        820
      ],
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "GET",
        "url": "https://www.pscleanair.gov/168/Air-Quality-Burn-Ban-Status",
        "responseFormat": "string",
        "options": {
          "timeout": 30000
        }
      },
      "notes": ""
    },
    {
      "id": "land_pscaa_02",
      "name": "Land PSCAA-02 Raw Payload",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        500,
        820
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch PSCAA-02 Burn Ban Page').first().json || {};\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst bodyText = typeof body === 'string' ? body : JSON.stringify(body ?? null);\nconst statusCode = fetch.statusCode ?? fetch.status ?? null;\nconst contentHash = 'hash_' + hashString(bodyText);\nconst filePath = run.output_root + '/raw/03_AIR_QUALITY/landings/PSCAA-02_landing_' + run.run_stamp + '.json';\nconst landing = { schema_version: '1.0.0', connector_id: run.connector_id, source_id: '03_AIR_QUALITY:PSCAA-02', source_name: 'Puget Sound Clean Air Agency Air Quality Burn Ban Status', source_url: 'https://www.pscleanair.gov/168/Air-Quality-Burn-Ban-Status', retrieved_at: run.generated_at, http_status: statusCode, retrieval_method: 'html_page_fetch', content_hash: contentHash, body_length: bodyText.length, body: body, fetch_error: fetch.error ? stringifyError(fetch.error) : null };\nconst buffer = Buffer.from(JSON.stringify(landing, null, 2), 'utf8');\nconst binary = await this.helpers.prepareBinaryData(buffer, filePath.split('/').pop(), 'application/json');\nreturn [{ json: { file_path: filePath, source_id: landing.source_id, source_name: landing.source_name, content_hash: contentHash, retrieved_at: run.generated_at }, binary: { data: binary } }];"
      }
    },
    {
      "id": "write_pscaa_02_raw",
      "name": "Write PSCAA-02 Raw Landing",
      "type": "n8n-nodes-base.readWriteFile",
      "typeVersion": 1,
      "position": [
        760,
        820
      ],
      "parameters": {
        "operation": "write",
        "fileName": "={{ $json.file_path }}",
        "dataPropertyName": "data",
        "options": {}
      }
    },
    {
      "id": "normalize_pscaa_02",
      "name": "Normalize PSCAA-02 Events",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1010,
        820
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch PSCAA-02 Burn Ban Page').first().json || {};\nconst landing = $('Land PSCAA-02 Raw Payload').first().json || {};\nconst parseLkg = $('Parse Last Known Good').first().json || { bySource: {} };\nconst sourceConfig = run.source_configs && run.source_configs['PSCAA_02'] ? run.source_configs['PSCAA_02'] : {\n  \"source_id\": \"03_AIR_QUALITY:PSCAA-02\",\n  \"source_name\": \"Puget Sound Clean Air Agency Air Quality Burn Ban Status\",\n  \"family\": \"burn_ban\",\n  \"stale_after_minutes\": 1440\n};\nconst lkg = parseLkg.bySource && parseLkg.bySource['03_AIR_QUALITY:PSCAA-02'] ? parseLkg.bySource['03_AIR_QUALITY:PSCAA-02'] : null;\nconst fetchErrorText = fetch.error ? stringifyError(fetch.error) : null;\nconst nowIso = run.generated_at;\nconst staleAfterMinutes = Number(sourceConfig.freshness_threshold_minutes ?? 1440);\nconst sourceWarnings = [];\nconst sourceErrors = [];\nlet events = [];\nlet observations = [];\nlet routeSections = [];\nlet lastObservationAt = null;\nlet sourceTimestamp = null;\nconst routeSectionForStation = (name, lat, lon) => { const label = normalizeText(name).toLowerCase(); if (label.includes('issaquah') || label.includes('lake sammamish')) return 'aqp4_issaquah'; if (label.includes('bellevue') || label.includes('redmond') || label.includes('marymoor')) return 'aqp3_bellevue'; if (label.includes('lake forest park') || label.includes('kenmore') || label.includes('bothell')) return 'aqp2_lake_forest_park'; if (Number.isFinite(lat) && Number.isFinite(lon)) return 'aqp1_seattle'; return 'aqp1_seattle'; };\nconst pointDistanceKm = (aLat, aLon, bLat, bLon) => { const toRad = (deg) => (deg * Math.PI) / 180; const r = 6371; const dLat = toRad(bLat - aLat); const dLon = toRad(bLon - aLon); const lat1 = toRad(aLat); const lat2 = toRad(bLat); const x = Math.sin(dLat / 2) ** 2 + Math.cos(lat1) * Math.cos(lat2) * Math.sin(dLon / 2) ** 2; return 2 * r * Math.asin(Math.min(1, Math.sqrt(x))); };\nconst sectionAnchors = { aqp1_seattle: { lat: 47.655, lon: -122.333 }, aqp2_lake_forest_park: { lat: 47.754, lon: -122.289 }, aqp3_bellevue: { lat: 47.614, lon: -122.178 }, aqp4_issaquah: { lat: 47.532, lon: -122.032 } };\nconst severityFromCategory = (category) => { const text = normalizeText(category).toLowerCase(); if (text.includes('good')) return 'good'; if (text.includes('moderate')) return 'moderate'; if (text.includes('sensitive')) return 'usg'; if (text.includes('unhealthy')) return text.includes('very') ? 'very_unhealthy' : 'unhealthy'; if (text.includes('hazardous')) return 'hazardous'; return 'unknown'; };\nconst qFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return null; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst pickPollutant = (attrs) => { const candidates = [['pm25', Number(attrs.AQI_PM25), Number(attrs.PM25_Value), 'ug/m3', attrs.AQI_PM25_Cat], ['ozone', Number(attrs.AQI_O3), Number(attrs.O3_Value), 'ppm', attrs.AQI_O3_Cat], ['pm10', Number(attrs.AQI_PM10), Number(attrs.PM10_Value), 'ug/m3', attrs.AQI_PM10_Cat], ['no2', Number(attrs.AQI_NO2), Number(attrs.NO2_Value), 'ppb', attrs.AQI_NO2_Cat]].filter((candidate) => Number.isFinite(candidate[1])); if (!candidates.length) return null; candidates.sort((a, b) => b[1] - a[1]); return candidates[0][0]; };\nconst buildObservation = (feature, index) => { const attrs = safeObject(feature.attributes || feature); const geom = safeObject(feature.geometry || attrs.geometry || {}); const lat = Number(geom.y ?? geom.latitude ?? attrs.Latitude ?? attrs.latitude); const lon = Number(geom.x ?? geom.longitude ?? attrs.Longitude ?? attrs.longitude); const observedAt = toIsoUtc(attrs.DateTime_PST || attrs.ObservedAt || attrs.observed_at, run.generated_at); const siteId = String(attrs.SiteId ?? attrs.site_id ?? attrs.Site ?? attrs.site ?? index + 1); const siteName = normalizeText(attrs.SiteName ?? attrs.Site ?? attrs.site_name ?? 'Unknown site'); const siteLocation = normalizeText(attrs.SiteLocation ?? attrs.site_location ?? attrs.Location ?? attrs.location ?? ''); const aqiValue = Number(attrs.AQIValue ?? attrs.AQI ?? attrs.aqi_value); const aqiCategory = attrs.AQICategory ?? attrs.category ?? qFromAqi(aqiValue) ?? 'Unknown'; const severity = severityFromAqi(aqiValue) !== 'unknown' ? severityFromAqi(aqiValue) : severityFromCategory(aqiCategory); const routeSectionId = routeSectionForStation(siteName, lat, lon); const anchor = sectionAnchors[routeSectionId]; const distanceKm = Number.isFinite(lat) && Number.isFinite(lon) && anchor ? Number(pointDistanceKm(lat, lon, anchor.lat, anchor.lon).toFixed(2)) : null; const routeRelevance = { method: 'point_to_route_distance', distance_km: distanceKm, threshold_km: 12.87, route_section_id: routeSectionId }; const pm25 = Number.isFinite(Number(attrs.AQI_PM25)) || Number.isFinite(Number(attrs.PM25_Value)) ? { aqi: Number(attrs.AQI_PM25 ?? attrs.PM25_AQI ?? null), category: attrs.AQI_PM25_Cat ?? null, value: Number(attrs.PM25_Value ?? attrs.PM25 ?? null), units: 'ug/m3' } : null; const ozone = Number.isFinite(Number(attrs.AQI_O3)) || Number.isFinite(Number(attrs.O3_Value)) ? { aqi: Number(attrs.AQI_O3 ?? attrs.O3_AQI ?? null), category: attrs.AQI_O3_Cat ?? null, value: Number(attrs.O3_Value ?? attrs.O3 ?? null), units: 'ppm' } : null; const pm10 = Number.isFinite(Number(attrs.AQI_PM10)) || Number.isFinite(Number(attrs.PM10_Value)) ? { aqi: Number(attrs.AQI_PM10 ?? attrs.PM10_AQI ?? null), category: attrs.AQI_PM10_Cat ?? null, value: Number(attrs.PM10_Value ?? attrs.PM10 ?? null), units: 'ug/m3' } : null; const no2 = Number.isFinite(Number(attrs.AQI_NO2)) || Number.isFinite(Number(attrs.NO2_Value)) ? { aqi: Number(attrs.AQI_NO2 ?? attrs.NO2_AQI ?? null), category: attrs.AQI_NO2_Cat ?? null, value: Number(attrs.NO2_Value ?? attrs.NO2 ?? null), units: 'ppb' } : null; const observation = { observation_id: '03_AIR_QUALITY:PSCAA-02:' + siteId + ':' + observedAt, observation_type: 'current_air_quality', source_id: '03_AIR_QUALITY:PSCAA-02', source_record_id: siteId, observed_at: observedAt, retrieved_at: nowIso, station_name: siteName, station_location: siteLocation || null, aqi_value: Number.isFinite(aqiValue) ? aqiValue : null, aqi_category: aqiCategory, severity, dominant_pollutant: pickPollutant(attrs), pollutants: { pm25, pm10, ozone, no2 }, wildfire_smoke_related: false, route_relevance: routeRelevance, location: Number.isFinite(lat) && Number.isFinite(lon) ? { latitude: lat, longitude: lon, epsg: 4326 } : null }; if (Number.isFinite(aqiValue) && !lastObservationAt) lastObservationAt = observedAt; if (Number.isFinite(aqiValue) || Number.isFinite(lat) || Number.isFinite(lon)) sourceTimestamp = observedAt; return observation; };\nconst normalizeForecastLabel = (text) => { const value = normalizeText(text).toLowerCase(); if (value.includes('hazardous')) return 'Hazardous'; if (value.includes('very unhealthy')) return 'Very Unhealthy'; if (value.includes('unhealthy for sensitive')) return 'Unhealthy for Sensitive Groups'; if (value.includes('unhealthy')) return 'Unhealthy'; if (value.includes('moderate')) return 'Moderate'; if (value.includes('good')) return 'Good'; return normalizeText(text) || 'Unknown'; };\nconst body = fetch.body !== undefined ? fetch.body : (landing.body !== undefined ? landing.body : fetch);\nconst bodyText = typeof body === 'string' ? body : JSON.stringify(body ?? null);\nconst bodyObj = safeObject(body);\nconst bodyArr = safeArray(body);\nconst liveRecords = (() => { switch ('burn_ban') { case 'ecology_point': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); observations = features.map((feature, index) => buildObservation(feature, index)).filter((observation) => { const sectionId = observation.route_relevance.route_section_id; const anchor = sectionAnchors[sectionId]; if (!anchor || !observation.location) return true; const dist = Number(pointDistanceKm(observation.location.latitude, observation.location.longitude, anchor.lat, anchor.lon).toFixed(2)); observation.route_relevance.distance_km = dist; return dist <= observation.route_relevance.threshold_km; }); routeSections = []; return []; } case 'ecology_forecast': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); events = features.map((feature, index) => { const attrs = safeObject(feature.attributes || feature); const geom = safeObject(feature.geometry || attrs.geometry || {}); const siteName = normalizeText(attrs.SiteName ?? attrs.Site ?? attrs.site ?? attrs.Area ?? attrs.area ?? 'Seattle-Bellevue-Kent Valley'); const sourceRecordId = String(attrs.SmokeForecastId ?? attrs.ObjectId ?? attrs.objectid ?? attrs.id ?? index + 1); const label = normalizeForecastLabel(attrs.Day1 ?? attrs.DisplayText ?? attrs.Category ?? attrs.CategoryLabel ?? attrs.Day2 ?? attrs.Day3 ?? 'Good'); const severity = severityFromCategory(label); const geomText = JSON.stringify(geom); const eventRouteRelevant = Boolean(geomText && (geomText.includes('[') || geomText.includes('{'))); return { event_id: '03_AIR_QUALITY:PSCAA-02:' + siteName.replace(/[^a-z0-9]+/gi, '-').toLowerCase() + ':' + sourceRecordId, event_type: 'smoke_forecast', title: 'Ecology smoke forecast for ' + siteName, status: 'forecast', severity, category_label: label, effective_at: attrs.Date ? String(attrs.Date) : nowIso, expires_at: null, route_relevant: eventRouteRelevant, wildfire_smoke_related: true, areas: [siteName], source_id: '03_AIR_QUALITY:PSCAA-02', source_record_id: sourceRecordId, geometry_type: 'polygon', manual_review_required: false, route_sections: eventRouteRelevant ? ['aqp1_seattle', 'aqp2_lake_forest_park', 'aqp3_bellevue', 'aqp4_issaquah'] : [] }; }).filter((event) => event.route_relevant); routeSections = []; return []; } case 'burn_ban': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); let burnBanStatus = 'unknown'; if (lower.includes('stage 2')) burnBanStatus = 'stage_2'; else if (lower.includes('stage 1')) burnBanStatus = 'stage_1'; else if (lower.includes('no burn ban') || lower.includes('no ban') || lower.includes('burn ban status: no')) burnBanStatus = 'no_ban'; const event = { event_id: '03_AIR_QUALITY:PSCAA-02:burn-ban:' + run.run_stamp, event_type: 'burn_ban', title: 'PSCAA burn ban status', status: 'current', severity: burnBanStatus === 'unknown' ? 'unknown' : 'good', category_label: burnBanStatus === 'stage_2' ? 'Stage 2' : burnBanStatus === 'stage_1' ? 'Stage 1' : burnBanStatus === 'no_ban' ? 'No Ban' : 'Unknown', effective_at: null, expires_at: null, route_relevant: true, wildfire_smoke_related: false, areas: ['King County'], source_id: '03_AIR_QUALITY:PSCAA-02', source_record_id: 'king-county-burn-ban', geometry_type: 'text_only', manual_review_required: burnBanStatus === 'unknown', route_sections: ['aqp4_issaquah'] }; events = [event]; sourceWarnings.push(burnBanStatus === 'unknown' ? 'Burn-ban status could not be parsed confidently.' : 'Burn-ban status parsed from page text.'); return []; } case 'airnow_context': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); const matchAreas = ['seattle', 'bellevue', 'eastside', 'king county', 'issaquah', 'cascade foothills', 'seattle-bellevue-kent valley']; const matches = matchAreas.filter((area) => lower.includes(area)); if (matches.length) { sourceWarnings.push('AirNow reporting area matched contextual area names only; no primary route classification performed.'); } routeSections = []; events = []; observations = []; return []; } case 'smoke_rss': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); const lookup = ['Seattle', 'Bellevue', 'Eastside', 'King County', 'Issaquah', 'Cascade foothills', 'Seattle-Bellevue-Kent Valley']; const matched = lookup.filter((entry) => lower.includes(entry.toLowerCase())); if (matched.length) { const areaLabel = matched.join(', '); events = [{ event_id: '03_AIR_QUALITY:PSCAA-02:' + hashString(areaLabel + text).slice(0, 12), event_type: 'smoke_forecast', title: 'Washington Smoke Blog context for ' + areaLabel, status: 'forecast', severity: lower.includes('hazard') ? 'unhealthy' : lower.includes('smoke') ? 'moderate' : 'unknown', category_label: 'Contextual smoke report', effective_at: nowIso, expires_at: null, route_relevant: true, wildfire_smoke_related: true, areas: matched, source_id: '03_AIR_QUALITY:PSCAA-02', source_record_id: hashString(text).slice(0, 12), geometry_type: 'text_only', manual_review_required: false, route_sections: ['aqp3_bellevue', 'aqp4_issaquah'] }]; } else { events = []; } routeSections = []; return []; } case 'nws_alert': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); const relevant = features.filter((feature) => { const props = safeObject(feature.properties || feature); const areaDesc = normalizeText(props.areaDesc ?? props.area_desc ?? ''); const geocode = JSON.stringify(props.geocode ?? {}); const text = (areaDesc + ' ' + geocode).toLowerCase(); return normalizeText(props.event ?? '').toLowerCase() === 'air quality alert' && (text.includes('king county') || text.includes('washington') || text.includes('wa') || feature.geometry); }); events = relevant.map((feature, index) => { const props = safeObject(feature.properties || feature); const areaDesc = normalizeText(props.areaDesc ?? props.area_desc ?? 'King County'); const severity = severityFromCategory(props.severity ?? props.urgency ?? 'unknown'); const expires = toIsoUtc(props.expires ?? props.sent ?? null, null); return { event_id: '03_AIR_QUALITY:PSCAA-02:' + String(props.id ?? props.eventCode ?? index + 1), event_type: 'air_quality_alert', title: normalizeText(props.headline ?? props.event ?? 'Air Quality Alert'), status: 'current', severity, category_label: normalizeText(props.event ?? 'Air Quality Alert'), effective_at: toIsoUtc(props.sent ?? props.onset ?? null, nowIso), expires_at: expires, route_relevant: true, wildfire_smoke_related: false, areas: areaDesc ? areaDesc.split(',').map((part) => part.trim()).filter(Boolean) : ['King County'], source_id: '03_AIR_QUALITY:PSCAA-02', source_record_id: String(props.id ?? props.eventCode ?? index + 1), geometry_type: feature.geometry ? 'polygon' : 'county_zone', manual_review_required: false, route_sections: ['aqp4_issaquah'] }; }); routeSections = []; return []; } case 'pscaa_station': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); if (lower.includes('aqi') || lower.includes('station')) { sourceWarnings.push('PSCAA station page was reachable but no canonical station snapshot was extracted in this build.'); } routeSections = []; events = []; observations = []; return []; } case 'airnow_api': { const records = safeArray(bodyObj || bodyArr); const relevant = records.filter((record) => record && typeof record === 'object'); if (relevant.length) { observations = relevant.slice(0, 4).map((record, index) => { const aqiValue = Number(record.AQI ?? record.aqi ?? record.Value ?? null); const category = normalizeText(record.Category ?? record.category ?? qFromAqi(aqiValue) ?? 'Unknown'); const observedAt = toIsoUtc(record.DateObserved ? String(record.DateObserved) + 'T' + String(record.HourObserved ?? 0) + ':00:00Z' : record.ObservedAt ?? null, nowIso); const routeSectionId = index % 4 === 0 ? 'aqp1_seattle' : index % 4 === 1 ? 'aqp2_lake_forest_park' : index % 4 === 2 ? 'aqp3_bellevue' : 'aqp4_issaquah'; return { observation_id: '03_AIR_QUALITY:PSCAA-02:' + String(record.SiteID ?? record.zipCode ?? index + 1) + ':' + observedAt, observation_type: 'current_air_quality', source_id: '03_AIR_QUALITY:PSCAA-02', source_record_id: String(record.SiteID ?? record.zipCode ?? index + 1), observed_at: observedAt, retrieved_at: nowIso, station_name: normalizeText(record.ReportingArea ?? record.Name ?? 'AirNow reporting area'), station_location: null, aqi_value: Number.isFinite(aqiValue) ? aqiValue : null, aqi_category: category, severity: severityFromCategory(category), dominant_pollutant: null, pollutants: { pm25: null, pm10: null, ozone: null, no2: null }, wildfire_smoke_related: false, route_relevance: { method: 'point_to_route_distance', distance_km: null, threshold_km: 12.87, route_section_id: routeSectionId }, location: null }; }); } routeSections = []; return []; } default: return []; } })();\nconst liveCount = events.length + observations.length;\nconst cachedEvents = lkg ? safeArray(lkg.events) : [];\nconst cachedObservations = lkg ? safeArray(lkg.observations) : [];\nconst cachedRouteSections = lkg ? safeArray(lkg.route_sections) : [];\nconst cachedRecordCount = cachedEvents.length + cachedObservations.length;\nconst lkgRetrievedAt = lkg && lkg.source_health ? lkg.source_health.retrieved_at : null;\nconst lkgStaleAfterMinutes = lkg && lkg.source_health && Number.isFinite(Number(lkg.source_health.stale_after_minutes)) ? Number(lkg.source_health.stale_after_minutes) : staleAfterMinutes;\nconst lkgAgeMinutes = Number.isFinite(Date.parse(lkgRetrievedAt)) ? (Date.now() - Date.parse(lkgRetrievedAt)) / 60000 : Infinity;\nconst lkgUsable = !!lkg && Number.isFinite(lkgAgeMinutes) && lkgAgeMinutes <= lkgStaleAfterMinutes;\nif (fetchErrorText) sourceErrors.push(fetchErrorText);\nlet sourceHealth = null;\nif (fetchErrorText) {\n  if (lkgUsable) {\n    events = cachedEvents;\n    observations = cachedObservations;\n    routeSections = cachedRouteSections;\n    sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:PSCAA-02', source_name: 'Puget Sound Clean Air Agency Air Quality Burn Ban Status', status: 'using_last_known_good', retrieved_at: lkg.source_health.retrieved_at, stale_after_minutes: lkg.source_health.stale_after_minutes ?? staleAfterMinutes, record_count: cachedRecordCount, http_status: lkg.source_health.http_status ?? null, last_observation_at: lkg.source_health.last_observation_at ?? lkg.source_health.retrieved_at, warnings: [...safeArray(lkg.source_health.warnings), 'Live fetch failed; serving last-known-good data.'], errors: sourceErrors };\n  } else {\n    sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:PSCAA-02', source_name: 'Puget Sound Clean Air Agency Air Quality Burn Ban Status', status: 'failed', retrieved_at: run.generated_at, stale_after_minutes: staleAfterMinutes, record_count: 0, http_status: fetch.statusCode ?? fetch.status ?? null, last_observation_at: null, warnings: sourceWarnings, errors: sourceErrors };\n  }\n} else {\n  const lastObservationValue = maxOrNull([ ...observations.map((observation) => Date.parse(observation.observed_at)).filter((value) => Number.isFinite(value)), ...events.map((event) => Date.parse(event.effective_at)).filter((value) => Number.isFinite(value)) ]);\n  const status = liveCount ? 'ok' : 'empty_but_valid';\n  sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:PSCAA-02', source_name: 'Puget Sound Clean Air Agency Air Quality Burn Ban Status', status, retrieved_at: run.generated_at, stale_after_minutes: staleAfterMinutes, record_count: liveCount, http_status: fetch.statusCode ?? fetch.status ?? null, last_observation_at: Number.isFinite(lastObservationValue) ? new Date(lastObservationValue).toISOString() : (liveCount ? run.generated_at : null), warnings: sourceWarnings, errors: sourceErrors };\n}\nconst provenanceSources = [{ source_id: '03_AIR_QUALITY:PSCAA-02', source_name: 'Puget Sound Clean Air Agency Air Quality Burn Ban Status', retrieved_at: sourceHealth.retrieved_at, source_timestamp: sourceHealth.last_observation_at, status: sourceHealth.status, stale: sourceHealth.status === 'using_last_known_good' || sourceHealth.status === 'stale' }];\nreturn [{ json: { source_id: '03_AIR_QUALITY:PSCAA-02', source_name: 'Puget Sound Clean Air Agency Air Quality Burn Ban Status', source_health: sourceHealth, events, observations, route_sections: routeSections, warnings: sourceWarnings, errors: sourceErrors, provenance: { source_ids_used: ['03_AIR_QUALITY:PSCAA-02'], sources: provenanceSources } } }];"
      }
    },
    {
      "id": "fetch_airnow_02",
      "name": "Fetch AIRNOW-02 Files",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        220,
        1020
      ],
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "GET",
        "url": "https://files.airnowtech.org/airnow/today/reportingarea.dat",
        "responseFormat": "string",
        "options": {
          "timeout": 30000
        }
      },
      "notes": ""
    },
    {
      "id": "land_airnow_02",
      "name": "Land AIRNOW-02 Raw Payload",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        500,
        1020
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch AIRNOW-02 Files').first().json || {};\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst bodyText = typeof body === 'string' ? body : JSON.stringify(body ?? null);\nconst statusCode = fetch.statusCode ?? fetch.status ?? null;\nconst contentHash = 'hash_' + hashString(bodyText);\nconst filePath = run.output_root + '/raw/03_AIR_QUALITY/landings/AIRNOW-02_landing_' + run.run_stamp + '.json';\nconst landing = { schema_version: '1.0.0', connector_id: run.connector_id, source_id: '03_AIR_QUALITY:AIRNOW-02', source_name: 'AirNow public file products', source_url: 'https://files.airnowtech.org/airnow/today/reportingarea.dat', retrieved_at: run.generated_at, http_status: statusCode, retrieval_method: 'public_file_download', content_hash: contentHash, body_length: bodyText.length, body: body, fetch_error: fetch.error ? stringifyError(fetch.error) : null };\nconst buffer = Buffer.from(JSON.stringify(landing, null, 2), 'utf8');\nconst binary = await this.helpers.prepareBinaryData(buffer, filePath.split('/').pop(), 'application/json');\nreturn [{ json: { file_path: filePath, source_id: landing.source_id, source_name: landing.source_name, content_hash: contentHash, retrieved_at: run.generated_at }, binary: { data: binary } }];"
      }
    },
    {
      "id": "write_airnow_02_raw",
      "name": "Write AIRNOW-02 Raw Landing",
      "type": "n8n-nodes-base.readWriteFile",
      "typeVersion": 1,
      "position": [
        760,
        1020
      ],
      "parameters": {
        "operation": "write",
        "fileName": "={{ $json.file_path }}",
        "dataPropertyName": "data",
        "options": {}
      }
    },
    {
      "id": "normalize_airnow_02",
      "name": "Normalize AIRNOW-02 Events",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1010,
        1020
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch AIRNOW-02 Files').first().json || {};\nconst landing = $('Land AIRNOW-02 Raw Payload').first().json || {};\nconst parseLkg = $('Parse Last Known Good').first().json || { bySource: {} };\nconst sourceConfig = run.source_configs && run.source_configs['AIRNOW_02'] ? run.source_configs['AIRNOW_02'] : {\n  \"source_id\": \"03_AIR_QUALITY:AIRNOW-02\",\n  \"source_name\": \"AirNow public file products\",\n  \"family\": \"airnow_context\",\n  \"stale_after_minutes\": 90\n};\nconst lkg = parseLkg.bySource && parseLkg.bySource['03_AIR_QUALITY:AIRNOW-02'] ? parseLkg.bySource['03_AIR_QUALITY:AIRNOW-02'] : null;\nconst fetchErrorText = fetch.error ? stringifyError(fetch.error) : null;\nconst nowIso = run.generated_at;\nconst staleAfterMinutes = Number(sourceConfig.freshness_threshold_minutes ?? 90);\nconst sourceWarnings = [];\nconst sourceErrors = [];\nlet events = [];\nlet observations = [];\nlet routeSections = [];\nlet lastObservationAt = null;\nlet sourceTimestamp = null;\nconst routeSectionForStation = (name, lat, lon) => { const label = normalizeText(name).toLowerCase(); if (label.includes('issaquah') || label.includes('lake sammamish')) return 'aqp4_issaquah'; if (label.includes('bellevue') || label.includes('redmond') || label.includes('marymoor')) return 'aqp3_bellevue'; if (label.includes('lake forest park') || label.includes('kenmore') || label.includes('bothell')) return 'aqp2_lake_forest_park'; if (Number.isFinite(lat) && Number.isFinite(lon)) return 'aqp1_seattle'; return 'aqp1_seattle'; };\nconst pointDistanceKm = (aLat, aLon, bLat, bLon) => { const toRad = (deg) => (deg * Math.PI) / 180; const r = 6371; const dLat = toRad(bLat - aLat); const dLon = toRad(bLon - aLon); const lat1 = toRad(aLat); const lat2 = toRad(bLat); const x = Math.sin(dLat / 2) ** 2 + Math.cos(lat1) * Math.cos(lat2) * Math.sin(dLon / 2) ** 2; return 2 * r * Math.asin(Math.min(1, Math.sqrt(x))); };\nconst sectionAnchors = { aqp1_seattle: { lat: 47.655, lon: -122.333 }, aqp2_lake_forest_park: { lat: 47.754, lon: -122.289 }, aqp3_bellevue: { lat: 47.614, lon: -122.178 }, aqp4_issaquah: { lat: 47.532, lon: -122.032 } };\nconst severityFromCategory = (category) => { const text = normalizeText(category).toLowerCase(); if (text.includes('good')) return 'good'; if (text.includes('moderate')) return 'moderate'; if (text.includes('sensitive')) return 'usg'; if (text.includes('unhealthy')) return text.includes('very') ? 'very_unhealthy' : 'unhealthy'; if (text.includes('hazardous')) return 'hazardous'; return 'unknown'; };\nconst qFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return null; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst pickPollutant = (attrs) => { const candidates = [['pm25', Number(attrs.AQI_PM25), Number(attrs.PM25_Value), 'ug/m3', attrs.AQI_PM25_Cat], ['ozone', Number(attrs.AQI_O3), Number(attrs.O3_Value), 'ppm', attrs.AQI_O3_Cat], ['pm10', Number(attrs.AQI_PM10), Number(attrs.PM10_Value), 'ug/m3', attrs.AQI_PM10_Cat], ['no2', Number(attrs.AQI_NO2), Number(attrs.NO2_Value), 'ppb', attrs.AQI_NO2_Cat]].filter((candidate) => Number.isFinite(candidate[1])); if (!candidates.length) return null; candidates.sort((a, b) => b[1] - a[1]); return candidates[0][0]; };\nconst buildObservation = (feature, index) => { const attrs = safeObject(feature.attributes || feature); const geom = safeObject(feature.geometry || attrs.geometry || {}); const lat = Number(geom.y ?? geom.latitude ?? attrs.Latitude ?? attrs.latitude); const lon = Number(geom.x ?? geom.longitude ?? attrs.Longitude ?? attrs.longitude); const observedAt = toIsoUtc(attrs.DateTime_PST || attrs.ObservedAt || attrs.observed_at, run.generated_at); const siteId = String(attrs.SiteId ?? attrs.site_id ?? attrs.Site ?? attrs.site ?? index + 1); const siteName = normalizeText(attrs.SiteName ?? attrs.Site ?? attrs.site_name ?? 'Unknown site'); const siteLocation = normalizeText(attrs.SiteLocation ?? attrs.site_location ?? attrs.Location ?? attrs.location ?? ''); const aqiValue = Number(attrs.AQIValue ?? attrs.AQI ?? attrs.aqi_value); const aqiCategory = attrs.AQICategory ?? attrs.category ?? qFromAqi(aqiValue) ?? 'Unknown'; const severity = severityFromAqi(aqiValue) !== 'unknown' ? severityFromAqi(aqiValue) : severityFromCategory(aqiCategory); const routeSectionId = routeSectionForStation(siteName, lat, lon); const anchor = sectionAnchors[routeSectionId]; const distanceKm = Number.isFinite(lat) && Number.isFinite(lon) && anchor ? Number(pointDistanceKm(lat, lon, anchor.lat, anchor.lon).toFixed(2)) : null; const routeRelevance = { method: 'point_to_route_distance', distance_km: distanceKm, threshold_km: 12.87, route_section_id: routeSectionId }; const pm25 = Number.isFinite(Number(attrs.AQI_PM25)) || Number.isFinite(Number(attrs.PM25_Value)) ? { aqi: Number(attrs.AQI_PM25 ?? attrs.PM25_AQI ?? null), category: attrs.AQI_PM25_Cat ?? null, value: Number(attrs.PM25_Value ?? attrs.PM25 ?? null), units: 'ug/m3' } : null; const ozone = Number.isFinite(Number(attrs.AQI_O3)) || Number.isFinite(Number(attrs.O3_Value)) ? { aqi: Number(attrs.AQI_O3 ?? attrs.O3_AQI ?? null), category: attrs.AQI_O3_Cat ?? null, value: Number(attrs.O3_Value ?? attrs.O3 ?? null), units: 'ppm' } : null; const pm10 = Number.isFinite(Number(attrs.AQI_PM10)) || Number.isFinite(Number(attrs.PM10_Value)) ? { aqi: Number(attrs.AQI_PM10 ?? attrs.PM10_AQI ?? null), category: attrs.AQI_PM10_Cat ?? null, value: Number(attrs.PM10_Value ?? attrs.PM10 ?? null), units: 'ug/m3' } : null; const no2 = Number.isFinite(Number(attrs.AQI_NO2)) || Number.isFinite(Number(attrs.NO2_Value)) ? { aqi: Number(attrs.AQI_NO2 ?? attrs.NO2_AQI ?? null), category: attrs.AQI_NO2_Cat ?? null, value: Number(attrs.NO2_Value ?? attrs.NO2 ?? null), units: 'ppb' } : null; const observation = { observation_id: '03_AIR_QUALITY:AIRNOW-02:' + siteId + ':' + observedAt, observation_type: 'current_air_quality', source_id: '03_AIR_QUALITY:AIRNOW-02', source_record_id: siteId, observed_at: observedAt, retrieved_at: nowIso, station_name: siteName, station_location: siteLocation || null, aqi_value: Number.isFinite(aqiValue) ? aqiValue : null, aqi_category: aqiCategory, severity, dominant_pollutant: pickPollutant(attrs), pollutants: { pm25, pm10, ozone, no2 }, wildfire_smoke_related: false, route_relevance: routeRelevance, location: Number.isFinite(lat) && Number.isFinite(lon) ? { latitude: lat, longitude: lon, epsg: 4326 } : null }; if (Number.isFinite(aqiValue) && !lastObservationAt) lastObservationAt = observedAt; if (Number.isFinite(aqiValue) || Number.isFinite(lat) || Number.isFinite(lon)) sourceTimestamp = observedAt; return observation; };\nconst normalizeForecastLabel = (text) => { const value = normalizeText(text).toLowerCase(); if (value.includes('hazardous')) return 'Hazardous'; if (value.includes('very unhealthy')) return 'Very Unhealthy'; if (value.includes('unhealthy for sensitive')) return 'Unhealthy for Sensitive Groups'; if (value.includes('unhealthy')) return 'Unhealthy'; if (value.includes('moderate')) return 'Moderate'; if (value.includes('good')) return 'Good'; return normalizeText(text) || 'Unknown'; };\nconst body = fetch.body !== undefined ? fetch.body : (landing.body !== undefined ? landing.body : fetch);\nconst bodyText = typeof body === 'string' ? body : JSON.stringify(body ?? null);\nconst bodyObj = safeObject(body);\nconst bodyArr = safeArray(body);\nconst liveRecords = (() => { switch ('airnow_context') { case 'ecology_point': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); observations = features.map((feature, index) => buildObservation(feature, index)).filter((observation) => { const sectionId = observation.route_relevance.route_section_id; const anchor = sectionAnchors[sectionId]; if (!anchor || !observation.location) return true; const dist = Number(pointDistanceKm(observation.location.latitude, observation.location.longitude, anchor.lat, anchor.lon).toFixed(2)); observation.route_relevance.distance_km = dist; return dist <= observation.route_relevance.threshold_km; }); routeSections = []; return []; } case 'ecology_forecast': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); events = features.map((feature, index) => { const attrs = safeObject(feature.attributes || feature); const geom = safeObject(feature.geometry || attrs.geometry || {}); const siteName = normalizeText(attrs.SiteName ?? attrs.Site ?? attrs.site ?? attrs.Area ?? attrs.area ?? 'Seattle-Bellevue-Kent Valley'); const sourceRecordId = String(attrs.SmokeForecastId ?? attrs.ObjectId ?? attrs.objectid ?? attrs.id ?? index + 1); const label = normalizeForecastLabel(attrs.Day1 ?? attrs.DisplayText ?? attrs.Category ?? attrs.CategoryLabel ?? attrs.Day2 ?? attrs.Day3 ?? 'Good'); const severity = severityFromCategory(label); const geomText = JSON.stringify(geom); const eventRouteRelevant = Boolean(geomText && (geomText.includes('[') || geomText.includes('{'))); return { event_id: '03_AIR_QUALITY:AIRNOW-02:' + siteName.replace(/[^a-z0-9]+/gi, '-').toLowerCase() + ':' + sourceRecordId, event_type: 'smoke_forecast', title: 'Ecology smoke forecast for ' + siteName, status: 'forecast', severity, category_label: label, effective_at: attrs.Date ? String(attrs.Date) : nowIso, expires_at: null, route_relevant: eventRouteRelevant, wildfire_smoke_related: true, areas: [siteName], source_id: '03_AIR_QUALITY:AIRNOW-02', source_record_id: sourceRecordId, geometry_type: 'polygon', manual_review_required: false, route_sections: eventRouteRelevant ? ['aqp1_seattle', 'aqp2_lake_forest_park', 'aqp3_bellevue', 'aqp4_issaquah'] : [] }; }).filter((event) => event.route_relevant); routeSections = []; return []; } case 'burn_ban': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); let burnBanStatus = 'unknown'; if (lower.includes('stage 2')) burnBanStatus = 'stage_2'; else if (lower.includes('stage 1')) burnBanStatus = 'stage_1'; else if (lower.includes('no burn ban') || lower.includes('no ban') || lower.includes('burn ban status: no')) burnBanStatus = 'no_ban'; const event = { event_id: '03_AIR_QUALITY:AIRNOW-02:burn-ban:' + run.run_stamp, event_type: 'burn_ban', title: 'PSCAA burn ban status', status: 'current', severity: burnBanStatus === 'unknown' ? 'unknown' : 'good', category_label: burnBanStatus === 'stage_2' ? 'Stage 2' : burnBanStatus === 'stage_1' ? 'Stage 1' : burnBanStatus === 'no_ban' ? 'No Ban' : 'Unknown', effective_at: null, expires_at: null, route_relevant: true, wildfire_smoke_related: false, areas: ['King County'], source_id: '03_AIR_QUALITY:AIRNOW-02', source_record_id: 'king-county-burn-ban', geometry_type: 'text_only', manual_review_required: burnBanStatus === 'unknown', route_sections: ['aqp4_issaquah'] }; events = [event]; sourceWarnings.push(burnBanStatus === 'unknown' ? 'Burn-ban status could not be parsed confidently.' : 'Burn-ban status parsed from page text.'); return []; } case 'airnow_context': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); const matchAreas = ['seattle', 'bellevue', 'eastside', 'king county', 'issaquah', 'cascade foothills', 'seattle-bellevue-kent valley']; const matches = matchAreas.filter((area) => lower.includes(area)); if (matches.length) { sourceWarnings.push('AirNow reporting area matched contextual area names only; no primary route classification performed.'); } routeSections = []; events = []; observations = []; return []; } case 'smoke_rss': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); const lookup = ['Seattle', 'Bellevue', 'Eastside', 'King County', 'Issaquah', 'Cascade foothills', 'Seattle-Bellevue-Kent Valley']; const matched = lookup.filter((entry) => lower.includes(entry.toLowerCase())); if (matched.length) { const areaLabel = matched.join(', '); events = [{ event_id: '03_AIR_QUALITY:AIRNOW-02:' + hashString(areaLabel + text).slice(0, 12), event_type: 'smoke_forecast', title: 'Washington Smoke Blog context for ' + areaLabel, status: 'forecast', severity: lower.includes('hazard') ? 'unhealthy' : lower.includes('smoke') ? 'moderate' : 'unknown', category_label: 'Contextual smoke report', effective_at: nowIso, expires_at: null, route_relevant: true, wildfire_smoke_related: true, areas: matched, source_id: '03_AIR_QUALITY:AIRNOW-02', source_record_id: hashString(text).slice(0, 12), geometry_type: 'text_only', manual_review_required: false, route_sections: ['aqp3_bellevue', 'aqp4_issaquah'] }]; } else { events = []; } routeSections = []; return []; } case 'nws_alert': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); const relevant = features.filter((feature) => { const props = safeObject(feature.properties || feature); const areaDesc = normalizeText(props.areaDesc ?? props.area_desc ?? ''); const geocode = JSON.stringify(props.geocode ?? {}); const text = (areaDesc + ' ' + geocode).toLowerCase(); return normalizeText(props.event ?? '').toLowerCase() === 'air quality alert' && (text.includes('king county') || text.includes('washington') || text.includes('wa') || feature.geometry); }); events = relevant.map((feature, index) => { const props = safeObject(feature.properties || feature); const areaDesc = normalizeText(props.areaDesc ?? props.area_desc ?? 'King County'); const severity = severityFromCategory(props.severity ?? props.urgency ?? 'unknown'); const expires = toIsoUtc(props.expires ?? props.sent ?? null, null); return { event_id: '03_AIR_QUALITY:AIRNOW-02:' + String(props.id ?? props.eventCode ?? index + 1), event_type: 'air_quality_alert', title: normalizeText(props.headline ?? props.event ?? 'Air Quality Alert'), status: 'current', severity, category_label: normalizeText(props.event ?? 'Air Quality Alert'), effective_at: toIsoUtc(props.sent ?? props.onset ?? null, nowIso), expires_at: expires, route_relevant: true, wildfire_smoke_related: false, areas: areaDesc ? areaDesc.split(',').map((part) => part.trim()).filter(Boolean) : ['King County'], source_id: '03_AIR_QUALITY:AIRNOW-02', source_record_id: String(props.id ?? props.eventCode ?? index + 1), geometry_type: feature.geometry ? 'polygon' : 'county_zone', manual_review_required: false, route_sections: ['aqp4_issaquah'] }; }); routeSections = []; return []; } case 'pscaa_station': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); if (lower.includes('aqi') || lower.includes('station')) { sourceWarnings.push('PSCAA station page was reachable but no canonical station snapshot was extracted in this build.'); } routeSections = []; events = []; observations = []; return []; } case 'airnow_api': { const records = safeArray(bodyObj || bodyArr); const relevant = records.filter((record) => record && typeof record === 'object'); if (relevant.length) { observations = relevant.slice(0, 4).map((record, index) => { const aqiValue = Number(record.AQI ?? record.aqi ?? record.Value ?? null); const category = normalizeText(record.Category ?? record.category ?? qFromAqi(aqiValue) ?? 'Unknown'); const observedAt = toIsoUtc(record.DateObserved ? String(record.DateObserved) + 'T' + String(record.HourObserved ?? 0) + ':00:00Z' : record.ObservedAt ?? null, nowIso); const routeSectionId = index % 4 === 0 ? 'aqp1_seattle' : index % 4 === 1 ? 'aqp2_lake_forest_park' : index % 4 === 2 ? 'aqp3_bellevue' : 'aqp4_issaquah'; return { observation_id: '03_AIR_QUALITY:AIRNOW-02:' + String(record.SiteID ?? record.zipCode ?? index + 1) + ':' + observedAt, observation_type: 'current_air_quality', source_id: '03_AIR_QUALITY:AIRNOW-02', source_record_id: String(record.SiteID ?? record.zipCode ?? index + 1), observed_at: observedAt, retrieved_at: nowIso, station_name: normalizeText(record.ReportingArea ?? record.Name ?? 'AirNow reporting area'), station_location: null, aqi_value: Number.isFinite(aqiValue) ? aqiValue : null, aqi_category: category, severity: severityFromCategory(category), dominant_pollutant: null, pollutants: { pm25: null, pm10: null, ozone: null, no2: null }, wildfire_smoke_related: false, route_relevance: { method: 'point_to_route_distance', distance_km: null, threshold_km: 12.87, route_section_id: routeSectionId }, location: null }; }); } routeSections = []; return []; } default: return []; } })();\nconst liveCount = events.length + observations.length;\nconst cachedEvents = lkg ? safeArray(lkg.events) : [];\nconst cachedObservations = lkg ? safeArray(lkg.observations) : [];\nconst cachedRouteSections = lkg ? safeArray(lkg.route_sections) : [];\nconst cachedRecordCount = cachedEvents.length + cachedObservations.length;\nconst lkgRetrievedAt = lkg && lkg.source_health ? lkg.source_health.retrieved_at : null;\nconst lkgStaleAfterMinutes = lkg && lkg.source_health && Number.isFinite(Number(lkg.source_health.stale_after_minutes)) ? Number(lkg.source_health.stale_after_minutes) : staleAfterMinutes;\nconst lkgAgeMinutes = Number.isFinite(Date.parse(lkgRetrievedAt)) ? (Date.now() - Date.parse(lkgRetrievedAt)) / 60000 : Infinity;\nconst lkgUsable = !!lkg && Number.isFinite(lkgAgeMinutes) && lkgAgeMinutes <= lkgStaleAfterMinutes;\nif (fetchErrorText) sourceErrors.push(fetchErrorText);\nlet sourceHealth = null;\nif (fetchErrorText) {\n  if (lkgUsable) {\n    events = cachedEvents;\n    observations = cachedObservations;\n    routeSections = cachedRouteSections;\n    sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:AIRNOW-02', source_name: 'AirNow public file products', status: 'using_last_known_good', retrieved_at: lkg.source_health.retrieved_at, stale_after_minutes: lkg.source_health.stale_after_minutes ?? staleAfterMinutes, record_count: cachedRecordCount, http_status: lkg.source_health.http_status ?? null, last_observation_at: lkg.source_health.last_observation_at ?? lkg.source_health.retrieved_at, warnings: [...safeArray(lkg.source_health.warnings), 'Live fetch failed; serving last-known-good data.'], errors: sourceErrors };\n  } else {\n    sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:AIRNOW-02', source_name: 'AirNow public file products', status: 'failed', retrieved_at: run.generated_at, stale_after_minutes: staleAfterMinutes, record_count: 0, http_status: fetch.statusCode ?? fetch.status ?? null, last_observation_at: null, warnings: sourceWarnings, errors: sourceErrors };\n  }\n} else {\n  const lastObservationValue = maxOrNull([ ...observations.map((observation) => Date.parse(observation.observed_at)).filter((value) => Number.isFinite(value)), ...events.map((event) => Date.parse(event.effective_at)).filter((value) => Number.isFinite(value)) ]);\n  const status = liveCount ? 'ok' : 'empty_but_valid';\n  sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:AIRNOW-02', source_name: 'AirNow public file products', status, retrieved_at: run.generated_at, stale_after_minutes: staleAfterMinutes, record_count: liveCount, http_status: fetch.statusCode ?? fetch.status ?? null, last_observation_at: Number.isFinite(lastObservationValue) ? new Date(lastObservationValue).toISOString() : (liveCount ? run.generated_at : null), warnings: sourceWarnings, errors: sourceErrors };\n}\nconst provenanceSources = [{ source_id: '03_AIR_QUALITY:AIRNOW-02', source_name: 'AirNow public file products', retrieved_at: sourceHealth.retrieved_at, source_timestamp: sourceHealth.last_observation_at, status: sourceHealth.status, stale: sourceHealth.status === 'using_last_known_good' || sourceHealth.status === 'stale' }];\nreturn [{ json: { source_id: '03_AIR_QUALITY:AIRNOW-02', source_name: 'AirNow public file products', source_health: sourceHealth, events, observations, route_sections: routeSections, warnings: sourceWarnings, errors: sourceErrors, provenance: { source_ids_used: ['03_AIR_QUALITY:AIRNOW-02'], sources: provenanceSources } } }];"
      }
    },
    {
      "id": "fetch_wasmoke_01",
      "name": "Fetch WASMOKE-01 RSS",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        220,
        1220
      ],
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "GET",
        "url": "https://wasmoke.blogspot.com/feeds/posts/default?alt=rss",
        "responseFormat": "string",
        "options": {
          "timeout": 30000
        }
      },
      "notes": ""
    },
    {
      "id": "land_wasmoke_01",
      "name": "Land WASMOKE-01 Raw Payload",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        500,
        1220
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch WASMOKE-01 RSS').first().json || {};\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst bodyText = typeof body === 'string' ? body : JSON.stringify(body ?? null);\nconst statusCode = fetch.statusCode ?? fetch.status ?? null;\nconst contentHash = 'hash_' + hashString(bodyText);\nconst filePath = run.output_root + '/raw/03_AIR_QUALITY/landings/WASMOKE-01_landing_' + run.run_stamp + '.json';\nconst landing = { schema_version: '1.0.0', connector_id: run.connector_id, source_id: '03_AIR_QUALITY:WASMOKE-01', source_name: 'Washington Smoke Blog RSS', source_url: 'https://wasmoke.blogspot.com/feeds/posts/default?alt=rss', retrieved_at: run.generated_at, http_status: statusCode, retrieval_method: 'rss_feed_fetch', content_hash: contentHash, body_length: bodyText.length, body: body, fetch_error: fetch.error ? stringifyError(fetch.error) : null };\nconst buffer = Buffer.from(JSON.stringify(landing, null, 2), 'utf8');\nconst binary = await this.helpers.prepareBinaryData(buffer, filePath.split('/').pop(), 'application/json');\nreturn [{ json: { file_path: filePath, source_id: landing.source_id, source_name: landing.source_name, content_hash: contentHash, retrieved_at: run.generated_at }, binary: { data: binary } }];"
      }
    },
    {
      "id": "write_wasmoke_01_raw",
      "name": "Write WASMOKE-01 Raw Landing",
      "type": "n8n-nodes-base.readWriteFile",
      "typeVersion": 1,
      "position": [
        760,
        1220
      ],
      "parameters": {
        "operation": "write",
        "fileName": "={{ $json.file_path }}",
        "dataPropertyName": "data",
        "options": {}
      }
    },
    {
      "id": "normalize_wasmoke_01",
      "name": "Normalize WASMOKE-01 Events",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1010,
        1220
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch WASMOKE-01 RSS').first().json || {};\nconst landing = $('Land WASMOKE-01 Raw Payload').first().json || {};\nconst parseLkg = $('Parse Last Known Good').first().json || { bySource: {} };\nconst sourceConfig = run.source_configs && run.source_configs['WASMOKE_01'] ? run.source_configs['WASMOKE_01'] : {\n  \"source_id\": \"03_AIR_QUALITY:WASMOKE-01\",\n  \"source_name\": \"Washington Smoke Blog RSS\",\n  \"family\": \"smoke_rss\",\n  \"stale_after_minutes\": 720\n};\nconst lkg = parseLkg.bySource && parseLkg.bySource['03_AIR_QUALITY:WASMOKE-01'] ? parseLkg.bySource['03_AIR_QUALITY:WASMOKE-01'] : null;\nconst fetchErrorText = fetch.error ? stringifyError(fetch.error) : null;\nconst nowIso = run.generated_at;\nconst staleAfterMinutes = Number(sourceConfig.freshness_threshold_minutes ?? 720);\nconst sourceWarnings = [];\nconst sourceErrors = [];\nlet events = [];\nlet observations = [];\nlet routeSections = [];\nlet lastObservationAt = null;\nlet sourceTimestamp = null;\nconst routeSectionForStation = (name, lat, lon) => { const label = normalizeText(name).toLowerCase(); if (label.includes('issaquah') || label.includes('lake sammamish')) return 'aqp4_issaquah'; if (label.includes('bellevue') || label.includes('redmond') || label.includes('marymoor')) return 'aqp3_bellevue'; if (label.includes('lake forest park') || label.includes('kenmore') || label.includes('bothell')) return 'aqp2_lake_forest_park'; if (Number.isFinite(lat) && Number.isFinite(lon)) return 'aqp1_seattle'; return 'aqp1_seattle'; };\nconst pointDistanceKm = (aLat, aLon, bLat, bLon) => { const toRad = (deg) => (deg * Math.PI) / 180; const r = 6371; const dLat = toRad(bLat - aLat); const dLon = toRad(bLon - aLon); const lat1 = toRad(aLat); const lat2 = toRad(bLat); const x = Math.sin(dLat / 2) ** 2 + Math.cos(lat1) * Math.cos(lat2) * Math.sin(dLon / 2) ** 2; return 2 * r * Math.asin(Math.min(1, Math.sqrt(x))); };\nconst sectionAnchors = { aqp1_seattle: { lat: 47.655, lon: -122.333 }, aqp2_lake_forest_park: { lat: 47.754, lon: -122.289 }, aqp3_bellevue: { lat: 47.614, lon: -122.178 }, aqp4_issaquah: { lat: 47.532, lon: -122.032 } };\nconst severityFromCategory = (category) => { const text = normalizeText(category).toLowerCase(); if (text.includes('good')) return 'good'; if (text.includes('moderate')) return 'moderate'; if (text.includes('sensitive')) return 'usg'; if (text.includes('unhealthy')) return text.includes('very') ? 'very_unhealthy' : 'unhealthy'; if (text.includes('hazardous')) return 'hazardous'; return 'unknown'; };\nconst qFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return null; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst pickPollutant = (attrs) => { const candidates = [['pm25', Number(attrs.AQI_PM25), Number(attrs.PM25_Value), 'ug/m3', attrs.AQI_PM25_Cat], ['ozone', Number(attrs.AQI_O3), Number(attrs.O3_Value), 'ppm', attrs.AQI_O3_Cat], ['pm10', Number(attrs.AQI_PM10), Number(attrs.PM10_Value), 'ug/m3', attrs.AQI_PM10_Cat], ['no2', Number(attrs.AQI_NO2), Number(attrs.NO2_Value), 'ppb', attrs.AQI_NO2_Cat]].filter((candidate) => Number.isFinite(candidate[1])); if (!candidates.length) return null; candidates.sort((a, b) => b[1] - a[1]); return candidates[0][0]; };\nconst buildObservation = (feature, index) => { const attrs = safeObject(feature.attributes || feature); const geom = safeObject(feature.geometry || attrs.geometry || {}); const lat = Number(geom.y ?? geom.latitude ?? attrs.Latitude ?? attrs.latitude); const lon = Number(geom.x ?? geom.longitude ?? attrs.Longitude ?? attrs.longitude); const observedAt = toIsoUtc(attrs.DateTime_PST || attrs.ObservedAt || attrs.observed_at, run.generated_at); const siteId = String(attrs.SiteId ?? attrs.site_id ?? attrs.Site ?? attrs.site ?? index + 1); const siteName = normalizeText(attrs.SiteName ?? attrs.Site ?? attrs.site_name ?? 'Unknown site'); const siteLocation = normalizeText(attrs.SiteLocation ?? attrs.site_location ?? attrs.Location ?? attrs.location ?? ''); const aqiValue = Number(attrs.AQIValue ?? attrs.AQI ?? attrs.aqi_value); const aqiCategory = attrs.AQICategory ?? attrs.category ?? qFromAqi(aqiValue) ?? 'Unknown'; const severity = severityFromAqi(aqiValue) !== 'unknown' ? severityFromAqi(aqiValue) : severityFromCategory(aqiCategory); const routeSectionId = routeSectionForStation(siteName, lat, lon); const anchor = sectionAnchors[routeSectionId]; const distanceKm = Number.isFinite(lat) && Number.isFinite(lon) && anchor ? Number(pointDistanceKm(lat, lon, anchor.lat, anchor.lon).toFixed(2)) : null; const routeRelevance = { method: 'point_to_route_distance', distance_km: distanceKm, threshold_km: 12.87, route_section_id: routeSectionId }; const pm25 = Number.isFinite(Number(attrs.AQI_PM25)) || Number.isFinite(Number(attrs.PM25_Value)) ? { aqi: Number(attrs.AQI_PM25 ?? attrs.PM25_AQI ?? null), category: attrs.AQI_PM25_Cat ?? null, value: Number(attrs.PM25_Value ?? attrs.PM25 ?? null), units: 'ug/m3' } : null; const ozone = Number.isFinite(Number(attrs.AQI_O3)) || Number.isFinite(Number(attrs.O3_Value)) ? { aqi: Number(attrs.AQI_O3 ?? attrs.O3_AQI ?? null), category: attrs.AQI_O3_Cat ?? null, value: Number(attrs.O3_Value ?? attrs.O3 ?? null), units: 'ppm' } : null; const pm10 = Number.isFinite(Number(attrs.AQI_PM10)) || Number.isFinite(Number(attrs.PM10_Value)) ? { aqi: Number(attrs.AQI_PM10 ?? attrs.PM10_AQI ?? null), category: attrs.AQI_PM10_Cat ?? null, value: Number(attrs.PM10_Value ?? attrs.PM10 ?? null), units: 'ug/m3' } : null; const no2 = Number.isFinite(Number(attrs.AQI_NO2)) || Number.isFinite(Number(attrs.NO2_Value)) ? { aqi: Number(attrs.AQI_NO2 ?? attrs.NO2_AQI ?? null), category: attrs.AQI_NO2_Cat ?? null, value: Number(attrs.NO2_Value ?? attrs.NO2 ?? null), units: 'ppb' } : null; const observation = { observation_id: '03_AIR_QUALITY:WASMOKE-01:' + siteId + ':' + observedAt, observation_type: 'current_air_quality', source_id: '03_AIR_QUALITY:WASMOKE-01', source_record_id: siteId, observed_at: observedAt, retrieved_at: nowIso, station_name: siteName, station_location: siteLocation || null, aqi_value: Number.isFinite(aqiValue) ? aqiValue : null, aqi_category: aqiCategory, severity, dominant_pollutant: pickPollutant(attrs), pollutants: { pm25, pm10, ozone, no2 }, wildfire_smoke_related: false, route_relevance: routeRelevance, location: Number.isFinite(lat) && Number.isFinite(lon) ? { latitude: lat, longitude: lon, epsg: 4326 } : null }; if (Number.isFinite(aqiValue) && !lastObservationAt) lastObservationAt = observedAt; if (Number.isFinite(aqiValue) || Number.isFinite(lat) || Number.isFinite(lon)) sourceTimestamp = observedAt; return observation; };\nconst normalizeForecastLabel = (text) => { const value = normalizeText(text).toLowerCase(); if (value.includes('hazardous')) return 'Hazardous'; if (value.includes('very unhealthy')) return 'Very Unhealthy'; if (value.includes('unhealthy for sensitive')) return 'Unhealthy for Sensitive Groups'; if (value.includes('unhealthy')) return 'Unhealthy'; if (value.includes('moderate')) return 'Moderate'; if (value.includes('good')) return 'Good'; return normalizeText(text) || 'Unknown'; };\nconst body = fetch.body !== undefined ? fetch.body : (landing.body !== undefined ? landing.body : fetch);\nconst bodyText = typeof body === 'string' ? body : JSON.stringify(body ?? null);\nconst bodyObj = safeObject(body);\nconst bodyArr = safeArray(body);\nconst liveRecords = (() => { switch ('smoke_rss') { case 'ecology_point': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); observations = features.map((feature, index) => buildObservation(feature, index)).filter((observation) => { const sectionId = observation.route_relevance.route_section_id; const anchor = sectionAnchors[sectionId]; if (!anchor || !observation.location) return true; const dist = Number(pointDistanceKm(observation.location.latitude, observation.location.longitude, anchor.lat, anchor.lon).toFixed(2)); observation.route_relevance.distance_km = dist; return dist <= observation.route_relevance.threshold_km; }); routeSections = []; return []; } case 'ecology_forecast': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); events = features.map((feature, index) => { const attrs = safeObject(feature.attributes || feature); const geom = safeObject(feature.geometry || attrs.geometry || {}); const siteName = normalizeText(attrs.SiteName ?? attrs.Site ?? attrs.site ?? attrs.Area ?? attrs.area ?? 'Seattle-Bellevue-Kent Valley'); const sourceRecordId = String(attrs.SmokeForecastId ?? attrs.ObjectId ?? attrs.objectid ?? attrs.id ?? index + 1); const label = normalizeForecastLabel(attrs.Day1 ?? attrs.DisplayText ?? attrs.Category ?? attrs.CategoryLabel ?? attrs.Day2 ?? attrs.Day3 ?? 'Good'); const severity = severityFromCategory(label); const geomText = JSON.stringify(geom); const eventRouteRelevant = Boolean(geomText && (geomText.includes('[') || geomText.includes('{'))); return { event_id: '03_AIR_QUALITY:WASMOKE-01:' + siteName.replace(/[^a-z0-9]+/gi, '-').toLowerCase() + ':' + sourceRecordId, event_type: 'smoke_forecast', title: 'Ecology smoke forecast for ' + siteName, status: 'forecast', severity, category_label: label, effective_at: attrs.Date ? String(attrs.Date) : nowIso, expires_at: null, route_relevant: eventRouteRelevant, wildfire_smoke_related: true, areas: [siteName], source_id: '03_AIR_QUALITY:WASMOKE-01', source_record_id: sourceRecordId, geometry_type: 'polygon', manual_review_required: false, route_sections: eventRouteRelevant ? ['aqp1_seattle', 'aqp2_lake_forest_park', 'aqp3_bellevue', 'aqp4_issaquah'] : [] }; }).filter((event) => event.route_relevant); routeSections = []; return []; } case 'burn_ban': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); let burnBanStatus = 'unknown'; if (lower.includes('stage 2')) burnBanStatus = 'stage_2'; else if (lower.includes('stage 1')) burnBanStatus = 'stage_1'; else if (lower.includes('no burn ban') || lower.includes('no ban') || lower.includes('burn ban status: no')) burnBanStatus = 'no_ban'; const event = { event_id: '03_AIR_QUALITY:WASMOKE-01:burn-ban:' + run.run_stamp, event_type: 'burn_ban', title: 'PSCAA burn ban status', status: 'current', severity: burnBanStatus === 'unknown' ? 'unknown' : 'good', category_label: burnBanStatus === 'stage_2' ? 'Stage 2' : burnBanStatus === 'stage_1' ? 'Stage 1' : burnBanStatus === 'no_ban' ? 'No Ban' : 'Unknown', effective_at: null, expires_at: null, route_relevant: true, wildfire_smoke_related: false, areas: ['King County'], source_id: '03_AIR_QUALITY:WASMOKE-01', source_record_id: 'king-county-burn-ban', geometry_type: 'text_only', manual_review_required: burnBanStatus === 'unknown', route_sections: ['aqp4_issaquah'] }; events = [event]; sourceWarnings.push(burnBanStatus === 'unknown' ? 'Burn-ban status could not be parsed confidently.' : 'Burn-ban status parsed from page text.'); return []; } case 'airnow_context': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); const matchAreas = ['seattle', 'bellevue', 'eastside', 'king county', 'issaquah', 'cascade foothills', 'seattle-bellevue-kent valley']; const matches = matchAreas.filter((area) => lower.includes(area)); if (matches.length) { sourceWarnings.push('AirNow reporting area matched contextual area names only; no primary route classification performed.'); } routeSections = []; events = []; observations = []; return []; } case 'smoke_rss': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); const lookup = ['Seattle', 'Bellevue', 'Eastside', 'King County', 'Issaquah', 'Cascade foothills', 'Seattle-Bellevue-Kent Valley']; const matched = lookup.filter((entry) => lower.includes(entry.toLowerCase())); if (matched.length) { const areaLabel = matched.join(', '); events = [{ event_id: '03_AIR_QUALITY:WASMOKE-01:' + hashString(areaLabel + text).slice(0, 12), event_type: 'smoke_forecast', title: 'Washington Smoke Blog context for ' + areaLabel, status: 'forecast', severity: lower.includes('hazard') ? 'unhealthy' : lower.includes('smoke') ? 'moderate' : 'unknown', category_label: 'Contextual smoke report', effective_at: nowIso, expires_at: null, route_relevant: true, wildfire_smoke_related: true, areas: matched, source_id: '03_AIR_QUALITY:WASMOKE-01', source_record_id: hashString(text).slice(0, 12), geometry_type: 'text_only', manual_review_required: false, route_sections: ['aqp3_bellevue', 'aqp4_issaquah'] }]; } else { events = []; } routeSections = []; return []; } case 'nws_alert': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); const relevant = features.filter((feature) => { const props = safeObject(feature.properties || feature); const areaDesc = normalizeText(props.areaDesc ?? props.area_desc ?? ''); const geocode = JSON.stringify(props.geocode ?? {}); const text = (areaDesc + ' ' + geocode).toLowerCase(); return normalizeText(props.event ?? '').toLowerCase() === 'air quality alert' && (text.includes('king county') || text.includes('washington') || text.includes('wa') || feature.geometry); }); events = relevant.map((feature, index) => { const props = safeObject(feature.properties || feature); const areaDesc = normalizeText(props.areaDesc ?? props.area_desc ?? 'King County'); const severity = severityFromCategory(props.severity ?? props.urgency ?? 'unknown'); const expires = toIsoUtc(props.expires ?? props.sent ?? null, null); return { event_id: '03_AIR_QUALITY:WASMOKE-01:' + String(props.id ?? props.eventCode ?? index + 1), event_type: 'air_quality_alert', title: normalizeText(props.headline ?? props.event ?? 'Air Quality Alert'), status: 'current', severity, category_label: normalizeText(props.event ?? 'Air Quality Alert'), effective_at: toIsoUtc(props.sent ?? props.onset ?? null, nowIso), expires_at: expires, route_relevant: true, wildfire_smoke_related: false, areas: areaDesc ? areaDesc.split(',').map((part) => part.trim()).filter(Boolean) : ['King County'], source_id: '03_AIR_QUALITY:WASMOKE-01', source_record_id: String(props.id ?? props.eventCode ?? index + 1), geometry_type: feature.geometry ? 'polygon' : 'county_zone', manual_review_required: false, route_sections: ['aqp4_issaquah'] }; }); routeSections = []; return []; } case 'pscaa_station': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); if (lower.includes('aqi') || lower.includes('station')) { sourceWarnings.push('PSCAA station page was reachable but no canonical station snapshot was extracted in this build.'); } routeSections = []; events = []; observations = []; return []; } case 'airnow_api': { const records = safeArray(bodyObj || bodyArr); const relevant = records.filter((record) => record && typeof record === 'object'); if (relevant.length) { observations = relevant.slice(0, 4).map((record, index) => { const aqiValue = Number(record.AQI ?? record.aqi ?? record.Value ?? null); const category = normalizeText(record.Category ?? record.category ?? qFromAqi(aqiValue) ?? 'Unknown'); const observedAt = toIsoUtc(record.DateObserved ? String(record.DateObserved) + 'T' + String(record.HourObserved ?? 0) + ':00:00Z' : record.ObservedAt ?? null, nowIso); const routeSectionId = index % 4 === 0 ? 'aqp1_seattle' : index % 4 === 1 ? 'aqp2_lake_forest_park' : index % 4 === 2 ? 'aqp3_bellevue' : 'aqp4_issaquah'; return { observation_id: '03_AIR_QUALITY:WASMOKE-01:' + String(record.SiteID ?? record.zipCode ?? index + 1) + ':' + observedAt, observation_type: 'current_air_quality', source_id: '03_AIR_QUALITY:WASMOKE-01', source_record_id: String(record.SiteID ?? record.zipCode ?? index + 1), observed_at: observedAt, retrieved_at: nowIso, station_name: normalizeText(record.ReportingArea ?? record.Name ?? 'AirNow reporting area'), station_location: null, aqi_value: Number.isFinite(aqiValue) ? aqiValue : null, aqi_category: category, severity: severityFromCategory(category), dominant_pollutant: null, pollutants: { pm25: null, pm10: null, ozone: null, no2: null }, wildfire_smoke_related: false, route_relevance: { method: 'point_to_route_distance', distance_km: null, threshold_km: 12.87, route_section_id: routeSectionId }, location: null }; }); } routeSections = []; return []; } default: return []; } })();\nconst liveCount = events.length + observations.length;\nconst cachedEvents = lkg ? safeArray(lkg.events) : [];\nconst cachedObservations = lkg ? safeArray(lkg.observations) : [];\nconst cachedRouteSections = lkg ? safeArray(lkg.route_sections) : [];\nconst cachedRecordCount = cachedEvents.length + cachedObservations.length;\nconst lkgRetrievedAt = lkg && lkg.source_health ? lkg.source_health.retrieved_at : null;\nconst lkgStaleAfterMinutes = lkg && lkg.source_health && Number.isFinite(Number(lkg.source_health.stale_after_minutes)) ? Number(lkg.source_health.stale_after_minutes) : staleAfterMinutes;\nconst lkgAgeMinutes = Number.isFinite(Date.parse(lkgRetrievedAt)) ? (Date.now() - Date.parse(lkgRetrievedAt)) / 60000 : Infinity;\nconst lkgUsable = !!lkg && Number.isFinite(lkgAgeMinutes) && lkgAgeMinutes <= lkgStaleAfterMinutes;\nif (fetchErrorText) sourceErrors.push(fetchErrorText);\nlet sourceHealth = null;\nif (fetchErrorText) {\n  if (lkgUsable) {\n    events = cachedEvents;\n    observations = cachedObservations;\n    routeSections = cachedRouteSections;\n    sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:WASMOKE-01', source_name: 'Washington Smoke Blog RSS', status: 'using_last_known_good', retrieved_at: lkg.source_health.retrieved_at, stale_after_minutes: lkg.source_health.stale_after_minutes ?? staleAfterMinutes, record_count: cachedRecordCount, http_status: lkg.source_health.http_status ?? null, last_observation_at: lkg.source_health.last_observation_at ?? lkg.source_health.retrieved_at, warnings: [...safeArray(lkg.source_health.warnings), 'Live fetch failed; serving last-known-good data.'], errors: sourceErrors };\n  } else {\n    sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:WASMOKE-01', source_name: 'Washington Smoke Blog RSS', status: 'failed', retrieved_at: run.generated_at, stale_after_minutes: staleAfterMinutes, record_count: 0, http_status: fetch.statusCode ?? fetch.status ?? null, last_observation_at: null, warnings: sourceWarnings, errors: sourceErrors };\n  }\n} else {\n  const lastObservationValue = maxOrNull([ ...observations.map((observation) => Date.parse(observation.observed_at)).filter((value) => Number.isFinite(value)), ...events.map((event) => Date.parse(event.effective_at)).filter((value) => Number.isFinite(value)) ]);\n  const status = liveCount ? 'ok' : 'empty_but_valid';\n  sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:WASMOKE-01', source_name: 'Washington Smoke Blog RSS', status, retrieved_at: run.generated_at, stale_after_minutes: staleAfterMinutes, record_count: liveCount, http_status: fetch.statusCode ?? fetch.status ?? null, last_observation_at: Number.isFinite(lastObservationValue) ? new Date(lastObservationValue).toISOString() : (liveCount ? run.generated_at : null), warnings: sourceWarnings, errors: sourceErrors };\n}\nconst provenanceSources = [{ source_id: '03_AIR_QUALITY:WASMOKE-01', source_name: 'Washington Smoke Blog RSS', retrieved_at: sourceHealth.retrieved_at, source_timestamp: sourceHealth.last_observation_at, status: sourceHealth.status, stale: sourceHealth.status === 'using_last_known_good' || sourceHealth.status === 'stale' }];\nreturn [{ json: { source_id: '03_AIR_QUALITY:WASMOKE-01', source_name: 'Washington Smoke Blog RSS', source_health: sourceHealth, events, observations, route_sections: routeSections, warnings: sourceWarnings, errors: sourceErrors, provenance: { source_ids_used: ['03_AIR_QUALITY:WASMOKE-01'], sources: provenanceSources } } }];"
      }
    },
    {
      "id": "fetch_nws_aq_01",
      "name": "Fetch NWS-AQ-01 Alerts",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        220,
        1420
      ],
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "GET",
        "url": "https://api.weather.gov/alerts/active?area=WA&event=Air%20Quality%20Alert",
        "responseFormat": "json",
        "options": {
          "timeout": 30000
        }
      },
      "notes": ""
    },
    {
      "id": "land_nws_aq_01",
      "name": "Land NWS-AQ-01 Raw Payload",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        500,
        1420
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch NWS-AQ-01 Alerts').first().json || {};\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst bodyText = typeof body === 'string' ? body : JSON.stringify(body ?? null);\nconst statusCode = fetch.statusCode ?? fetch.status ?? null;\nconst contentHash = 'hash_' + hashString(bodyText);\nconst filePath = run.output_root + '/raw/03_AIR_QUALITY/landings/NWS-AQ-01_landing_' + run.run_stamp + '.json';\nconst landing = { schema_version: '1.0.0', connector_id: run.connector_id, source_id: '03_AIR_QUALITY:NWS-AQ-01', source_name: 'NWS Air Quality Alert feed', source_url: 'https://api.weather.gov/alerts/active?area=WA&event=Air%20Quality%20Alert', retrieved_at: run.generated_at, http_status: statusCode, retrieval_method: 'geojson_api', content_hash: contentHash, body_length: bodyText.length, body: body, fetch_error: fetch.error ? stringifyError(fetch.error) : null };\nconst buffer = Buffer.from(JSON.stringify(landing, null, 2), 'utf8');\nconst binary = await this.helpers.prepareBinaryData(buffer, filePath.split('/').pop(), 'application/json');\nreturn [{ json: { file_path: filePath, source_id: landing.source_id, source_name: landing.source_name, content_hash: contentHash, retrieved_at: run.generated_at }, binary: { data: binary } }];"
      }
    },
    {
      "id": "write_nws_aq_01_raw",
      "name": "Write NWS-AQ-01 Raw Landing",
      "type": "n8n-nodes-base.readWriteFile",
      "typeVersion": 1,
      "position": [
        760,
        1420
      ],
      "parameters": {
        "operation": "write",
        "fileName": "={{ $json.file_path }}",
        "dataPropertyName": "data",
        "options": {}
      }
    },
    {
      "id": "normalize_nws_aq_01",
      "name": "Normalize NWS-AQ-01 Events",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1010,
        1420
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch NWS-AQ-01 Alerts').first().json || {};\nconst landing = $('Land NWS-AQ-01 Raw Payload').first().json || {};\nconst parseLkg = $('Parse Last Known Good').first().json || { bySource: {} };\nconst sourceConfig = run.source_configs && run.source_configs['NWS_AQ_01'] ? run.source_configs['NWS_AQ_01'] : {\n  \"source_id\": \"03_AIR_QUALITY:NWS-AQ-01\",\n  \"source_name\": \"NWS Air Quality Alert feed\",\n  \"family\": \"nws_alert\",\n  \"stale_after_minutes\": 15\n};\nconst lkg = parseLkg.bySource && parseLkg.bySource['03_AIR_QUALITY:NWS-AQ-01'] ? parseLkg.bySource['03_AIR_QUALITY:NWS-AQ-01'] : null;\nconst fetchErrorText = fetch.error ? stringifyError(fetch.error) : null;\nconst nowIso = run.generated_at;\nconst staleAfterMinutes = Number(sourceConfig.freshness_threshold_minutes ?? 15);\nconst sourceWarnings = [];\nconst sourceErrors = [];\nlet events = [];\nlet observations = [];\nlet routeSections = [];\nlet lastObservationAt = null;\nlet sourceTimestamp = null;\nconst routeSectionForStation = (name, lat, lon) => { const label = normalizeText(name).toLowerCase(); if (label.includes('issaquah') || label.includes('lake sammamish')) return 'aqp4_issaquah'; if (label.includes('bellevue') || label.includes('redmond') || label.includes('marymoor')) return 'aqp3_bellevue'; if (label.includes('lake forest park') || label.includes('kenmore') || label.includes('bothell')) return 'aqp2_lake_forest_park'; if (Number.isFinite(lat) && Number.isFinite(lon)) return 'aqp1_seattle'; return 'aqp1_seattle'; };\nconst pointDistanceKm = (aLat, aLon, bLat, bLon) => { const toRad = (deg) => (deg * Math.PI) / 180; const r = 6371; const dLat = toRad(bLat - aLat); const dLon = toRad(bLon - aLon); const lat1 = toRad(aLat); const lat2 = toRad(bLat); const x = Math.sin(dLat / 2) ** 2 + Math.cos(lat1) * Math.cos(lat2) * Math.sin(dLon / 2) ** 2; return 2 * r * Math.asin(Math.min(1, Math.sqrt(x))); };\nconst sectionAnchors = { aqp1_seattle: { lat: 47.655, lon: -122.333 }, aqp2_lake_forest_park: { lat: 47.754, lon: -122.289 }, aqp3_bellevue: { lat: 47.614, lon: -122.178 }, aqp4_issaquah: { lat: 47.532, lon: -122.032 } };\nconst severityFromCategory = (category) => { const text = normalizeText(category).toLowerCase(); if (text.includes('good')) return 'good'; if (text.includes('moderate')) return 'moderate'; if (text.includes('sensitive')) return 'usg'; if (text.includes('unhealthy')) return text.includes('very') ? 'very_unhealthy' : 'unhealthy'; if (text.includes('hazardous')) return 'hazardous'; return 'unknown'; };\nconst qFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return null; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst pickPollutant = (attrs) => { const candidates = [['pm25', Number(attrs.AQI_PM25), Number(attrs.PM25_Value), 'ug/m3', attrs.AQI_PM25_Cat], ['ozone', Number(attrs.AQI_O3), Number(attrs.O3_Value), 'ppm', attrs.AQI_O3_Cat], ['pm10', Number(attrs.AQI_PM10), Number(attrs.PM10_Value), 'ug/m3', attrs.AQI_PM10_Cat], ['no2', Number(attrs.AQI_NO2), Number(attrs.NO2_Value), 'ppb', attrs.AQI_NO2_Cat]].filter((candidate) => Number.isFinite(candidate[1])); if (!candidates.length) return null; candidates.sort((a, b) => b[1] - a[1]); return candidates[0][0]; };\nconst buildObservation = (feature, index) => { const attrs = safeObject(feature.attributes || feature); const geom = safeObject(feature.geometry || attrs.geometry || {}); const lat = Number(geom.y ?? geom.latitude ?? attrs.Latitude ?? attrs.latitude); const lon = Number(geom.x ?? geom.longitude ?? attrs.Longitude ?? attrs.longitude); const observedAt = toIsoUtc(attrs.DateTime_PST || attrs.ObservedAt || attrs.observed_at, run.generated_at); const siteId = String(attrs.SiteId ?? attrs.site_id ?? attrs.Site ?? attrs.site ?? index + 1); const siteName = normalizeText(attrs.SiteName ?? attrs.Site ?? attrs.site_name ?? 'Unknown site'); const siteLocation = normalizeText(attrs.SiteLocation ?? attrs.site_location ?? attrs.Location ?? attrs.location ?? ''); const aqiValue = Number(attrs.AQIValue ?? attrs.AQI ?? attrs.aqi_value); const aqiCategory = attrs.AQICategory ?? attrs.category ?? qFromAqi(aqiValue) ?? 'Unknown'; const severity = severityFromAqi(aqiValue) !== 'unknown' ? severityFromAqi(aqiValue) : severityFromCategory(aqiCategory); const routeSectionId = routeSectionForStation(siteName, lat, lon); const anchor = sectionAnchors[routeSectionId]; const distanceKm = Number.isFinite(lat) && Number.isFinite(lon) && anchor ? Number(pointDistanceKm(lat, lon, anchor.lat, anchor.lon).toFixed(2)) : null; const routeRelevance = { method: 'point_to_route_distance', distance_km: distanceKm, threshold_km: 12.87, route_section_id: routeSectionId }; const pm25 = Number.isFinite(Number(attrs.AQI_PM25)) || Number.isFinite(Number(attrs.PM25_Value)) ? { aqi: Number(attrs.AQI_PM25 ?? attrs.PM25_AQI ?? null), category: attrs.AQI_PM25_Cat ?? null, value: Number(attrs.PM25_Value ?? attrs.PM25 ?? null), units: 'ug/m3' } : null; const ozone = Number.isFinite(Number(attrs.AQI_O3)) || Number.isFinite(Number(attrs.O3_Value)) ? { aqi: Number(attrs.AQI_O3 ?? attrs.O3_AQI ?? null), category: attrs.AQI_O3_Cat ?? null, value: Number(attrs.O3_Value ?? attrs.O3 ?? null), units: 'ppm' } : null; const pm10 = Number.isFinite(Number(attrs.AQI_PM10)) || Number.isFinite(Number(attrs.PM10_Value)) ? { aqi: Number(attrs.AQI_PM10 ?? attrs.PM10_AQI ?? null), category: attrs.AQI_PM10_Cat ?? null, value: Number(attrs.PM10_Value ?? attrs.PM10 ?? null), units: 'ug/m3' } : null; const no2 = Number.isFinite(Number(attrs.AQI_NO2)) || Number.isFinite(Number(attrs.NO2_Value)) ? { aqi: Number(attrs.AQI_NO2 ?? attrs.NO2_AQI ?? null), category: attrs.AQI_NO2_Cat ?? null, value: Number(attrs.NO2_Value ?? attrs.NO2 ?? null), units: 'ppb' } : null; const observation = { observation_id: '03_AIR_QUALITY:NWS-AQ-01:' + siteId + ':' + observedAt, observation_type: 'current_air_quality', source_id: '03_AIR_QUALITY:NWS-AQ-01', source_record_id: siteId, observed_at: observedAt, retrieved_at: nowIso, station_name: siteName, station_location: siteLocation || null, aqi_value: Number.isFinite(aqiValue) ? aqiValue : null, aqi_category: aqiCategory, severity, dominant_pollutant: pickPollutant(attrs), pollutants: { pm25, pm10, ozone, no2 }, wildfire_smoke_related: false, route_relevance: routeRelevance, location: Number.isFinite(lat) && Number.isFinite(lon) ? { latitude: lat, longitude: lon, epsg: 4326 } : null }; if (Number.isFinite(aqiValue) && !lastObservationAt) lastObservationAt = observedAt; if (Number.isFinite(aqiValue) || Number.isFinite(lat) || Number.isFinite(lon)) sourceTimestamp = observedAt; return observation; };\nconst normalizeForecastLabel = (text) => { const value = normalizeText(text).toLowerCase(); if (value.includes('hazardous')) return 'Hazardous'; if (value.includes('very unhealthy')) return 'Very Unhealthy'; if (value.includes('unhealthy for sensitive')) return 'Unhealthy for Sensitive Groups'; if (value.includes('unhealthy')) return 'Unhealthy'; if (value.includes('moderate')) return 'Moderate'; if (value.includes('good')) return 'Good'; return normalizeText(text) || 'Unknown'; };\nconst body = fetch.body !== undefined ? fetch.body : (landing.body !== undefined ? landing.body : fetch);\nconst bodyText = typeof body === 'string' ? body : JSON.stringify(body ?? null);\nconst bodyObj = safeObject(body);\nconst bodyArr = safeArray(body);\nconst liveRecords = (() => { switch ('nws_alert') { case 'ecology_point': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); observations = features.map((feature, index) => buildObservation(feature, index)).filter((observation) => { const sectionId = observation.route_relevance.route_section_id; const anchor = sectionAnchors[sectionId]; if (!anchor || !observation.location) return true; const dist = Number(pointDistanceKm(observation.location.latitude, observation.location.longitude, anchor.lat, anchor.lon).toFixed(2)); observation.route_relevance.distance_km = dist; return dist <= observation.route_relevance.threshold_km; }); routeSections = []; return []; } case 'ecology_forecast': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); events = features.map((feature, index) => { const attrs = safeObject(feature.attributes || feature); const geom = safeObject(feature.geometry || attrs.geometry || {}); const siteName = normalizeText(attrs.SiteName ?? attrs.Site ?? attrs.site ?? attrs.Area ?? attrs.area ?? 'Seattle-Bellevue-Kent Valley'); const sourceRecordId = String(attrs.SmokeForecastId ?? attrs.ObjectId ?? attrs.objectid ?? attrs.id ?? index + 1); const label = normalizeForecastLabel(attrs.Day1 ?? attrs.DisplayText ?? attrs.Category ?? attrs.CategoryLabel ?? attrs.Day2 ?? attrs.Day3 ?? 'Good'); const severity = severityFromCategory(label); const geomText = JSON.stringify(geom); const eventRouteRelevant = Boolean(geomText && (geomText.includes('[') || geomText.includes('{'))); return { event_id: '03_AIR_QUALITY:NWS-AQ-01:' + siteName.replace(/[^a-z0-9]+/gi, '-').toLowerCase() + ':' + sourceRecordId, event_type: 'smoke_forecast', title: 'Ecology smoke forecast for ' + siteName, status: 'forecast', severity, category_label: label, effective_at: attrs.Date ? String(attrs.Date) : nowIso, expires_at: null, route_relevant: eventRouteRelevant, wildfire_smoke_related: true, areas: [siteName], source_id: '03_AIR_QUALITY:NWS-AQ-01', source_record_id: sourceRecordId, geometry_type: 'polygon', manual_review_required: false, route_sections: eventRouteRelevant ? ['aqp1_seattle', 'aqp2_lake_forest_park', 'aqp3_bellevue', 'aqp4_issaquah'] : [] }; }).filter((event) => event.route_relevant); routeSections = []; return []; } case 'burn_ban': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); let burnBanStatus = 'unknown'; if (lower.includes('stage 2')) burnBanStatus = 'stage_2'; else if (lower.includes('stage 1')) burnBanStatus = 'stage_1'; else if (lower.includes('no burn ban') || lower.includes('no ban') || lower.includes('burn ban status: no')) burnBanStatus = 'no_ban'; const event = { event_id: '03_AIR_QUALITY:NWS-AQ-01:burn-ban:' + run.run_stamp, event_type: 'burn_ban', title: 'PSCAA burn ban status', status: 'current', severity: burnBanStatus === 'unknown' ? 'unknown' : 'good', category_label: burnBanStatus === 'stage_2' ? 'Stage 2' : burnBanStatus === 'stage_1' ? 'Stage 1' : burnBanStatus === 'no_ban' ? 'No Ban' : 'Unknown', effective_at: null, expires_at: null, route_relevant: true, wildfire_smoke_related: false, areas: ['King County'], source_id: '03_AIR_QUALITY:NWS-AQ-01', source_record_id: 'king-county-burn-ban', geometry_type: 'text_only', manual_review_required: burnBanStatus === 'unknown', route_sections: ['aqp4_issaquah'] }; events = [event]; sourceWarnings.push(burnBanStatus === 'unknown' ? 'Burn-ban status could not be parsed confidently.' : 'Burn-ban status parsed from page text.'); return []; } case 'airnow_context': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); const matchAreas = ['seattle', 'bellevue', 'eastside', 'king county', 'issaquah', 'cascade foothills', 'seattle-bellevue-kent valley']; const matches = matchAreas.filter((area) => lower.includes(area)); if (matches.length) { sourceWarnings.push('AirNow reporting area matched contextual area names only; no primary route classification performed.'); } routeSections = []; events = []; observations = []; return []; } case 'smoke_rss': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); const lookup = ['Seattle', 'Bellevue', 'Eastside', 'King County', 'Issaquah', 'Cascade foothills', 'Seattle-Bellevue-Kent Valley']; const matched = lookup.filter((entry) => lower.includes(entry.toLowerCase())); if (matched.length) { const areaLabel = matched.join(', '); events = [{ event_id: '03_AIR_QUALITY:NWS-AQ-01:' + hashString(areaLabel + text).slice(0, 12), event_type: 'smoke_forecast', title: 'Washington Smoke Blog context for ' + areaLabel, status: 'forecast', severity: lower.includes('hazard') ? 'unhealthy' : lower.includes('smoke') ? 'moderate' : 'unknown', category_label: 'Contextual smoke report', effective_at: nowIso, expires_at: null, route_relevant: true, wildfire_smoke_related: true, areas: matched, source_id: '03_AIR_QUALITY:NWS-AQ-01', source_record_id: hashString(text).slice(0, 12), geometry_type: 'text_only', manual_review_required: false, route_sections: ['aqp3_bellevue', 'aqp4_issaquah'] }]; } else { events = []; } routeSections = []; return []; } case 'nws_alert': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); const relevant = features.filter((feature) => { const props = safeObject(feature.properties || feature); const areaDesc = normalizeText(props.areaDesc ?? props.area_desc ?? ''); const geocode = JSON.stringify(props.geocode ?? {}); const text = (areaDesc + ' ' + geocode).toLowerCase(); return normalizeText(props.event ?? '').toLowerCase() === 'air quality alert' && (text.includes('king county') || text.includes('washington') || text.includes('wa') || feature.geometry); }); events = relevant.map((feature, index) => { const props = safeObject(feature.properties || feature); const areaDesc = normalizeText(props.areaDesc ?? props.area_desc ?? 'King County'); const severity = severityFromCategory(props.severity ?? props.urgency ?? 'unknown'); const expires = toIsoUtc(props.expires ?? props.sent ?? null, null); return { event_id: '03_AIR_QUALITY:NWS-AQ-01:' + String(props.id ?? props.eventCode ?? index + 1), event_type: 'air_quality_alert', title: normalizeText(props.headline ?? props.event ?? 'Air Quality Alert'), status: 'current', severity, category_label: normalizeText(props.event ?? 'Air Quality Alert'), effective_at: toIsoUtc(props.sent ?? props.onset ?? null, nowIso), expires_at: expires, route_relevant: true, wildfire_smoke_related: false, areas: areaDesc ? areaDesc.split(',').map((part) => part.trim()).filter(Boolean) : ['King County'], source_id: '03_AIR_QUALITY:NWS-AQ-01', source_record_id: String(props.id ?? props.eventCode ?? index + 1), geometry_type: feature.geometry ? 'polygon' : 'county_zone', manual_review_required: false, route_sections: ['aqp4_issaquah'] }; }); routeSections = []; return []; } case 'pscaa_station': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); if (lower.includes('aqi') || lower.includes('station')) { sourceWarnings.push('PSCAA station page was reachable but no canonical station snapshot was extracted in this build.'); } routeSections = []; events = []; observations = []; return []; } case 'airnow_api': { const records = safeArray(bodyObj || bodyArr); const relevant = records.filter((record) => record && typeof record === 'object'); if (relevant.length) { observations = relevant.slice(0, 4).map((record, index) => { const aqiValue = Number(record.AQI ?? record.aqi ?? record.Value ?? null); const category = normalizeText(record.Category ?? record.category ?? qFromAqi(aqiValue) ?? 'Unknown'); const observedAt = toIsoUtc(record.DateObserved ? String(record.DateObserved) + 'T' + String(record.HourObserved ?? 0) + ':00:00Z' : record.ObservedAt ?? null, nowIso); const routeSectionId = index % 4 === 0 ? 'aqp1_seattle' : index % 4 === 1 ? 'aqp2_lake_forest_park' : index % 4 === 2 ? 'aqp3_bellevue' : 'aqp4_issaquah'; return { observation_id: '03_AIR_QUALITY:NWS-AQ-01:' + String(record.SiteID ?? record.zipCode ?? index + 1) + ':' + observedAt, observation_type: 'current_air_quality', source_id: '03_AIR_QUALITY:NWS-AQ-01', source_record_id: String(record.SiteID ?? record.zipCode ?? index + 1), observed_at: observedAt, retrieved_at: nowIso, station_name: normalizeText(record.ReportingArea ?? record.Name ?? 'AirNow reporting area'), station_location: null, aqi_value: Number.isFinite(aqiValue) ? aqiValue : null, aqi_category: category, severity: severityFromCategory(category), dominant_pollutant: null, pollutants: { pm25: null, pm10: null, ozone: null, no2: null }, wildfire_smoke_related: false, route_relevance: { method: 'point_to_route_distance', distance_km: null, threshold_km: 12.87, route_section_id: routeSectionId }, location: null }; }); } routeSections = []; return []; } default: return []; } })();\nconst liveCount = events.length + observations.length;\nconst cachedEvents = lkg ? safeArray(lkg.events) : [];\nconst cachedObservations = lkg ? safeArray(lkg.observations) : [];\nconst cachedRouteSections = lkg ? safeArray(lkg.route_sections) : [];\nconst cachedRecordCount = cachedEvents.length + cachedObservations.length;\nconst lkgRetrievedAt = lkg && lkg.source_health ? lkg.source_health.retrieved_at : null;\nconst lkgStaleAfterMinutes = lkg && lkg.source_health && Number.isFinite(Number(lkg.source_health.stale_after_minutes)) ? Number(lkg.source_health.stale_after_minutes) : staleAfterMinutes;\nconst lkgAgeMinutes = Number.isFinite(Date.parse(lkgRetrievedAt)) ? (Date.now() - Date.parse(lkgRetrievedAt)) / 60000 : Infinity;\nconst lkgUsable = !!lkg && Number.isFinite(lkgAgeMinutes) && lkgAgeMinutes <= lkgStaleAfterMinutes;\nif (fetchErrorText) sourceErrors.push(fetchErrorText);\nlet sourceHealth = null;\nif (fetchErrorText) {\n  if (lkgUsable) {\n    events = cachedEvents;\n    observations = cachedObservations;\n    routeSections = cachedRouteSections;\n    sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:NWS-AQ-01', source_name: 'NWS Air Quality Alert feed', status: 'using_last_known_good', retrieved_at: lkg.source_health.retrieved_at, stale_after_minutes: lkg.source_health.stale_after_minutes ?? staleAfterMinutes, record_count: cachedRecordCount, http_status: lkg.source_health.http_status ?? null, last_observation_at: lkg.source_health.last_observation_at ?? lkg.source_health.retrieved_at, warnings: [...safeArray(lkg.source_health.warnings), 'Live fetch failed; serving last-known-good data.'], errors: sourceErrors };\n  } else {\n    sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:NWS-AQ-01', source_name: 'NWS Air Quality Alert feed', status: 'failed', retrieved_at: run.generated_at, stale_after_minutes: staleAfterMinutes, record_count: 0, http_status: fetch.statusCode ?? fetch.status ?? null, last_observation_at: null, warnings: sourceWarnings, errors: sourceErrors };\n  }\n} else {\n  const lastObservationValue = maxOrNull([ ...observations.map((observation) => Date.parse(observation.observed_at)).filter((value) => Number.isFinite(value)), ...events.map((event) => Date.parse(event.effective_at)).filter((value) => Number.isFinite(value)) ]);\n  const status = liveCount ? 'ok' : 'empty_but_valid';\n  sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:NWS-AQ-01', source_name: 'NWS Air Quality Alert feed', status, retrieved_at: run.generated_at, stale_after_minutes: staleAfterMinutes, record_count: liveCount, http_status: fetch.statusCode ?? fetch.status ?? null, last_observation_at: Number.isFinite(lastObservationValue) ? new Date(lastObservationValue).toISOString() : (liveCount ? run.generated_at : null), warnings: sourceWarnings, errors: sourceErrors };\n}\nconst provenanceSources = [{ source_id: '03_AIR_QUALITY:NWS-AQ-01', source_name: 'NWS Air Quality Alert feed', retrieved_at: sourceHealth.retrieved_at, source_timestamp: sourceHealth.last_observation_at, status: sourceHealth.status, stale: sourceHealth.status === 'using_last_known_good' || sourceHealth.status === 'stale' }];\nreturn [{ json: { source_id: '03_AIR_QUALITY:NWS-AQ-01', source_name: 'NWS Air Quality Alert feed', source_health: sourceHealth, events, observations, route_sections: routeSections, warnings: sourceWarnings, errors: sourceErrors, provenance: { source_ids_used: ['03_AIR_QUALITY:NWS-AQ-01'], sources: provenanceSources } } }];"
      }
    },
    {
      "id": "fetch_pscaa_01",
      "name": "Fetch PSCAA-01 NetworkMap",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        220,
        1620
      ],
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "GET",
        "url": "https://secure.pscleanair.org/AirQuality/NetworkMap",
        "responseFormat": "string",
        "options": {
          "timeout": 30000
        }
      },
      "notes": ""
    },
    {
      "id": "land_pscaa_01",
      "name": "Land PSCAA-01 Raw Payload",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        500,
        1620
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch PSCAA-01 NetworkMap').first().json || {};\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst bodyText = typeof body === 'string' ? body : JSON.stringify(body ?? null);\nconst statusCode = fetch.statusCode ?? fetch.status ?? null;\nconst contentHash = 'hash_' + hashString(bodyText);\nconst filePath = run.output_root + '/raw/03_AIR_QUALITY/landings/PSCAA-01_landing_' + run.run_stamp + '.json';\nconst landing = { schema_version: '1.0.0', connector_id: run.connector_id, source_id: '03_AIR_QUALITY:PSCAA-01', source_name: 'Puget Sound Clean Air Agency NetworkMap session source', source_url: 'https://secure.pscleanair.org/AirQuality/NetworkMap', retrieved_at: run.generated_at, http_status: statusCode, retrieval_method: 'session_bootstrap_page_fetch', content_hash: contentHash, body_length: bodyText.length, body: body, fetch_error: fetch.error ? stringifyError(fetch.error) : null };\nconst buffer = Buffer.from(JSON.stringify(landing, null, 2), 'utf8');\nconst binary = await this.helpers.prepareBinaryData(buffer, filePath.split('/').pop(), 'application/json');\nreturn [{ json: { file_path: filePath, source_id: landing.source_id, source_name: landing.source_name, content_hash: contentHash, retrieved_at: run.generated_at }, binary: { data: binary } }];"
      }
    },
    {
      "id": "write_pscaa_01_raw",
      "name": "Write PSCAA-01 Raw Landing",
      "type": "n8n-nodes-base.readWriteFile",
      "typeVersion": 1,
      "position": [
        760,
        1620
      ],
      "parameters": {
        "operation": "write",
        "fileName": "={{ $json.file_path }}",
        "dataPropertyName": "data",
        "options": {}
      }
    },
    {
      "id": "normalize_pscaa_01",
      "name": "Normalize PSCAA-01 Events",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1010,
        1620
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch PSCAA-01 NetworkMap').first().json || {};\nconst landing = $('Land PSCAA-01 Raw Payload').first().json || {};\nconst parseLkg = $('Parse Last Known Good').first().json || { bySource: {} };\nconst sourceConfig = run.source_configs && run.source_configs['PSCAA_01'] ? run.source_configs['PSCAA_01'] : {\n  \"source_id\": \"03_AIR_QUALITY:PSCAA-01\",\n  \"source_name\": \"Puget Sound Clean Air Agency NetworkMap session source\",\n  \"family\": \"pscaa_station\",\n  \"stale_after_minutes\": 90\n};\nconst lkg = parseLkg.bySource && parseLkg.bySource['03_AIR_QUALITY:PSCAA-01'] ? parseLkg.bySource['03_AIR_QUALITY:PSCAA-01'] : null;\nconst fetchErrorText = fetch.error ? stringifyError(fetch.error) : null;\nconst nowIso = run.generated_at;\nconst staleAfterMinutes = Number(sourceConfig.freshness_threshold_minutes ?? 90);\nconst sourceWarnings = [];\nconst sourceErrors = [];\nlet events = [];\nlet observations = [];\nlet routeSections = [];\nlet lastObservationAt = null;\nlet sourceTimestamp = null;\nconst routeSectionForStation = (name, lat, lon) => { const label = normalizeText(name).toLowerCase(); if (label.includes('issaquah') || label.includes('lake sammamish')) return 'aqp4_issaquah'; if (label.includes('bellevue') || label.includes('redmond') || label.includes('marymoor')) return 'aqp3_bellevue'; if (label.includes('lake forest park') || label.includes('kenmore') || label.includes('bothell')) return 'aqp2_lake_forest_park'; if (Number.isFinite(lat) && Number.isFinite(lon)) return 'aqp1_seattle'; return 'aqp1_seattle'; };\nconst pointDistanceKm = (aLat, aLon, bLat, bLon) => { const toRad = (deg) => (deg * Math.PI) / 180; const r = 6371; const dLat = toRad(bLat - aLat); const dLon = toRad(bLon - aLon); const lat1 = toRad(aLat); const lat2 = toRad(bLat); const x = Math.sin(dLat / 2) ** 2 + Math.cos(lat1) * Math.cos(lat2) * Math.sin(dLon / 2) ** 2; return 2 * r * Math.asin(Math.min(1, Math.sqrt(x))); };\nconst sectionAnchors = { aqp1_seattle: { lat: 47.655, lon: -122.333 }, aqp2_lake_forest_park: { lat: 47.754, lon: -122.289 }, aqp3_bellevue: { lat: 47.614, lon: -122.178 }, aqp4_issaquah: { lat: 47.532, lon: -122.032 } };\nconst severityFromCategory = (category) => { const text = normalizeText(category).toLowerCase(); if (text.includes('good')) return 'good'; if (text.includes('moderate')) return 'moderate'; if (text.includes('sensitive')) return 'usg'; if (text.includes('unhealthy')) return text.includes('very') ? 'very_unhealthy' : 'unhealthy'; if (text.includes('hazardous')) return 'hazardous'; return 'unknown'; };\nconst qFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return null; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst pickPollutant = (attrs) => { const candidates = [['pm25', Number(attrs.AQI_PM25), Number(attrs.PM25_Value), 'ug/m3', attrs.AQI_PM25_Cat], ['ozone', Number(attrs.AQI_O3), Number(attrs.O3_Value), 'ppm', attrs.AQI_O3_Cat], ['pm10', Number(attrs.AQI_PM10), Number(attrs.PM10_Value), 'ug/m3', attrs.AQI_PM10_Cat], ['no2', Number(attrs.AQI_NO2), Number(attrs.NO2_Value), 'ppb', attrs.AQI_NO2_Cat]].filter((candidate) => Number.isFinite(candidate[1])); if (!candidates.length) return null; candidates.sort((a, b) => b[1] - a[1]); return candidates[0][0]; };\nconst buildObservation = (feature, index) => { const attrs = safeObject(feature.attributes || feature); const geom = safeObject(feature.geometry || attrs.geometry || {}); const lat = Number(geom.y ?? geom.latitude ?? attrs.Latitude ?? attrs.latitude); const lon = Number(geom.x ?? geom.longitude ?? attrs.Longitude ?? attrs.longitude); const observedAt = toIsoUtc(attrs.DateTime_PST || attrs.ObservedAt || attrs.observed_at, run.generated_at); const siteId = String(attrs.SiteId ?? attrs.site_id ?? attrs.Site ?? attrs.site ?? index + 1); const siteName = normalizeText(attrs.SiteName ?? attrs.Site ?? attrs.site_name ?? 'Unknown site'); const siteLocation = normalizeText(attrs.SiteLocation ?? attrs.site_location ?? attrs.Location ?? attrs.location ?? ''); const aqiValue = Number(attrs.AQIValue ?? attrs.AQI ?? attrs.aqi_value); const aqiCategory = attrs.AQICategory ?? attrs.category ?? qFromAqi(aqiValue) ?? 'Unknown'; const severity = severityFromAqi(aqiValue) !== 'unknown' ? severityFromAqi(aqiValue) : severityFromCategory(aqiCategory); const routeSectionId = routeSectionForStation(siteName, lat, lon); const anchor = sectionAnchors[routeSectionId]; const distanceKm = Number.isFinite(lat) && Number.isFinite(lon) && anchor ? Number(pointDistanceKm(lat, lon, anchor.lat, anchor.lon).toFixed(2)) : null; const routeRelevance = { method: 'point_to_route_distance', distance_km: distanceKm, threshold_km: 12.87, route_section_id: routeSectionId }; const pm25 = Number.isFinite(Number(attrs.AQI_PM25)) || Number.isFinite(Number(attrs.PM25_Value)) ? { aqi: Number(attrs.AQI_PM25 ?? attrs.PM25_AQI ?? null), category: attrs.AQI_PM25_Cat ?? null, value: Number(attrs.PM25_Value ?? attrs.PM25 ?? null), units: 'ug/m3' } : null; const ozone = Number.isFinite(Number(attrs.AQI_O3)) || Number.isFinite(Number(attrs.O3_Value)) ? { aqi: Number(attrs.AQI_O3 ?? attrs.O3_AQI ?? null), category: attrs.AQI_O3_Cat ?? null, value: Number(attrs.O3_Value ?? attrs.O3 ?? null), units: 'ppm' } : null; const pm10 = Number.isFinite(Number(attrs.AQI_PM10)) || Number.isFinite(Number(attrs.PM10_Value)) ? { aqi: Number(attrs.AQI_PM10 ?? attrs.PM10_AQI ?? null), category: attrs.AQI_PM10_Cat ?? null, value: Number(attrs.PM10_Value ?? attrs.PM10 ?? null), units: 'ug/m3' } : null; const no2 = Number.isFinite(Number(attrs.AQI_NO2)) || Number.isFinite(Number(attrs.NO2_Value)) ? { aqi: Number(attrs.AQI_NO2 ?? attrs.NO2_AQI ?? null), category: attrs.AQI_NO2_Cat ?? null, value: Number(attrs.NO2_Value ?? attrs.NO2 ?? null), units: 'ppb' } : null; const observation = { observation_id: '03_AIR_QUALITY:PSCAA-01:' + siteId + ':' + observedAt, observation_type: 'current_air_quality', source_id: '03_AIR_QUALITY:PSCAA-01', source_record_id: siteId, observed_at: observedAt, retrieved_at: nowIso, station_name: siteName, station_location: siteLocation || null, aqi_value: Number.isFinite(aqiValue) ? aqiValue : null, aqi_category: aqiCategory, severity, dominant_pollutant: pickPollutant(attrs), pollutants: { pm25, pm10, ozone, no2 }, wildfire_smoke_related: false, route_relevance: routeRelevance, location: Number.isFinite(lat) && Number.isFinite(lon) ? { latitude: lat, longitude: lon, epsg: 4326 } : null }; if (Number.isFinite(aqiValue) && !lastObservationAt) lastObservationAt = observedAt; if (Number.isFinite(aqiValue) || Number.isFinite(lat) || Number.isFinite(lon)) sourceTimestamp = observedAt; return observation; };\nconst normalizeForecastLabel = (text) => { const value = normalizeText(text).toLowerCase(); if (value.includes('hazardous')) return 'Hazardous'; if (value.includes('very unhealthy')) return 'Very Unhealthy'; if (value.includes('unhealthy for sensitive')) return 'Unhealthy for Sensitive Groups'; if (value.includes('unhealthy')) return 'Unhealthy'; if (value.includes('moderate')) return 'Moderate'; if (value.includes('good')) return 'Good'; return normalizeText(text) || 'Unknown'; };\nconst body = fetch.body !== undefined ? fetch.body : (landing.body !== undefined ? landing.body : fetch);\nconst bodyText = typeof body === 'string' ? body : JSON.stringify(body ?? null);\nconst bodyObj = safeObject(body);\nconst bodyArr = safeArray(body);\nconst liveRecords = (() => { switch ('pscaa_station') { case 'ecology_point': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); observations = features.map((feature, index) => buildObservation(feature, index)).filter((observation) => { const sectionId = observation.route_relevance.route_section_id; const anchor = sectionAnchors[sectionId]; if (!anchor || !observation.location) return true; const dist = Number(pointDistanceKm(observation.location.latitude, observation.location.longitude, anchor.lat, anchor.lon).toFixed(2)); observation.route_relevance.distance_km = dist; return dist <= observation.route_relevance.threshold_km; }); routeSections = []; return []; } case 'ecology_forecast': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); events = features.map((feature, index) => { const attrs = safeObject(feature.attributes || feature); const geom = safeObject(feature.geometry || attrs.geometry || {}); const siteName = normalizeText(attrs.SiteName ?? attrs.Site ?? attrs.site ?? attrs.Area ?? attrs.area ?? 'Seattle-Bellevue-Kent Valley'); const sourceRecordId = String(attrs.SmokeForecastId ?? attrs.ObjectId ?? attrs.objectid ?? attrs.id ?? index + 1); const label = normalizeForecastLabel(attrs.Day1 ?? attrs.DisplayText ?? attrs.Category ?? attrs.CategoryLabel ?? attrs.Day2 ?? attrs.Day3 ?? 'Good'); const severity = severityFromCategory(label); const geomText = JSON.stringify(geom); const eventRouteRelevant = Boolean(geomText && (geomText.includes('[') || geomText.includes('{'))); return { event_id: '03_AIR_QUALITY:PSCAA-01:' + siteName.replace(/[^a-z0-9]+/gi, '-').toLowerCase() + ':' + sourceRecordId, event_type: 'smoke_forecast', title: 'Ecology smoke forecast for ' + siteName, status: 'forecast', severity, category_label: label, effective_at: attrs.Date ? String(attrs.Date) : nowIso, expires_at: null, route_relevant: eventRouteRelevant, wildfire_smoke_related: true, areas: [siteName], source_id: '03_AIR_QUALITY:PSCAA-01', source_record_id: sourceRecordId, geometry_type: 'polygon', manual_review_required: false, route_sections: eventRouteRelevant ? ['aqp1_seattle', 'aqp2_lake_forest_park', 'aqp3_bellevue', 'aqp4_issaquah'] : [] }; }).filter((event) => event.route_relevant); routeSections = []; return []; } case 'burn_ban': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); let burnBanStatus = 'unknown'; if (lower.includes('stage 2')) burnBanStatus = 'stage_2'; else if (lower.includes('stage 1')) burnBanStatus = 'stage_1'; else if (lower.includes('no burn ban') || lower.includes('no ban') || lower.includes('burn ban status: no')) burnBanStatus = 'no_ban'; const event = { event_id: '03_AIR_QUALITY:PSCAA-01:burn-ban:' + run.run_stamp, event_type: 'burn_ban', title: 'PSCAA burn ban status', status: 'current', severity: burnBanStatus === 'unknown' ? 'unknown' : 'good', category_label: burnBanStatus === 'stage_2' ? 'Stage 2' : burnBanStatus === 'stage_1' ? 'Stage 1' : burnBanStatus === 'no_ban' ? 'No Ban' : 'Unknown', effective_at: null, expires_at: null, route_relevant: true, wildfire_smoke_related: false, areas: ['King County'], source_id: '03_AIR_QUALITY:PSCAA-01', source_record_id: 'king-county-burn-ban', geometry_type: 'text_only', manual_review_required: burnBanStatus === 'unknown', route_sections: ['aqp4_issaquah'] }; events = [event]; sourceWarnings.push(burnBanStatus === 'unknown' ? 'Burn-ban status could not be parsed confidently.' : 'Burn-ban status parsed from page text.'); return []; } case 'airnow_context': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); const matchAreas = ['seattle', 'bellevue', 'eastside', 'king county', 'issaquah', 'cascade foothills', 'seattle-bellevue-kent valley']; const matches = matchAreas.filter((area) => lower.includes(area)); if (matches.length) { sourceWarnings.push('AirNow reporting area matched contextual area names only; no primary route classification performed.'); } routeSections = []; events = []; observations = []; return []; } case 'smoke_rss': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); const lookup = ['Seattle', 'Bellevue', 'Eastside', 'King County', 'Issaquah', 'Cascade foothills', 'Seattle-Bellevue-Kent Valley']; const matched = lookup.filter((entry) => lower.includes(entry.toLowerCase())); if (matched.length) { const areaLabel = matched.join(', '); events = [{ event_id: '03_AIR_QUALITY:PSCAA-01:' + hashString(areaLabel + text).slice(0, 12), event_type: 'smoke_forecast', title: 'Washington Smoke Blog context for ' + areaLabel, status: 'forecast', severity: lower.includes('hazard') ? 'unhealthy' : lower.includes('smoke') ? 'moderate' : 'unknown', category_label: 'Contextual smoke report', effective_at: nowIso, expires_at: null, route_relevant: true, wildfire_smoke_related: true, areas: matched, source_id: '03_AIR_QUALITY:PSCAA-01', source_record_id: hashString(text).slice(0, 12), geometry_type: 'text_only', manual_review_required: false, route_sections: ['aqp3_bellevue', 'aqp4_issaquah'] }]; } else { events = []; } routeSections = []; return []; } case 'nws_alert': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); const relevant = features.filter((feature) => { const props = safeObject(feature.properties || feature); const areaDesc = normalizeText(props.areaDesc ?? props.area_desc ?? ''); const geocode = JSON.stringify(props.geocode ?? {}); const text = (areaDesc + ' ' + geocode).toLowerCase(); return normalizeText(props.event ?? '').toLowerCase() === 'air quality alert' && (text.includes('king county') || text.includes('washington') || text.includes('wa') || feature.geometry); }); events = relevant.map((feature, index) => { const props = safeObject(feature.properties || feature); const areaDesc = normalizeText(props.areaDesc ?? props.area_desc ?? 'King County'); const severity = severityFromCategory(props.severity ?? props.urgency ?? 'unknown'); const expires = toIsoUtc(props.expires ?? props.sent ?? null, null); return { event_id: '03_AIR_QUALITY:PSCAA-01:' + String(props.id ?? props.eventCode ?? index + 1), event_type: 'air_quality_alert', title: normalizeText(props.headline ?? props.event ?? 'Air Quality Alert'), status: 'current', severity, category_label: normalizeText(props.event ?? 'Air Quality Alert'), effective_at: toIsoUtc(props.sent ?? props.onset ?? null, nowIso), expires_at: expires, route_relevant: true, wildfire_smoke_related: false, areas: areaDesc ? areaDesc.split(',').map((part) => part.trim()).filter(Boolean) : ['King County'], source_id: '03_AIR_QUALITY:PSCAA-01', source_record_id: String(props.id ?? props.eventCode ?? index + 1), geometry_type: feature.geometry ? 'polygon' : 'county_zone', manual_review_required: false, route_sections: ['aqp4_issaquah'] }; }); routeSections = []; return []; } case 'pscaa_station': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); if (lower.includes('aqi') || lower.includes('station')) { sourceWarnings.push('PSCAA station page was reachable but no canonical station snapshot was extracted in this build.'); } routeSections = []; events = []; observations = []; return []; } case 'airnow_api': { const records = safeArray(bodyObj || bodyArr); const relevant = records.filter((record) => record && typeof record === 'object'); if (relevant.length) { observations = relevant.slice(0, 4).map((record, index) => { const aqiValue = Number(record.AQI ?? record.aqi ?? record.Value ?? null); const category = normalizeText(record.Category ?? record.category ?? qFromAqi(aqiValue) ?? 'Unknown'); const observedAt = toIsoUtc(record.DateObserved ? String(record.DateObserved) + 'T' + String(record.HourObserved ?? 0) + ':00:00Z' : record.ObservedAt ?? null, nowIso); const routeSectionId = index % 4 === 0 ? 'aqp1_seattle' : index % 4 === 1 ? 'aqp2_lake_forest_park' : index % 4 === 2 ? 'aqp3_bellevue' : 'aqp4_issaquah'; return { observation_id: '03_AIR_QUALITY:PSCAA-01:' + String(record.SiteID ?? record.zipCode ?? index + 1) + ':' + observedAt, observation_type: 'current_air_quality', source_id: '03_AIR_QUALITY:PSCAA-01', source_record_id: String(record.SiteID ?? record.zipCode ?? index + 1), observed_at: observedAt, retrieved_at: nowIso, station_name: normalizeText(record.ReportingArea ?? record.Name ?? 'AirNow reporting area'), station_location: null, aqi_value: Number.isFinite(aqiValue) ? aqiValue : null, aqi_category: category, severity: severityFromCategory(category), dominant_pollutant: null, pollutants: { pm25: null, pm10: null, ozone: null, no2: null }, wildfire_smoke_related: false, route_relevance: { method: 'point_to_route_distance', distance_km: null, threshold_km: 12.87, route_section_id: routeSectionId }, location: null }; }); } routeSections = []; return []; } default: return []; } })();\nconst liveCount = events.length + observations.length;\nconst cachedEvents = lkg ? safeArray(lkg.events) : [];\nconst cachedObservations = lkg ? safeArray(lkg.observations) : [];\nconst cachedRouteSections = lkg ? safeArray(lkg.route_sections) : [];\nconst cachedRecordCount = cachedEvents.length + cachedObservations.length;\nconst lkgRetrievedAt = lkg && lkg.source_health ? lkg.source_health.retrieved_at : null;\nconst lkgStaleAfterMinutes = lkg && lkg.source_health && Number.isFinite(Number(lkg.source_health.stale_after_minutes)) ? Number(lkg.source_health.stale_after_minutes) : staleAfterMinutes;\nconst lkgAgeMinutes = Number.isFinite(Date.parse(lkgRetrievedAt)) ? (Date.now() - Date.parse(lkgRetrievedAt)) / 60000 : Infinity;\nconst lkgUsable = !!lkg && Number.isFinite(lkgAgeMinutes) && lkgAgeMinutes <= lkgStaleAfterMinutes;\nif (fetchErrorText) sourceErrors.push(fetchErrorText);\nlet sourceHealth = null;\nif (fetchErrorText) {\n  if (lkgUsable) {\n    events = cachedEvents;\n    observations = cachedObservations;\n    routeSections = cachedRouteSections;\n    sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:PSCAA-01', source_name: 'Puget Sound Clean Air Agency NetworkMap session source', status: 'using_last_known_good', retrieved_at: lkg.source_health.retrieved_at, stale_after_minutes: lkg.source_health.stale_after_minutes ?? staleAfterMinutes, record_count: cachedRecordCount, http_status: lkg.source_health.http_status ?? null, last_observation_at: lkg.source_health.last_observation_at ?? lkg.source_health.retrieved_at, warnings: [...safeArray(lkg.source_health.warnings), 'Live fetch failed; serving last-known-good data.'], errors: sourceErrors };\n  } else {\n    sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:PSCAA-01', source_name: 'Puget Sound Clean Air Agency NetworkMap session source', status: 'failed', retrieved_at: run.generated_at, stale_after_minutes: staleAfterMinutes, record_count: 0, http_status: fetch.statusCode ?? fetch.status ?? null, last_observation_at: null, warnings: sourceWarnings, errors: sourceErrors };\n  }\n} else {\n  const lastObservationValue = maxOrNull([ ...observations.map((observation) => Date.parse(observation.observed_at)).filter((value) => Number.isFinite(value)), ...events.map((event) => Date.parse(event.effective_at)).filter((value) => Number.isFinite(value)) ]);\n  const status = liveCount ? 'ok' : 'empty_but_valid';\n  sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:PSCAA-01', source_name: 'Puget Sound Clean Air Agency NetworkMap session source', status, retrieved_at: run.generated_at, stale_after_minutes: staleAfterMinutes, record_count: liveCount, http_status: fetch.statusCode ?? fetch.status ?? null, last_observation_at: Number.isFinite(lastObservationValue) ? new Date(lastObservationValue).toISOString() : (liveCount ? run.generated_at : null), warnings: sourceWarnings, errors: sourceErrors };\n}\nconst provenanceSources = [{ source_id: '03_AIR_QUALITY:PSCAA-01', source_name: 'Puget Sound Clean Air Agency NetworkMap session source', retrieved_at: sourceHealth.retrieved_at, source_timestamp: sourceHealth.last_observation_at, status: sourceHealth.status, stale: sourceHealth.status === 'using_last_known_good' || sourceHealth.status === 'stale' }];\nreturn [{ json: { source_id: '03_AIR_QUALITY:PSCAA-01', source_name: 'Puget Sound Clean Air Agency NetworkMap session source', source_health: sourceHealth, events, observations, route_sections: routeSections, warnings: sourceWarnings, errors: sourceErrors, provenance: { source_ids_used: ['03_AIR_QUALITY:PSCAA-01'], sources: provenanceSources } } }];"
      }
    },
    {
      "id": "fetch_airnow_01",
      "name": "Fetch AIRNOW-01 API Alerts",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        220,
        1820
      ],
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "GET",
        "url": "={{ $env.AIRNOW_API_KEY ? (\"https://www.airnowapi.org/aq/observation/zipCode/current/?format=application/json&zipCode=98027&distance=25&API_KEY=\" + $env.AIRNOW_API_KEY) : \"https://www.airnowapi.org/aq/observation/zipCode/current/?format=application/json&zipCode=98027&distance=25&API_KEY=\" }}",
        "responseFormat": "json",
        "options": {
          "timeout": 30000
        }
      },
      "notes": ""
    },
    {
      "id": "land_airnow_01",
      "name": "Land AIRNOW-01 Raw Payload",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        500,
        1820
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch AIRNOW-01 API Alerts').first().json || {};\nconst body = fetch.body !== undefined ? fetch.body : (fetch.data !== undefined ? fetch.data : fetch);\nconst bodyText = typeof body === 'string' ? body : JSON.stringify(body ?? null);\nconst statusCode = fetch.statusCode ?? fetch.status ?? null;\nconst contentHash = 'hash_' + hashString(bodyText);\nconst filePath = run.output_root + '/raw/03_AIR_QUALITY/landings/AIRNOW-01_landing_' + run.run_stamp + '.json';\nconst landing = { schema_version: '1.0.0', connector_id: run.connector_id, source_id: '03_AIR_QUALITY:AIRNOW-01', source_name: 'AirNow API observations', source_url: '={{ $env.AIRNOW_API_KEY ? (\"https://www.airnowapi.org/aq/observation/zipCode/current/?format=application/json&zipCode=98027&distance=25&API_KEY=\" + $env.AIRNOW_API_KEY) : \"https://www.airnowapi.org/aq/observation/zipCode/current/?format=application/json&zipCode=98027&distance=25&API_KEY=\" }}', retrieved_at: run.generated_at, http_status: statusCode, retrieval_method: 'api_key_query_parameter', content_hash: contentHash, body_length: bodyText.length, body: body, fetch_error: fetch.error ? stringifyError(fetch.error) : null };\nconst buffer = Buffer.from(JSON.stringify(landing, null, 2), 'utf8');\nconst binary = await this.helpers.prepareBinaryData(buffer, filePath.split('/').pop(), 'application/json');\nreturn [{ json: { file_path: filePath, source_id: landing.source_id, source_name: landing.source_name, content_hash: contentHash, retrieved_at: run.generated_at }, binary: { data: binary } }];"
      }
    },
    {
      "id": "write_airnow_01_raw",
      "name": "Write AIRNOW-01 Raw Landing",
      "type": "n8n-nodes-base.readWriteFile",
      "typeVersion": 1,
      "position": [
        760,
        1820
      ],
      "parameters": {
        "operation": "write",
        "fileName": "={{ $json.file_path }}",
        "dataPropertyName": "data",
        "options": {}
      }
    },
    {
      "id": "normalize_airnow_01",
      "name": "Normalize AIRNOW-01 Events",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1010,
        1820
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst run = $('Initialize Run Metadata').first().json;\nconst fetch = $('Fetch AIRNOW-01 API Alerts').first().json || {};\nconst landing = $('Land AIRNOW-01 Raw Payload').first().json || {};\nconst parseLkg = $('Parse Last Known Good').first().json || { bySource: {} };\nconst sourceConfig = run.source_configs && run.source_configs['AIRNOW_01'] ? run.source_configs['AIRNOW_01'] : {\n  \"source_id\": \"03_AIR_QUALITY:AIRNOW-01\",\n  \"source_name\": \"AirNow API observations\",\n  \"family\": \"airnow_api\",\n  \"stale_after_minutes\": 90\n};\nconst lkg = parseLkg.bySource && parseLkg.bySource['03_AIR_QUALITY:AIRNOW-01'] ? parseLkg.bySource['03_AIR_QUALITY:AIRNOW-01'] : null;\nconst fetchErrorText = fetch.error ? stringifyError(fetch.error) : null;\nconst nowIso = run.generated_at;\nconst staleAfterMinutes = Number(sourceConfig.freshness_threshold_minutes ?? 90);\nconst sourceWarnings = [];\nconst sourceErrors = [];\nlet events = [];\nlet observations = [];\nlet routeSections = [];\nlet lastObservationAt = null;\nlet sourceTimestamp = null;\nconst routeSectionForStation = (name, lat, lon) => { const label = normalizeText(name).toLowerCase(); if (label.includes('issaquah') || label.includes('lake sammamish')) return 'aqp4_issaquah'; if (label.includes('bellevue') || label.includes('redmond') || label.includes('marymoor')) return 'aqp3_bellevue'; if (label.includes('lake forest park') || label.includes('kenmore') || label.includes('bothell')) return 'aqp2_lake_forest_park'; if (Number.isFinite(lat) && Number.isFinite(lon)) return 'aqp1_seattle'; return 'aqp1_seattle'; };\nconst pointDistanceKm = (aLat, aLon, bLat, bLon) => { const toRad = (deg) => (deg * Math.PI) / 180; const r = 6371; const dLat = toRad(bLat - aLat); const dLon = toRad(bLon - aLon); const lat1 = toRad(aLat); const lat2 = toRad(bLat); const x = Math.sin(dLat / 2) ** 2 + Math.cos(lat1) * Math.cos(lat2) * Math.sin(dLon / 2) ** 2; return 2 * r * Math.asin(Math.min(1, Math.sqrt(x))); };\nconst sectionAnchors = { aqp1_seattle: { lat: 47.655, lon: -122.333 }, aqp2_lake_forest_park: { lat: 47.754, lon: -122.289 }, aqp3_bellevue: { lat: 47.614, lon: -122.178 }, aqp4_issaquah: { lat: 47.532, lon: -122.032 } };\nconst severityFromCategory = (category) => { const text = normalizeText(category).toLowerCase(); if (text.includes('good')) return 'good'; if (text.includes('moderate')) return 'moderate'; if (text.includes('sensitive')) return 'usg'; if (text.includes('unhealthy')) return text.includes('very') ? 'very_unhealthy' : 'unhealthy'; if (text.includes('hazardous')) return 'hazardous'; return 'unknown'; };\nconst qFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return null; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst pickPollutant = (attrs) => { const candidates = [['pm25', Number(attrs.AQI_PM25), Number(attrs.PM25_Value), 'ug/m3', attrs.AQI_PM25_Cat], ['ozone', Number(attrs.AQI_O3), Number(attrs.O3_Value), 'ppm', attrs.AQI_O3_Cat], ['pm10', Number(attrs.AQI_PM10), Number(attrs.PM10_Value), 'ug/m3', attrs.AQI_PM10_Cat], ['no2', Number(attrs.AQI_NO2), Number(attrs.NO2_Value), 'ppb', attrs.AQI_NO2_Cat]].filter((candidate) => Number.isFinite(candidate[1])); if (!candidates.length) return null; candidates.sort((a, b) => b[1] - a[1]); return candidates[0][0]; };\nconst buildObservation = (feature, index) => { const attrs = safeObject(feature.attributes || feature); const geom = safeObject(feature.geometry || attrs.geometry || {}); const lat = Number(geom.y ?? geom.latitude ?? attrs.Latitude ?? attrs.latitude); const lon = Number(geom.x ?? geom.longitude ?? attrs.Longitude ?? attrs.longitude); const observedAt = toIsoUtc(attrs.DateTime_PST || attrs.ObservedAt || attrs.observed_at, run.generated_at); const siteId = String(attrs.SiteId ?? attrs.site_id ?? attrs.Site ?? attrs.site ?? index + 1); const siteName = normalizeText(attrs.SiteName ?? attrs.Site ?? attrs.site_name ?? 'Unknown site'); const siteLocation = normalizeText(attrs.SiteLocation ?? attrs.site_location ?? attrs.Location ?? attrs.location ?? ''); const aqiValue = Number(attrs.AQIValue ?? attrs.AQI ?? attrs.aqi_value); const aqiCategory = attrs.AQICategory ?? attrs.category ?? qFromAqi(aqiValue) ?? 'Unknown'; const severity = severityFromAqi(aqiValue) !== 'unknown' ? severityFromAqi(aqiValue) : severityFromCategory(aqiCategory); const routeSectionId = routeSectionForStation(siteName, lat, lon); const anchor = sectionAnchors[routeSectionId]; const distanceKm = Number.isFinite(lat) && Number.isFinite(lon) && anchor ? Number(pointDistanceKm(lat, lon, anchor.lat, anchor.lon).toFixed(2)) : null; const routeRelevance = { method: 'point_to_route_distance', distance_km: distanceKm, threshold_km: 12.87, route_section_id: routeSectionId }; const pm25 = Number.isFinite(Number(attrs.AQI_PM25)) || Number.isFinite(Number(attrs.PM25_Value)) ? { aqi: Number(attrs.AQI_PM25 ?? attrs.PM25_AQI ?? null), category: attrs.AQI_PM25_Cat ?? null, value: Number(attrs.PM25_Value ?? attrs.PM25 ?? null), units: 'ug/m3' } : null; const ozone = Number.isFinite(Number(attrs.AQI_O3)) || Number.isFinite(Number(attrs.O3_Value)) ? { aqi: Number(attrs.AQI_O3 ?? attrs.O3_AQI ?? null), category: attrs.AQI_O3_Cat ?? null, value: Number(attrs.O3_Value ?? attrs.O3 ?? null), units: 'ppm' } : null; const pm10 = Number.isFinite(Number(attrs.AQI_PM10)) || Number.isFinite(Number(attrs.PM10_Value)) ? { aqi: Number(attrs.AQI_PM10 ?? attrs.PM10_AQI ?? null), category: attrs.AQI_PM10_Cat ?? null, value: Number(attrs.PM10_Value ?? attrs.PM10 ?? null), units: 'ug/m3' } : null; const no2 = Number.isFinite(Number(attrs.AQI_NO2)) || Number.isFinite(Number(attrs.NO2_Value)) ? { aqi: Number(attrs.AQI_NO2 ?? attrs.NO2_AQI ?? null), category: attrs.AQI_NO2_Cat ?? null, value: Number(attrs.NO2_Value ?? attrs.NO2 ?? null), units: 'ppb' } : null; const observation = { observation_id: '03_AIR_QUALITY:AIRNOW-01:' + siteId + ':' + observedAt, observation_type: 'current_air_quality', source_id: '03_AIR_QUALITY:AIRNOW-01', source_record_id: siteId, observed_at: observedAt, retrieved_at: nowIso, station_name: siteName, station_location: siteLocation || null, aqi_value: Number.isFinite(aqiValue) ? aqiValue : null, aqi_category: aqiCategory, severity, dominant_pollutant: pickPollutant(attrs), pollutants: { pm25, pm10, ozone, no2 }, wildfire_smoke_related: false, route_relevance: routeRelevance, location: Number.isFinite(lat) && Number.isFinite(lon) ? { latitude: lat, longitude: lon, epsg: 4326 } : null }; if (Number.isFinite(aqiValue) && !lastObservationAt) lastObservationAt = observedAt; if (Number.isFinite(aqiValue) || Number.isFinite(lat) || Number.isFinite(lon)) sourceTimestamp = observedAt; return observation; };\nconst normalizeForecastLabel = (text) => { const value = normalizeText(text).toLowerCase(); if (value.includes('hazardous')) return 'Hazardous'; if (value.includes('very unhealthy')) return 'Very Unhealthy'; if (value.includes('unhealthy for sensitive')) return 'Unhealthy for Sensitive Groups'; if (value.includes('unhealthy')) return 'Unhealthy'; if (value.includes('moderate')) return 'Moderate'; if (value.includes('good')) return 'Good'; return normalizeText(text) || 'Unknown'; };\nconst body = fetch.body !== undefined ? fetch.body : (landing.body !== undefined ? landing.body : fetch);\nconst bodyText = typeof body === 'string' ? body : JSON.stringify(body ?? null);\nconst bodyObj = safeObject(body);\nconst bodyArr = safeArray(body);\nconst liveRecords = (() => { switch ('airnow_api') { case 'ecology_point': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); observations = features.map((feature, index) => buildObservation(feature, index)).filter((observation) => { const sectionId = observation.route_relevance.route_section_id; const anchor = sectionAnchors[sectionId]; if (!anchor || !observation.location) return true; const dist = Number(pointDistanceKm(observation.location.latitude, observation.location.longitude, anchor.lat, anchor.lon).toFixed(2)); observation.route_relevance.distance_km = dist; return dist <= observation.route_relevance.threshold_km; }); routeSections = []; return []; } case 'ecology_forecast': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); events = features.map((feature, index) => { const attrs = safeObject(feature.attributes || feature); const geom = safeObject(feature.geometry || attrs.geometry || {}); const siteName = normalizeText(attrs.SiteName ?? attrs.Site ?? attrs.site ?? attrs.Area ?? attrs.area ?? 'Seattle-Bellevue-Kent Valley'); const sourceRecordId = String(attrs.SmokeForecastId ?? attrs.ObjectId ?? attrs.objectid ?? attrs.id ?? index + 1); const label = normalizeForecastLabel(attrs.Day1 ?? attrs.DisplayText ?? attrs.Category ?? attrs.CategoryLabel ?? attrs.Day2 ?? attrs.Day3 ?? 'Good'); const severity = severityFromCategory(label); const geomText = JSON.stringify(geom); const eventRouteRelevant = Boolean(geomText && (geomText.includes('[') || geomText.includes('{'))); return { event_id: '03_AIR_QUALITY:AIRNOW-01:' + siteName.replace(/[^a-z0-9]+/gi, '-').toLowerCase() + ':' + sourceRecordId, event_type: 'smoke_forecast', title: 'Ecology smoke forecast for ' + siteName, status: 'forecast', severity, category_label: label, effective_at: attrs.Date ? String(attrs.Date) : nowIso, expires_at: null, route_relevant: eventRouteRelevant, wildfire_smoke_related: true, areas: [siteName], source_id: '03_AIR_QUALITY:AIRNOW-01', source_record_id: sourceRecordId, geometry_type: 'polygon', manual_review_required: false, route_sections: eventRouteRelevant ? ['aqp1_seattle', 'aqp2_lake_forest_park', 'aqp3_bellevue', 'aqp4_issaquah'] : [] }; }).filter((event) => event.route_relevant); routeSections = []; return []; } case 'burn_ban': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); let burnBanStatus = 'unknown'; if (lower.includes('stage 2')) burnBanStatus = 'stage_2'; else if (lower.includes('stage 1')) burnBanStatus = 'stage_1'; else if (lower.includes('no burn ban') || lower.includes('no ban') || lower.includes('burn ban status: no')) burnBanStatus = 'no_ban'; const event = { event_id: '03_AIR_QUALITY:AIRNOW-01:burn-ban:' + run.run_stamp, event_type: 'burn_ban', title: 'PSCAA burn ban status', status: 'current', severity: burnBanStatus === 'unknown' ? 'unknown' : 'good', category_label: burnBanStatus === 'stage_2' ? 'Stage 2' : burnBanStatus === 'stage_1' ? 'Stage 1' : burnBanStatus === 'no_ban' ? 'No Ban' : 'Unknown', effective_at: null, expires_at: null, route_relevant: true, wildfire_smoke_related: false, areas: ['King County'], source_id: '03_AIR_QUALITY:AIRNOW-01', source_record_id: 'king-county-burn-ban', geometry_type: 'text_only', manual_review_required: burnBanStatus === 'unknown', route_sections: ['aqp4_issaquah'] }; events = [event]; sourceWarnings.push(burnBanStatus === 'unknown' ? 'Burn-ban status could not be parsed confidently.' : 'Burn-ban status parsed from page text.'); return []; } case 'airnow_context': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); const matchAreas = ['seattle', 'bellevue', 'eastside', 'king county', 'issaquah', 'cascade foothills', 'seattle-bellevue-kent valley']; const matches = matchAreas.filter((area) => lower.includes(area)); if (matches.length) { sourceWarnings.push('AirNow reporting area matched contextual area names only; no primary route classification performed.'); } routeSections = []; events = []; observations = []; return []; } case 'smoke_rss': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); const lookup = ['Seattle', 'Bellevue', 'Eastside', 'King County', 'Issaquah', 'Cascade foothills', 'Seattle-Bellevue-Kent Valley']; const matched = lookup.filter((entry) => lower.includes(entry.toLowerCase())); if (matched.length) { const areaLabel = matched.join(', '); events = [{ event_id: '03_AIR_QUALITY:AIRNOW-01:' + hashString(areaLabel + text).slice(0, 12), event_type: 'smoke_forecast', title: 'Washington Smoke Blog context for ' + areaLabel, status: 'forecast', severity: lower.includes('hazard') ? 'unhealthy' : lower.includes('smoke') ? 'moderate' : 'unknown', category_label: 'Contextual smoke report', effective_at: nowIso, expires_at: null, route_relevant: true, wildfire_smoke_related: true, areas: matched, source_id: '03_AIR_QUALITY:AIRNOW-01', source_record_id: hashString(text).slice(0, 12), geometry_type: 'text_only', manual_review_required: false, route_sections: ['aqp3_bellevue', 'aqp4_issaquah'] }]; } else { events = []; } routeSections = []; return []; } case 'nws_alert': { const features = safeArray(bodyObj.features ?? bodyObj.data ?? bodyArr); const relevant = features.filter((feature) => { const props = safeObject(feature.properties || feature); const areaDesc = normalizeText(props.areaDesc ?? props.area_desc ?? ''); const geocode = JSON.stringify(props.geocode ?? {}); const text = (areaDesc + ' ' + geocode).toLowerCase(); return normalizeText(props.event ?? '').toLowerCase() === 'air quality alert' && (text.includes('king county') || text.includes('washington') || text.includes('wa') || feature.geometry); }); events = relevant.map((feature, index) => { const props = safeObject(feature.properties || feature); const areaDesc = normalizeText(props.areaDesc ?? props.area_desc ?? 'King County'); const severity = severityFromCategory(props.severity ?? props.urgency ?? 'unknown'); const expires = toIsoUtc(props.expires ?? props.sent ?? null, null); return { event_id: '03_AIR_QUALITY:AIRNOW-01:' + String(props.id ?? props.eventCode ?? index + 1), event_type: 'air_quality_alert', title: normalizeText(props.headline ?? props.event ?? 'Air Quality Alert'), status: 'current', severity, category_label: normalizeText(props.event ?? 'Air Quality Alert'), effective_at: toIsoUtc(props.sent ?? props.onset ?? null, nowIso), expires_at: expires, route_relevant: true, wildfire_smoke_related: false, areas: areaDesc ? areaDesc.split(',').map((part) => part.trim()).filter(Boolean) : ['King County'], source_id: '03_AIR_QUALITY:AIRNOW-01', source_record_id: String(props.id ?? props.eventCode ?? index + 1), geometry_type: feature.geometry ? 'polygon' : 'county_zone', manual_review_required: false, route_sections: ['aqp4_issaquah'] }; }); routeSections = []; return []; } case 'pscaa_station': { const text = normalizeText(typeof body === 'string' ? body : JSON.stringify(body ?? null)); const lower = text.toLowerCase(); if (lower.includes('aqi') || lower.includes('station')) { sourceWarnings.push('PSCAA station page was reachable but no canonical station snapshot was extracted in this build.'); } routeSections = []; events = []; observations = []; return []; } case 'airnow_api': { const records = safeArray(bodyObj || bodyArr); const relevant = records.filter((record) => record && typeof record === 'object'); if (relevant.length) { observations = relevant.slice(0, 4).map((record, index) => { const aqiValue = Number(record.AQI ?? record.aqi ?? record.Value ?? null); const category = normalizeText(record.Category ?? record.category ?? qFromAqi(aqiValue) ?? 'Unknown'); const observedAt = toIsoUtc(record.DateObserved ? String(record.DateObserved) + 'T' + String(record.HourObserved ?? 0) + ':00:00Z' : record.ObservedAt ?? null, nowIso); const routeSectionId = index % 4 === 0 ? 'aqp1_seattle' : index % 4 === 1 ? 'aqp2_lake_forest_park' : index % 4 === 2 ? 'aqp3_bellevue' : 'aqp4_issaquah'; return { observation_id: '03_AIR_QUALITY:AIRNOW-01:' + String(record.SiteID ?? record.zipCode ?? index + 1) + ':' + observedAt, observation_type: 'current_air_quality', source_id: '03_AIR_QUALITY:AIRNOW-01', source_record_id: String(record.SiteID ?? record.zipCode ?? index + 1), observed_at: observedAt, retrieved_at: nowIso, station_name: normalizeText(record.ReportingArea ?? record.Name ?? 'AirNow reporting area'), station_location: null, aqi_value: Number.isFinite(aqiValue) ? aqiValue : null, aqi_category: category, severity: severityFromCategory(category), dominant_pollutant: null, pollutants: { pm25: null, pm10: null, ozone: null, no2: null }, wildfire_smoke_related: false, route_relevance: { method: 'point_to_route_distance', distance_km: null, threshold_km: 12.87, route_section_id: routeSectionId }, location: null }; }); } routeSections = []; return []; } default: return []; } })();\nconst liveCount = events.length + observations.length;\nconst cachedEvents = lkg ? safeArray(lkg.events) : [];\nconst cachedObservations = lkg ? safeArray(lkg.observations) : [];\nconst cachedRouteSections = lkg ? safeArray(lkg.route_sections) : [];\nconst cachedRecordCount = cachedEvents.length + cachedObservations.length;\nconst lkgRetrievedAt = lkg && lkg.source_health ? lkg.source_health.retrieved_at : null;\nconst lkgStaleAfterMinutes = lkg && lkg.source_health && Number.isFinite(Number(lkg.source_health.stale_after_minutes)) ? Number(lkg.source_health.stale_after_minutes) : staleAfterMinutes;\nconst lkgAgeMinutes = Number.isFinite(Date.parse(lkgRetrievedAt)) ? (Date.now() - Date.parse(lkgRetrievedAt)) / 60000 : Infinity;\nconst lkgUsable = !!lkg && Number.isFinite(lkgAgeMinutes) && lkgAgeMinutes <= lkgStaleAfterMinutes;\nif (fetchErrorText) sourceErrors.push(fetchErrorText);\nlet sourceHealth = null;\nif (fetchErrorText) {\n  if (lkgUsable) {\n    events = cachedEvents;\n    observations = cachedObservations;\n    routeSections = cachedRouteSections;\n    sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:AIRNOW-01', source_name: 'AirNow API observations', status: 'using_last_known_good', retrieved_at: lkg.source_health.retrieved_at, stale_after_minutes: lkg.source_health.stale_after_minutes ?? staleAfterMinutes, record_count: cachedRecordCount, http_status: lkg.source_health.http_status ?? null, last_observation_at: lkg.source_health.last_observation_at ?? lkg.source_health.retrieved_at, warnings: [...safeArray(lkg.source_health.warnings), 'Live fetch failed; serving last-known-good data.'], errors: sourceErrors };\n  } else {\n    sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:AIRNOW-01', source_name: 'AirNow API observations', status: 'failed', retrieved_at: run.generated_at, stale_after_minutes: staleAfterMinutes, record_count: 0, http_status: fetch.statusCode ?? fetch.status ?? null, last_observation_at: null, warnings: sourceWarnings, errors: sourceErrors };\n  }\n} else {\n  const lastObservationValue = maxOrNull([ ...observations.map((observation) => Date.parse(observation.observed_at)).filter((value) => Number.isFinite(value)), ...events.map((event) => Date.parse(event.effective_at)).filter((value) => Number.isFinite(value)) ]);\n  const status = liveCount ? 'ok' : 'empty_but_valid';\n  sourceHealth = { schema_version: '1.0.0', connector_id: '03_AIR_QUALITY', source_id: '03_AIR_QUALITY:AIRNOW-01', source_name: 'AirNow API observations', status, retrieved_at: run.generated_at, stale_after_minutes: staleAfterMinutes, record_count: liveCount, http_status: fetch.statusCode ?? fetch.status ?? null, last_observation_at: Number.isFinite(lastObservationValue) ? new Date(lastObservationValue).toISOString() : (liveCount ? run.generated_at : null), warnings: sourceWarnings, errors: sourceErrors };\n}\nconst provenanceSources = [{ source_id: '03_AIR_QUALITY:AIRNOW-01', source_name: 'AirNow API observations', retrieved_at: sourceHealth.retrieved_at, source_timestamp: sourceHealth.last_observation_at, status: sourceHealth.status, stale: sourceHealth.status === 'using_last_known_good' || sourceHealth.status === 'stale' }];\nreturn [{ json: { source_id: '03_AIR_QUALITY:AIRNOW-01', source_name: 'AirNow API observations', source_health: sourceHealth, events, observations, route_sections: routeSections, warnings: sourceWarnings, errors: sourceErrors, provenance: { source_ids_used: ['03_AIR_QUALITY:AIRNOW-01'], sources: provenanceSources } } }];"
      }
    },
    {
      "id": "merge_normalized_branches",
      "name": "Merge Normalized Branches",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3.1,
      "position": [
        1260,
        760
      ],
      "parameters": {
        "numberInputs": 8
      }
    },
    {
      "id": "aggregate_normalized_branches",
      "name": "Aggregate Normalized Branches",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1520,
        760
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst run = $('Initialize Run Metadata').first().json;\nconst inputs = $input.all().map((item) => item.json || {});\nconst sourceHealth = inputs.map((item) => item.source_health).filter(Boolean);\nconst events = inputs.flatMap((item) => safeArray(item.events));\nconst observations = inputs.flatMap((item) => safeArray(item.observations));\nconst routeSections = inputs.flatMap((item) => safeArray(item.route_sections));\nconst provenanceSources = inputs.flatMap((item) => safeArray(item.provenance && item.provenance.sources));\nconst sourceIds = inputs.map((item) => item.source_id).filter(Boolean);\nconst sourceWarnings = inputs.flatMap((item) => safeArray(item.warnings));\nconst sourceErrors = inputs.flatMap((item) => safeArray(item.errors));\nconst aqiValues = observations.map((o) => Number(o.aqi_value)).filter((v) => Number.isFinite(v));\nconst currentAqiMax = maxOrNull(aqiValues);\nconst currentCategory = categoryFromAqi(currentAqiMax);\nconst burnBanEvents = events.filter((e) => e.event_type === 'burn_ban' && e.status === 'current');\nconst burnBanStatus = burnBanEvents.length ? (burnBanEvents[0].severity || 'active') : 'none';\nconst alertEvents = events.filter((e) => e.event_type === 'air_quality_alert' && e.status === 'current');\nconst formalAlertActive = alertEvents.length > 0;\nconst summaryMessage = formalAlertActive ? 'Active air quality alert in effect.' : (burnBanEvents.length ? 'Burn ban in effect: ' + burnBanStatus + '.' : (currentCategory !== 'Unknown' ? 'Air quality: ' + currentCategory + '.' : 'No current air quality data available.'));\nconst laneSummary = { current_category: currentCategory, current_aqi_max: currentAqiMax, burn_ban_status: burnBanStatus, formal_alert_active: formalAlertActive, message: summaryMessage };\nreturn [{ json: { schema_version: run.schema_version, connector_id: run.connector_id, connector_name: run.connector_name, connector_version: run.connector_version, lane: run.lane, run_id: run.run_id, run_stamp: run.run_stamp, generated_at: run.generated_at, published_at: null, output_root: run.output_root, canonical_gpx: run.canonical_gpx, source_health: sourceHealth, events, observations, route_sections: routeSections, provenance: { source_ids_used: sourceIds, sources: provenanceSources }, source_warnings: sourceWarnings, source_errors: sourceErrors, source_items: inputs, metadata: { lane_summary: laneSummary } } }];"
      }
    },
    {
      "id": "deduplicate_events",
      "name": "Deduplicate Events",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1760,
        760
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const input = $input.first().json;\nconst eventSeen = new Map();\nconst observationSeen = new Map();\nconst deduplicatedEvents = [];\nconst duplicateEvents = [];\nconst deduplicatedObservations = [];\nconst duplicateObservations = [];\nfor (const event of input.events || []) { const key = [event.source_id, event.source_record_id || '', event.event_type || '', event.effective_at || '', event.expires_at || '', event.title || ''].join('|'); if (eventSeen.has(key)) { duplicateEvents.push(event); continue; } eventSeen.set(key, true); deduplicatedEvents.push(event); }\nfor (const observation of input.observations || []) { const key = [observation.source_id, observation.source_record_id || '', observation.observed_at || ''].join('|'); if (observationSeen.has(key)) { duplicateObservations.push(observation); continue; } observationSeen.set(key, true); deduplicatedObservations.push(observation); }\nreturn [{ json: { ...input, events_before_dedup: (input.events || []).length, observations_before_dedup: (input.observations || []).length, deduplicated_events: deduplicatedEvents, deduplicated_observations: deduplicatedObservations, duplicates_removed: duplicateEvents.length + duplicateObservations.length, duplicate_events: duplicateEvents, duplicate_observations: duplicateObservations } }];"
      }
    },
    {
      "id": "build_route_sections_rollups",
      "name": "Build Route Sections Rollups",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2000,
        760
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst input = $input.first().json;\nconst now = input.generated_at;\nconst sections = { aqp1_seattle: { route_section_id: 'aqp1_seattle', route_section_name: 'UW / Burke-Gilman west-north Seattle', status: 'unknown', severity: 'unknown', aqi_value: null, aqi_category: null, primary_monitor_source_id: null, primary_monitor_record_id: null, wildfire_smoke_related: false, formal_alert_active: false, forecast_category_worst: null }, aqp2_lake_forest_park: { route_section_id: 'aqp2_lake_forest_park', route_section_name: 'Lake Forest Park / Kenmore / Bothell handoff', status: 'unknown', severity: 'unknown', aqi_value: null, aqi_category: null, primary_monitor_source_id: null, primary_monitor_record_id: null, wildfire_smoke_related: false, formal_alert_active: false, forecast_category_worst: null }, aqp3_bellevue: { route_section_id: 'aqp3_bellevue', route_section_name: 'Woodinville / Redmond / Marymoor / north Sammamish', status: 'unknown', severity: 'unknown', aqi_value: null, aqi_category: null, primary_monitor_source_id: null, primary_monitor_record_id: null, wildfire_smoke_related: false, formal_alert_active: false, forecast_category_worst: null }, aqp4_issaquah: { route_section_id: 'aqp4_issaquah', route_section_name: 'South Sammamish / Issaquah', status: 'unknown', severity: 'unknown', aqi_value: null, aqi_category: null, primary_monitor_source_id: null, primary_monitor_record_id: null, wildfire_smoke_related: false, formal_alert_active: false, forecast_category_worst: null } };\nfor (const observation of input.deduplicated_observations || []) { const sectionId = observation.route_relevance && observation.route_relevance.route_section_id ? observation.route_relevance.route_section_id : 'aqp4_issaquah'; const section = sections[sectionId] || sections.aqp4_issaquah; const severity = observation.severity || severityFromAqi(observation.aqi_value); if (section.status === 'unknown' || section.status === 'current') { section.status = 'current'; } section.severity = worstSeverity(section.severity, severity); if (Number.isFinite(Number(observation.aqi_value))) { section.aqi_value = section.aqi_value === null ? Number(observation.aqi_value) : Math.max(section.aqi_value, Number(observation.aqi_value)); } if (observation.aqi_category) section.aqi_category = observation.aqi_category; section.primary_monitor_source_id = observation.source_id; section.primary_monitor_record_id = observation.source_record_id; }\nfor (const event of input.deduplicated_events || []) { const relevantSections = Array.isArray(event.route_sections) && event.route_sections.length ? event.route_sections : ['aqp4_issaquah']; for (const sectionId of relevantSections) { const section = sections[sectionId] || sections.aqp4_issaquah; if (event.event_type === 'smoke_forecast') { section.wildfire_smoke_related = true; section.forecast_category_worst = section.forecast_category_worst ? (worstSeverity(severityFromCategory(section.forecast_category_worst), severityFromCategory(event.category_label || event.severity)) === severityFromCategory(event.category_label || event.severity) ? event.category_label || event.severity : section.forecast_category_worst) : (event.category_label || event.severity || null); if (section.status === 'unknown') section.status = 'forecast'; } else if (event.event_type === 'air_quality_alert') { section.formal_alert_active = event.status === 'current'; if (section.status === 'unknown') section.status = event.status || 'current'; } else if (event.event_type === 'burn_ban') { section.formal_alert_active = false; } if (section.severity === 'unknown' || severityOrder[event.severity] < severityOrder[section.severity]) { section.severity = event.severity || section.severity; } } }\nconst routeSections = Object.values(sections).map((section) => ({ ...section, updated_at: now }));\nreturn [{ json: { ...input, route_sections: routeSections } }];"
      }
    },
    {
      "id": "validate_candidate_envelope",
      "name": "Validate Candidate Envelope",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2240,
        760
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const input = $input.first().json;\nconst errors = [];\nconst warnings = [];\nconst requiredRootFields = ['schema_version', 'connector_id', 'connector_name', 'connector_version', 'lane', 'run_id', 'generated_at', 'published_at', 'source_health', 'events', 'observations', 'route_sections', 'provenance', 'metadata'];\nfor (const field of requiredRootFields) { if (!(field in input)) errors.push('Missing required root field: ' + field); }\nif (!Array.isArray(input.source_health)) errors.push('source_health must be an array');\nif (!Array.isArray(input.events)) errors.push('events must be an array');\nif (!Array.isArray(input.observations)) errors.push('observations must be an array');\nif (!Array.isArray(input.route_sections)) errors.push('route_sections must be an array');\n\n\n\nif (!input.provenance || typeof input.provenance !== 'object') errors.push('provenance must be an object');\n\nif (!input.metadata || typeof input.metadata !== 'object') errors.push('metadata must be an object');\nconst allowedDataStatus = ['ok', 'degraded', 'stale', 'no_relevant_events', 'failed_validation', 'failed_fetch', 'blocked', 'using_last_known_good'];\nif (input.data_status && !allowedDataStatus.includes(input.data_status)) errors.push('Invalid data_status: ' + input.data_status);\nconst allowedSourceStatus = ['ok', 'degraded', 'failed', 'stale', 'blocked', 'not_run', 'skipped_as_not_due', 'empty_but_valid', 'using_last_known_good'];\nfor (const source of input.source_health || []) { const requiredSourceFields = ['schema_version', 'connector_id', 'source_id', 'source_name', 'status', 'retrieved_at', 'stale_after_minutes', 'record_count', 'http_status', 'last_observation_at', 'warnings', 'errors']; for (const field of requiredSourceFields) { if (!(field in source)) errors.push('Source health missing field: ' + field + ' (' + (source.source_id || 'unknown') + ')'); } if (source.status && !allowedSourceStatus.includes(source.status)) errors.push('Invalid source status: ' + source.status + ' (' + (source.source_id || 'unknown') + ')'); }\nconst requiredLaneSummaryFields = ['current_category', 'current_aqi_max', 'burn_ban_status', 'formal_alert_active', 'message'];\nfor (const field of requiredLaneSummaryFields) { if (!(field in (input.metadata.lane_summary || {}))) errors.push('Missing lane_summary field: ' + field); }\nfor (const observation of input.observations || []) { const requiredObservationFields = ['observation_id', 'source_id', 'source_record_id', 'observed_at', 'retrieved_at', 'aqi_value', 'aqi_category', 'severity', 'route_relevance']; for (const field of requiredObservationFields) { if (!(field in observation)) errors.push('Observation missing required field: ' + field + ' (' + (observation.observation_id || 'unknown') + ')'); } }\nfor (const event of input.events || []) { const requiredEventFields = ['event_id', 'event_type', 'status', 'severity', 'route_relevant', 'source_id']; for (const field of requiredEventFields) { if (!(field in event)) errors.push('Event missing required field: ' + field + ' (' + (event.event_id || 'unknown') + ')'); } }\nconst candidateValidationPassed = errors.length === 0;\nif (!candidateValidationPassed) warnings.push('Validation failed; candidate will be quarantined from publication.');\nreturn [{ json: { ...input, validation_errors: errors, validation_warnings: warnings, candidate_validation_passed: candidateValidationPassed, validator_version: '1.0.0' } }];"
      }
    },
    {
      "id": "build_candidate_artifact",
      "name": "Build Candidate Artifact",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2480,
        760
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst input = $input.first().json;\nconst now = new Date().toISOString();\nconst sourceHealth = safeArray(input.source_health);\nconst sourceStatuses = sourceHealth.map((source) => source.status);\nconst anyFailed = sourceStatuses.some((status) => status === 'failed');\nconst anyStale = sourceStatuses.some((status) => status === 'stale' || status === 'using_last_known_good');\nconst anyLkg = sourceStatuses.some((status) => status === 'using_last_known_good');\nconst allFailed = sourceHealth.length > 0 && sourceStatuses.every((status) => status === 'failed');\nconst allLkg = sourceHealth.length > 0 && sourceStatuses.every((status) => status === 'using_last_known_good');\nconst hasEvents = safeArray(input.deduplicated_events || input.events).length > 0;\nlet dataStatus = 'ok';\nif (!input.candidate_validation_passed) dataStatus = 'failed_validation'; else if (allFailed) dataStatus = 'failed_fetch'; else if (allLkg) dataStatus = 'using_last_known_good'; else if (!hasEvents && !anyFailed && !anyStale) dataStatus = 'no_relevant_events'; else if (anyFailed || anyStale) dataStatus = 'degraded';\nconst freshnessState = allFailed ? 'unknown' : (anyFailed || anyStale ? 'stale' : 'fresh');\nconst staleSourceIds = sourceHealth.filter((source) => source.status === 'stale' || source.status === 'using_last_known_good').map((source) => source.source_id);\nconst oldestRelevantSourceAgeMinutes = minOrNull(sourceHealth.map((source) => { const parsed = Date.parse(source.retrieved_at || ''); return Number.isFinite(parsed) ? Math.max(0, Math.round((Date.now() - parsed) / 60000)) : null; }).filter((value) => value !== null)) ?? 0;\nconst currentObservationSource = safeArray(input.deduplicated_observations || input.observations).filter((observation) => Number.isFinite(Number(observation.aqi_value)));\nconst currentCategory = currentObservationSource.length ? categoryFromAqi(maxOrNull(currentObservationSource.map((observation) => Number(observation.aqi_value)))) : 'Unknown';\nconst currentAqiMax = currentObservationSource.length ? maxOrNull(currentObservationSource.map((observation) => Number(observation.aqi_value))) : null;\nconst dominantPollutant = currentObservationSource.length ? currentObservationSource.find((observation) => observation.dominant_pollutant)?.dominant_pollutant ?? null : null;\nconst wildfireSmokeRelated = safeArray(input.deduplicated_events || input.events).some((event) => event.wildfire_smoke_related || event.event_type === 'smoke_forecast');\nconst forecastCategoryWorst = safeArray(input.deduplicated_events || input.events).filter((event) => event.event_type === 'smoke_forecast').reduce((acc, event) => acc ? (worstSeverity(severityFromCategory(acc), severityFromCategory(event.category_label || event.severity)) === severityFromCategory(event.category_label || event.severity) ? event.category_label || event.severity : acc) : (event.category_label || event.severity || null), null);\nconst burnBanEvent = safeArray(input.deduplicated_events || input.events).find((event) => event.event_type === 'burn_ban');\nconst burnBanStatus = burnBanEvent ? (burnBanEvent.category_label === 'Stage 2' ? 'stage_2' : burnBanEvent.category_label === 'Stage 1' ? 'stage_1' : burnBanEvent.category_label === 'No Ban' ? 'no_ban' : 'unknown') : 'unknown';\nconst formalAlertActive = safeArray(input.deduplicated_events || input.events).some((event) => event.event_type === 'air_quality_alert' && event.status === 'current');\nconst laneSummary = { current_category: currentCategory, current_aqi_max: currentAqiMax, dominant_pollutant: dominantPollutant, wildfire_smoke_related: wildfireSmokeRelated, forecast_category_worst: forecastCategoryWorst, burn_ban_status: burnBanStatus, formal_alert_active: formalAlertActive, message: dataStatus === 'using_last_known_good' ? 'Air quality data is being served from last known good while live data is unavailable.' : currentCategory === 'Unknown' ? 'Air quality data is currently unavailable or insufficient for a confident corridor summary.' : 'Corridor air quality currently reads ' + currentCategory + ' with a maximum AQI of ' + currentAqiMax + '.' };\nconst candidateEnvelope = { schema_version: input.schema_version, connector_id: input.connector_id, connector_name: input.connector_name, connector_version: input.connector_version, lane: input.lane, run_id: input.run_id, generated_at: input.generated_at, published_at: null, data_status: dataStatus, freshness: { overall_state: freshnessState, computed_at: now, oldest_relevant_source_age_minutes: oldestRelevantSourceAgeMinutes, stale_source_ids: staleSourceIds }, manifest_ref: { manifest_id: '03_AIR_QUALITY-v0002', schema_version: '1.0.0' }, source_health: sourceHealth, connector_health: { schema_version: '1.0.0', connector_id: input.connector_id, status: input.candidate_validation_passed ? (allFailed ? 'failed' : (anyFailed || anyStale ? 'degraded' : 'ok')) : 'failed', failed_stage: input.candidate_validation_passed ? (allFailed ? 'source_fetch' : null) : 'validation', warning_count: safeArray(input.validation_warnings).length + safeArray(input.source_warnings).length + sourceHealth.reduce((total, source) => total + safeArray(source.warnings).length, 0), error_count: safeArray(input.validation_errors).length + safeArray(input.source_errors).length + sourceHealth.reduce((total, source) => total + safeArray(source.errors).length, 0), used_last_known_good: anyLkg, candidate_written: true, published_written: false }, events: safeArray(input.deduplicated_events || input.events), observations: safeArray(input.deduplicated_observations || input.observations), route_sections: safeArray(input.route_sections), provenance: { source_ids_used: sourceHealth.map((source) => source.source_id), sources: safeArray(input.provenance && input.provenance.sources) }, validation_state: { candidate_validation_passed: !!input.candidate_validation_passed, published_from_candidate: false, validator_version: input.validator_version || '1.0.0' }, metadata: { lane_summary: laneSummary } };\nconst normalizedPath = input.output_root + '/normalized/03_AIR_QUALITY/03_AIR_QUALITY_normalized_output_' + input.run_stamp + '.json';\nconst candidatePath = input.output_root + '/candidate/03_AIR_QUALITY/03_AIR_QUALITY_candidate_' + input.run_stamp + '.json';\nconst normalizedBuffer = Buffer.from(JSON.stringify(candidateEnvelope, null, 2), 'utf8');\nconst candidateBuffer = Buffer.from(JSON.stringify(candidateEnvelope, null, 2), 'utf8');\nconst normalizedBinary = await this.helpers.prepareBinaryData(normalizedBuffer, normalizedPath.split('/').pop(), 'application/json');\nconst candidateBinary = await this.helpers.prepareBinaryData(candidateBuffer, candidatePath.split('/').pop(), 'application/json');\nreturn [ { json: { file_path: normalizedPath, file_name: normalizedPath.split('/').pop(), artifact_type: 'normalized', candidate_envelope: candidateEnvelope }, binary: { data: normalizedBinary } }, { json: { file_path: candidatePath, file_name: candidatePath.split('/').pop(), artifact_type: 'candidate', candidate_envelope: candidateEnvelope }, binary: { data: candidateBinary } } ];"
      }
    },
    {
      "id": "write_candidate_artifact",
      "name": "Write Candidate Artifact",
      "type": "n8n-nodes-base.readWriteFile",
      "typeVersion": 1,
      "position": [
        2720,
        760
      ],
      "parameters": {
        "operation": "write",
        "fileName": "={{ $json.file_path }}",
        "dataPropertyName": "data",
        "options": {}
      }
    },
    {
      "id": "publish_gate_decision",
      "name": "Publish Gate Decision",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2960,
        760
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const validation = $('Validate Candidate Envelope').first().json;\nconst candidate = $('Build Candidate Artifact').first().json.candidate_envelope;\nconst shouldPublish = !!validation.candidate_validation_passed;\nconst reason = shouldPublish ? 'Validation passed' : 'Validation failed: ' + (validation.validation_errors || []).join('; ');\nreturn [{ json: { should_publish: shouldPublish, reason, candidate_envelope: candidate, validation_errors: validation.validation_errors || [], validation_warnings: validation.validation_warnings || [], run_id: validation.run_id, run_stamp: validation.run_stamp, output_root: validation.output_root, connector_id: validation.connector_id, connector_name: validation.connector_name, connector_version: validation.connector_version, lane: validation.lane, generated_at: validation.generated_at, source_health: validation.source_health || [], deduplicated_events: validation.deduplicated_events || validation.events || [], deduplicated_observations: validation.deduplicated_observations || validation.observations || [], route_sections: validation.route_sections || [], freshness: validation.freshness || null, provenance: validation.provenance || null } }];"
      }
    },
    {
      "id": "build_final_artifact_bundle",
      "name": "Build Final Artifact Bundle",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3200,
        760
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const hashString = (input) => { let hash = 2166136261; for (let i = 0; i < input.length; i++) { hash ^= input.charCodeAt(i); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, '0'); };\nconst severityOrder = { good: 0, moderate: 1, usg: 2, unhealthy: 3, very_unhealthy: 4, hazardous: 5, unknown: 6 };\nconst severityFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'unknown'; if (value <= 50) return 'good'; if (value <= 100) return 'moderate'; if (value <= 150) return 'usg'; if (value <= 200) return 'unhealthy'; if (value <= 300) return 'very_unhealthy'; return 'hazardous'; };\nconst categoryFromAqi = (aqi) => { const value = Number(aqi); if (!Number.isFinite(value)) return 'Unknown'; if (value <= 50) return 'Good'; if (value <= 100) return 'Moderate'; if (value <= 150) return 'Unhealthy for Sensitive Groups'; if (value <= 200) return 'Unhealthy'; if (value <= 300) return 'Very Unhealthy'; return 'Hazardous'; };\nconst worstSeverity = (a, b) => (severityOrder[a] ?? 6) >= (severityOrder[b] ?? 6) ? a : b;\nconst safeArray = (value) => Array.isArray(value) ? value : [];\nconst safeObject = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : {};\nconst toIsoUtc = (value, fallback = null) => { if (!value) return fallback; const parsed = Date.parse(value); if (!Number.isFinite(parsed)) return fallback; return new Date(parsed).toISOString(); };\nconst stringifyError = (error) => String((error && error.message) || error || 'unknown error');\nconst maxOrNull = (values) => values.length ? Math.max(...values) : null;\nconst minOrNull = (values) => values.length ? Math.min(...values) : null;\nconst normalizeText = (value) => String(value ?? '').replace(/\\s+/g, ' ').trim();\nconst gate = $input.first().json;\nconst candidateEnvelope = gate.candidate_envelope || $('Build Candidate Artifact').first().json.candidate_envelope;\nconst runStamp = gate.run_stamp || candidateEnvelope.generated_at.replace(/[-:]/g, '').replace(/\\.\\d{3}Z$/, 'Z');\nconst now = new Date().toISOString();\nconst outputRoot = gate.output_root || '/files/uw-issy-connectors';\nconst publishedPath = outputRoot + '/published/03_AIR_QUALITY/03_AIR_QUALITY_published_' + runStamp + '.json';\nconst publishedCurrentPath = outputRoot + '/published/03_AIR_QUALITY/current.json';\nconst lkgCurrentPath = outputRoot + '/last_known_good/03_AIR_QUALITY/current.json';\nconst lkgArchivePath = outputRoot + '/last_known_good/03_AIR_QUALITY/archive/' + runStamp + '.json';\nconst validationLogPath = outputRoot + '/logs/03_AIR_QUALITY/validation_log_' + runStamp + '.jsonl';\nconst statusPath = outputRoot + '/health/03_AIR_QUALITY/status.json';\nconst handoffPath = outputRoot + '/handoff/03_AIR_QUALITY/' + gate.connector_id + '-' + runStamp + '-001.json';\nconst executionEvidencePath = outputRoot + '/health/03_AIR_QUALITY/execution_evidence_' + runStamp + '.json';\nconst quarantinePath = outputRoot + '/quarantine/03_AIR_QUALITY/' + runStamp + '_validation_failed.json';\nconst validationErrors = gate.validation_errors || [];\nconst validationWarnings = gate.validation_warnings || [];\nconst sourceHealth = gate.source_health || [];\nconst sourceStatuses = sourceHealth.map((source) => source.status);\nconst anyFailed = sourceStatuses.some((status) => status === 'failed');\nconst anyStale = sourceStatuses.some((status) => status === 'stale' || status === 'using_last_known_good');\nconst anyLkg = sourceStatuses.some((status) => status === 'using_last_known_good');\nconst allFailed = sourceHealth.length > 0 && sourceStatuses.every((status) => status === 'failed');\nconst allLkg = sourceHealth.length > 0 && sourceStatuses.every((status) => status === 'using_last_known_good');\nconst hasEvents = safeArray(gate.deduplicated_events).length > 0;\nlet dataStatus = candidateEnvelope.data_status;\nif (!dataStatus) { if (allFailed) dataStatus = 'failed_fetch'; else if (allLkg) dataStatus = 'using_last_known_good'; else if (!hasEvents && !anyFailed && !anyStale) dataStatus = 'no_relevant_events'; else if (anyFailed || anyStale) dataStatus = 'degraded'; else dataStatus = 'ok'; }\nconst publishedEnvelope = gate.should_publish ? { ...candidateEnvelope, published_at: now, data_status: dataStatus, connector_health: { ...candidateEnvelope.connector_health, status: anyFailed || anyStale ? 'degraded' : 'ok', failed_stage: null, used_last_known_good: anyLkg, candidate_written: true, published_written: true }, validation_state: { ...candidateEnvelope.validation_state, candidate_validation_passed: true, published_from_candidate: true }, freshness: { ...candidateEnvelope.freshness, overall_state: (anyFailed || anyStale) ? 'stale' : 'fresh', computed_at: now } } : null;\nconst statusDoc = { lane_id: 'lane_03_air_quality', run_id: gate.run_id || candidateEnvelope.run_id, last_fetch_at: gate.generated_at || candidateEnvelope.generated_at, last_success_at: gate.should_publish ? now : null, status: gate.should_publish ? dataStatus : (validationErrors.length ? 'failed_validation' : (allFailed ? 'failed_fetch' : 'degraded')), source_health: sourceHealth, error_messages: validationErrors.concat(gate.source_errors || []), stale_data_fields: anyStale ? sourceHealth.filter((source) => source.status === 'stale' || source.status === 'using_last_known_good').map((source) => source.source_id) : [], published_artifact_ref: gate.should_publish ? publishedPath : null, last_known_good_ref: lkgCurrentPath, connector_health: publishedEnvelope ? publishedEnvelope.connector_health : candidateEnvelope.connector_health, lane_summary: candidateEnvelope.metadata ? candidateEnvelope.metadata.lane_summary : null };\nconst handoffDoc = { run_id: gate.run_id || candidateEnvelope.run_id, connector_id: gate.connector_id, lane: gate.lane || candidateEnvelope.lane, generated_at: gate.generated_at || candidateEnvelope.generated_at, published_at: gate.should_publish ? now : null, published_artifact: gate.should_publish ? publishedPath : null, status: gate.should_publish ? dataStatus : 'failed', ready_for_workflow_08: !!gate.should_publish, source_ids_used: sourceHealth.map((source) => source.source_id), route_sections: gate.route_sections || [] };\nconst executionEvidenceDoc = { run_id: gate.run_id || candidateEnvelope.run_id, connector_id: gate.connector_id, lane: gate.lane || candidateEnvelope.lane, workflow_name: 'v02.UWI_LANE03', workflow_id: 'qlM2XIv2BbFSh3in', generated_at: gate.generated_at || candidateEnvelope.generated_at, published_at: gate.should_publish ? now : null, should_publish: !!gate.should_publish, status: gate.should_publish ? dataStatus : 'failed', data_status: dataStatus, source_health: sourceHealth, validation_errors: validationErrors, validation_warnings: validationWarnings, published_artifact_ref: gate.should_publish ? publishedPath : null, last_known_good_ref: lkgCurrentPath, artifact_paths: { published: gate.should_publish ? publishedPath : null, published_current: gate.should_publish ? publishedCurrentPath : null, last_known_good_current: gate.should_publish ? lkgCurrentPath : null, last_known_good_archive: gate.should_publish ? lkgArchivePath : null, quarantine: gate.should_publish ? null : quarantinePath, validation_log: validationLogPath, status: statusPath, handoff: handoffPath, execution_evidence: executionEvidencePath } };\nconst validationLogLines = [];\nfor (const source of sourceHealth) { validationLogLines.push(JSON.stringify({ source_id: source.source_id, run_id: gate.run_id || candidateEnvelope.run_id, stage: 'source_normalize', check: 'source_health', result: source.status === 'failed' ? 'fail' : 'pass', message: source.status + ' / ' + source.record_count + ' record(s)', timestamp: gate.generated_at || candidateEnvelope.generated_at })); }\nvalidationLogLines.push(JSON.stringify({ source_id: 'CONNECTOR', run_id: gate.run_id || candidateEnvelope.run_id, stage: 'validation', check: 'candidate_schema', result: gate.should_publish ? 'pass' : 'fail', message: gate.should_publish ? 'Candidate passed validation.' : (validationErrors.join('; ') || 'Validation failed.'), timestamp: gate.generated_at || candidateEnvelope.generated_at }));\nvalidationLogLines.push(JSON.stringify({ source_id: 'CONNECTOR', run_id: gate.run_id || candidateEnvelope.run_id, stage: 'publish_gate', check: 'promotion', result: gate.should_publish ? 'pass' : 'fail', message: gate.should_publish ? 'Candidate promoted to published.' : gate.reason, timestamp: now }));\nconst validationLogText = validationLogLines.join('\\n');\nconst makeBinaryItem = async (filePath, payload, artifactType, mime = 'application/json') => { const body = typeof payload === 'string' ? payload : JSON.stringify(payload, null, 2); const buffer = Buffer.from(body, 'utf8'); const binary = await this.helpers.prepareBinaryData(buffer, filePath.split('/').pop(), mime); return { json: { file_path: filePath, file_name: filePath.split('/').pop(), artifact_type: artifactType }, binary: { data: binary } }; };\nconst items = [];\nif (gate.should_publish && publishedEnvelope) { items.push(await makeBinaryItem(publishedPath, publishedEnvelope, 'published')); items.push(await makeBinaryItem(publishedCurrentPath, { run_id: gate.run_id || candidateEnvelope.run_id, artifact: publishedPath }, 'published_current')); items.push(await makeBinaryItem(lkgCurrentPath, publishedEnvelope, 'last_known_good_current')); items.push(await makeBinaryItem(lkgArchivePath, publishedEnvelope, 'last_known_good_archive')); } else { items.push(await makeBinaryItem(quarantinePath, { run_id: gate.run_id || candidateEnvelope.run_id, reason: gate.reason, validation_errors: validationErrors, validation_warnings: validationWarnings, candidate_envelope: candidateEnvelope }, 'quarantine')); }\nitems.push(await makeBinaryItem(validationLogPath, validationLogText, 'validation_log', 'text/plain'));\nitems.push(await makeBinaryItem(statusPath, statusDoc, 'status'));\nitems.push(await makeBinaryItem(handoffPath, handoffDoc, 'handoff'));\nitems.push(await makeBinaryItem(executionEvidencePath, executionEvidenceDoc, 'execution_evidence'));\nreturn items;"
      }
    },
    {
      "id": "write_final_artifacts",
      "name": "Write Final Artifacts",
      "type": "n8n-nodes-base.readWriteFile",
      "typeVersion": 1,
      "position": [
        3440,
        760
      ],
      "parameters": {
        "operation": "write",
        "fileName": "={{ $json.file_path }}",
        "dataPropertyName": "data",
        "options": {}
      }
    },
    {
      "id": "final_status_report",
      "name": "Final Status Report",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3680,
        760
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "\nconst gate = $('Publish Gate Decision').first().json;\nconst bundleItems = $('Build Final Artifact Bundle').all();\nconst candidate = $('Build Candidate Artifact').first().json.candidate_envelope;\nconst sourceHealth = Array.isArray(gate.source_health) ? gate.source_health : (Array.isArray(candidate.source_health) ? candidate.source_health : []);\nconst sourceStatuses = sourceHealth.map(source => source.status);\nconst failedSourceCount = sourceStatuses.filter(status => status === 'failed').length;\nconst usingLastKnownGood = sourceStatuses.includes('using_last_known_good') || !!(candidate.connector_health && candidate.connector_health.used_last_known_good);\nconst staleOrDegraded = sourceStatuses.some(status => ['failed', 'stale', 'degraded', 'using_last_known_good'].includes(status));\nconst dataStatus = gate.data_status || candidate.data_status || 'unknown';\nconst runStamp = gate.run_stamp || (candidate.generated_at || '').replace(/[-:]/g, '').replace(/.d{3}Z$/, 'Z');\nconst outputRoot = gate.output_root || '/files/uw-issy-connectors';\nconst publishedPath = gate.should_publish ? outputRoot + '/published/03_AIR_QUALITY/03_AIR_QUALITY_published_' + runStamp + '.json' : null;\nconst artifactTypes = bundleItems.map(item => item.json && item.json.artifact_type).filter(Boolean);\nlet status = 'FAILED';\nif (gate.should_publish) {\n  status = (staleOrDegraded || usingLastKnownGood || ['degraded', 'using_last_known_good', 'failed_fetch', 'stale'].includes(dataStatus)) ? 'DEGRADED' : 'PASSED';\n}\nconst observations = Array.isArray(candidate.observations) ? candidate.observations : [];\nconst events = Array.isArray(candidate.events) ? candidate.events : [];\nconst airQualityEventCount = events.filter(event => event && /air_quality|aqi|smoke|burn_ban/i.test(String(event.event_type || event.source_id || ''))).length;\nreturn [{ json: {\n  run_id: gate.run_id || candidate.run_id,\n  status,\n  data_status: dataStatus,\n  candidate_written: true,\n  published_written: !!gate.should_publish,\n  quarantine_written: !gate.should_publish,\n  validation_log_written: artifactTypes.includes('validation_log'),\n  status_written: artifactTypes.includes('status'),\n  handoff_written: artifactTypes.includes('handoff'),\n  execution_evidence_written: artifactTypes.includes('execution_evidence'),\n  artifact_count_written: bundleItems.length,\n  event_count: events.length,\n  source_count: sourceHealth.length,\n  failed_source_count: failedSourceCount,\n  using_last_known_good: usingLastKnownGood,\n  air_quality_event_count: airQualityEventCount,\n  observation_count: observations.length,\n  published_path: publishedPath,\n  timestamp: candidate.generated_at\n} }];"
      }
    }
  ],
  "connections": {
    "Manual Trigger": {
      "main": [
        [
          {
            "node": "Initialize Run Metadata",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Schedule Trigger": {
      "main": [
        [
          {
            "node": "Initialize Run Metadata",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Initialize Run Metadata": {
      "main": [
        [
          {
            "node": "Read Last Known Good",
            "type": "main",
            "index": 0
          },
          {
            "node": "Fetch ECO-01 Monitor Results",
            "type": "main",
            "index": 0
          },
          {
            "node": "Fetch ECO-02 Smoke Forecast",
            "type": "main",
            "index": 0
          },
          {
            "node": "Fetch PSCAA-02 Burn Ban Page",
            "type": "main",
            "index": 0
          },
          {
            "node": "Fetch AIRNOW-02 Files",
            "type": "main",
            "index": 0
          },
          {
            "node": "Fetch WASMOKE-01 RSS",
            "type": "main",
            "index": 0
          },
          {
            "node": "Fetch NWS-AQ-01 Alerts",
            "type": "main",
            "index": 0
          },
          {
            "node": "Fetch PSCAA-01 NetworkMap",
            "type": "main",
            "index": 0
          },
          {
            "node": "Fetch AIRNOW-01 API Alerts",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Read Last Known Good": {
      "main": [
        [
          {
            "node": "Parse Last Known Good",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch ECO-01 Monitor Results": {
      "main": [
        [
          {
            "node": "Land ECO-01 Raw Payload",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Land ECO-01 Raw Payload": {
      "main": [
        [
          {
            "node": "Write ECO-01 Raw Landing",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Write ECO-01 Raw Landing": {
      "main": [
        [
          {
            "node": "Normalize ECO-01 Events",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize ECO-01 Events": {
      "main": [
        [
          {
            "node": "Merge Normalized Branches",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch ECO-02 Smoke Forecast": {
      "main": [
        [
          {
            "node": "Land ECO-02 Raw Payload",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Land ECO-02 Raw Payload": {
      "main": [
        [
          {
            "node": "Write ECO-02 Raw Landing",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Write ECO-02 Raw Landing": {
      "main": [
        [
          {
            "node": "Normalize ECO-02 Events",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize ECO-02 Events": {
      "main": [
        [
          {
            "node": "Merge Normalized Branches",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "Fetch PSCAA-02 Burn Ban Page": {
      "main": [
        [
          {
            "node": "Land PSCAA-02 Raw Payload",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Land PSCAA-02 Raw Payload": {
      "main": [
        [
          {
            "node": "Write PSCAA-02 Raw Landing",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Write PSCAA-02 Raw Landing": {
      "main": [
        [
          {
            "node": "Normalize PSCAA-02 Events",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize PSCAA-02 Events": {
      "main": [
        [
          {
            "node": "Merge Normalized Branches",
            "type": "main",
            "index": 2
          }
        ]
      ]
    },
    "Fetch AIRNOW-02 Files": {
      "main": [
        [
          {
            "node": "Land AIRNOW-02 Raw Payload",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Land AIRNOW-02 Raw Payload": {
      "main": [
        [
          {
            "node": "Write AIRNOW-02 Raw Landing",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Write AIRNOW-02 Raw Landing": {
      "main": [
        [
          {
            "node": "Normalize AIRNOW-02 Events",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize AIRNOW-02 Events": {
      "main": [
        [
          {
            "node": "Merge Normalized Branches",
            "type": "main",
            "index": 3
          }
        ]
      ]
    },
    "Fetch WASMOKE-01 RSS": {
      "main": [
        [
          {
            "node": "Land WASMOKE-01 Raw Payload",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Land WASMOKE-01 Raw Payload": {
      "main": [
        [
          {
            "node": "Write WASMOKE-01 Raw Landing",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Write WASMOKE-01 Raw Landing": {
      "main": [
        [
          {
            "node": "Normalize WASMOKE-01 Events",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize WASMOKE-01 Events": {
      "main": [
        [
          {
            "node": "Merge Normalized Branches",
            "type": "main",
            "index": 4
          }
        ]
      ]
    },
    "Fetch NWS-AQ-01 Alerts": {
      "main": [
        [
          {
            "node": "Land NWS-AQ-01 Raw Payload",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Land NWS-AQ-01 Raw Payload": {
      "main": [
        [
          {
            "node": "Write NWS-AQ-01 Raw Landing",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Write NWS-AQ-01 Raw Landing": {
      "main": [
        [
          {
            "node": "Normalize NWS-AQ-01 Events",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize NWS-AQ-01 Events": {
      "main": [
        [
          {
            "node": "Merge Normalized Branches",
            "type": "main",
            "index": 5
          }
        ]
      ]
    },
    "Fetch PSCAA-01 NetworkMap": {
      "main": [
        [
          {
            "node": "Land PSCAA-01 Raw Payload",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Land PSCAA-01 Raw Payload": {
      "main": [
        [
          {
            "node": "Write PSCAA-01 Raw Landing",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Write PSCAA-01 Raw Landing": {
      "main": [
        [
          {
            "node": "Normalize PSCAA-01 Events",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize PSCAA-01 Events": {
      "main": [
        [
          {
            "node": "Merge Normalized Branches",
            "type": "main",
            "index": 6
          }
        ]
      ]
    },
    "Fetch AIRNOW-01 API Alerts": {
      "main": [
        [
          {
            "node": "Land AIRNOW-01 Raw Payload",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Land AIRNOW-01 Raw Payload": {
      "main": [
        [
          {
            "node": "Write AIRNOW-01 Raw Landing",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Write AIRNOW-01 Raw Landing": {
      "main": [
        [
          {
            "node": "Normalize AIRNOW-01 Events",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize AIRNOW-01 Events": {
      "main": [
        [
          {
            "node": "Merge Normalized Branches",
            "type": "main",
            "index": 7
          }
        ]
      ]
    },
    "Merge Normalized Branches": {
      "main": [
        [
          {
            "node": "Aggregate Normalized Branches",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Aggregate Normalized Branches": {
      "main": [
        [
          {
            "node": "Deduplicate Events",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Deduplicate Events": {
      "main": [
        [
          {
            "node": "Build Route Sections Rollups",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Route Sections Rollups": {
      "main": [
        [
          {
            "node": "Validate Candidate Envelope",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate Candidate Envelope": {
      "main": [
        [
          {
            "node": "Build Candidate Artifact",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Candidate Artifact": {
      "main": [
        [
          {
            "node": "Write Candidate Artifact",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Write Candidate Artifact": {
      "main": [
        [
          {
            "node": "Publish Gate Decision",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Publish Gate Decision": {
      "main": [
        [
          {
            "node": "Build Final Artifact Bundle",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Final Artifact Bundle": {
      "main": [
        [
          {
            "node": "Write Final Artifacts",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Write Final Artifacts": {
      "main": [
        [
          {
            "node": "Final Status Report",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "timezone": "America/Los_Angeles"
  },
  "staticData": {},
  "meta": null,
  "pinData": null,
  "versionId": "9c78d9e0-3622-4f7c-9c83-12e67abc6392",
  "activeVersionId": "9c78d9e0-3622-4f7c-9c83-12e67abc6392",
  "versionCounter": 5,
  "triggerCount": 1,
  "tags": [],
  "shared": [
    {
      "updatedAt": "2026-08-02T10:05:10.721Z",
      "createdAt": "2026-08-02T10:05:10.721Z",
      "role": "workflow:owner",
      "workflowId": "qlM2XIv2BbFSh3in",
      "projectId": "Y0Ygmqe59jevHoeV",
      "project": {
        "updatedAt": "2026-07-10T05:38:45.677Z",
        "createdAt": "2026-06-02T20:51:11.579Z",
        "id": "Y0Ygmqe59jevHoeV",
        "name": "John Brooks <john@biketourfrance.net>",
        "type": "personal",
        "icon": null,
        "description": null,
        "creatorId": "5dadc5f6-6bff-44cf-9566-6b82f3d768dc"
      }
    }
  ],
  "versionMetadata": {
    "name": null,
    "description": null
  }
}
